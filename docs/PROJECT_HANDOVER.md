# Wizard (Soothsayer-Wizard) — Project Handover Summary

> อัปเดตล่าสุด: 2026-08-11 — §1-§9 ด้านล่างคือ**ภาพรวม/reference ที่ sync กับสภาพโค้ดปัจจุบันแล้ว** (ตรวจสอบซ้ำและแก้ให้ตรงความจริงในรอบนี้) ส่วน §6.1 เป็นต้นไปคือ**บันทึกประวัติงานแบบเรียงตามเวลา** (แต่ละหัวข้อ 🆕 คือสิ่งที่เกิดขึ้น ณ วันที่นั้นๆ ไม่ใช่สถานะปัจจุบันเสมอไป — ถ้าเรื่องไหนถูกแก้/revert ต่อในภายหลัง จะมีหัวข้อใหม่ต่อท้ายบอกไว้)
>
> ⚠️ **โปรเจกต์ย้าย path แล้ว**: จากเดิม `C:\00_DATA\OneDrive - P-DICTOR\repo\Soothsayer-wizard-app` (อยู่ใน OneDrive sync) ➜ ปัจจุบันอยู่ที่ **`C:\00_DATA\Soothsayer-wizard-app`** (โฟลเดอร์ปกติ นอก OneDrive) — ดูเหตุผลใน §9
>
> 📌 **เอกสารนี้อัปเดตอัตโนมัติทุกครั้งที่ทำงานเสร็จ 1 อย่าง** (ผู้ใช้ทำงานสลับ 2 เครื่อง และ Claude Code ไม่มี session history sync ข้ามเครื่อง — เอกสารนี้คือตัวแทนความต่อเนื่องแทน chat history) — **⚠️ 2026-08-18: เพิ่งทำเสร็จรอบล่าสุด — ออกแบบ + implement ระบบ Failure Group / Predictive Model ใหม่ทั้งหมด** (FG มี description/recommendation, model แยก 3 kind (Individual/Relationship/Clustering) พร้อม category (Performance/Condition), PM config ผูกกับแต่ละ model แทน global slot เดิม, Build Model เป็นหน้าต่างแยกใหม่ `BuildModelWindow.tsx` เปิดได้หลายวินโดว์พร้อมกัน, FailureGroupsPanel เดิมเหลือแค่ preview) — `tsc --noEmit` สะอาด + เทสต์ **761/761 ผ่านทั้ง repo** — **ยังไม่ได้ทดสอบบนแอปจริงเลย** (`npm run tauri dev`) เพราะพึ่ง native multi-window spawning ทดสอบผ่าน Browser preview ไม่ได้ — ผู้ใช้เองบอกไว้ว่าจะทดสอบแล้วบอกกลับถ้าต้องแก้อะไร ดู entry ท้ายสุดของเอกสารนี้สำหรับรายละเอียดเต็ม — **commit ที่ push แล้วล่าสุดที่ `personal` (toeyps/soothsayer-wizard-app): ดู `git log` (จะอัปเดต hash ที่นี่ในรอบถัดไป)** — **push ไปที่ `personal` remote เท่านั้นเสมอ ห้าม push เข้า `origin` (Alpha-Com-Thailand) โดยเด็ดขาดแม้จะมีสิทธิ์ก็ตาม เป็นการตัดสินใจของผู้ใช้ ไม่ใช่แค่ปัญหาสิทธิ์ push (403)** — ดู §7 กับ entry ท้ายสุดของเอกสารนี้

---

## 1. ภาพรวมโปรเจกต์

**Wizard** เป็น desktop app (Tauri v2) สำหรับ:
1. Import CSV ข้อมูลเซนเซอร์
2. สำรวจข้อมูล (dashboard: table / line chart / scatter / pair-plot / alarm setpoint lines)
3. จัดกลุ่ม Failure Group (**2026-08-06: ย้ายเข้ามาอยู่ใน Dashboard แล้ว** — เป็นแท็บ "Failure Groups" ในแผง Sensor ฝั่งขวา ไม่ใช่หน้าต่างแยกอีกต่อไป ดู §3/§5 และ entry `🆕 2026-08-06 — ⚡ งานใหญ่` ด้านล่าง)
4. เทรน Predictive Model (3 แบบ: Individual, Clustering, Relationship) — เปิดจากปุ่ม Build Model ในแท็บ Failure Groups โดยตรง

สถาปัตยกรรมแบ่งเป็น 3 tier: **React frontend** ↔ **Rust core (Tauri)** ↔ **Python sidecar** (เฉพาะโมเดล Relationship)

---

## 2. Stack ที่ใช้จริง (ตรวจสอบจาก `package.json` / `Cargo.toml`)

### Frontend (`src/`)
| หมวด | Library | หมายเหตุ |
|---|---|---|
| Framework | React 19.1 + TypeScript 5.8 | strict, ไม่มี React Router — เปลี่ยนหน้าด้วย local state |
| Build | Vite 5, dev port `1420` | |
| Styling | Tailwind CSS v4 (`@tailwindcss/postcss`) | |
| Line chart | `echarts` 6 + `echarts-for-react` + `echarts-gl` | |
| Scatter / pair-plot | `regl-scatterplot` (WebGL) | มี context-loss guard ในตัว |
| Layout | `split.js` (resizable panes) | |
| Icons | `lucide-react` | |
| PDF export | `@react-pdf/renderer` (ใช้ yoga-layout WASM runtime) | ดูข้อควรระวัง §8 |
| PNG export | `html-to-image` + ECharts canvas compositing | |
| Formula UI | `d3-scale` | ใช้ร่วมกับ formula engine ฝั่ง Rust |
| Test | Vitest 4 + jsdom + @testing-library/react | `src/__tests__/` — coverage ครอบคลุมทุกไฟล์แล้ว (586 เทสต์/45 ไฟล์ ณ 2026-08-10, ดู entry ท้ายเอกสาร) |

### Rust core (`src-tauri/src/`, Tauri v2)
| Crate | ใช้ทำอะไร |
|---|---|
| `csv` + `rayon` | parse CSV แบบ parallel |
| `chrono` | timestamp parsing |
| `nalgebra` | GMM ellipse fit (clustering) |
| `fasteval` | formula engine (calculated sensors) |
| `tauri-plugin-{dialog,fs,store,shell,opener}` | native dialogs, scoped FS, persisted settings, sidecar spawn |
| dev-dep `tempfile` | Rust unit/integration tests |

`Cargo.toml` ตั้ง `opt-level = 1` สำหรับ crate เราเองใน dev profile (เจตนา — เพื่อให้ `chart_query.rs` เร็วพอตอน dev บนข้อมูลหลายล้านแถว) และ `opt-level = 2` สำหรับ dependency ทั้งหมด

### Python sidecar (`src-tauri/python/backend.py`, 486 บรรทัด)
- Compile ด้วย **Nuitka `--onefile`** เป็น native binary — ห้าม ship เป็น `.py` ตรงๆ
- สื่อสารกับ Rust ผ่าน stdin/stdout เป็น JSON เท่านั้น
- ใช้ LinearGAM fit ความสัมพันธ์ระหว่างเซนเซอร์ — **ชื่ออัลกอริทึมนี้ห้ามโผล่ใน UI** (ดู §8)
- ทดสอบแล้วว่า compile ได้ด้วย **Python 3.14** (numpy 2.5.1 / scipy 1.16.3 / pygam 0.12.0 / nuitka 4.1.3) แม้ Nuitka จะเตือนว่า 3.14 ยัง "experimental" — ใช้งานได้จริง สอบผ่าน smoke test ของ `build_sidecar.sh`

---

## 3. โครงสร้างโปรเจกต์ (ย่อ)

```
Soothsayer-wizard-app/
├─ src/                        # React frontend
│  ├─ main.tsx                 # entry — เลือก root component ตาม ?window= query param
│  ├─ App.tsx                  # entry ของหน้าต่างหลัก (main window)
│  ├─ components/
│  │  ├─ upload/                DataUploadPage (ตอนนี้เป็น 3-step wizard — ดู §6) + ลูกๆ
│  │  ├─ dashboard/              Dashboard, DataTable, FilterPanel, SensorSelection, **FailureGroupsPanel** (ใหม่ 2026-08-06)
│  │  ├─ charts/                 LineChart, ScatterChart, PairPlotCell/Chart, ResponsiveECharts
│  │  ├─ windows/                **3 sub-window** (ลดจาก 4 เมื่อ 2026-08-06 — `FailureGroupCreation.tsx` ถูกลบทิ้ง): AddSensor / PredictiveModelBuild / SaveAs (SaveAs ตอนนี้ทำหน้าที่แค่ "Rename Workspace" เท่านั้น โหมด duplicate-workspace เดิมถูกลบออกด้วย)
│  │  └─ reports/                PM PDF report template (react-pdf)
│  ├─ hooks/                    useDataUpload, useMappingData, useChartData, useScatterSample,
│  │                             useTablePage, useCalculationEngine, useFormulaEditor,
│  │                             useAppMenu, useSubWindowMenu, usePMReport, useCoalescedDraw
│  ├─ types/commands.ts         # ⭐ single source of truth ของทุก Tauri command signature
│  ├─ workspaceManager.ts       # save/load/list/delete/duplicate/rename workspace
│  └─ errorReporter.ts          # window.onerror / unhandledrejection → log ไฟล์ผ่าน Rust
│
├─ src-tauri/
│  ├─ src/
│  │  ├─ lib.rs (2647 บรรทัด)   # entry จริง — invoke_handler รวม 22 command, window management
│  │  ├─ csv_processor.rs (1098) # parse/merge CSV → ColumnarData (in-RAM store)
│  │  ├─ chart_query.rs (1001)  # bounded pipeline: filter → transform → aggregate → downsample
│  │  ├─ clustering.rs (166)     # GMM ellipse fit
│  │  ├─ operation_registry.rs  # calculated-sensor operation catalog
│  │  ├─ metrics.rs (295)        # สถิติต่อเซนเซอร์
│  │  └─ main.rs (6)             # เรียก tauri_app_lib::run()
│  ├─ python/backend.py          # LinearGAM sidecar source
│  ├─ bin/backend-x86_64-pc-windows-msvc.exe  # compiled sidecar (gitignored, ต้อง build เองทุกเครื่อง)
│  ├─ tests/predictive_model_tests.rs
│  ├─ capabilities/{default,add-sensor}.json
│  └─ tauri.conf.json            # CSP, window config, externalBin, bundle targets
│
├─ docs/                        # requirements, task breakdown (zone ของ pm-agent), เอกสารนี้
├─ scripts/                     # installer build script ต่อแพลตฟอร์ม
├─ .github/workflows/release.yml # CI: build ทั้ง sidecar + Tauri bundle, publish GitHub Release
└─ .claude/agents/               # นิยาม sub-agent 5 ตัว + zone การเขียนไฟล์ (ดู CLAUDE.md)
```

---

## 4. วิธีรันโปรแกรม (บนเครื่องที่ตั้งค่าไว้แล้ว)

```bash
cd C:\00_DATA\Soothsayer-wizard-app   # path ใหม่ — ไม่ใช่ OneDrive แล้ว
npm install

npm run tauri dev        # รันแอปเต็ม (compile Rust + เปิดหน้าต่าง) — sidecar ต้องมีอยู่แล้วที่ src-tauri/bin/
npm run dev               # frontend อย่างเดียว (Vite, port 1420) — ไม่มี Tauri backend
npm run build              # tsc type-check + vite build (รันก่อน handoff เสมอ)
npm run tauri:build        # release bundle (ต้องมี sidecar binary พร้อมแล้ว)

npx vitest run                                          # frontend test ทั้งหมด (586 เทสต์/45 ไฟล์ ณ 2026-08-10)
npx vitest run src/__tests__/useScatterSample.test.ts    # ไฟล์เดียว
cd src-tauri && cargo test                               # Rust unit + integration test
cd src-tauri && cargo test --test predictive_model_tests # integration เท่านั้น
```

**CI** (`.github/workflows/release.yml`): push tag `v*` → build sidecar (Nuitka) + Tauri bundle บน macOS arm64 และ Windows x64 → สร้าง draft GitHub Release พร้อม `.dmg` / `.exe` (NSIS) / `.msi`

### ตั้งเครื่อง dev ใหม่จากศูนย์ (checklist)
ถ้าเป็นเครื่องใหม่ที่ยังไม่เคย build เลย ให้ทำตามลำดับนี้ — ดูรายละเอียด/สาเหตุปัญหาที่เจอจริงใน §9:
1. ติดตั้ง Rust (`rustup`) + Visual Studio Build Tools (C++ workload)
2. ติดตั้ง Python 3.11+ (หรือ 3.13/3.14 ก็ใช้ได้ — ทดสอบแล้ว)
3. **ถ้าเครื่องมี Kaspersky**: เพิ่ม exclusion ให้ Intrusion Prevention ครอบคลุมโฟลเดอร์โปรเจกต์ทั้งหมด + `%USERPROFILE%\.cargo` + `%USERPROFILE%\.rustup` (ไม่งั้น build จะพังด้วย error "Application Control policy has blocked this file")
4. **อย่า clone/วางโปรเจกต์ไว้ใน path ที่ OneDrive sync อยู่** — ทำให้ cargo build ล้มด้วย "output path is not a writable directory" แบบสุ่มๆ (ดู §9)
5. compile Python sidecar: `cd src-tauri/python && python -m venv .venv && .venv\Scripts\pip install -r requirements.txt && bash build_sidecar.sh` (ต้องมี Git Bash) แล้ว copy ผลลัพธ์ไปที่ `src-tauri/bin/backend-<target-triple>.exe`
6. `npm install && npm run tauri dev`

---

## 5. Entry point

- **Rust**: `src-tauri/src/main.rs` → `tauri_app_lib::run()` ใน `lib.rs` — ตั้งค่า plugin, register 22 command ผ่าน `invoke_handler`, และ handle window-lifecycle (ปิดแอปเมื่อไม่มีหน้าต่าง *visible* เหลือ — มี logic เฉพาะกันเคส race ตอนสลับไป sub-window)
- **Frontend**: `src/main.tsx` อ่าน query param `?window=` เพื่อเลือก root component:
  - ไม่มี param → `App.tsx` (main window: upload → dashboard → **Failure Groups tab ก็อยู่ในนี้ด้วยแล้ว 2026-08-06**)
  - `add-sensor` → `AddSensorWindow`
  - `predictive-model` → `PredictiveModelBuild`
  - `save-as` → `SaveAsWindow` (**2026-08-06: เหลือแค่โหมด `?mode=rename` เท่านั้นที่ยังถูกเรียกจริง** — โหมด `save-as`/duplicate-workspace ถูกลบทุก entry point แล้ว โค้ดในไฟล์ยังมี branch เดิมค้างไว้เป็น dead code โดยตั้งใจ ไม่เสี่ยงแก้เพราะกลัวพัง rename)
  - ~~`failure-group` → `FailureGroupCreation`~~ — **ลบออกแล้ว 2026-08-06** (ทั้ง route นี้และไฟล์ component ถูกลบ — ดู entry `🆕 2026-08-06 — ⚡ งานใหญ่` ด้านล่างสำหรับรายละเอียดเต็ม)

  แต่ละ sub-window คือ Tauri window แยก คนละ capability file (`default.json` เต็ม, `add-sensor.json` แคบกว่า)

---

## 6. Data flow หลัก (หัวใจของแอป)

1. **Load**: `load_csv` (Rust) parse หลายไฟล์แบบ parallel ด้วย rayon (cap **2 GB/ไฟล์**), merge ตาม timestamp (ค่าใหม่ non-null ชนะเมื่อ timestamp ซ้ำ; โหลดไฟล์เดียวจะข้าม merge) → เก็บใน Tauri managed state
2. **In-RAM store = `ColumnarData`** (`csv_processor.rs`): column-major โดยเจตนา — แต่ละเซนเซอร์เป็น `Vec<f64>` ต่อเนื่อง, **NaN = missing**; `ts_parsed` เก็บ epoch-microseconds ที่ parse ครั้งเดียวตอน load (`TS_MISSING` ถ้าไม่มี); `headers[0]` คือ timestamp column เสมอ
3. **Frontend ไม่เคยได้ dataset เต็ม** (ตั้งแต่ v0.2.1) — query แสดงผลทำฝั่ง Rust ใน `chart_query.rs`: filter → operation transform → hourly aggregation (ถ้ามี) → min/max downsample → ส่งกลับแค่ O(max_points) แถว
   - `get_chart_data` (ceiling 100k จุด) ผ่าน `useChartData` — line chart
   - `get_table_page` (page ≤ 1000) ผ่าน `useTablePage` — data table
   - `get_scatter_sample` (reservoir sampling) ผ่าน `useScatterSample` — scatter + pair-plot
   - `export_chart_csv` เขียนไฟล์ลง disk ตรงจาก Rust (ไม่ผ่าน JS)
4. **GPU guard ชั้นสุดท้าย**: `ScatterChart` / `PairPlotCell` จับ WebGL context-loss (retry overlay) + จำกัด stride สูงสุด (500k / 100k จุด)

**Predictive model 3 แบบ**:
- Individual → Rust คำนวณสถิติ/ขอบเขตต่อเซนเซอร์ → `INDV_INFO_*.json`
- Clustering → Rust GMM ellipse fit (nalgebra) → `CLUS_INFO_*.json`
- Relationship → Rust preprocess/project ข้อมูล → ส่ง array เล็กๆ ผ่าน stdin ไปยัง Python sidecar (LinearGAM) → sidecar เขียน `.pkl` และคืน JSON → Rust เขียน `REL_INFO_*.json`

**Report export** (`usePMReport`): PNG ผ่าน html-to-image + ECharts canvas compositing, PDF ผ่าน `@react-pdf/renderer` (คอมไพล์ WASM runtime — ต้อง test บน installed build จริงเวลาแก้ CSP/report)

### 6.1 การเปลี่ยนแปลง UI รอบล่าสุด (ยังไม่ commit — ดู §8)

**หน้า Data Upload** (`DataUploadPage.tsx`) เปลี่ยนจากหน้าเดียวเป็น **3 step**:
- **Step 0** (`StartChoiceStep`) — จุดตัดสินใจแรกสุด: การ์ด "Create new project" กับการ์ด "Open recent project" (โชว์ preview 3 workspace ล่าสุด, "Show all" ถ้ามีมากกว่านั้น) — sidebar รายชื่อ workspace เดิมถูก**ซ่อนไว้เฉพาะ step นี้**เพื่อไม่ให้ list ซ้ำกับการ์ด (เคยมีบั๊ก "Found multiple elements" จาก test เพราะ list โผล่ 2 ที่พร้อมกัน)
- **Step 1** (`CreateProjectStep`) — ตั้งชื่อ + คำอธิบาย project (มี "‹ Back" กลับไป step 0)
- **Step 2** — เนื้อหาเดิม (upload CSV + mapping) sidebar โผล่กลับมา

**Dashboard** (`Dashboard.tsx` + ลูกๆ):
- **`SensorSelection.tsx`** (แผง "Sensor" ทางขวา) — ปรับใหม่ทั้งหมด: sensor **group by component** อัตโนมัติ ปิดไว้ทั้งหมดโดย default (ต้องกดขยายทีละ component) มีช่องค้นหาที่ auto-expand กลุ่มที่มีผลลัพธ์ตรง ไม่มี filter panel/component-multiselect แบบเดิมแล้ว
- **Data Insight panel** (`renderDataContent` ใน Dashboard.tsx) — ตอนนี้เป็น **2 tab**: **"Selected Sensor"** (default) แสดง sensor ที่ plot อยู่ทุกตัว พร้อม checkbox hide/unhide บนกราฟ, ไอคอน pipette เปลี่ยนสีเส้น, ไอคอน line-chart เปิดช่อง pin Y-axis scale (min/max คงที่), ปุ่มลบออกจาก plot — กับ **"Data Insight"** (ตารางข้อมูลเดิม)
- **Legend ของกราฟ** (`LineChart.tsx`) ยังอยู่เหมือนเดิม (เคยลองเอาออกแล้วเอากลับมา) แต่ **เอาชื่อ sensor ที่เคยโผล่ซ้ำเหนือแกน Y ออกแล้ว** (ซ้ำกับ legend)
- **TIME RANGE** เพิ่มปุ่ม quick-select แบบ relative (`Y`/`M`/`W`/`D`/`H` + เลขที่กรอก + ✓) นอกเหนือจาก date picker เดิม
- **สี/Y-axis scale ต่อ sensor เป็น session state เท่านั้น** (`sensorColors`, `sensorAxisRange` ใน Dashboard.tsx) — **ยังไม่ persist ลง `WorkspaceState`** ปิดแอปแล้วค่าหาย ถ้าต้องการ persist ต้องเพิ่มเข้า `WorkspaceState` + `buildWorkspaceState` เอง
- โฟลเดอร์ "collapsed panels" sidebar (ไอคอน DATA/FILTER ตอนซ่อน panel) — label "Data" เปลี่ยนเป็น **"Pickup Sensor"** แล้ว, ตำแหน่งแก้ไม่ให้ทับ Y-axis ของกราฟแล้ว (เพิ่ม `padding-left` แบบมีเงื่อนไขใน `App.css`)
- **`FilterPanel.tsx`** — dropdown เลือก sensor สำหรับ value filter ตอนนี้โชว์ `description (tag)` แทน tag ดิบๆ (ต้องมี `sensorMetadata` prop ส่งเข้าไป)

**🆕 Selected Sensor tab แยกตาม chart type แล้ว** (`Dashboard.tsx`, `renderDataContent`) — เดิม checkbox/pipette/pin-Y-axis โชว์เหมือนกันทุก chart type แต่จริงๆ มีผลแค่ Line Plot เท่านั้น (`sensorColors`/`sensorAxisRange` ส่งให้ `LineChart` อย่างเดียว, Scatter/Pair ใช้ `selectedSensors` เต็มชุดเสมอไม่สนใจ `visibleSensors`) ตอนนี้แก้เป็น:
- **Line**: เหมือนเดิมทุกอย่าง (checkbox hide/unhide, pipette สี, pin Y-axis, ลบ)
- **Scatter/Pair Plot**: เหลือแค่ปุ่มลบ + กล่องคำอธิบายสั้นๆ ชี้ไปที่ controls จริงของ chart แต่ละแบบ (Scatter มี X/Y dropdown + toolbox ในตัวกราฟเอง, Pair Plot มี lasso-cluster coloring ในตัวกราฟเอง) — สีตัวหนังสือ fallback เป็น `var(--text-primary)` แทนสีต่อเซนเซอร์

**🆕 "Clear all" ปุ่มใหม่ใน Selected Sensor tab** (`Dashboard.tsx`) — เดิมลบ sensor ออกจากกราฟได้ทีละตัวผ่านไอคอนถังขยะต่อแถวเท่านั้น ตอนนี้เพิ่มปุ่ม "Clear all" (ไอคอน X) ที่ header ของ panel โผล่เฉพาะตอน tab เป็น "Selected Sensor" และมี sensor เลือกอยู่อย่างน้อย 1 ตัว — เรียก `clearAllSensors()` (เพิ่มใหม่คู่กับ `removeSensor` เดิม, แค่ `setSelectedSensors([])`) `visibleSensors` จะตามไปว่างเองผ่าน sync effect ที่มีอยู่แล้ว — ยึดตามดีไซน์อ้างอิงจาก platform เดิม (ปุ่ม X ข้าง "ALL PICKED") ที่ผู้ใช้ส่ง screenshot มา — `tsc --noEmit` ผ่านแล้ว **ยังไม่ได้ทดสอบบนหน้าต่างแอปจริงเพราะ browser preview ใช้ไม่ได้กับแอปนี้ (ดูด้านล่าง) — ต้องรอผู้ใช้ยืนยันผ่าน `npm run tauri dev`**

**🆕 Color picker: สร้าง `ColorPlatePicker.tsx` เอง (ไม่พึ่ง library) แทน native `<input type="color">` แล้ว — รอบที่ 3, สำเร็จ** (`src/components/dashboard/ColorPlatePicker.tsx` ใหม่ + `Dashboard.tsx`) — สรุปเส้นทางที่ลองมา: รอบ 1 custom popover แบบ preset-swatch + hex input ล้วนๆ (ไม่มีจานสีลาก) → ผู้ใช้ไม่ชอบ revert; รอบ 2 ลอง `npm install react-colorful` แต่ Vite error `Missing "./dist/color-picker.css" specifier` (เวอร์ชันที่ดึงมา export path ไม่ตรง) → uninstall + revert กลับ native ทั้งหมด; **รอบ 3 (ปัจจุบัน)** เขียน HSV picker เองล้วนๆ ไม่มี dependency ใหม่เลย กันปัญหา import path ซ้ำ — ประกอบด้วยแค่ **saturation/value square (ลากได้) + hue slider** เท่านั้น ตามที่ผู้ใช้ขอชัดเจนว่า "เหลือแค่ plate สีด้านบนพอ" (ไม่มี RGB number field, ไม่มี hex input, ไม่มี preset swatch — ตัดออกหมดจากรอบ 1 เพราะไม่ได้ขอ) hue คงที่ระหว่างลากบน square (กันปัญหา hue หายตอนลากเข้าโซน grayscale) ไฟล์มี helper hex↔rgb↔hsv ในตัวเอง (`hexToRgb`, `rgbToHex`, `rgbToHsv`, `hsvToRgb`) ลาก/คลิกผ่าน Pointer Events (`setPointerCapture`) ไม่ใช้ global mousemove listener — toggle เปิด/ปิดทีละ sensor แบบเดียวกับ Y-axis pin editor (state `colorPickerFor` ใน `Dashboard.tsx`) — `tsc --noEmit` ผ่าน **ยังไม่ได้ทดสอบบนแอปจริง รอ `npm run tauri dev`** (ไม่ต้อง reinstall/restart พิเศษรอบนี้เพราะไม่มี dependency ใหม่ hot-reload ควรพอ) — ยังไม่มีการเทสต์ pointer-drag ทั้ง 2 แกน (square) และ 1 แกน (hue) ด้วยตาจริงเลย ควรลองลากดูให้ทั่วก่อน commit

**🆕 แก้บั๊ก: เข้า Failure Group แล้วกลับ Dashboard ไม่ได้เลย (dead-end)** — Dashboard set `lastRoute: 'failure-group'` ค้างไว้ตอนเปิด FG ([Dashboard.tsx:1509](src/components/dashboard/Dashboard.tsx:1509)) และไม่มีจุดไหนเคย reset กลับเป็น `'dashboard'` เลย ("Back to Upload" ก็ตั้งใจไม่ auto-resume อยู่แล้วตาม design เดิม — [useSubWindowMenu.ts:24](src/hooks/useSubWindowMenu.ts:24)) ผลคือ workspace ไหนที่เคยเปิด FG จะ**เข้า Dashboard/analyze ไม่ได้อีกเลย**ไม่ว่าจะลองทางไหน (ผู้ใช้เจอเองแล้วรายงาน) แก้โดยเพิ่มปุ่ม **"Back to Dashboard"** ใหม่แยกจาก "Back to Upload" เดิม (คงไว้ทั้งคู่ เจตนาต่างกัน): `handleBackToDashboard` ใน [FailureGroupCreation.tsx](src/components/windows/FailureGroupCreation.tsx) patch `lastRoute` กลับเป็น `dashboard` ก่อน reopen/focus main window ผ่าน URL param `?resume=<id>` (fresh spawn) หรือ event `resume-workspace` (reuse) — ฝั่งรับใน [DataUploadPage.tsx](src/components/upload/DataUploadPage.tsx) เพิ่ม effect เรียก `handleLoadWorkspace()` เดิมที่มีอยู่แล้วให้ auto-resume ตรงเข้า Dashboard — `tsc --noEmit` ผ่าน **ยังไม่ได้ทดสอบบนแอปจริง** (flow: Dashboard → Create Failure Group → กด "Back to Dashboard" ใหม่ → ควรกลับเข้า Dashboard เดิมพร้อม sensor ที่เลือกไว้ครบ ไม่ใช่หน้า Import) — 🆕 ผู้ใช้ให้ลบปุ่ม "Back to Upload" เดิมออกจาก FG toolbar ทั้งหมดแล้ว (รู้สึกว่าข้าม sequence ที่วางไว้) เหลือแค่ "Back to Dashboard" ปุ่มเดียว — ลบ `handleBackToUpload` + `ask` import ที่ไม่ใช้แล้วออกด้วย (`useSubWindowMenu.ts`'s File-menu "New Workspace"/"Close Workspace" ยังคงพฤติกรรมเดิม ไม่ auto-resume — จงใจไม่แตะ เพราะเจตนาต่างจากปุ่ม toolbar)

🆕 **แก้บั๊กรอบ 2**: ผู้ใช้ทดสอบแล้ว "Back to Dashboard" ไม่ได้พาเข้า Dashboard ตรงๆ — พาไปหน้า Import เฉยๆ ต้องกด recent-workspace card เองอีกทีถึงจะเข้า Dashboard (auto-resume ไม่ทำงาน) สาเหตุที่คาดคือ logic เดิม "เช็คว่ามี window 'main' เดิมไหม ถ้ามีก็ reuse" ไปเจอ window ค้าง (stale, hidden แต่ไม่ถูก destroy จริง) จากการทดสอบหลายรอบในเซสชันเดียวกัน ซึ่งมี React state เก่าไม่รู้จัก resume logic เลย แก้โดยเปลี่ยนเป็น **destroy ของเก่าทิ้งเสมอแล้ว spawn ใหม่เสมอ** (ไม่มี branch "reuse" อีกต่อไป) ใน `handleBackToDashboard` ([FailureGroupCreation.tsx](src/components/windows/FailureGroupCreation.tsx)) — ลบ `resume-workspace` event listener ที่ไม่จำเป็นแล้วออกจาก [DataUploadPage.tsx](src/components/upload/DataUploadPage.tsx) เหลือแค่อ่าน `?resume=<id>` จาก URL ตอน mount ทางเดียว เช็ค exit-guard ใน [lib.rs:2592](src-tauri/src/lib.rs:2592) แล้วว่าลำดับ destroy-then-create ปลอดภัย (FG ยังเปิด/visible อยู่ตลอดจนกว่าจะปิดตัวเองท้ายสุด ไม่มีจังหวะ "0 visible windows" หลอกให้แอปปิด) — `tsc --noEmit` ผ่าน

🆕 **แก้บั๊กรอบ 3 (ร้ายแรงกว่าเดิม — ปิดทั้งแอป)**: ทดสอบแล้ว "Back to Dashboard" ทำ**แอปทั้งตัวปิดตัวเอง**กลางคัน! สาเหตุคือ race condition กับ Rust exit-guard ([lib.rs:2592](src-tauri/src/lib.rs:2592)): โค้ดรอบ 2 สร้าง `main` ใหม่แบบไม่รอ event ยืนยันว่าสร้างเสร็จ แล้วรีบปิด FG ทันที — ถ้า FG ปิดเสร็จเร็วกว่า `main` ใหม่จะถูกนับเป็น "visible" (เพราะ native window creation เป็น async ฝั่ง Rust) exit-guard จะเห็น "remaining visible windows: []" ชั่วขณะแล้วสั่ง `app_handle.exit(0)` ฆ่าทั้งแอปทันที ตัดตอน `handleLoadWorkspace` ที่กำลังโหลดอยู่กลางทาง (เห็นจาก log "Total read_csv took: 307ms" ที่ทำเสร็จแต่ไปไม่ถึง Dashboard) — แก้โดยเพิ่ม `await new Promise(...)` รอ event `tauri://created` ของ `main` ใหม่ก่อน แล้วค่อยปิด FG (เลียนแบบ pattern เดียวกับที่ Dashboard→FG ใช้อยู่แล้วสำเร็จ ซึ่งพลาดไม่ได้ใส่ไว้ตอนแก้รอบ 2) — ทดสอบแล้วยัง**ปิดแอปตัวเองเหมือนเดิม** ผู้ใช้ให้ **revert ทิ้งทั้งหมด** ใช้ "Back to Upload" แบบเดิมไปก่อน — revert แล้ว: `FailureGroupCreation.tsx` กลับไปมี `handleBackToUpload` + ปุ่ม + import `ask` เหมือนก่อนเริ่มทำ feature นี้ 100%, `DataUploadPage.tsx` ลบ auto-resume effect (`?resume=` param) ออกหมด — grep ยืนยันไม่มีเศษโค้ด `handleBackToDashboard`/`resume-workspace`/`?resume=` เหลือเลย `tsc --noEmit` ผ่าน — **ถ้าจะลองทำ "กลับไป Dashboard ตรงๆ" อีกในอนาคต ต้องระวัง Rust exit-guard ([lib.rs:2592](src-tauri/src/lib.rs:2592)) ให้ดี**: การ destroy/create window หลายตัวพร้อมกันเสี่ยงชนกับ "0 visible windows → exit" มาก แม้จะรอ `tauri://created` แล้วก็ยังพังในการทดสอบจริง (อาจต้องรอ event ที่บ่งบอกว่า window "visible" จริงๆ ไม่ใช่แค่ "created" หรือต้อง debug ฝั่ง Rust โดยตรงว่า `is_visible()` คืนค่าถูกจังหวะไหน) — ยังไม่มีวิธีแก้ที่ยืนยันว่าใช้ได้จริง

**🆕 Label "SAMPLING (1 HR)" เปลี่ยนเป็น "AGGREGATION (1 HR)"** ([Dashboard.tsx:1025](src/components/dashboard/Dashboard.tsx:1025)) — ผู้ใช้เข้าใจผิดว่า dropdown นี้ (Raw/Avg/Max/Min/First/Last) ซ้ำซ้อนกับ automatic min/max decimation ที่มีอยู่แล้ว เกือบขอให้ลบทิ้ง หลังอธิบายว่าเป็นคนละกลไก (aggregation ทำงาน**ก่อน** decimation เสมอ ดู [chart_query.rs:548-565](src-tauri/src/chart_query.rs:548) — ไม่ใช่หลังตามที่เข้าใจ) จึงตกลงแค่เปลี่ยนชื่อ label ให้สื่อความหมายชัดขึ้นแทนการลบฟีเจอร์ — แก้แค่ text ที่โชว์ผู้ใช้ ไม่แตะชื่อตัวแปร/type ภายใน (`samplingMethod`, `ChartSamplingMethod` ยังคงชื่อเดิม) `tsc --noEmit` ผ่าน

**🆕 แก้ "Save & Continue รู้สึกเหมือนเปิด window ใหม่"** — ไม่ใช่ปัญหา transition/CSS อย่างที่คิดตอนแรก แต่เป็นเพราะ `handleContinue` ([DataUploadPage.tsx:262](src/components/upload/DataUploadPage.tsx:262), flow "สร้างโปรเจกต์ใหม่ → Prepare dataset → Save & Continue") ไม่เคยเรียก `setLoadingWorkspace(true)` เลย ต่างจาก `handleLoadWorkspace` (flow "เปิด recent workspace" ที่รู้สึกลื่น) ที่เปิด full-screen loading overlay (blur backdrop + spinner "Loading workspace...") คั่นไว้เสมอ — เพิ่ม `setLoadingWorkspace(true)` + `try/catch` (reset `false` + โชว์ error ถ้า `saveWorkspaceData` fail) ให้ `handleContinue` แบบเดียวกัน — **ทดสอบแล้วยังไม่รู้สึกต่าง** เพราะ `saveWorkspaceData` เร็วมาก (<50ms) overlay กระพริบผ่านจนไม่ทันสังเกต

🆕 **แก้รอบ 2**: เพิ่ม CSS fade-in (`app-page-transition` class, `App.tsx`/`App.css`, ใช้ keyframe `fadeIn` ที่มีอยู่แล้ว) ครอบการสลับ Import↔Dashboard ทั้งสองทิศทาง — **ทดสอบแล้วผู้ใช้บอกยังรู้สึกเหมือนเดิม** (ส่ง screenshot 3 ภาพเทียบ Prepare-dataset / Dashboard / Failure-Group ให้ดู) 0.2s fade เบาเกินไปจนไม่รู้สึก

🆕 **แก้รอบ 3**: เพิ่ม `MIN_TRANSITION_MS = 500` ใน `handleContinue` ([DataUploadPage.tsx:262](src/components/upload/DataUploadPage.tsx:262)) — ใช้ `Promise.all([saveWorkspaceData(state), delay(500ms)])` บังคับ overlay "Loading workspace..." ให้อยู่อย่างน้อย 500ms เสมอ เลียนแบบ flow "เปิด recent workspace" ที่รู้สึกลื่นอยู่แล้วเพราะ re-parse CSV กินเวลาจริงหลักร้อย ms (ของ fade CSS รอบ 2 ยังเก็บไว้ ทำงานร่วมกันได้) — `tsc --noEmit` ผ่าน **ยังไม่ได้ทดสอบรอบนี้**

**🆕🆕 ใหญ่: "Failure Group" ตอนนี้เป็น panel ที่ 5 ในตัว Dashboard เอง (ไม่ใช่แค่ window แยกอีกต่อไป)** — ผู้ใช้ประกาศแผน **redesign รอบใหญ่** (จะทยอยบอก requirement เพิ่มทีละส่วน) จุดแรกคือย้าย "Create Failure Group" เข้ามาเป็นส่วนหนึ่งของ Dashboard แบบถาวร ไม่ต้องเปิด window แยกอีก — **เฉพาะส่วน "สร้าง/จัดการ failure group" เท่านั้น ยังไม่รวม "Build Model"** (ผู้ใช้บอกจะแจ้ง requirement นั้นแยกทีหลัง) วางแผนผ่าน EnterPlanMode ก่อนเริ่ม (แผนอยู่ที่ `C:\Users\toey_\.claude\plans\vast-bubbling-phoenix.md` ถ้าต้องอ้างอิง) ผู้ใช้เลือกให้เป็น **quadrant จริงในกริด ขนาดเท่าๆ กับ Sensors panel** (ไม่ใช่ overlay/modal ลอยทับ) รายละเอียดที่แก้:

- `types.ts`: `DashboardSlot` เพิ่ม `'right-mid'`, `DashboardPanel` เพิ่ม `'failure-group'`, `DashboardLayoutSizes.rightRows` ขยายจาก 2-tuple เป็น 3-tuple (`[sensors%, failure-group%, filter%]`, default `[34,33,33]`)
- `Dashboard.tsx`: ขวาคอลัมน์ขยายจาก 2 แถวเป็น 3 แถว (Sensors / Failure Group / Filter) — `PANELS`/`SLOT_LAYOUT`/`RIGHT_SLOTS` เพิ่ม entry ใหม่, เพิ่ม `slotRMRef`/`rmCollapsed`, **split.js effect ของขวาคอลัมน์เขียนใหม่ให้ generic รองรับ 0/1/2+ collapsed** (0 collapsed → 3-way split, 1 collapsed → 2-way split เฉพาะคู่ที่ยัง visible โดย renormalize % ให้รวม ~100, 2+ collapsed → ไม่ split เลยปล่อยให้ CSS flex:1 ทำงาน) — collapsed-tabs sidebar (ปุ่มลอยฝั่งซ้ายที่โชว์ FILTER/PICKUP SENSOR ตอน panel ถูกซ่อน) **ไม่ต้องแก้อะไรเลยเพราะ generic รองรับ N panels อยู่แล้ว**
- state ใหม่: `failureGroups`/`failureRows` (init จาก `initialState?.failureGroupState`, default `[{no:0,name:"Not in Group",...}]`) ผูกเข้า `buildWorkspaceState`/autosave effect ที่มีอยู่แล้ว (ผ่าน `failureGroupState` field ใน `WorkspaceState` ที่มีอยู่แล้วตั้งแต่เดิม ไม่ต้องแก้ schema)
- **ไฟล์ใหม่ [`src/components/dashboard/FailureGroupPanel.tsx`](src/components/dashboard/FailureGroupPanel.tsx)** — พอร์ตมาจาก `FailureGroupCreation.tsx` (window เดิม, 1165 บรรทัด) เฉพาะส่วน group/row CRUD + CSV upload/download/save (ตัด Build Model, PM window-spawn, window-lifecycle handlers ทิ้งหมด) เป็น controlled component (`groups`/`rows`/`onGroupsChange`/`onRowsChange`/`allSensors`/`sensorMetadata` props) — Inspector panel (แก้ Concept Sensor/Model Type/Notes/Mapped Sensor Tag) ทำเป็น**แถบเลื่อนลงมาด้านล่าง** แทนการ split 70:30 แนวนอนแบบ window เดิม เพราะพื้นที่แคบเกินไป — ปุ่ม "Build Model" **ไม่มีในนี้เลย**
- `App.css`: เพิ่ม `.fg-panel-embedded`/`.fg-inspector-embedded` (wrapper ใหม่สำหรับบริบทแคบ) ส่วน class อื่นๆ (`.fg-group-card`, `.fg-sensor-chip`, `.fg-inspector-field` ฯลฯ) **reuse ของเดิมทั้งหมด** เพราะเป็น top-level selector ไม่ได้ scope ผูกกับ window เดิม

**สถานะ**: ปุ่ม "Create Failure Group" เดิมบน Dashboard ที่เปิด window แยก **ยังอยู่เหมือนเดิม ไม่ได้ลบ** (คนละเรื่องกับ panel ใหม่นี้ — ยังไม่ได้ตัดสินใจว่าจะเอาออกไหม รอผู้ใช้บอก) `tsc --noEmit` ผ่านสะอาดทุกไฟล์ **ยังไม่ได้ทดสอบบนแอปจริงเลย** — ต้องเช็ค: panel โผล่ขนาดเท่า Sensors จริงไหม, สร้าง/ลบ/rename group ได้ไหม, เพิ่ม/ลบ sensor row ได้ไหม, collapse panel แล้วโผล่ใน sidebar ซ้าย + restore ได้ไหม, ลาก gutter ทั้ง 3 เส้นในคอลัมน์ขวาแล้ว persist ข้าม reload ไหม, CSV upload/download/save round-trip ถูกไหม

**🔙 REVERTED**: ผู้ใช้ลองแล้วไม่ถูกใจ ("มันไม่ถูกใจผมเท่าไหร่") ก่อนจะได้ทดสอบบนแอปจริงด้วยซ้ำ — บอกจะลอง design ใหม่เองแล้วค่อยแจ้ง requirement มาอีกที **revert กลับสู่สภาพก่อนเริ่ม feature นี้ 100%**: `types.ts` (`DashboardSlot`/`DashboardPanel`/`DashboardLayoutSizes.rightRows` กลับเป็น 4 slot/2-tuple เดิม), `Dashboard.tsx` (คอลัมน์ขวากลับเป็น 2 แถวเดิม, split.js effect กลับเป็นเวอร์ชัน 2-panel เดิม, ลบ state/import/render function ที่เกี่ยวข้องหมด), ลบไฟล์ `FailureGroupPanel.tsx` ทิ้ง, ลบ CSS block `.fg-panel-embedded`/`.fg-inspector-embedded` ออกจาก `App.css` — grep ยืนยันไม่มีเศษโค้ด (`FailureGroupPanel`/`failureGroups`/`failureRows`/`right-mid`/`rmCollapsed`) เหลือเลยสักจุด `tsc --noEmit` ผ่านสะอาด — **แผน redesign เดิมยังบันทึกไว้ที่ `C:\Users\toey_\.claude\plans\vast-bubbling-phoenix.md` เผื่ออยากเอากลับมาดูเป็น reference ตอนออกแบบใหม่** — รอ requirement ใหม่จากผู้ใช้ก่อนเริ่มงานต่อ

**🆕 แก้สีกราฟซ้ำ/ใกล้เคียงกันเวลาเลือกหลาย sensor** — `defaultSensorColor` เดิม ([LineChart.tsx:23](src/components/charts/LineChart.tsx:23)) hash ชื่อ sensor tag เพื่อสุ่มสีจาก palette 6 สี ทำให้ (1) sensor หลายตัวชน hash กันได้สีซ้ำเป๊ะ และ (2) แม้ไม่ชน ก็มีโอกาสได้สีโทนน้ำเงิน-ม่วง 3 สีที่ใกล้เคียงกันมาก (`#3b82f6`/`#6366f1`/`#8b5cf6`) แยกด้วยตาบนเส้นกราฟบางๆ ยาก — ผู้ใช้ส่ง screenshot มาชี้ปัญหานี้ตรงๆ (เลือก 4 sensor เห็นแค่ 2 สีในกราฟ) แก้โดยเพิ่ม `resolvedSensorColors` (`useMemo`) ใน `Dashboard.tsx` — คำนวณ default color จาก **ตำแหน่งใน `selectedSensors`** (ลำดับนิ่ง ไม่สลับตอน hide/show) แทน hash ชื่อ รวมกับสีที่ user override ผ่าน pipette แล้วส่ง map สำเร็จรูปเดียวกันให้ทั้ง `<Chart>` และ Selected Sensor tab ใช้ตรงกัน — การันตีไม่มีสีซ้ำกันจนกว่าจะเลือกเกิน 6 sensor พร้อมกัน (ข้อจำกัดตาม palette ขนาด 6 สี) `defaultSensorColor` ยังเก็บไว้เป็น fallback สุดท้ายเผื่อกรณี edge case `tsc --noEmit` ผ่าน

**🆕 ลบปุ่ม "Clear selection" ที่ซ้ำซ้อนออกจาก `SensorSelection.tsx` แล้ว** — ปุ่มนี้ (อยู่บน Sensor panel ฝั่งขวา เหนือ component tree) เรียก `onSensorChange([])` ซึ่งเคลียร์ `selectedSensors` ตัวเดียวกับปุ่ม "Clear all" ใน Selected Sensor tab (`Dashboard.tsx`) ที่เพิ่มไปก่อนหน้านี้แล้ว — ผู้ใช้สังเกตเห็นว่าซ้ำกัน เลยลบปุ่ม + handler `handleClearSelection` + import ไอคอน `X` ที่ไม่ใช้แล้วออก เหลือแค่ข้อความ "N sensors selected" แสดงจำนวนเฉยๆ ไม่มีปุ่ม — `tsc --noEmit` ผ่าน

**🆕 ตำแหน่ง popup ของทั้ง color picker และ Y-axis fix-scale editor ย้ายไปชิดขวาแล้ว** (`Dashboard.tsx`) — เดิมทั้งคู่ชิดซ้าย (`padding-left: 42px` ใต้ checkbox) ไม่ตรงกับตำแหน่งไอคอนที่กดเปิด (pipette/line-chart อยู่ขวาสุดของแถว) ผู้ใช้ขอให้ทั้งสอง popup ลอยไปทางขวาใต้กลุ่มไอคอนแทน แก้ด้วย wrapper `display:flex; justifyContent:'flex-end'` ทั้งสองจุด (เดิม axis editor เคยลองสลับเป็นขวาอย่างเดียวก่อนแล้วโดนขอให้กลับซ้ายรอบนึง แต่สุดท้ายสรุปว่าให้ทั้งคู่ขวาเหมือนกัน — ผู้ใช้ยอมรับว่า diagram/ลูกศรที่ส่งมารอบแรกทำให้เข้าใจผิด) — `tsc --noEmit` ผ่าน ยังไม่ได้เทสต์บนแอปจริง

**🆕 Scatter Plot มี axis-scale pin เป็นของตัวเอง** (`ScatterChart.tsx`) — ปุ่ม Ruler ในกล่อง toolbox เปิดแผงตั้ง min/max ของแกน X และ Y แยกกัน (ใส่แค่ด้านเดียวได้ ฝั่งที่ไม่ใส่ auto-fit ต่อ) เป็น state **ของ ScatterChart เอง** ไม่ยุ่งกับ `sensorColors`/`sensorAxisRange` ของ Line เลย pin ผูกกับ "เซนเซอร์ที่กำลังอยู่บนแกนนั้น" (ไม่ใช่ตำแหน่งแกนเฉยๆ) ถ้าสลับ dropdown X/Y เป็นเซนเซอร์อื่น pin จะไม่ตามไปทับผิดตัว — ผ่านการรีวิว 2 รอบ (agent คู่ขนาน + ตรวจเอง) เจอและแก้บั๊กจริง 5 จุด: flicker ตอนสลับเซนเซอร์, `Infinity` หลุดผ่านการ validate, ช่องกรอกไม่ sync ตอนสลับเซนเซอร์ระหว่างเปิดแผงค้าง, แกนกลับด้านเมื่อ pin ข้างเดียวเกินขอบเขตจริง, กล้อง zoom/pan ไม่ sync กับสเกลใหม่ (แก้ด้วย `sc.reset()` ทุกครั้งที่วาดใหม่ — แก้ปัญหานี้ให้ตอนสลับเซนเซอร์ธรรมดาด้วย)

**⚠️ Browser preview ใช้ตรวจ UI ของแอปนี้ไม่ได้เลย** — เคยลองเปิด `http://localhost:1420` (Vite dev server) ผ่าน browser tab ธรรมดา แต่แอป crash ตั้งแต่ root เพราะ `TitleBar.tsx` เรียก Tauri's `getCurrentWindow()` ทันทีที่ mount โดยไม่มี Tauri IPC bridge ให้ (ไม่ใช่แค่บางฟีเจอร์ที่พังนะ — **ทั้งแอปไม่ขึ้นเลย**) ต้องทดสอบผ่านหน้าต่าง `npm run tauri dev` จริงเท่านั้น

---

## 7. สถานะ Git (แก้ใหม่ทั้งหมด 2026-08-06 — ของเดิมผิดไปแล้ว)

**✅ Commit + push แล้ว** เมื่อ 2026-08-06 — เดิม section นี้เขียนไว้ว่า "ยังไม่มีอะไร push หรือ commit เลย" ซึ่งจริงตอนเขียนครั้งแรก (16 ก.ค.) แต่ผิดไปแล้วตอนนี้ **อย่าเชื่อบรรทัดนี้เกิน 1 วันโดยไม่เช็ค `git log`/`git status` เองก่อน**:

- Commit เดียวรวมทุกอย่าง: `6dfb269` — "Fold Failure Group Creation into Dashboard; fix several UI bugs; git hygiene" (50 ไฟล์, รวมงานสะสมของหลาย session ตั้งแต่ก่อน 16 ก.ค. จนถึงงาน Failure Groups migration + ลบปุ่ม Save As วันนี้)
- Push ไปที่ remote **`personal`** (`https://github.com/toeyps/soothsayer-wizard-app.git`) branch `main` — เป็น branch ใหม่บน repo ว่างเปล่า (ยังไม่เคย push มาก่อน)
- Repo นี้มี **2 remotes**: `origin` (`https://github.com/Alpha-Com-Thailand/Soothsayer-wizard-app.git` — org repo, local `main` ยัง track อยู่กับตัวนี้โดย default) กับ `personal` (ของผู้ใช้เอง toeyps) — **`git push` เฉยๆ (ไม่ระบุ remote) จะไปที่ `origin` ไม่ใช่ `personal`** ถ้าต้องการ push ไป personal ต้องพิมพ์ `git push personal main` ให้ครบทุกครั้ง (หรือถามผู้ใช้ว่าต้องการตั้ง tracking ใหม่ไหม)
- ก่อน commit ต้องตั้ง git identity ก่อน (เครื่องนี้ไม่เคยตั้งมาก่อนเลย ต้องให้ผู้ใช้รัน `git config --global user.name/user.email` เอง — ห้าม Claude ไปแก้ git config เองเด็ดขาดตาม Git Safety Protocol)
- ไฟล์แปลกปลอมที่เคยพูดถึงใน §8 ข้อ 3 เดิม (`src-tauri/2`, `check_output.txt`, `nuitka-crash-report.xml`, `.claude/settings.local.json`, `.claude/worktrees/*`) **ถูกลบออกจาก git แล้วใน commit นี้** (ยังอยู่ในเครื่อง local แต่ gitignore ไว้แล้ว ไม่ track อีกต่อไป)

---

## 8. จุดที่ควรระวังตอน handover (พบจากการตรวจโค้ดจริง — อัปเดตสถานะแล้ว)

1. ~~**`README.md` ล้าสมัย**~~ → **✅ แก้แล้ว 2026-08-06** — เขียนใหม่ทั้งไฟล์ให้ตรงสภาพปัจจุบัน (features, sub-window list ที่ถูกต้อง, setup/run/test commands ที่ตรวจกับ `package.json` จริง) commit ไปพร้อมกับ README fix รอบนี้ (ดู entry ท้ายสุดของเอกสารนี้)

2. **ข้อขัดแย้งเรื่อง auto-resume ยังไม่ได้แก้**: `CLAUDE.md` เขียนว่า auto-resume เป็น hard requirement แต่คอมเมนต์จริงใน `src/App.tsx` บอกว่า "No auto-resume" — ผู้ใช้ต้องเลือกเองจาก **step 0 "Open recent project"** (จุดใหม่ที่แทนที่ sidebar เดิมในหน้าแรก) ยังไม่ได้ยืนยันกับทีมว่าอันไหนคือพฤติกรรมที่ตั้งใจ

3. ~~**ไฟล์แปลกปลอมที่ commit เข้า git ยังอยู่**~~ → **✅ แก้แล้ว** — `src-tauri/2`, `src-tauri/check_output.txt`, `src-tauri/nuitka-crash-report.xml` ลบออกจาก git แล้ว (commit `6dfb269`) และเพิ่มเข้า `.gitignore` กันไม่ให้กลับมาอีก

4. **Sidecar binary compile แล้วบนเครื่องนี้** — `src-tauri/bin/backend-x86_64-pc-windows-msvc.exe` มีอยู่แล้ว (ไม่ track ใน git ตามปกติ) แต่**เครื่องอื่นต้อง build เองใหม่** ตามขั้นตอนใน §4

5. **CSP กับ PDF export**: production CSP มี `'unsafe-eval'` ใน `script-src` เพราะ react-pdf's yoga-layout compile WASM runtime — ถ้าจะ tighten CSP ในอนาคต ต้องเทสต์ export บน **installed build จริง** เพราะ dev mode ใช้ `devCsp` ที่หลวมกว่าและจะไม่เจอบั๊กนี้

6. **ห้ามขยาย CSP หรือ fs scope โดยไม่ขออนุมัติ** — fs plugin scope เฉพาะ `$APPDATA`; เขียนไฟล์ path ที่ผู้ใช้เลือกเอง (CSV/PDF/PNG export) ต้องผ่าน Rust command `write_user_file` เท่านั้น

7. **"LinearGAM" ห้ามโผล่ในข้อความที่ผู้ใช้เห็น** — UI ต้องเรียกว่า "Relation model" เสมอ (ชื่ออัลกอริทึมเป็นความลับทางธุรกิจ) — คอมเมนต์โค้ดพูดได้

8. **IPC arg-key casing เป็นจุดพังเงียบอันดับ 1**: TS ส่ง key แบบ snake_case (`max_points`) แต่ Tauri v2 macro default เป็น camelCase → command ใหม่ที่มี multi-word parameter **ต้องมี** `#[tauri::command(rename_all = "snake_case")]` ไม่งั้น invoke จะ reject แบบเงียบๆ

9. **Agent zone ownership** — sub-agent (`fe-ui-agent`, `fe-logic-agent`, `rust-agent`, `qa-agent`, `pm-agent`) นิยามไว้ใน `.claude/agents/` แต่**ใช้ไม่ได้ในเซสชันปัจจุบัน**เพราะ path ของโปรเจกต์เปลี่ยนกลางเซสชัน (harness ผูก registry กับ path ตอนเริ่ม) — ต้องเปิด session ใหม่จาก path `C:\00_DATA\Soothsayer-wizard-app` ถึงจะ scan `.claude/agents/` ใหม่และเรียกใช้ได้

10. ~~**หนี้ test ค้างจำนวนมาก**~~ → **✅ แก้แล้ว 2026-08-10** — เดิมยังไม่มีเทสต์เลยสำหรับ `SensorSelection.tsx`, `FilterPanel.tsx`, Dashboard's Selected Sensor tab — ตอนนี้เขียนเทสต์ครบทั้งโปรเจกต์แล้ว (frontend 97→**586 เทสต์/45 ไฟล์**, Rust unit 98→**139 เทสต์**) รวมไฟล์ที่เคยไม่มีเทสต์เลยทุกไฟล์ ดูรายละเอียดที่ entry ท้ายเอกสาร — ข้อยกเว้นเดียวที่ยังทำไม่ได้คือ command ที่ใช้ `State<AppState>` โดยตรง (ติด Windows Application Control policy บนเครื่องนี้ ไม่ใช่หนี้ที่เลี่ยงได้ง่ายๆ)

11. ~~**สี/Y-axis scale ต่อ sensor ไม่ persist**~~ → **✅ แก้แล้ว 2026-08-06** — เป็น Class B point ที่ 1-2 ใน `docs/PERSISTENCE_PLAN.md`, แก้พร้อมกับ Scatter axis pin และค่า shortcut Y/M/W/D/H ในรอบเดียวกัน (ดู entry "แก้ Class B ทั้ง 4 จุด" ด้านล่าง) — จุดนี้เคยลืมตัดออกจากลิสต์นี้ตอนแก้เสร็จ แก้ไขให้ตรงแล้วรอบนี้

12. **Repo มีเอกสารเก่าอื่นๆ ที่ควรอ่านคู่กัน**: `docs/requirements.md`, `docs/requirements-failure-group.md`, `docs/requirements-rust-port-hybrid.md`, `docs/task.md` (task breakdown — Feature 1 Data Upload Page Phase 1-2 เสร็จ, Phase 3 testing ยัง pending — **ไม่รวมงาน step-flow/Dashboard รอบล่าสุดที่ทำในเซสชันนี้ เพราะไม่ได้ผ่าน pm-agent**), `docs/contracts/*.md`, และ `multi_agent_orchestration_design.md` ที่ root

---

## 9. บันทึกปัญหาเครื่อง dev + วิธีแก้ (เจอจริงตอนตั้งเครื่องนี้ครั้งแรก)

เครื่องนี้ไม่เคย build Rust ได้เลยจนกว่าจะไล่แก้ปัญหาต่อไปนี้ทีละอัน — เก็บไว้กันคนต่อไปเจอซ้ำ:

1. **`cargo`/`cargo check` fail ด้วย `"An Application Control policy has blocked this file. (os error 4551)"`**
   สาเหตุ: **Kaspersky Plus's Intrusion Prevention** จัดไฟล์ .exe ที่เพิ่ง compile ใหม่ (ไม่มีลายเซ็นดิจิทัล) เข้ากลุ่ม "Low Restricted" แล้วบล็อกการรัน
   วิธีแก้: Kaspersky Plus → Security settings → Advanced protection → Intrusion Prevention → Manage resources → แท็บ **Exclusions** → เพิ่มโฟลเดอร์โปรเจกต์ทั้งหมด + `%USERPROFILE%\.cargo` + `%USERPROFILE%\.rustup` → **Save**
   ข้อควรรู้: การเพิ่ม exclusion แบบนี้แก้ปัญหา "เขียนไฟล์ไม่ได้" ระหว่าง build ได้ แต่**ไม่ครอบคลุมการรัน .exe ที่ build เสร็จแล้ว** — ไฟล์ที่เคยโดนบล็อกไปแล้วยังต้องลบทิ้งให้ compile ใหม่ (fresh file ถึงจะผ่าน exclusion) ส่วนการ "รัน" ไฟล์ที่ build เสร็จ (เช่น `tauri-app.exe`) ต้องไปที่ **Manage applications** แทน ไม่ใช่ Manage resources

2. **`cargo build` fail ด้วย `"output path is not a writable directory"`** (error จาก `autocfg` เวลา compile `indexmap`)
   สาเหตุ: โปรเจกต์อยู่ใน **OneDrive-synced folder** — OneDrive ล็อกไฟล์ที่ cargo สร้าง/ลบรัวๆ ระหว่าง build
   วิธีแก้ที่ใช้ตอนนั้น (ไม่จำเป็นแล้วหลังย้าย path): redirect `target-dir` ออกไปนอก OneDrive ผ่าน `.cargo/config.toml` (gitignored, machine-local)
   **วิธีแก้ถาวรที่ใช้จริงตอนนี้**: ย้ายทั้งโปรเจกต์ออกจาก OneDrive ไปที่ `C:\00_DATA\Soothsayer-wizard-app` แทน — เลิกใช้ workaround ทั้งหมดแล้ว (ลบ `.cargo/config.toml` ออกแล้ว, target-dir กลับไปเป็น default `src-tauri/target`)

3. **PowerShell (Windows PowerShell 5.1 ตัวเดิมของเครื่อง) รัน `npm run tauri dev` ไม่ได้**: `"running scripts is disabled on this system"`
   สาเหตุ: PowerShell execution policy default (`Restricted`) บล็อก `.ps1` script ทุกตัวรวมถึง `npm.ps1`
   วิธีแก้: `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` (รันครั้งเดียวต่อ user account)

4. **Python จริงอยู่คนละที่กับที่ `python` บน PATH ชี้ไป** — `python`/`py` บน PATH เป็นแค่ Windows Store alias (ใช้งานไม่ได้จริง) ส่วน Python ตัวจริง (ติดตั้งผ่าน Python install manager ใหม่) อยู่ที่ `%LOCALAPPDATA%\Python\bin\python.exe` — ต้องเรียก path เต็มตรงๆ หรือผ่าน `py` launcher (`py -3.14` เป็นต้น)

**สรุปโครงสร้าง exclusion ของ Kaspersky ที่ตั้งไว้ตอนนี้** (Intrusion Prevention → Manage resources → Exclusions): `C:\00_DATA\Soothsayer-wizard-app`, `%USERPROFILE%\.cargo`, `%USERPROFILE%\.rustup` — path เก่าที่เคย exclude ไว้ตอนอยู่ใน OneDrive (`C:\rust-builds`, OneDrive path เดิม) ไม่จำเป็นแล้ว ปล่อยทิ้งไว้ได้ไม่มีผลเสีย

---

## 10. สถานะ repo ล่าสุด (อ้างอิงจาก git log + git status ณ 2026-07-16)

- `main` @ v0.2.1 (`6ed6559`…`cb6d105`) คือ branch หลัก — มีงาน RAM optimization ครบ 2 ขั้น: columnar store (PR #1) + bounded chart pipeline/error reporting (`3620eb6`)
- Step 3 (disk-backed store เช่น DuckDB/mmap Arrow) วางแผนไว้เฉพาะถ้า RAM ยังไม่พอหลัง step 2 — ยังไม่เริ่ม
- Branch `feat/columnar-ram-opt` และ `claude/gifted-kepler-*` ถูก merge/superseded แล้ว — ไม่ควร base งานใหม่จากตรงนั้น
- **🆕 มีงาน UI/UX รอบใหญ่ที่ทำในเซสชันนี้ยังไม่ commit เลย** (13 ไฟล์แก้ + `docs/PROJECT_HANDOVER.md` + `src/hooks/useSensorMetaMap.ts` ใหม่) ครอบคลุม: DataUploadPage 3-step flow, SensorSelection group-by-component, Dashboard Selected Sensor tab (hide/color/pin-scale/remove + ตอนนี้แยกตาม chart type แล้ว), relative time-range picker, FilterPanel sensor labels, collapsed-sidebar positioning fix, Scatter Plot axis-scale pin (`ScatterChart.tsx`) — **ควร commit เป็นชุดๆ ตามฟีเจอร์ก่อนเริ่มงานถัดไป** เพื่อไม่ให้ diff ใหญ่เกินจน review ยาก
- **🆕 ผู้ใช้ทำงานสลับ 2 เครื่อง** — Claude Code ไม่มี session history sync ข้ามเครื่อง (เก็บ local ที่ `~/.claude/projects/...` ต่อเครื่อง) เอกสารนี้เลยถูกอัปเดตอัตโนมัติทุกครั้งที่ทำงานเสร็จแทน chat history — **ต้อง commit+push ไฟล์นี้เองด้วยถ้าอยากให้เครื่องที่ 2 เห็น** (ยังเป็น untracked file อยู่ตอนนี้)
- **🆕 2026-08-03 — App flow → Figma SVG mockups (6 ไฟล์)**: สร้าง SVG mockup จำลองหน้าจอหลักของแอปเพื่อ import เข้า Figma แล้วแก้ไขต่อได้ (ผู้ใช้ระบุ requirement: "ไฟล์อะไรก็ได้ที่ import เข้า figma แล้วแก้ไขใน figma ได้" — เลือก SVG เพราะ Figma import เป็น editable vector layer ได้ตรงๆ ไม่มี tool สร้าง `.fig` จริงหรือเรียก Figma API ในเซสชันนี้). ไฟล์ไม่ใช่ export จริงจากแอป (Browser preview render local SVG ได้แค่ static snapshot ในเซสชันนี้ ไม่สามารถ screenshot local Tauri app ได้) แต่ประกอบจาก layout/สี/ข้อความจริงที่อ่านโค้ดต้นฉบับ (`Dashboard.tsx`, `FailureGroupCreation.tsx`, `PredictiveModelBuild.tsx`, `DataUploadPage.tsx`) + screenshot ที่ผู้ใช้ส่งมาตลอด session ทั้งหมดอยู่ที่ **`docs/figma/`**:
  - Wizard onboarding (3 steps): `wizard_step0_get_started.svg`, `wizard_step1_create_project.svg`, `wizard_step2_prepare_dataset.svg`
  - `dashboard_analysis.svg` — หน้า Dashboard 4-panel (Chart / Control Chart / Sensors / Filter) + ปุ่ม Create Failure Group
  - `failure_group_creation.svg` — หน้าต่าง Failure Group (toolbar, progress strip, group cards + sensor chips, inspector panel + Build Model)
  - `predictive_model_build.svg` — หน้าต่าง Predictive Model (command bar, target/predictor/filter ซ้าย, Individual+Relationship/Clustering chart กลาง, model config ขวา)

  ยังเป็น untracked ต้อง `git add`/commit เอง
  - **🆕 2026-08-03 — Figma MCP connect + Assign sensor to Failure Group จาก Dashboard (real feature)**: ผู้ใช้ต่อ Figma MCP สำเร็จ (official Dev Mode MCP server ของ Figma เอง ผ่าน team plan ที่มี Full seat — ไม่ใช่ Free plan ธรรมดา ตอนแรกลองใช้ local community MCP (`figma-developer-mcp` + PAT) ไปด้วยแต่ลบทิ้งแล้วหลัง official ใช้ได้จริง) ใช้ดึง screenshot จริงของหน้า Dashboard/Failure Group Creation จาก Figma มาช่วยออกแบบ — ผู้ใช้ระบุปัญหาจริง: วิเคราะห์เซนเซอร์ใน Dashboard แล้วต้องมาเลือกใหม่ทั้งหมดตอนสร้าง Failure Group เพราะสองหน้าไม่คุยกัน — ออกแบบ + validate ด้วย interactive mockup ในแชทก่อน แล้ว implement จริงหลังผู้ใช้ approve (แผนอยู่ที่ `C:\Users\toey_\.claude\plans\vast-bubbling-phoenix.md` เขียนทับแผนเก่าที่ revert ไปแล้ว):
    - **Scope ที่ confirm กับผู้ใช้**: v1 ทำแค่ per-row (ไม่ทำ bulk-select เพราะ checkbox เดิมใน Selected Sensor tab ใช้ความหมาย "แสดง/ซ่อนบนกราฟ" อยู่แล้ว ชนกัน) และเฉพาะแท็บ **Selected Sensor** เท่านั้น (ไม่ทำที่ Sensor picker 132 ตัว)
    - **`Dashboard.tsx`**: เพิ่ม state `fgGroups`/`fgRows` (reuse `FailureGroup`/`FailureSensorRow`/`FailureGroupStateSlice` เดิมจาก types.ts ที่ `FailureGroupCreation.tsx` ใช้อยู่แล้ว ไม่ต้องเพิ่ม type ใหม่), helper `commitAssignment`/`handleAssignExisting`/`handleCreateGroup`/`handleRemoveFromGroup` เขียนผ่าน `updateWorkspaceData` (read-modify-write) ไม่ใช่ autosave เดิมของ Dashboard (full-overwrite) เพื่อไม่ให้ชนกับ FG window ที่เปิดพร้อมกัน — เพิ่มปุ่ม `FolderPlus` (คลิก + คลิกขวา) ต่อแถวใน Selected Sensor tab เปิด inline panel (สไตล์เดียวกับ color-picker/axis-editor เดิม) ให้เลือกกลุ่มที่มีอยู่/สร้างใหม่/เอาออกจากกลุ่ม พร้อม badge สีแสดงกลุ่มที่ assign ไว้แล้ว (reuse CSS class `.fg-group-color-*`/`.fg-group-dot` เดิมจาก App.css ให้สีตรงกับหน้า FG เป๊ะ)
    - **`FailureGroupCreation.tsx`**: เพิ่ม listener `failure-group-updated` (mirror pattern เดียวกับ `workspace-renamed-internal` เดิม) reload จาก disk เมื่อ Dashboard assign ขณะ FG window เปิดค้างอยู่
    - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual-test ผ่าน `npm run tauri dev` จริง** (Browser preview ใช้กับแอปนี้ไม่ได้) รอผู้ใช้ทดสอบเองแล้ว confirm ตาม verification steps ในแผน
    - **🐛 บั๊กที่เจอตอน manual test รอบแรก + แก้แล้ว**: assign สำเร็จ (badge ขึ้นถูก) แต่กด "Create Failure Group" แล้วข้อมูลหาย — เช็คไฟล์ workspace จริงบนดิสก์ (`%APPDATA%\com.prompt-solution.tauri-app\workspaces\*.json`) พบว่า `failureGroupState` หายไปทั้ง key เลย สาเหตุคือ Dashboard มี autosave เดิม (`saveWorkspaceData(buildWorkspaceState())`, full-overwrite, ไม่ debounce) ที่ตอนแรกตั้งใจไม่ให้รู้จัก `fgGroups`/`fgRows` เพื่อกันชนกับ FG window แต่ผลข้างเคียงคือพอ autosave เดิมทำงานด้วยเหตุผลอื่น มันเขียนทับไฟล์ด้วยข้อมูลที่ไม่มี `failureGroupState` เลย — **แก้โดยเพิ่ม `failureGroupState: { groups: fgGroups, rows: fgRows }` เข้าไปใน `buildWorkspaceState()`'s return object ด้วย** (ใช้ค่าล่าสุดจาก state เสมอ ไม่ใช่ `initialState` ตอน mount) ทำให้ autosave ตัวไหนทำงานก็เขียนข้อมูลถูกต้องเหมือนกันหมด — เจอเคสนี้เพราะ manual test จริงเท่านั้น ไม่มีทาง unit-test/tsc จับได้ เป็นบั๊ก logic ไม่ใช่ type
    - **⚠️ เจอ (ยัง unconfirmed ว่าเกี่ยวกับโค้ดนี้หรือเป็นของเดิม)**: กด "Back to Dashboard" ใน FG window แล้วไปหน้า "Get Started" ของ Wizard แทนที่จะกลับ Dashboard — โค้ดที่เกี่ยวข้องคือ `FailureGroupCreation.tsx:681-716` (`handleBackToDashboard`): ถ้า `WebviewWindow.getByLabel('main')` หา window ไม่เจอ จะ spawn window ใหม่ที่ `url: '/'` (หน้า Get Started) แทน — เป็น fallback ที่ตั้งใจไว้อยู่แล้ว (pre-existing code ไม่ได้แก้อะไรตรงนี้) คำถามคือทำไม `main` window หาไม่เจอ — ขอให้ผู้ใช้ restart dev server สะอาดแล้วลองใหม่ ยังไม่ได้รับ confirm ว่าแก้ไปแล้วหรือยังเป็นอยู่ — **ต้องติดตามต่อ**
    - **🔄 Design revision รอบ 2 (หลัง review Figma mockup ของผู้ใช้เอง)**: ผู้ใช้ขอย้าย UI จาก Selected Sensor tab ไปแท็บ **Sensor** (ตัวเลือก 133 ตัว) แทน เพราะการผูกกับ "กำลัง plot กราฟอยู่" ทำให้ถ้า unplot สถานะ FG จะดูไม่ออก อีกอย่างคือ **1 เซนเซอร์ต้องอยู่ได้หลายกลุ่มพร้อมกัน** (ของเดิม 1 เซนเซอร์ = 1 กลุ่มเท่านั้น) — validate ด้วย interactive mockup แบบ multi-select checkbox toggle ก่อน แล้วจึง implement จริง:
      - ลบปุ่ม/badge/panel ออกจาก Selected Sensor tab ทั้งหมด (revert กลับไปโค้ดเดิม)
      - ย้าย logic ไป `SensorSelection.tsx` แทน — เพิ่ม props ใหม่ `fgGroups`, `fgRows`, `getGroupColor`, `onToggleSensorGroup`, `onCreateGroupForSensor` (state `groupMenuFor`/`newGroupDraft` ก็ย้ายไปเป็น local state ใน `SensorSelection.tsx` ด้วย เพราะ UI อยู่ที่นั่นแล้ว)
      - `Dashboard.tsx` เปลี่ยน `commitAssignment` (หา row เดิมแล้วย้าย groupNo) → `toggleSensorGroup`/`createGroupForSensor` (เพิ่ม/ลบ row เฉพาะคู่ `(tag, groupNo)` นั้น ไม่กระทบ row ของกลุ่มอื่นที่เซนเซอร์เดียวกันเป็นสมาชิกอยู่) — รองรับ multi-group แล้ว
      - `getFgGroupColor` ส่งเป็น prop function ลงไปแทนการ duplicate logic สีใน `SensorSelection.tsx`
      - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test รอบใหม่** รอผู้ใช้ลองแล้ว confirm
    - **🔄 Design revision รอบ 3**: ผู้ใช้ manual test แล้วบ่นว่า panel ที่โชว์ทุกกลุ่มพร้อม checkbox toggle (แม้จะ auto-expand ได้แล้ว) ดูสับสน แยกไม่ออกว่าติ๊กอยู่หรือเปล่าถ้าไม่มองดีๆ — validate ด้วย mockup ใหม่ก่อน แล้ว implement ใน `SensorSelection.tsx`:
      - **Badge เอง = จุดลบ** — เพิ่มปุ่ม X เล็กๆ ต่อท้ายชื่อในแต่ละ badge กดแล้ว `onToggleSensorGroup(sensor, g.no)` ทันที ไม่ต้องเปิด panel
      - **Panel (กด icon `FolderPlus`) = เพิ่มอย่างเดียว** — filter เหลือแค่กลุ่มที่**ยังไม่ได้เป็นสมาชิก** (`!memberGroups.some(...)`) แสดงเป็นแถวกด "+" เพิ่มได้เลย ไม่มี checkbox/สถานะ toggle ให้งงอีกต่อไป — คืน panel กลับไปเป็น "กดไอคอนถึงเปิด" (`menuOpen` เฉยๆ ไม่ auto-expand ตาม membership แล้ว เพราะตอนนี้ดู membership ผ่าน badge ได้อยู่แล้วไม่ต้องเปิด panel)
      - Empty state แยก 2 กรณี: "No failure groups yet" (ยังไม่มีกลุ่มในระบบเลย) vs "Already in every group." (มีกลุ่มแต่เซนเซอร์ตัวนี้เข้าหมดแล้ว)
      - เอาตัวเลขนับจำนวนเซนเซอร์ต่อกลุ่มออกจาก panel ด้วย (ผู้ใช้บอกไม่ได้อยากเห็น)
      - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test รอบนี้** รอผู้ใช้ลองแล้ว confirm (รวมถึงประเด็น "Back to Dashboard → ไปหน้า Get Started" ที่ยังค้างจากรอบก่อนด้วย)
    - **🔄 Design revision รอบ 4 — เพิ่ม rename/delete group**: ผู้ใช้เจอว่า group ที่สร้างไปแล้วแก้ชื่อ/ลบไม่ได้เลยจากหน้า Dashboard — validate ด้วย mockup ก่อนเหมือนเดิม แล้ว implement:
      - `Dashboard.tsx`: เพิ่ม `renameGroup(groupNo, name)` / `deleteGroup(groupNo)` เป็น global operation (ไม่ผูกกับเซนเซอร์ตัวไหน) เขียนผ่าน `persistFailureGroupState` เดียวกับตัวอื่นๆ — `deleteGroup` ลบ row ของทุกเซนเซอร์ในกลุ่มนั้นทิ้งหมด **ไม่มี confirm dialog** (ตั้งใจให้ตรงกับ `FailureGroupCreation.tsx`'s ของเดิม `removeGroup` ซึ่งก็ไม่มี confirm เหมือนกัน — ผู้ใช้ไม่ได้ตอบคำถามเรื่อง confirm ที่ถามไปตรงๆ แต่บอก "เอาตามนี้เลย" เลยยึด precedent เดิมในโค้ด)
      - `SensorSelection.tsx`: รวม "add-only list" กับ badge เดิมเป็น**ลิสต์เดียวที่โชว์ทุกกลุ่ม** แต่ละแถวมี: จุดสี + ชื่อ (คลิกดินสอ = แก้ไข inline), ปุ่ม "+" (โชว์เฉพาะกลุ่มที่ยังไม่ได้เป็นสมาชิก), ปุ่มดินสอ (rename ทุกกลุ่ม), ปุ่มถังขยะ (delete ทุกกลุ่ม) — เพิ่ม state `editingGroupNo`/`editGroupDraft` สำหรับโหมดแก้ไขชื่อ inline
      - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test** รอผู้ใช้ลองแล้ว confirm
  - **แก้ไข 2026-08-03**: ผู้ใช้ส่ง screenshot หน้า Dashboard จริงมาเทียบ พบว่า `dashboard_analysis.svg` เวอร์ชันแรกไม่ตรงกับ UI จริง (proportion ผิด, ไม่มี range slider ใต้กราฟ, ไม่มีแถว TIME RANGE/AGGREGATION ที่ถูกต้อง, Sensor panel ไม่มี group header "BOILER" + "2 sensors selected" pill, Filter panel มี section "Sampling/Operation" ที่ไม่มีอยู่จริง) — เขียนใหม่ทั้งไฟล์ให้ตรงกับ screenshot จริง (title bar มี Workspace label + save/theme icon, chart มี dual-axis + brush slider + legend, Selected Sensor list มี 3-icon action ต่อแถว, Filter panel เหลือแค่ Sensor Filters + Apply Filter ตามจริง)

- **🆕 2026-08-03 — Dashboard: ย้าย Filter เข้าเป็นแท็บที่ 3 ในพาเนลล่างซ้าย (real code change, ไม่ใช่ mockup)**: ผู้ใช้ออกแบบ redesign ไว้เองใน Figma (แอปที่เข้าใจผิดว่าชื่อ "Claude Design" — จริงๆ คือ **Figma Desktop app** ที่มีฟีเจอร์ AI ในตัวชื่อ Figma Make ซึ่งใช้โมเดล Claude เป็น backend ให้เลือกได้ ไม่ใช่ product แยกของ Anthropic) แล้วเลือกแนวทาง "1c: แท็บใหม่ในพาเนลล่างซ้าย" — ย้าย Filter & Controls panel (เดิมอยู่ right-bottom slot) เข้าไปเป็นแท็บที่ 3 ชื่อ **"Filter"** ถัดจาก "Selected Sensor" / "Data Insight" ใน data widget panel, **ไม่ย้าย** ปุ่ม "Create Failure Group" (ผู้ใช้สั่งให้เก็บไว้ที่เดิม — bottom ของ right column). ผลคือ Sensor panel ขยายเต็มคอลัมน์ขวา (single slot แทนที่จะ split 2 พาเนล).
  - **`src/types.ts`**: `DashboardSlot` เอา `'right-bottom'` ออก (เหลือ 3 slot), `DashboardPanel` เอา `'filter'` ออก (เหลือ `chart|data|sensors`), `DashboardLayoutSizes` เอา field `rightRows` ออกทั้งหมด (ไม่จำเป็นอีกต่อไปเพราะ right column เหลือพาเนลเดียว ไม่ต้อง split)
  - **`Dashboard.tsx`**: ลบ `renderFilterContent()` ทั้งฟังก์ชัน, ลบ `case 'filter'` ใน `renderPanel`, ลบ RT↔RB split.js `useEffect` (right column ไม่ split แล้ว), ลบ `slotRBRef`/`rbCollapsed`, ลบ `Filter` icon import (ไม่ได้ใช้แล้ว), เพิ่มแท็บ `'filter'` ใน `activeDataTab` union type + ปุ่มแท็บ + branch render `<FilterPanel .../>` ในเนื้อหาแท็บ (reuse component เดิม ไม่มีการแก้ `FilterPanel.tsx`)
  - **Backward-compat fix ที่พบระหว่างทำ**: ถ้า workspace เก่าที่ save ไว้ก่อนหน้านี้มี `collapsedPanels: ["filter", ...]` ค้างอยู่ ตอนโหลดจะ crash เพราะ `PANELS['filter']` เป็น `undefined` — แก้โดย filter เฉพาะ panel id ที่ยังมีอยู่จริงตอน hydrate initial state (`(initialState?.collapsedPanels ?? []).filter(id => id in PANELS)`)
  - **`App.css`**: ลบ `.filter-widget` (dead — เคย wrap พาเนลแบบเดิมที่ตอนนี้ไม่มีแล้ว), เก็บ `.filter-content` ไว้ใช้ต่อกับแท็บใหม่
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (exit 0) ทั้งก่อนและหลังใส่ backward-compat guard — **ยังไม่ได้ manual-test ผ่าน `npm run tauri dev` จริง** (Browser preview ใช้กับแอปนี้ไม่ได้ตามที่บันทึกไว้ก่อนหน้า) รอผู้ใช้ทดสอบเองแล้ว confirm

- **🆕 2026-08-03 — Light theme: hardcoded white ทำ icon/scatter-dot หายตอน hover + duplicate CSS theme blocks**: ผู้ใช้เจอ 2 เคสแยกกันหลังลอง light theme จริงจัง:
  1. **`App.css` มี `:root`/`[data-theme="light"]` block ซ้ำกัน 2 ชุด** (จาก CSS ที่ merge ทับกันในอดีต — เจอ `.fg-save-btn` ซ้ำ 3 รอบ, `.fg-group-badge`/`.fg-build-model-btn` ซ้ำ 2 รอบด้วย เป็นสัญญาณว่าไฟล์นี้ merge สกปรกมานาน) ชุดที่สอง (บรรทัดหลัง 4593+) ชนะเพราะมาทีหลังใน cascade ทำให้ชุดแรก (บรรทัด 4-26 เดิม) กลายเป็น dead code 100% — **ลบชุดแรกทิ้ง** แล้วพบว่าชุดที่ใช้จริง (ที่สอง) ลืมกำหนด light-mode ให้ `--ok`/`--warn`/`--danger` (สียังคงความสว่างแบบ dark theme แม้สลับเป็น light) — **เพิ่มค่า light ให้ 3 ตัวนี้**
  2. **`pointColorHover: [1.0, 1.0, 1.0, 1.0]` (ขาวล้วน hardcode)** ใน `ScatterChart.tsx` และ `PairPlotCell.tsx` — จุดข้อมูลที่ hover กลายเป็นขาวเสมอไม่ว่า theme ไหน แถม `backgroundColor` ของ WebGL canvas (`regl-scatterplot`) ก็ hardcode ไม่ผูก theme เลยทั้งคู่ (Scatter = กรมท่าเข้ม, Pair Plot = ดำล้วน) เท่ากับกราฟ 2 ชนิดนี้ไม่รองรับ light theme มาตั้งแต่ต้น — ผู้ใช้เลือกให้แก้แบบเต็ม (ไม่ใช่แค่จุดเดียว):
     - สร้าง hook ใหม่ **`src/hooks/useThemeMode.ts`** (mirror MutationObserver pattern เดิมที่ `PredictiveModelBuild.tsx` ใช้อยู่แล้ว แต่ไม่ได้ refactor ไฟล์นั้นให้มาใช้ hook นี้ด้วย — ไม่อยู่ใน scope งานนี้)
     - `ScatterChart.tsx`: เรียก `useThemeMode()` ตรงๆ (เป็น instance เดียว ไม่ได้ mount พร้อมกันหลายตัว), `backgroundColor` สลับตาม theme ผ่าน `sc.set({backgroundColor})` ใน effect แยก (ไม่ recreate WebGL context ทั้งก้อนตอน toggle theme — จะกระพริบ/รีเซ็ตกล้อง), `pointColorHover` เปลี่ยนเป็นสี magenta คงที่ (ไม่ต้องสลับตาม theme ก็ contrast พอทั้ง 2 แบบ) + SVG axis line/tick/label เปลี่ยนจาก hex ตายตัวเป็น `var(--text-secondary)`/`var(--border-strong)`/`var(--text-primary)`
     - `PairPlotCell.tsx`: รับ `themeMode` เป็น prop จาก parent แทนการเรียก `useThemeMode()` เอง (matrix มีได้หลายสิบ cell พร้อมกัน ไม่อยากมี MutationObserver ซ้ำเป็นสิบตัว) — logic เดียวกับ ScatterChart
     - `PairPlotChart.tsx`: เรียก `useThemeMode()` ครั้งเดียว ส่ง `themeMode` ลงไปให้ทุก `<PairPlotCell>` (3 จุด) และ `<DiagonalHistogram>` (2 จุด) ที่อยู่ในไฟล์เดียวกัน, แก้ `DiagonalHistogram`'s hardcoded `background:'#000'`/label/border ให้ theme-aware ด้วย (แท่งกราฟ indigo คงเดิม ไม่ใช่ text ไม่ต้องเปลี่ยน)
  - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test** (ทั้ง theme block fix และ scatter/pair-plot fix) รอผู้ใช้ลองแล้ว confirm

- **🆕 2026-08-03 — สร้าง `docs/BACKLOG.md`**: เก็บ 3 เรื่องที่เจอระหว่าง manual test แต่ user ยังไม่อยากทำตอนนี้ (ทำทีละ step ตาม workflow จริง): (1) FG↔Dashboard sync ทางเดียว [emit ขาด ฝั่ง `FailureGroupCreation.tsx`], (2) ASSIGNED GROUP panel ใน FG ยัง single-select [`updateRow(...,'groupNo',...)` คือ "ย้าย" ไม่ใช่ "toggle"], (3) "Ready/Pending" status toggle บน sensor chip ไม่สื่อความหมาย รอ redesign

- **🆕 2026-08-03 — Bug: ScatterChart crash ตอนย้ายแอประหว่างจอ (multi-monitor, DPI ต่างกัน)**: user รายงาน error `Cannot read properties of undefined (reading 'addEventListener')` จาก `regl-scatterplot`'s `dom2dCamera`/`initCamera`/`reset()`, trigger จาก `ScatterChart.tsx` — root cause: resize event ยิงถี่มากตอนลากหน้าต่างข้ามจอ (โดยเฉพาะจอ DPI ไม่เท่ากัน) → effect สร้าง WebGL instance ใหม่ทุกครั้งที่ resize (`[innerDims.width, innerDims.height, rebuildNonce]`) → effect push-data (deps มี `sc`) re-fire ทันทีแล้วเรียก `sc.reset()` บน instance ที่**เพิ่งสร้างเสร็จหมาดๆ** ซึ่ง regl-scatterplot ยัง setup camera ภายในไม่เสร็จ → crash. เสนอ 2 ทาง (root-cause fix vs safety-net try/catch) ผู้ใช้เลือกทำแค่ root-cause fix:
  - `ScatterChart.tsx`: เพิ่ม `prevScForResetRef` เก็บ instance `sc` ที่ effect รันล่าสุด — เรียก `sc.reset()` เฉพาะตอน `sc` เป็น**ตัวเดิม**กับรอบก่อน (แปลว่าเปลี่ยนเซนเซอร์/pin บน instance ที่มีอยู่แล้ว) ถ้า `sc` เพิ่งเปลี่ยน (มาจาก resize recreate) จะข้าม reset ไปเลย เพราะ instance ใหม่ reset อยู่แล้วโดยธรรมชาติ ไม่จำเป็นต้องเรียกซ้ำ (และเป็นตัวที่ทำให้ crash พอดี)
  - **ไม่ได้ทำ**: try/catch safety net รอบ `.reset()` (ผู้ใช้บอกไม่ต้องเผื่อไว้ตอนนี้) — ถ้า regl-scatterplot มีปัญหาภายในแบบอื่นอีกในอนาคต ยังไม่มีตาข่ายกันตกตรงนี้
  - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test จริงบนเครื่องที่มี multi-monitor** รอผู้ใช้ลองย้ายจอแล้ว confirm

- **🆕 2026-08-03 — 2 บั๊กเพิ่มเติมจาก light-theme fix รอบก่อน (ผู้ใช้เจอตอน manual test)**:
  1. **Scatter/Pair Plot canvas พื้นหลังไม่เปลี่ยนสีจริงตอนสลับ theme** — รอบก่อนแก้ด้วย `sc.set({backgroundColor})` แต่ `.set()` แค่อัปเดต config ภายใน **ไม่ได้สั่งวาดใหม่** (ยืนยันจาก `useCoalescedDraw.ts` — ต้องเรียก `.draw()`/`requestDraw()` ถึงจะ repaint canvas จริง) แคนวาสเลยค้างภาพเดิมจนกว่าจะมี event อื่นมากระตุ้น (hover/zoom) — เห็นชัดใน Pair Plot: histogram (SVG) เปลี่ยนตาม theme ทันที แต่ cell ที่เป็น WebGL scatter ไม่เปลี่ยน
     - `ScatterChart.tsx`: เพิ่ม `lastDrawRef` เก็บ x/y array ล่าสุดที่วาดไปแล้ว, theme-effect เรียก `requestDraw(sc, lastDrawRef.current)` ตาม `.set()` ทุกครั้ง
     - `PairPlotCell.tsx`: reuse `drawDataRef`/`valueARef` ที่มีอยู่แล้ว (ของเดิมใช้กับ cluster-change effect) เรียก `requestDraw()` ตาม `.set()` เหมือนกัน
  2. **ไอคอนปฏิทิน (native browser calendar picker) หายตอน light theme** — เจอ `filter: invert(1)` ที่ `.date-input-wrapper input::-webkit-calendar-picker-indicator` และ `.config-input::-webkit-calendar-picker-indicator` ใน `App.css` **ไม่มีเงื่อนไข theme เลย** — ตั้งใจกลับสีไอคอนดำเริ่มต้นของเบราว์เซอร์ให้เป็นขาวสำหรับ dark theme แต่พอสลับเป็น light theme ก็ยังกลับสีอยู่ดี (ไอคอนดำที่มองเห็นได้บนพื้นขาวอยู่แล้ว โดนกลับเป็นขาวจนหายไปกับพื้นขาว) — เพิ่ม `[data-theme="light"] ... { filter: none; }` cancel ไว้ทั้ง 2 จุด
  - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test** รอผู้ใช้ลองแล้ว confirm

- **🆕 2026-08-04 — แก้จริงเรื่อง Scatter/Pair Plot canvas พื้นหลังไม่เปลี่ยนตาม theme (รอบก่อนแก้ไม่สำเร็จ)**: ผู้ใช้ส่ง screenshot Pair Plot คู่ dark/light ใหม่ยืนยันว่า cell scatter/time ยังดำสนิทอยู่ ("ปัญหานี้ยังไม่ถูกแก้นะ") แม้แก้ด้วย `sc.set({backgroundColor}) + requestDraw()` ไปแล้วรอบก่อน — เข้าไปอ่าน source จริงของ `node_modules/regl-scatterplot/dist/regl-scatterplot.esm.js` พบสาเหตุจริง: `backgroundColor` ถูก bake เข้า **WebGL color texture** ตอนสร้าง instance เท่านั้น (ผ่าน `createColorTexture`), ฟังก์ชัน `setBackgroundColor()` ที่ `.set()` เรียกใต้ฝาครอบ อัปเดตแค่ตัวแปร JS ภายใน + สไตล์ cursor ของ lasso เท่านั้น **ไม่ trigger การ rebuild texture หรือ repaint ใดๆ** เลย ต่อให้ตามด้วย `requestDraw()` ก็วาดด้วย texture สีเดิม — เป็นข้อจำกัดของ lib เอง แก้ด้วย `.set()` ไม่ได้จริงๆ
  - **ทางแก้ที่ใช้**: เปลี่ยนจาก re-tint (`.set()` + replay draw) เป็น **recreate WebGL instance เมื่อ theme เปลี่ยน** (วิธีเดียวกับตอน resize ที่พิสูจน์แล้วว่าสีถูกต้องเสมอ เพราะ texture ถูกสร้างใหม่ทุกครั้ง):
    - `ScatterChart.tsx`: เพิ่ม `themeMode` เข้า deps ของ create-effect (`[innerDims.width, innerDims.height, rebuildNonce, themeMode]`), ลบ effect re-tint แยกที่เคยเพิ่มไว้ (`sc.set({backgroundColor}) + requestDraw(lastDrawRef.current)`) ทิ้งทั้งก้อน, ลบ `lastDrawRef` ด้วยเพราะไม่มีจุดอ่านค่าเหลือแล้ว (unused var จริง — เช็คแล้วไม่มีใครใช้ต่อ)
    - `PairPlotCell.tsx`: เพิ่ม `themeMode` เข้า deps ของ create-effect เดิม (`[innerDims.width, innerDims.height]` → เพิ่ม `themeMode`), ลบ effect re-tint แยกทิ้งเหมือนกัน — **เก็บ** `drawDataRef`/`valueARef` ไว้เหมือนเดิม เพราะ effect "cluster changes" อีกตัวยังใช้อยู่จริง
    - `App.css`: เจอบั๊กเพิ่มอีกจุดระหว่างไล่ดู — `.pair-regl-wrap { background: #000; }` **ไม่มีเงื่อนไข theme เลยสักจุด** (ต่างจาก `.scatter-regl-wrap` ที่ไม่มี background hardcode ในตัวมันเอง เป็นปัญหาเฉพาะ Pair Plot) — แก้เป็น `background: var(--bg-primary)` (ตัวแปร token เดิมที่มีค่า light/dark อยู่แล้ว) กันไม่ให้ wrapper ใต้ WebGL canvas โผล่เป็นดำตอน light theme แม้ instance ข้างในจะ recreate ถูกต้องแล้วก็ตาม (ช่องว่าง/ขอบเล็กๆ ระหว่าง canvas กับ wrapper ยังต้องพึ่ง CSS นี้)
  - **ทำไมไม่กลัวชนกับ crash-fix เดิม (`prevScForResetRef`)**: การ recreate instance ตอน theme toggle ไม่เกี่ยวกับ resize event ที่เป็นสาเหตุ crash เดิม — และ guard เดิมก็ยัง apply ถูกต้องอยู่ดี (ข้าม `.reset()` เมื่อ `sc` เพิ่งเปลี่ยนไม่ว่าจะเปลี่ยนเพราะ resize หรือ theme toggle ก็ตาม) ไม่ต้องแก้ guard เพิ่ม
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (ไม่มี unused-var ค้างจากการลบ `lastDrawRef`/effect) — **ยังไม่ได้ manual test** รอผู้ใช้สลับ theme จริงบน Scatter Chart + Pair Plot แล้ว confirm ว่าพื้นหลัง canvas เปลี่ยนสีถูกต้องทั้งคู่

- **🆕 2026-08-03 — เอา ECharts legend ออกจาก Line Chart (`LineChart.tsx`)**: ผู้ใช้ชี้ว่าแถบ legend ใต้กราฟ (จุดสี + ชื่อเซนเซอร์ คลิกเพื่อ toggle แสดง/ซ่อน) ซ้ำซ้อนกับ list "Selected Sensor" ด้านล่างที่มี checkbox toggle เดียวกันอยู่แล้ว — สั่งเอาออกตรงๆ ("เอาตรงนี้ออกได้เลย") ไม่ต้อง design ก่อน:
  - ลบ key `legend: {...}` ออกจาก ECharts option object ทั้งหมด (ไม่ใช้ `show:false` เฉยๆ — เอาออกให้สะอาดไปเลย)
  - ปรับ layout math ที่เดิม stack กัน legend→slider→x-axis-label จากล่างขึ้นบน ให้เหลือแค่ slider→x-axis-label (เอา `LEGEND_H`/`GAP_ABOVE_LEGEND`/`legendH`/`gapLegend`/`legendBottom` ออกหมด ไม่ใช่ตัวแปรค้าง) — พื้นที่ plot area เลยขยายมาแทนที่ ไม่เหลือช่องว่างจากที่ legend เคยอยู่
  - Verify: `npx tsc --noEmit` ผ่านสะอาด — **ยังไม่ได้ manual test** รอผู้ใช้ลองแล้ว confirm

- **🆕 2026-08-04 — เปลี่ยน format วันที่ทั้งระบบเป็น `YYYY/MM/DD`**: ผู้ใช้บอกว่าแกน X ของ Line Chart ที่แสดงเป็น "เวลา" (`12:05:00 AM`) ดูไม่รู้เรื่องว่าเป็นวันไหน สั่งให้เปลี่ยนเป็นวันที่อย่างเดียว แล้วต่อมาสั่งเพิ่มว่าให้เปลี่ยน `MM/DD/YYYY` → `YYYY/MM/DD` **ทั้งระบบ ไม่ใช่แค่กราฟ**:
  - สร้าง **`src/utils/dateFormat.ts`** (ไฟล์ใหม่) เป็น formatter กลาง: `formatDate` (`YYYY/MM/DD`), `formatDateTime` (`YYYY/MM/DD HH:mm:ss`, 24 ชม.), `formatYearMonth` (`YYYY/MM` สำหรับ tick ที่พื้นที่แคบ) — เขียนเองด้วย `getFullYear/getMonth/getDate` ไม่ใช้ `toLocaleDateString()` เพราะตัวนั้นผลลัพธ์ขึ้นกับ locale ของเครื่อง (en-US ให้ `MM/DD/YYYY` ที่กำกวม)
  - แทนที่ทุกจุดที่แสดง timestamp จริง: `LineChart.tsx` (แกน X + tooltip), `PairPlotChart.tsx` (tooltip แกนเวลา), `PairPlotCell.tsx` (axis tick — เดิม `"Aug 26"` เป็น `"2026/08"`), `DataUploadPage.tsx` (ชื่อ workspace default ที่ auto-gen), `PMReportTemplate.tsx` (วันที่ generate ใน footer PDF)
  - เดิม `toLocaleString()` เป็น locale-dependent + มี AM/PM — เปลี่ยนเป็น 24 ชม. fixed ไปด้วย เพราะเขียน formatter เองแล้วไม่มีเหตุผลจะคง AM/PM ที่กำกวมไว้
  - **จุดที่จงใจไม่แก้**: ช่อง TIME RANGE ใน Dashboard เป็น native `<input type="datetime-local">` — ข้อความที่เห็นถูกวาดโดย browser/OS ตาม locale ไม่ใช่ text ที่โค้ดเราสั่งได้ ต้องเขียน custom date-picker เองทั้งตัวถึงจะบังคับ format ได้ **ผู้ใช้ตัดสินใจไม่แก้** ("รู้สึกว่าเปลี่ยนเยอะไป จะทำให้โปรแกรมหนักเกินไป") — ยอมให้จุดนี้ format ต่างจากที่อื่นแลกกับความเบาของโปรแกรม
  - เจอ `RecentWorkspaces.tsx` ที่มี format เก่าเหมือนกัน แต่เช็คแล้ว**ไม่มีไฟล์ไหน import ไปใช้เลย** (dead code) จึงไม่แตะ
  - Verify: `npx tsc --noEmit` ผ่านสะอาด

- **🆕 2026-08-05 — 🐛 แก้บั๊ก "กด Back แล้วกลับ Dashboard ไม่ได้" (ปิดคดีที่ค้างมาตั้งแต่ 2026-08-03)**: ผู้ใช้รายงานว่าถ้าใช้งานต่อเนื่องในรอบเดียว กด next/back ปกติดี แต่ถ้า**ปิดโปรแกรมแล้วเปิด project จาก recent** การกด Back จะทำงานผิดปกติ หลายครั้งกลับ Dashboard ไม่ได้เลย **ต้องสร้าง project ใหม่ทิ้ง** — ไล่โค้ดจนเจอสาเหตุครบทั้งวงจร (เป็นบั๊กเดียวกับที่บันทึกไว้ว่า "unconfirmed" เมื่อ 2026-08-03 ตอนนี้ยืนยันและแก้แล้ว):
  - **ทำไมใช้งานปกติไม่พัง**: ปุ่ม "Create Failure Group" ใน Dashboard ใช้ coexisting-window pattern — เปิด FG โดย**ไม่ปิด** `main` (Dashboard ยัง mount ค้างอยู่ข้างใน) กด Back เลยหา `main` เจอ แล้วกลับมาที่ Dashboard เดิมได้
  - **ทำไมเปิดจาก recent แล้วพัง**: `handleLoadWorkspace` ใน `DataUploadPage.tsx` ถ้า `lastRoute` เป็น `failure-group`/`predictive-model` จะสั่ง **`getCurrentWindow().destroy()` ทำลาย `main` ทิ้ง** แล้วเปิด FG แทน → กด Back หา `main` ไม่เจอ → ตกไป fallback ที่ `FailureGroupCreation.tsx` ซึ่งสร้างหน้าต่างใหม่ชี้ `url: '/'` = **หน้า Get Started** ไม่ใช่ Dashboard
  - **ทำไมติดวนถาวร**: กด Back แล้วไม่มีใครรีเซ็ต `lastRoute` (ถูกตั้งเป็น `failure-group` ตั้งแต่ตอน FG mount) พอกด project เดิมซ้ำจาก Get Started ก็วนเข้า FG → ทำลาย main → Back → Get Started วนไม่จบ **เข้า Dashboard ของ workspace นั้นไม่ได้อีกเลย** = ตรงกับที่ผู้ใช้บอกว่าต้องสร้าง project ใหม่
  - **แก้ที่ 1 (`FailureGroupCreation.tsx`)**: `handleBackToDashboard` อัปเดต `lastRoute` กลับเป็น `'dashboard'` ก่อนปิดหน้าต่าง — ตัดวงวน ครั้งต่อไปเปิด workspace นี้จะเข้า Dashboard ตรงๆ
  - **แก้ที่ 2 (`DataUploadPage.tsx`)**: เอา `destroy()` + `hide()` ออก เปลี่ยนเป็นเรียก `onDataReady(...)` ให้ `main` เรนเดอร์ Dashboard ไว้ก่อน แล้วค่อยเปิด FG ทับ — สภาพหน้าต่างเหมือน flow ปกติเป๊ะ Back เลยเจอ Dashboard จริง (การ unmount `DataUploadPage` ไม่กระทบ handshake listener เพราะ listener อยู่บน Tauri event bus ไม่ใช่ React state)
  - PM ไม่มีปัญหานี้ — ปิด PM แล้ว FG รีเซ็ต `lastRoute` กลับเป็น `failure-group` ให้ถูกต้องอยู่แล้ว
  - Verify: `npx tsc --noEmit` ผ่านสะอาด + **ทดสอบ `DataUploadPage.test.tsx` แล้ว: กลุ่ม F (Load workspace flow, F27–F31) ซึ่งเป็นเทสต์ที่ครอบคลุมฟังก์ชันที่แก้ ผ่านครบทั้ง 5 ตัว** — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: ปิดโปรแกรม → เปิด project จาก recent ที่ค้างอยู่ที่ FG → กด Back → ต้องเจอ Dashboard (ไม่ใช่ Get Started)
  - **⚠️ หมายเหตุสำคัญเรื่องเทสต์ที่ fail อยู่ (ไม่เกี่ยวกับการแก้นี้)**: รัน `DataUploadPage.test.tsx` ตอนนี้จะ fail 29/44 — **พิสูจน์แล้วว่าไม่ได้เกิดจากการแก้บั๊กนี้** โดยการ `git stash` ไฟล์ที่แก้แล้วรันเทียบ: ที่ HEAD ผ่าน 44/44 ครบ สาเหตุจริงคือ**งาน onboarding step 0/1/2 ที่ค้าง uncommitted อยู่ก่อนแล้ว** (หน้าจอ "Get started" → "Create project" → "Prepare dataset") ซึ่งทำให้ component เปิดมาที่ step 0 แต่เทสต์เก่าเขียนไว้สมัยที่หน้า upload แสดงทันที เลยหา element อย่าง `browse`/`Continue` ไม่เจอ — **เทสต์ชุดนี้ต้องเขียนใหม่ให้รองรับ step flow** → ✅ **ทำเสร็จแล้ววันเดียวกัน ผ่าน 53/53** ดูรายละเอียดที่ entry 2026-08-05 ถัดลงไป
  - **🔎 ต้นเหตุจริงที่ผู้ใช้เจอเองทีหลัง (สำคัญ — แก้เพิ่มรอบ 2)**: ผู้ใช้สังเกตว่า **ถ้ากด "Back to Dashboard" ไม่มีปัญหา แต่ถ้ากดปุ่ม X ปิดหน้าต่างจะพังทันที** — ไล่โค้ดแล้วยืนยันว่าถูกต้อง: ปุ่ม X บน titlebar (Windows ใช้ titlebar ที่เขียนเองเพราะ `decorations: false`) เรียก `handleClose` ซึ่งทำแค่เช็คว่า PM เปิดอยู่ไหมแล้ว `close()` — **ไม่แตะ `lastRoute` เลย** ต่างจาก `handleBackToDashboard` ที่รีเซ็ตให้ ผลคือปิดด้วย X แล้ว `lastRoute` ค้างเป็น `'failure-group'` ตลอด
    - ก่อนแก้รอบแรก: เปิดจาก recent → `main` ถูก destroy → ปิด FG ด้วย X → **ไม่เหลือหน้าต่างเลย โปรแกรมดับ** + `lastRoute` ยังผิด → เปิดใหม่วนเข้า FG อีก = อาการ "ต้องสร้าง project ใหม่"
    - หลังแก้รอบแรก (ไม่ destroy main แล้ว): ปิดด้วย X เจอ Dashboard ก็จริง **แต่แค่บังอาการ** — `lastRoute` ยังค้างผิด ครั้งหน้าเปิดจาก recent ก็ยังเด้งเข้า FG เหมือนเดิม
  - **แก้เพิ่มรอบ 2 — ดักการปิดจากหน้าต่างฝั่งที่ spawn FG**: ไม่ไปแปะโค้ดที่ `handleClose` เพราะมันครอบแค่ปุ่ม X ของ titlebar ตัวเอง ยังหลุด Alt+F4 / ปิดจาก taskbar / native red-close บน macOS (ซึ่งจงใจไม่มี `onCloseRequested` เพราะเคยทำให้หน้าต่างปิดไม่ลงใน Tauri 2) — ใช้ `webview.once('tauri://destroyed', ...)` แทน ซึ่งจับได้ทุกทางปิดรวมถึง crash (เป็น pattern เดียวกับที่ FG ใช้ดักการปิด PM อยู่แล้ว ไม่ใช่ของใหม่):
    - `Dashboard.tsx` (ปุ่ม "Create Failure Group") + `DataUploadPage.tsx` (ตอน spawn FG จาก recent) → ทั้งคู่ติด observer รีเซ็ต `lastRoute` เป็น `'dashboard'`
    - มีเงื่อนไขกันพลาด: ถ้าตอน FG ปิดแล้ว **PM ยังเปิดอยู่** จะไม่รีเซ็ต (ปล่อยให้ PM เป็นเจ้าของ route)
    - เก็บการรีเซ็ตใน `handleBackToDashboard` ไว้เหมือนเดิมเป็นตาข่ายชั้นสอง เผื่อหน้าต่างที่ดักอยู่ตายไปก่อน
  - **แก้คอมเมนต์ที่เขียนคลาดเคลื่อน** ใน `FailureGroupCreation.tsx` (เดิมเขียนว่า `lastRoute` ถูกจัดการตอน "window close → 'failure-group'" ซึ่งไม่จริง — ตัวที่ทำคือตอน *PM* ปิด ไม่ใช่ตอน FG ปิด) คอมเมนต์นี้เป็นเหตุผลหนึ่งที่บั๊กนี้หายากเพราะอ่านแล้วนึกว่ามีคนดูแลอยู่แล้ว — เขียนใหม่ให้ระบุครบทุก transition รวมถึงว่าเคสปิดหน้าต่างถูกจัดการจาก**ข้างนอก**ไฟล์นี้
  - Verify รอบ 2: `npx tsc --noEmit` ผ่านสะอาด + เทสต์กลุ่ม F (Load workspace flow) ยังผ่านครบ 5/5

- **🆕 2026-08-05 — ✅ เขียน `DataUploadPage.test.tsx` ใหม่ให้รองรับ 3-step onboarding flow (ปิดหนี้เทสต์ 29/44 ที่ค้างจากรายการก่อนหน้า)**: **แก้เฉพาะไฟล์เทสต์ ไม่แตะ `DataUploadPage.tsx` เลยแม้แต่บรรทัดเดียว** (step flow เป็นพฤติกรรมที่ตั้งใจ เทสต์ต่างหากที่ล้าสมัย) — ผลลัพธ์: **53/53 ผ่าน** (รัน 3 รอบติดกันเพื่อเช็ค flaky ผ่านหมดทุกรอบ)
  - **เพิ่ม helper เดินขั้นตอนให้อัตโนมัติ** ไว้ต้นไฟล์: `renderPage()` (step 0 เฉยๆ), `renderAtStep1()` (คลิก "Create new project"), `renderAtStep2({name, description})` (ต่อด้วยกรอกชื่อ project + คลิก Continue) และ `continueButton()` — เทสต์กลุ่ม **B/C/D/E/G/I37–I42** เปลี่ยนแค่บรรทัด `render(...)` เป็น `renderAtStep2()` **assertion เดิมทั้งหมดใช้ต่อได้ไม่ต้องแก้**
  - กลุ่ม **A2/A3/A4** (sidebar: "No workspaces yet" / ค้นหา / "No matches") ย้ายไปเริ่มที่ `renderAtStep1()` เพราะ sidebar ถูกซ่อนบน step 0 โดยตั้งใจ — ส่วน **A1/A5–A8** ปล่อยไว้ที่ step 0 ตามเดิม (StartChoiceStep แสดง recent list + ปุ่มลบเองอยู่แล้ว)
  - **กลุ่ม F (F27–F31) ไม่ได้แตะเลย** ตามที่กำชับ — ยังผ่านครบ 5/5 (เป็นเทสต์ที่คุ้มบั๊ก Back-button ที่เพิ่งแก้ไปด้านบน) เช่นเดียวกับ H35/H36 (theme) และ I43/I44 (stuck-spinner) ที่ผ่านอยู่แล้วเพราะไม่ผูกกับ markup ของ step 2
  - **เทสต์ใหม่ 9 ตัว กลุ่ม J (Onboarding flow)**: J45 boot มาที่ step 0 พร้อม 2 การ์ดและไม่มี UI ของ step 2 / J46 "No saved projects yet" (คนละข้อความกับ sidebar) / J47 preview 3 อัน + ปุ่ม "Show all N projects…" / J48 เข้า step 1 แล้ว sidebar + step indicator โผล่ / J49 Continue ถูก disable จนกว่าจะกรอกชื่อ (เว้นวรรคล้วนก็ยังไม่ผ่าน) / J50 กด Continue ตอน disable แล้วไม่เด้งไป step 2 / J51 กรอกชื่อแล้วไป step 2 ได้ / J52 ปุ่ม Back เดิน 2→1→0 และค่าที่พิมพ์ไว้ไม่หาย / J53 เลข step บนหัวคลิกถอยหลังได้อย่างเดียว กดไปข้างหน้าไม่ได้
  - **E25 ปรับ assertion**: เดิมเช็ค `saved.name` มีคำว่า "Workspace" (ชื่อ auto-gen) — ตอนนี้ชื่อมาจาก step 1 เสมอ (fallback auto-gen กลายเป็น dead path เพราะ step 2 เข้าไม่ถึงถ้าไม่มีชื่อ) เลยเปลี่ยนเป็นเช็คว่าชื่อ+description ที่กรอกไว้ถูกเขียนลง `WorkspaceState` จริง
  - **🐛 เจอบั๊ก test-isolation ระหว่างทาง (แก้แล้ว)**: E26 fail แบบงงๆ ว่า `sensorMetadata` เป็น `null` — สาเหตุคือ `MIN_TRANSITION_MS = 500` ใน `handleContinue` ทำให้ `onDataReady` ของ **E25** ยิงหลังเทสต์ E25 จบไปแล้ว ~500ms ไปตกใส่ E26 (ซึ่ง `beforeEach` เพิ่ง `clearAllMocks()` ไป) แล้วถูกนับเป็น call แรกของ E26 — แก้โดยให้ E25 `await waitFor(onDataReady)` ปิดท้ายเพื่อ drain timer ให้จบภายในเทสต์ตัวเอง (ระวังเรื่องนี้ถ้าจะเพิ่มเทสต์ที่กด Continue เพิ่มในอนาคต)
  - Verify: `npx vitest run src/__tests__/DataUploadPage.test.tsx` → **Tests 53 passed (53)** ผ่านครบ 3 รอบติด
  - **⛔ REVERTED (2026-08-05, ทันทีหลัง manual test) — "แก้ที่ 2" ทำโปรแกรมพัง ห้ามทำซ้ำ**: การเปลี่ยน `handleLoadWorkspace` ให้**ไม่ destroy `main`** แล้วเรียก `onDataReady(...)` ให้ main เรนเดอร์ Dashboard ไว้ข้างหลัง FG แทน — ผู้ใช้ทดสอบจริงแล้ว **เปิด project จาก recent ไม่ได้เลย เด้งกลับหน้าแรกสุด (Get Started)** จึง revert กลับไปเป็นพฤติกรรมเดิมของ HEAD ทั้งหมด (destroy main หลัง handshake + `hide()` ตอน `tauri://created`) ยืนยันว่าตรงกับ HEAD แล้วโดยเทียบจำนวน marker ทุกตัว
    - **บทเรียนสำคัญสำหรับคนที่มาทำต่อ**: `src/__tests__/DataUploadPage.test.tsx` **mock Tauri window API ทั้งหมด** (`WebviewWindow`, `getCurrentWindow`, ฯลฯ) เทสต์กลุ่ม F ผ่านครบ 5/5 ทั้งๆ ที่โค้ดจริงพัง — **unit test ชุดนี้พิสูจน์เรื่อง window lifecycle ไม่ได้เลย** ห้ามใช้ผลเทสต์กลุ่มนี้เป็นหลักฐานว่าการแก้เรื่องหน้าต่างปลอดภัย ต้องรัน `npm run tauri dev` แล้วลองมือจริงเท่านั้น
    - ใส่ NOTE เตือนไว้ในโค้ดตรงจุดนั้นแล้วว่าเคยลองแล้วพัง อย่าทำซ้ำโดยไม่เทสจริง
    - เอา `tauri://destroyed` observer ฝั่ง `DataUploadPage.tsx` ออกด้วย — พอ main ถูก destroy ตามเดิม JS context ก็ตาย callback ไม่มีทางยิงอยู่แล้ว (เก็บไว้ก็เป็น dead code หลอกคนอ่าน)
  - **ส่วนที่ยังคงไว้ (additive ล้วน ไม่แตะ window lifecycle จึงไม่ใช่ต้นเหตุที่พัง)**: (1) `handleBackToDashboard` รีเซ็ต `lastRoute` เป็น `'dashboard'`, (2) `Dashboard.tsx` ติด `tauri://destroyed` observer ตอนกดปุ่ม "Create Failure Group" (เส้นทาง in-session ซึ่ง main ยังอยู่ตามดีไซน์เดิมอยู่แล้ว), (3) คอมเมนต์ที่เขียนคลาดเคลื่อนใน `FailureGroupCreation.tsx`
  - **⚠️ ยังไม่ได้แก้ — ปัญหาที่ผู้ใช้เจอต้นเหตุจริง**: ปิด FG ด้วยปุ่ม X ในเส้นทาง "เปิดจาก recent" ยังทำให้ `lastRoute` ค้างเป็น `'failure-group'` เพราะ `handleClose` ไม่แตะ `lastRoute` และ main ถูก destroy ไปแล้วเลยไม่มีใครดักการปิดได้ — ทางแก้ที่เล็กและปลอดภัยที่สุดคือใส่การรีเซ็ต `lastRoute` เข้าไปใน `handleClose` ของ `FailureGroupCreation.tsx` ตรงๆ (ครอบปุ่ม X ซึ่งเป็นเคสที่ผู้ใช้เจอจริง แต่ยังไม่ครอบ Alt+F4 / native close บน macOS) — **ยังไม่ได้ทำ รอตัดสินใจ**

- **🆕 2026-08-05 — แยก config ตาม OS: `decorations` (โปรเจกต์เดิม dev บน macOS ผู้ใช้จริง Windows 100%)**: ตรวจทั้งโปรเจกต์ว่ามีอะไรที่เขียนเผื่อ macOS แล้วกระทบ Windows บ้าง เจอจุดที่กระทบจริง 1 จุด:
  - **ปัญหา**: `tauri.conf.json` ตั้ง `"decorations": true` (เพื่อให้ macOS มี traffic lights ตาม `titleBarStyle: "Overlay"`) แล้ว `lib.rs` ค่อยสั่ง `set_decorations(false)` ตอน `setup()` สำหรับ non-macOS — แต่ config ตั้ง `visible: true` ไว้ด้วย แปลว่าบน Windows หน้าต่างถูกสร้าง**พร้อมกรอบ native ก่อน** แล้วค่อยถอดทีหลัง → มีโอกาสเห็นกรอบกระพริบตอนเปิดแอป
  - **ทำไมแก้ตรงๆ ไม่ได้**: ถ้าตั้ง `decorations: false` ใน config หลักเลย macOS จะไม่มีทั้ง traffic lights และปุ่มที่เราวาดเอง (เพราะ `TitleBar.tsx` render ปุ่ม custom เฉพาะ `!isMacOS`) = ปิด/ย่อหน้าต่างไม่ได้เลยตอน dev บน mac — ผู้ใช้ทักประเด็นนี้ขึ้นมาเอง
  - **ทางแก้ที่ใช้ — platform-specific config ของ Tauri v2** (ยืนยันว่ารองรับจริงจาก `node_modules/@tauri-apps/cli/config.schema.json` ที่มากับโปรเจกต์ ไม่ได้เดา): สร้าง **`src-tauri/tauri.windows.conf.json`** ใหม่ ใส่ window object ครบทุก field + `decorations: false` — Tauri merge ทับ `tauri.conf.json` **เฉพาะตอน build/dev บน Windows**
    - Windows → หน้าต่างเกิดมาไม่มีกรอบตั้งแต่แรก ไม่มีทางกระพริบ
    - macOS → อ่านแค่ `tauri.conf.json` เดิม `decorations: true` + traffic lights ครบเหมือนเดิม **ไม่ต้องแก้อะไรกลับตอนย้ายไป dev บน mac**
    - ใส่ field ครบทุกตัว (label/title/width/height/visible/maximized) เพราะ `app.windows` เป็น array ยังไม่ชัวร์ว่า Tauri merge แบบแทนทั้งก้อนหรือราย element — ใส่ครบแล้วปลอดภัยทั้งสองแบบ
    - **`label: "main"` ต้องตรงเป๊ะ** เพราะโค้ดทั้งโปรเจกต์หา window ด้วย `getByLabel('main')`
  - **`lib.rs`**: เปลี่ยน `#[cfg(not(target_os = "macos"))]` → `#[cfg(target_os = "linux")]` (Linux ยังไม่มี platform config ของตัวเอง เลยยังต้องพึ่ง runtime strip อยู่ — ไม่ได้ลบทิ้งเพื่อไม่ให้ Linux เสียของ ถึงแม้ CI จะ build แค่ mac + windows)
  - Verify: JSON valid ทั้งสองไฟล์ + **`cargo check` ผ่าน ซึ่งรวมถึง `tauri-build` อ่านและ merge `tauri.windows.conf.json` สำเร็จ** (ถ้า schema ผิดจะ fail ตรงนั้น) — **ยังไม่ได้ manual test** รอผู้ใช้รัน `npm run tauri dev` เช็ค 3 ข้อ: (1) เปิดมา maximize เต็มจอ มี custom titlebar (2) ลาก/ย่อ/ขยาย/ปิดได้ครบ (3) ไม่มีกรอบ Windows โผล่
  - **⚠️ ไฟล์ใหม่ต้อง `git add`** — `src-tauri/tauri.windows.conf.json` ยังเป็น untracked ถ้าลืม commit ไป Windows build จะกลับไปใช้ `decorations: true` เงียบๆ
  - **จุดอื่นที่ตรวจแล้วไม่กระทบ Windows จึงไม่แตะ**: `useIsMacOS`/branch `!isMacOS` (บน Windows เข้า branch custom titlebar ถูกอยู่แล้ว), `decorations: isMac` ตอน spawn FG/PM, accelerator `CmdOrCtrl+*` (Tauri แปลงเป็น Ctrl ให้เอง), `menu.setAsWindowMenu` ใน try/catch, `bundle.macOS` + `icon.icns`, `titleBarStyle`/`hiddenTitle` (option เฉพาะ macOS Windows เพิกเฉย), CI job macos-arm64 + Apple signing, `scripts/build-installer-mac-*.sh`, สาขา `Darwin-*` ใน `build_sidecar.sh`
  - **✅ ผู้ใช้ manual test แล้ว (2026-08-05): หน้าต่างเปิดมาปกติทุกอย่าง ไม่มีกรอบ Windows โผล่** — ยืนยันว่า Tauri merge `tauri.windows.conf.json` ถูกต้อง (window ยัง maximize + custom titlebar ครบ) การแยก config ตาม OS ใช้งานได้จริง

- **🆕 2026-08-05 — แก้บั๊ก "ปิด FG ด้วย X แล้ว `lastRoute` ค้าง" ด้วย `onCloseRequested` (ทางเลือก B — ครอบทุกทางปิด)**: ผู้ใช้เลือกทำทีเดียวให้ครบแทนที่จะแก้เฉพาะปุ่ม X:
  - **`FailureGroupCreation.tsx`**: เพิ่ม `onCloseRequested` handler ที่รีเซ็ต `lastRoute` เป็น `'dashboard'` แล้ว `destroy()` — ครอบ**ทุกทางปิด**ในที่เดียว (ปุ่ม X ของ custom titlebar, Alt+F4, ปิดจาก taskbar, native red-close บน macOS) เพราะทุกทางล้วนยิง `CloseRequested` รวมถึง `close()` ที่ `handleClose` เรียก (ต่างจาก `destroy()` ที่ข้าม chain นี้)
  - **เพิ่ม `workspaceIdRef`** — handler ลงทะเบียนครั้งเดียวตอน mount ซึ่งตอนนั้น `workspaceId` ยังเป็น `null` (FG hydrate แบบ async จาก handshake) ถ้าอ่าน state ตรงๆ จะได้ `null` ตลอดกาล = บั๊กเดิมกลับมาแบบเงียบๆ (ใช้ pattern เดียวกับ `isBuildModelOpenRef` ที่มีอยู่แล้ว)
  - **กันไม่ให้ประวัติศาสตร์ซ้ำรอย**: handler ตัวเดิมเคยถูกถอดออกเพราะทำให้**หน้าต่างปิดไม่ลง** — สาเหตุคือมีสาขาที่ `preventDefault()` แล้ว return โดยไม่ปิดต่อ กฎ 2 ข้อที่ห้ามแก้: (1) `destroy()` อยู่ใน `finally` เสมอ ไม่มีทางไหนที่ไม่ปิดหน้าต่าง (2) handler นี้**ไม่บล็อกการปิด** — ตรรกะ "เขย่า PM แทนการปิดตอน PM เปิดอยู่" ยังอยู่ใน `handleClose` ซึ่ง return ก่อนเรียก `close()` ทำให้ handler นี้ไม่ถูกเรียกเลยในเคสนั้น (พฤติกรรมเดิมไม่เปลี่ยน) + `closingRef` กัน event ซ้ำ
  - ถ้า PM ยังเปิดอยู่ตอน FG ถูกปิด → ไม่รีเซ็ต route (PM เป็นเจ้าของ `'predictive-model'` อยู่) เช็คผ่าน `isBuildModelOpenRef`
  - **เทสต์ใหม่ `src/__tests__/FailureGroupCloseHandling.test.tsx` (6 เคส)**: ลงทะเบียน handler ตอน mount / รีเซ็ต route + destroy / **destroy ต้องทำงานแม้ตอน save reject** (regression guard ของอาการปิดไม่ลง) / ยังไม่ hydrate → ไม่ save แต่ต้องปิดได้ / ใช้ id จาก handshake ไม่ใช่ค่าตอน mount / กด 2 ครั้งไม่ destroy ซ้ำ
  - **ตรวจว่าเทสต์จับบั๊กได้จริงด้วย mutation testing**: ลองย้าย `destroy()` ออกจาก `finally` แล้วรัน → เคส "destroy แม้ save reject" แดงทันที (1 failed / 5 passed) แล้วคืนโค้ดกลับ ยืนยันว่าเทสต์ไม่ได้ผ่านลอยๆ
  - Verify: `tsc` ผ่าน + **เทสต์ทั้งโปรเจกต์ 103/103 ผ่าน** (เดิม 97 + ใหม่ 6) — **แต่ย้ำอีกครั้ง: Tauri window API ถูก mock ทั้งหมด เทสต์ชุดนี้พิสูจน์ได้แค่ตรรกะของ handler ไม่ได้พิสูจน์พฤติกรรมหน้าต่างจริง** ต้อง `npm run tauri dev` แล้วลองมือ: ปิด FG ด้วย X / Alt+F4 → เปิด project เดิมจาก recent ต้องเข้า Dashboard ไม่ใช่เด้งเข้า FG
  - **⛔ REVERTED ทันทีวันเดียวกัน — `onCloseRequested` ทำหน้าต่างปิดไม่ได้ (ครั้งที่ 2)**: ผู้ใช้ manual test แล้ว **กด X ปิดไม่ได้เลย ต้อง force-quit ทั้งโปรแกรม** + แอปค้างที่ "Loading workspace…" ตอนเปิดใหม่ + กด Back ไปหน้าแรกแทน Dashboard
    - **บทเรียนที่แพงที่สุดของ session นี้**: คอมเมนต์เดิมในไฟล์เขียนเตือนไว้ตรงๆ ว่า "async handler awaited indefinitely by Tauri 2" แต่ผมตีความเองว่าสาเหตุจริงคือ "มีสาขาที่ preventDefault แล้วไม่ปิดต่อ" แล้วเขียนใหม่โดยบังคับ `destroy()` ใน `finally` — **ตีความผิด** ตัว async callback ใน `onCloseRequested` เองคือปัญหาใน Tauri 2 ไม่ใช่เรื่องสาขาที่ลืมปิด
    - **เทสต์ 6 เคส (รวม mutation test) ผ่านหมดทั้งที่ของจริงพังสนิท** เพราะ `onCloseRequested` เป็น mock — ย้ำอีกครั้งว่า mocked test พิสูจน์ window lifecycle ไม่ได้เลย ลบไฟล์เทสต์นั้นทิ้งแล้ว (เทสต์ handler ที่ไม่มีอยู่จริงแล้ว)
    - ใส่คอมเมนต์ ⛔ ตัวใหญ่ไว้ในไฟล์ว่า **ห้ามใส่ `onCloseRequested` อีก — ลองมาแล้ว 2 ครั้ง พังทั้ง 2 ครั้ง** พร้อมบันทึกว่าครั้งที่ 2 พังทั้งที่ออกแบบมากันเคสของครั้งแรกโดยเฉพาะ
  - **ทางแก้ที่ใช้แทน (ทางเลือก A ที่เคยเสนอไว้ตั้งแต่แรก)**: ใส่การรีเซ็ต `lastRoute` ไว้ใน `handleClose` ตรงๆ — เป็น click handler ธรรมดา ไม่ยุ่งกับ close chain ใช้รูปแบบ await-แล้ว-close เหมือน `handleBackToDashboard` ที่พิสูจน์แล้วว่าทำงานได้ **ครอบเฉพาะปุ่ม X** (เคสที่ผู้ใช้เจอจริง) ส่วน Alt+F4 / native close ยอมปล่อยไว้ แลกกับหน้าต่างที่ปิดได้จริง
  - **🐛 แก้บั๊ก titlebar ซ้อนกัน 2 ชั้น (ผู้ใช้เจอใหม่)**: `new WebviewWindow('main', ...)` ที่สร้างตอน runtime ทั้ง 2 จุด (`handleBackToDashboard` ใน FG + `openMainAndClose` ใน `useSubWindowMenu.ts`) **ไม่ได้ระบุ `decorations` เลย** เลย default เป็น `true` → กรอบ native ของ Windows ซ้อนบน custom titlebar ของแอป — **`tauri.windows.conf.json` ช่วยไม่ได้เพราะ platform config มีผลเฉพาะหน้าต่างที่ประกาศไว้ใน config ไม่ใช่ที่สร้างตอน runtime** เพิ่ม `decorations: isMac` ให้ทั้ง 2 จุดแล้ว (บั๊กนี้มีมาก่อนแล้ว ไม่ได้เกิดจากการแก้รอบนี้ แต่ผู้ใช้เพิ่งเจอเพราะถูกเด้งไปเส้นทาง spawn main ใหม่บ่อยขึ้น)
  - Verify: `tsc` ผ่าน + เทสต์ 97/97 ผ่าน (กลับมาเท่าเดิมหลังลบไฟล์เทสต์ที่ใช้ไม่ได้แล้ว) — **ต้อง manual test**: กด X ต้องปิดได้จริง
  - **✅ ผู้ใช้ manual test ครบแล้ว (2026-08-05): ใช้งานได้ปกติทุกจุด** — กด X ปิด FG ได้จริง, กด Back ได้ Dashboard ไม่มี titlebar ซ้อน, เปิด project จาก recent หลังปิดด้วย X เข้า Dashboard ถูกต้อง ปิดเคสนี้

- **🆕 2026-08-05 — เพิ่มฟีเจอร์ Alarm setpoints: mapping → Dashboard menu → เส้นบนกราฟ**: ผู้ใช้ต้องการเพิ่ม alarm setpoint (L/LL/H/HH) จาก mapping CSV เข้ามาเป็น master data แล้วโชว์เป็นเส้นอ้างอิงบนกราฟได้ ออกแบบ + validate ผ่าน mockup ในแชทและไฟล์ Figma จริง (สร้างผ่าน Figma MCP: https://www.figma.com/design/osmQeKiBaqHyELUAYyeXo8) ก่อน implement — ปรับ 2 รอบตาม ref screenshot ที่ผู้ใช้ส่งมา (format label ในเมนูให้เขียนติดกัน `"High (8.5)"` ไม่แยกคอลัมน์, สีเส้นบนกราฟให้ตามสีของ sensor นั้นๆ ไม่ใช่สีส้มคงที่) แล้วค่อย implement จริง:
  - **`types.ts`**: เพิ่ม `alarmL/alarmLL/alarmH/alarmHH?: number` ใน `SensorMetadata`, เพิ่ม type `AlarmLevel = 'LL'|'L'|'H'|'HH'`, เพิ่ม `alarmLinesEnabled?: Record<string, AlarmLevel[]>` ใน `WorkspaceState` (persist ต่อ workspace ตามที่ผู้ใช้เลือกไว้)
  - **`useMappingData.ts`**: เพิ่ม auto-detect คอลัมน์ `ALARM_L/ALARM_LL/ALARM_H/ALARM_HH` ใน `buildSensorMetadataFromMapping` (pattern เดียวกับ unit/component เดิม) + `parseAlarmValue()` แปลงเป็นตัวเลข ถ้า cell ว่าง/parse ไม่ได้ → `undefined` (ไม่ใช่ 0) เพื่อให้ "ไม่มีค่า = ไม่โชว์" ทำงานถูกต้อง — **ไม่ต้องแก้ Rust เลย** เพราะ `load_mapping_csv` อ่านทุกคอลัมน์แบบ raw string มาให้อยู่แล้ว
  - **`Chart.tsx`**: เจอบั๊กระหว่างทาง — `MainChartProps` destructure ไม่ได้ดึง `markLines` ออกมาส่งต่อให้ `<LineChart>` เลย ทั้งที่ `ChartTypes.ts`/`LineChart.tsx` รองรับเต็มรูปแบบอยู่แล้ว (ใช้วาดเส้น mean/±1σ ใน Predictive Model Build) — เพิ่ม `markLines` เข้า destructure + ส่งต่อ ไม่ต้องเขียน rendering logic ใหม่เลย
  - **`Dashboard.tsx`**:
    - state ใหม่: `alarmLinesEnabled` (init จาก `initialState?.alarmLinesEnabled ?? {}`), `alarmPanelFor` (one-open-at-a-time เหมือน `colorPickerFor`/`axisEditorFor`)
    - `toggleAlarmLine(tag, level)` — เพิ่ม/ลบ level ออกจาก array ของ tag นั้น ลบ key ทิ้งถ้า array ว่าง
    - ต่อเข้า effect เดิมที่ prune `sensorColors`/`sensorAxisRange` ตอนเซนเซอร์ถูกเอาออกจากกราฟ — เพิ่ม prune `alarmLinesEnabled` + ปิด `alarmPanelFor` ด้วย ป้องกัน state ค้างจากเซนเซอร์ที่ลบไปแล้ว
    - `buildWorkspaceState()` เพิ่ม `alarmLinesEnabled` เข้า return object + deps array (ตามรูปแบบเดียวกับที่เคยแก้บั๊ก fgGroups/fgRows หายก่อนหน้านี้)
    - `markLines` useMemo: ไล่ `displayHeaders` × `alarmLinesEnabled` × `getSensorMeta` สร้าง `ChartMarkLine[]` — **ข้ามทั้งหมดตอน multi-op mode** (`displayHeaders` ตอนนั้นเป็นคอลัมน์ "Result" สังเคราะห์ ไม่ใช่ sensor tag จริง หา metadata ไม่ได้) — **ไม่ set `color` เอง** ปล่อยให้ `LineChart.tsx` fallback เป็นสีของ series นั้นเองตามที่ตกลงกับ ref
    - UI: ปุ่มกระดิ่ง (Bell) ต่อจากปุ่มแกน Y ในแถว Selected Sensor — โชว์**เฉพาะ sensor ที่มี alarm อย่างน้อย 1 ค่า** (`hasAlarmSetpoints`) กันปุ่มที่กดแล้วไม่มีอะไรให้เห็น — กดแล้วกาง panel checkbox เฉพาะ level ที่มีค่าจริง format `"High (8.5)"` (เขียนติดกันตาม ref, ไม่ใช่คอลัมน์แยก)
  - Verify: `npx tsc --noEmit` ผ่านสะอาด + เทสต์ทั้งโปรเจกต์ **97/97 ผ่าน** (ไม่มี regression) — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: อัปโหลด mapping CSV ที่มีคอลัมน์ ALARM_L/ALARM_LL/ALARM_H/ALARM_HH → เลือก sensor ที่มีค่า → กดกระดิ่งในแท็บ Selected Sensor → ติ๊ก level ใดก็ได้ → ต้องเห็นเส้นประบนกราฟสีเดียวกับเส้น sensor นั้น + label ตัวย่อ (H/L/HH/LL) กำกับ, ปิดโปรแกรมเปิดใหม่ต้องจำค่าที่ติ๊กไว้

- **🆕 2026-08-05 — แก้ 2 เรื่องหลัง manual test ฟีเจอร์ Alarm setpoints (ตำแหน่ง UI ผิด ref + บั๊ก auto-scale)**:
  - **1) ย้าย UI จากแท็บ "Selected Sensor" ไปแท็บ "Sensor" (133-picker)**: ผู้ใช้ชี้ว่า ref ภาพ 3 (Soothsayer) โชว์ checkbox alarm อยู่ใน **INSTRUMENTS list** (เทียบเท่าแท็บ "Sensor" ของ Wizard) ไม่ใช่ใน list ของ sensor ที่เลือกแล้ว (เทียบเท่าแท็บ "Selected Sensor" ที่ผมใส่ไปรอบแรก) แถม ref ไม่มีปุ่มกระดิ่งกดขยายเลย — checkbox โชว์ทันทีเมื่อ sensor ถูกติ๊กเลือกและมีค่า alarm เท่านั้น (ไม่ต้องมี step กดเปิดแยก)
    - ย้าย logic ไป **`SensorSelection.tsx`**: เพิ่ม prop `alarmLinesEnabled`/`onToggleAlarmLine`, แสดง checkbox list (indent ใต้แถวเซนเซอร์ เหมือนกับ badge ของ failure group) เมื่อ `selectedSensors.includes(sensor) && hasAlarmSetpoints(meta)` — ไม่มีปุ่มกระดิ่ง ไม่มี state เปิด/ปิด panel แยกอีกต่อไป
    - ย้าย `ALARM_LEVELS`/`ALARM_LABELS`/`hasAlarmSetpoints` ออกจาก Dashboard.tsx ไปเป็นไฟล์กลาง **`src/utils/alarmLevels.ts`** ใช้ร่วมกันทั้ง `Dashboard.tsx` (ต้องใช้ `ALARM_LEVELS` สร้าง `markLines`) และ `SensorSelection.tsx` (ต้องใช้ทั้งหมด) เพิ่ม `isCriticalAlarmLevel` (LL/HH) สำหรับ styling ตัวหนา+สีแดงต่างจาก L/H ปกติ ตาม ref ที่ทำ "High shutdown (HH)" เด่นกว่าอันอื่น
    - **`Dashboard.tsx`**: เอาปุ่มกระดิ่ง + `alarmPanelFor` state + inline panel ออกจาก Selected Sensor row ทั้งหมด — `alarmLinesEnabled`/`toggleAlarmLine`/persistence/`markLines` computation ยังอยู่เหมือนเดิม (ยังจำเป็นสำหรับวาดเส้นบนกราฟ) แค่ส่ง prop ต่อให้ `<SensorSelection>` แทนที่จะ render UI เอง
  - **2) 🐛 บั๊กจริง — แกน Y ไม่ auto-ขยายให้เห็นเส้น setpoint**: ผู้ใช้เจอว่ากราฟ scale อยู่ 20-80 (auto-fit ตามข้อมูลจริง) แต่ setpoint ที่ติ๊กไว้อยู่ที่ 90/100 → เส้นวาดไปแล้วแต่มองไม่เห็นเพราะอยู่นอกกรอบแกน — **สาเหตุ: เป็นข้อจำกัดของ ECharts เอง** `scale: true` คำนวณ auto min/max จากข้อมูลจริงของ series เท่านั้น **ไม่รวมค่า `markLine` เข้าไปด้วย** (markLine อยู่นอกการคำนวณ extent ของแกน)
    - แก้ใน **`LineChart.tsx`**: เพิ่มฟังก์ชัน `seriesMinMax(sensor)` สแกนค่าจริงของ series หา min/max แล้วเทียบกับค่า markLine ที่ active ของ sensor นั้น **เฉพาะตอนที่ setpoint จริงๆ อยู่นอกช่วงข้อมูล** ถึงจะขยาย `min`/`max` ของแกน (บวก padding 8% ของ span ที่ขยาย) — sensor ที่ไม่มี markLine หรือ markLine อยู่ในช่วงข้อมูลอยู่แล้ว **ไม่ถูกแตะเลย** ยังคง auto-fit แบบเดิมของ ECharts เป๊ะ (ตามที่ผู้ใช้แนะนำว่า "เช็คก่อนว่าจำเป็นต้อง auto change scale ไหม")
    - ไม่ overwrite ค่าที่ผู้ใช้ pin เอง (`sensorAxisRange`) — เช็ค `autoMin === undefined`/`autoMax === undefined` ก่อนเสมอ ฝั่งที่ pin ไว้ชนะเหมือนเดิม
  - Verify: `tsc --noEmit` ผ่านสะอาด + เทสต์ 97/97 ผ่าน (ไม่มี regression) — **ยังไม่ได้ manual test รอบใหม่** รอผู้ใช้ลอง: ไปแท็บ "Sensor" (ไม่ใช่ Selected Sensor) ติ๊กเลือก sensor ที่มี alarm → checkbox ต้องโชว์ทันทีใต้แถวไม่ต้องกดอะไรเพิ่ม, ติ๊ก setpoint ที่อยู่นอกช่วงข้อมูลจริง (เช่น High 90 ตอนข้อมูลอยู่ 20-80) → กราฟต้องขยายแกน Y ให้เห็นเส้นได้เอง

- **🆕 2026-08-05 — แก้ไข: alarm checkbox ต้องกดไอคอนถึงโชว์ ไม่ใช่ auto-โผล่ตอนติ๊ก sensor**: ผู้ใช้ manual test แล้วพบว่ารอบก่อน checkbox โชว์เองทันทีที่ติ๊ก sensor (auto-show ตาม `isSelected`) — **ผู้ใช้ไม่ต้องการแบบนั้น** สั่งกลับมาเป็นรูปแบบกดไอคอนก่อนถึงจะเห็นค่า (คล้ายของเดิมที่เคยลบไปตอนย้ายมาแท็บ Sensor รอบก่อน) พร้อมโชว์ตัวอย่าง ref เพิ่มเติมที่มีไอคอนเล็กๆ ต่อท้ายแถวเซนเซอร์ที่มี setpoint
  - **`SensorSelection.tsx`**: เพิ่ม state `alarmPanelFor` (local, ไม่ persist — เหมือน `groupMenuFor` ที่มีอยู่แล้วในไฟล์เดียวกัน) + ปุ่มไอคอนกระดิ่ง (`Bell`) ต่อจากปุ่ม FolderPlus (failure group) เดิม
  - **เงื่อนไขใหม่ 2 ชั้นแยกกัน**: (1) ปุ่มกระดิ่งเองโชว์ตาม `hasAlarmSetpoints(meta)` เท่านั้น **ไม่เกี่ยวกับว่า sensor ถูกติ๊กเลือกอยู่ไหม** (เรียกดู setpoint ได้แม้ยังไม่ได้เพิ่มเข้ากราฟ) — (2) checkbox list โชว์เมื่อ `hasAlarms && alarmPanelFor === sensor` เท่านั้น คือต้อง**กดไอคอนก่อนเสมอ** ไม่มี auto-show จากการติ๊ก sensor อีกต่อไป
  - ตัด `showAlarmLevels = isSelected && hasAlarmSetpoints(meta)` (auto-show เดิม) ทิ้ง แทนที่ด้วยตรรกะข้างบน
  - Verify: `tsc --noEmit` ผ่านสะอาด + เทสต์ 97/97 ผ่าน (ไม่มี regression) — **ยังไม่ได้ manual test รอบนี้** รอผู้ใช้ลอง: sensor ที่ไม่มี setpoint ต้องไม่มีไอคอนกระดิ่งเลย, sensor ที่มี setpoint ต้องเห็นไอคอนไม่ว่าจะติ๊กเลือกหรือไม่, checkbox ต้องไม่โผล่จนกว่าจะกดไอคอนกระดิ่ง

- **🆕 2026-08-05 — เปลี่ยนสี alarm line/text จาก "ตามสี sensor" เป็น "ตาม severity" (amber/red)**: ผู้ใช้เจอปัญหาจริงตอน manual test — เส้น setpoint สีเดียวกับเส้นข้อมูลของ sensor นั้น (ตามดีไซน์ที่ตกลงไว้ก่อนหน้าว่าให้เลียนแบบ ref) ทำให้เส้น setpoint กลืนกับเส้นข้อมูลจนแยกไม่ออกจริงในการใช้งาน (เห็นชัดในภาพที่ส่งมา — ทั้งเส้นข้อมูลกับเส้น HH/H เป็นสีเขียวเหมือนกันหมด) สั่งเปลี่ยนเป็น **H/L = ส้ม, LL/HH = แดง** ตามที่เคยเสนอไว้ตอนแรกๆ (ก่อนจะเปลี่ยนไปตามสี sensor ตาม ref) — พลิกกลับการตัดสินใจก่อนหน้าเพราะเจอจริงว่าใช้งานไม่ได้จริง
  - **`src/utils/alarmLevels.ts`**: เพิ่ม `alarmLevelColor(level)` — คืนค่า hex คงที่ (`#f59e0b` amber สำหรับ L/H, `#ef4444` แดงสำหรับ LL/HH) **ไม่ใช้ `var(--warn)`/`var(--danger)`** เพราะสีที่ป้อนเข้า ECharts (canvas-rendered) resolve CSS custom property ไม่ได้ ต้องเป็น hex ตรงๆ — ไม่ผูกกับ theme (สีอิ่มตัวแบบนี้อ่านชัดทั้ง dark/light อยู่แล้ว เหมือนที่เคยตัดสินใจกับสี hover ของจุดใน ScatterChart ก่อนหน้านี้)
  - **`Dashboard.tsx`**: `markLines` ใส่ `color: alarmLevelColor(level)` ตรงๆ ตอนสร้างแต่ละเส้น (เดิมไม่ set สีปล่อย fallback ไปสีของ sensor)
  - **`LineChart.tsx`**: เพิ่ม `label: { color: m.color ?? txtPrimary }` ต่อ markLine data-point แต่ละจุด (เดิม label สีเดียวกันหมดทั้งกลุ่มจาก `txtPrimary` ตัวเดียว ไม่ได้แยกสีตามเส้น) ให้ตัวอักษรกำกับ (H/L/HH/LL) บนกราฟสีตรงกับเส้นของมันเอง
  - **`SensorSelection.tsx`**: checkbox text เปลี่ยนจาก `critical ? 'var(--danger)' : 'var(--text-primary)'` (เดิมมีแค่ LL/HH ที่มีสี ส่วน L/H เป็นสีข้อความปกติ) เป็น `alarmLevelColor(level)` ทุก level — สีตัวหนังสือใน checkbox ตรงกับสีเส้นบนกราฟเป๊ะทั้ง 4 level แล้ว
  - Verify: `tsc --noEmit` ผ่านสะอาด + เทสต์ 97/97 ผ่าน (ไม่มี regression) — **ยังไม่ได้ manual test** รอผู้ใช้ลองดูว่าสีเส้น/ตัวอักษรบนกราฟและ checkbox ตรงกับที่ต้องการไหม (ส้ม = H/L, แดง = HH/LL)

- **🆕 2026-08-05 — ลบปุ่ม "Upload Filled" / "Template" / "Save Groups" ออกจาก FailureGroupCreation.tsx ทั้งฟีเจอร์**: ผู้ใช้สั่งเอาออกตรงๆ พร้อมย้ำว่า "ไม่ได้เอาออกแค่ปุ่ม" ให้ลบ code ที่เกี่ยวข้องกับการทำงานทั้งหมดด้วย — ไล่โค้ดก่อนแล้วพบว่าเป็น**ฟีเจอร์ CSV round-trip แยกต่างหาก** (ดาวน์โหลด template ว่าง → กรอกนอกแอป → Upload Filled กลับเข้ามาแทนที่ groups/rows ทั้งหมด, Save Groups = export ตารางปัจจุบันเป็น CSV) **ไม่ใช่กลไก persist หลักของแอป** (ตัวนั้นคือ debounced `updateWorkspaceData` autosave effect ที่แยกกันอยู่แล้ว ไม่ถูกแตะ) — ลบได้อย่างปลอดภัยโดยไม่กระทบการบันทึกข้อมูลจริงของแอป
  - **`FailureGroupCreation.tsx`**: ลบ `handleUpload`/`handleDownloadTemplate`/`handleSave` + helper `CSV_HEADERS`/`parseCsv`/`csvEscape` ทั้งหมด, ลบปุ่มทั้ง 3 + `fg-toolbar-right` wrapper div ออกจาก JSX, ลบ import ที่เหลือแต่ใช้เฉพาะ 3 ฟังก์ชันนี้ (`openDialog`, `saveDialog`, `message`, `readTextFile`, `writeUserTextFile`, ไอคอน `Upload`/`Download`/`Save`) — เช็คทีละตัวแล้วว่าไม่มีจุดอื่นในไฟล์ใช้ซ้ำก่อนลบ import
  - **เจอจุดที่พลาดง่าย**: `handleSave` ถูกเรียกซ้ำอีกที่ผ่าน `useSubWindowMenu({ localSaveLabel: 'Export Groups as CSV', onLocalSave: () => handleSave() })` — เป็นทางเข้าที่สองของฟีเจอร์เดียวกัน (เมนู File → Export Groups as CSV) ถ้าลบแค่ปุ่ม toolbar แต่ลืมจุดนี้จะเหลือทางเข้าที่ยังใช้งานได้อยู่ ขัดกับที่ผู้ใช้สั่งไว้ — ลบสองพารามิเตอร์นี้ออกจาก `useSubWindowMenu` call ด้วย (hook เองมี `hasLocalSave = !!handlers.onLocalSave` คอยซ่อนเมนูรายการนี้ให้อัตโนมัติเมื่อไม่ส่ง prop มา ไม่ต้องแก้ hook)
  - **`App.css`**: เจอ CSS ของปุ่มทั้ง 3 ซ้ำกันสูงถึง **4 บล็อก** (`.fg-toolbar-right`/`.fg-upload-btn`/`.fg-download-btn`/`.fg-save-btn`/`.fg-toolbar-divider` รวม responsive media query อีก 2 จุด) ตรงกับรูปแบบ "CSS merge ซ้อนทับประวัติศาสตร์" ที่เจอมาก่อนหน้านี้ในเซสชันนี้ (เช่น `.fg-save-btn` เคยพบว่าซ้ำ 3 รอบ) — ลบทุกบล็อกที่เกี่ยวกับปุ่มที่หายไปแล้วออก **แต่เก็บ `.fg-toolbar-left` ไว้ทุกจุดที่เจอ ไม่แตะเลย** (ยังใช้งานจริงอยู่ ปุ่ม Back to Dashboard + workspace label ยังอยู่ในนั้น) แยก selector แบบ `.fg-toolbar-left, .fg-toolbar-right { ... }` ที่เจอ 2 จุดออกจากกันแทนที่จะลบทั้งคู่
  - Verify: `tsc --noEmit` ผ่านสะอาด + เทสต์ 97/97 ผ่าน (ไม่มี regression) + grep ทั้ง `src/` ยืนยันไม่มี reference เหลือของ class/function ที่ลบไปเลย — **ยังไม่ได้ manual test** รอผู้ใช้ลองเปิดหน้า Failure Group Creation แล้วดูว่า toolbar ขวาบนหายไปแล้ว, เมนู File ไม่มี "Export Groups as CSV" ค้างอยู่, และปุ่ม/แถบอื่นๆ (Back to Dashboard, workspace label, progress strip) ยังทำงานปกติไม่กระทบ

- **🆕 2026-08-05 — 🐛 บั๊ก: ColorPlatePicker hue slider เด้งกลับซ้ายสุดตอนลากไปขวาสุด**: ผู้ใช้ส่งภาพให้ดู — ลาก hue slider (แถบสีรุ้งด้านล่างของ color picker) ไปสุดขวา แล้ว thumb เด้งกลับไปซ้ายสุดทันที
  - **สาเหตุ (ยืนยันด้วยการรันเลขจริง)**: [`ColorPlatePicker.tsx`](src/components/dashboard/ColorPlatePicker.tsx) เดิม derive ค่า `h` (hue) จาก prop `color` (hex) ใหม่**ทุก render** ผ่าน `rgbToHsv(hexToRgb(color))` — ลากไปสุดขวา = hue 360° ซึ่งคือสีแดงล้วน (`#ff0000`) เหมือนกับ hue 0° เป๊ะ พอ re-render แล้ว derive hue กลับจาก hex อีกครั้ง `rgbToHsv` หาทางแยก 360° ออกจาก 0° ไม่ได้ (RGB เก็บข้อมูลนี้ไม่ได้ เป็นข้อจำกัดคณิตศาสตร์ของ hue wheel ที่วนกลับมาจุดเดิม) เลย normalize กลับเป็น 0° เสมอ → thumb เด้งไปซ้ายสุด
  - **แก้**: เปลี่ยนจาก "derive h/s/v จาก prop ทุก render" เป็น **local state** ที่อัปเดตตรงจากการลากเอง ไม่ผ่าน round-trip ทาง hex — sync กลับจาก prop เฉพาะตอนที่ `color` เปลี่ยนมาจากแหล่งอื่นจริงๆ (เช่น เปิด picker ให้ sensor คนละตัว) โดยเทียบกับ `lastEmittedColor` (hex ล่าสุดที่ component เองเพิ่งส่งออกไปผ่าน `onChange`) — ถ้า prop ที่ได้รับตรงกับที่เพิ่งส่งไปเอง (แค่ React ส่ง prop เดิมกลับมาให้) จะไม่ derive ซ้ำ เก็บ hue 360° ไว้ตามเดิม
  - Verify: `tsc --noEmit` ผ่านสะอาด + เทสต์ 97/97 ผ่าน (ไม่มี regression) + จำลองด้วย Node สคริปต์แยกยืนยัน bug จริงก่อนแก้ (hue=360 → hex `#ff0000` → derive กลับได้ hue=0) — **ยังไม่ได้ manual test** รอผู้ใช้ลองลาก hue slider ไปสุดขวาอีกครั้งว่า thumb ค้างอยู่ขวาสุดไม่เด้งกลับแล้ว

- **🆕 2026-08-06 — 🐛 บั๊ก: Scatter chart ลืมคู่ sensor X/Y ที่เลือกไว้ตอนสลับไปดู chart type อื่นแล้วกลับมา**: ผู้ใช้ส่งภาพให้ดู — เลือก sensor A vs C บนแกน X/Y ของ Scatter แล้วสลับไป Line/Pair Plot แล้วกลับมา Scatter อีกครั้ง คู่ที่เลือกไว้หายไป กลายเป็น sensor 2 ตัวแรกของ list (A, B) เสมอ ขอให้หาสาเหตุก่อนแล้วค่อยแก้ ตอบแล้วผู้ใช้สั่งให้ persist ลง workspace file ด้วย (ไม่ใช่แค่จำระหว่างสลับแท็บในเซสชันเดียว)
  - **สาเหตุ**: [`ScatterChart.tsx`](src/components/charts/ScatterChart.tsx) เก็บ `scatterX`/`scatterY` เป็น **local `useState` ของตัวเอง** ไม่เคยถูกยกขึ้นไปที่ `Dashboard.tsx` เลย (ต่างจาก `sensorAxisRange` ของ Line chart ที่ยกไปแล้ว) — [`Chart.tsx`](src/components/charts/Chart.tsx) เลือก render `LineChart`/`PairPlotChart`/`ScatterChart` แบบ if/else มีแค่ตัวเดียว mount อยู่ในแต่ละครั้ง สลับ chart type = React unmount `ScatterChart` ทิ้งไปเลย (คนละ component ไม่ใช่ซ่อน) state ในนั้นหายหมด พอสลับกลับมาได้ instance ใหม่ค่าเริ่มต้นเป็น `''` แล้ว effect เดิม (บรรทัด 183-201 เดิม) เซ็ต default เป็น sensor 2 ตัวแรกใน list ให้อัตโนมัติ
  - **แก้**: ยก state ขึ้นไปที่ `Dashboard.tsx` (รูปแบบเดียวกับ `sensorAxisRange`) แล้ว persist ลง `WorkspaceState` ด้วย (ผู้ใช้ขอเพิ่มจากที่เสนอไว้แค่ระดับ session):
    - **`types.ts`**: เพิ่ม `scatterAxes?: { x: string; y: string }` ใน `WorkspaceState`
    - **`ChartTypes.ts`**: เพิ่ม `scatterX?`/`scatterY?`/`onScatterAxesChange?` ใน `ChartProps`
    - **`Chart.tsx`**: forward 3 prop ใหม่นี้ต่อให้ `<ScatterChart>` (ไม่แตะ Line/Pair Plot เพราะ prop ใหม่ไม่เกี่ยว)
    - **`ScatterChart.tsx`**: rename prop รับเข้ามาเป็น `persistedX`/`persistedY` (กันชนชื่อกับ local state `scatterX`/`scatterY` เดิม) ใช้ seed ค่าเริ่มต้นตอน mount แทน `''` (`useState(() => persistedX ?? '')`) — เก็บ local state ไว้เหมือนเดิมเพื่อไม่ต้อง rewrite effect เดิมที่ดูแล fallback ตอน sensor list เปลี่ยน/เหลือตัวเดียว, เพิ่ม effect ใหม่ยิง `onScatterAxesChange(x, y)` ทุกครั้งที่ค่าทั้งคู่ settle (คนละอันกับ effect เดิม แยกความรับผิดชอบชัดเจน)
    - **`Dashboard.tsx`**: state `scatterAxes` (init จาก `initialState?.scatterAxes ?? null`) + `handleScatterAxesChange` (guard เทียบค่าเดิมก่อน set กันสร้าง object ใหม่ทุกครั้งโดยไม่จำเป็น ซึ่งจะไป trigger `buildWorkspaceState` deps เปลี่ยนบ่อยเกินจำเป็น) ต่อเข้า `buildWorkspaceState()` return + deps array (รูปแบบเดียวกับ `alarmLinesEnabled`) และส่ง prop ที่จุด render `<Chart>` ฝั่ง scatter/pair
  - Verify: `npx tsc --noEmit` ผ่านสะอาด + เทสต์ทั้งโปรเจกต์ **97/97 ผ่าน** (ไม่มี regression) — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: เลือก sensor คู่ X/Y ที่ไม่ใช่ 2 ตัวแรกในกราฟ Scatter → สลับไป Line หรือ Pair Plot → สลับกลับ Scatter → คู่ที่เลือกไว้ต้องยังอยู่เหมือนเดิม, ปิดโปรแกรมแล้วเปิด workspace เดิมใหม่ → คู่ที่เลือกไว้ต้องยังอยู่เหมือนกัน

- **🆕 2026-08-06 — 🐛 พบสาเหตุ (ยังไม่แก้ ตามที่ผู้ใช้สั่ง "ค่อยแก้ทีหลัง"): Pair Plot spam error `(regl) context lost` x65254 ครั้ง**: ผู้ใช้ส่งภาพ error overlay ให้ดู — สอบสวนแล้วเป็นบั๊กคนละจุดจากการแก้ scatterAxes ด้านบน ไม่ได้แตะ `PairPlotChart.tsx`/`PairPlotCell.tsx` เลย บันทึกรายละเอียดเต็มไว้ที่ **`docs/BACKLOG.md` ข้อ 5** แล้ว สรุปสั้นๆ ที่นี่:
  - **สาเหตุ**: `PairPlotCell.tsx` สร้าง 1 WebGL context (`regl-scatterplot` instance) ต่อ 1 cell — รวมทั้ง matrix = `n(n+1)/2` context เมื่อ n = จำนวน sensor ที่เลือก (5 sensor = 15 context, 8 sensor = 36 context) เกิน limit ของเบราว์เซอร์ (ประมาณ 8-16 แล้วแต่เครื่อง/GPU) ได้ง่าย พอ context โดนเขี่ยทิ้ง **`PairPlotCell.tsx` ไม่มี `webglcontextlost` listener เลย** (ต่างจาก `ScatterChart.tsx` ที่มีอยู่แล้ว) เลย error ซ้ำไม่หยุดทุกเฟรมจนกลายเป็น x65254
  - **แนวทางแก้ที่คุยกันไว้ (ยังไม่ implement)**: (1) เพิ่ม context-loss guard ให้ `PairPlotCell.tsx` แบบเดียวกับ `ScatterChart.tsx` (2) พิจารณา soft warning เมื่อเลือก sensor เกิน ~5 ตัวใน Pair Plot — ผู้ใช้ถามด้วยว่าเปลี่ยน lib ช่วยไหม คำตอบคือช่วยได้เฉพาะถ้าเปลี่ยนสถาปัตยกรรมเป็น "1 context ใช้ร่วมกันทั้ง matrix" หรือ "เลี่ยง WebGL ไปใช้ Canvas 2D อย่าง ECharts" ไม่ใช่แค่สลับ WebGL lib ตัวอื่นที่ยังสร้าง 1 context ต่อ cell เหมือนเดิม
  - **สถานะ**: ผู้ใช้สั่ง "ค่อยแก้ทีหลัง" — parked ไว้ใน backlog ยังไม่ได้ลงมือทำอะไรกับโค้ดจริง

- **🆕 2026-08-06 — ⚡ งานใหญ่: ยุบหน้าต่าง Failure Group Creation เข้า Dashboard ทั้งหมด แล้วลบไฟล์เดิมทิ้ง**: ต่อยอดจาก design mockup ที่ทำไว้ก่อนหน้า (2 mockup: แท็บ "Failure Groups" ในคอลัมน์ขวา + modal สรุปเต็มจอ) ผู้ใช้ confirm ว่า design ถูกทางแล้วหลังจากถามคำถามเพิ่มเรื่องสถาปัตยกรรม (ใช้ `AskUserQuestion` 2 ข้อ) ได้คำตอบ: (1) modal สรุปเปิดแบบ on-demand ไม่ pin ติดจอ (2) ปุ่ม Build Model ยังเปิดหน้าต่าง Predictive Model แยกเหมือนเดิม แค่ตัดขั้นตอนผ่านหน้าต่าง FG เดิมออก แล้วสั่ง "ทำต่อเลย ถูกต้องแล้ว เทสดีๆด้วยละ อย่าให้มีบัค" — ก่อนเริ่มโค้ด ใช้ Explore agent สำรวจทุกจุดที่ reference `'failure-group'` ทั่วโค้ดเบสก่อน (routing ใน `main.tsx`, การ spawn ใน `Dashboard.tsx`/`DataUploadPage.tsx`, contract ของ `PredictiveModelBuild.tsx`) เพื่อไม่ให้เหลือจุดที่ลืมแก้:
  - **ไฟล์ใหม่ `src/components/dashboard/FailureGroupsPanel.tsx`**: มุมมอง "manage" แบบ group-centric สำหรับแท็บ Failure Groups — การ์ดกลุ่มพับได้ (เหมือนของเดิม), กดแถวเซนเซอร์ขยาย inline editor ด้านล่างตรงๆ (concept sensor/model type/model notes/status/ปุ่ม Build Model/Remove) — แถวที่ยังไม่มี sensor tag จะโชว์ตัวค้นหา+เลือก tag ก่อน (มี guard กันซ้ำ tag ในกลุ่มเดียวกันแบบเดิม) รีใช้ CSS class เดิมที่มีอยู่แล้วทั้งหมด (`.fg-group-color-*`, `.fg-status-pill`, `.fg-icon-btn*`, `.fg-inspector-field/input/textarea`, `.fg-build-model-btn`) ไม่ต้องเขียน CSS ใหม่เลย เพราะ App.css เป็น global stylesheet ไม่ได้ผูกกับไฟล์ FG เดิมโดยเฉพาะ — จงใจ**ไม่แสดงกลุ่ม "Not in Group" (no===0)** เพราะโมเดล assignment แบบใหม่ (toggle-based ผ่าน `toggleSensorGroup`/`createGroupForSensor`) ไม่เคยสร้าง row ให้กลุ่มนี้เลย (ต่างจาก FG เดิมที่ทุก sensor มี row เริ่มต้นในกลุ่ม 0 เสมอ)
  - **ไฟล์ใหม่ `src/components/dashboard/FailureGroupsSummaryModal.tsx`**: มุมมอง "อ่านอย่างเดียว" เปิดแบบ on-demand จากปุ่ม Summary ในแท็บ manage — progress strip (sensors/groups/ready/pending/completion bar) + ตารางแยกตามกลุ่ม พร้อมปุ่ม Build ยิงตรงเข้า PM ได้จากตรงนี้เลย รีใช้ `.pair-regl-modal*` (backdrop/header/title/body/table) ที่มีอยู่แล้วจาก `PairPlotChart.tsx` — เป็น modal chrome กลางที่ generic พอจะใช้ซ้ำได้เลยไม่ต้องเขียนใหม่ — **เจอบั๊กระหว่างทาง**: ใช้ `<>...</>` fragment เป็น top-level return ของ `.map()` ซึ่ง React ต้องการ `key` แต่ shorthand fragment รับ key ไม่ได้ — แก้เป็น `<Fragment key={group.no}>` (import จาก `react`) ก่อนที่จะเจอตอน manual test จริง
  - **`Dashboard.tsx`** (ไฟล์หลักที่แก้เยอะสุด):
    - state ใหม่ `activeSensorTab` ('sensor' | 'failure-groups', default อ่านจาก `initialState?.lastRoute === 'failure-group'` — **repurpose ความหมายเดิมของ route นี้**: เดิมมันคือ "เปิดหน้าต่าง FG แยก" ตอนนี้กลายเป็น "เปิด Dashboard แล้วแลนด์ที่แท็บ Failure Groups" — ไม่ต้องแก้ type `WorkspaceRoute` ใน `types.ts` เลยเพราะ literal เดิมยังใช้ค่าเดิม แค่เปลี่ยนพฤติกรรมตอนอ่าน) + `showFgSummary` (boolean เปิด/ปิด modal)
    - handler ใหม่ 5 ตัวต่อจาก `toggleSensorGroup`/`createGroupForSensor`/`renameGroup`/`deleteGroup` เดิม: `toggleGroupCollapse`, `createEmptyGroup` (สร้างกลุ่มเปล่าไม่ผูก sensor — ต่างจาก `createGroupForSensor` เดิมที่ต้องมี sensor เสมอ), `addBlankRowToGroup` (สร้าง row ว่าง คืน id แบบ synchronous ให้ panel เอาไปขยายต่อได้ทันที), `updateFgRow` (mirror `updateRow` เดิมของ FG — auto-sync `mappedSensorName` จาก metadata เมื่อแก้ `mappedSensorTag`), `removeFgRowById`
    - **ย้าย Build Model logic จาก FailureGroupCreation.tsx มาไว้ที่นี่ทั้งหมด** (`pendingModelDataRef`, effect ตอบ `request-predictive-data`, `spawnPredictiveModel`, `handleBuildModel`) — ก็อปพฤติกรรมเดิมของ `spawnPMHelper` มาแทบทุกบรรทัด (default `predictiveModelState`, idempotency check ผ่าน `WebviewWindow.getByLabel`, dual listener `tauri://destroyed` + `predictive-model-closed` เพื่อรีเซ็ต `lastRoute`) — **เจอบั๊กระหว่างทาง**: วางบล็อกนี้ไว้ตำแหน่งแรกสุด (ใกล้ๆ `fgGroups`/`fgRows`) ทำให้ `tsc` error "used before declaration" เพราะ `buildDashboardSnapshot` อ้างถึง `filters`/`samplingMethod` ที่ declare ทีหลังในไฟล์เดียวกัน (React state hooks ผูกกับ lexical order) — ย้ายทั้งบล็อกไปอยู่หลัง `samplingMethod` declare แล้ว `tsc` ผ่านสะอาด
    - **auto-resume PM effect ใหม่** (mount ครั้งเดียว ผ่าน ref guard): ถ้า `initialState.lastRoute === 'predictive-model'` และมี `predictiveModelState.targetSensor` อยู่ → เรียก `spawnPredictiveModel` ทันที — แทนที่กลไก "FG cascade" เดิมที่ `DataUploadPage.tsx` เคยส่ง `targetRoute` ไปให้ FG ตัดสินใจ spawn PM ต่อ
    - `renderSensorsContent()`: เพิ่ม tab bar 2 แท็บ (Sensor / Failure Groups) รูปแบบเดียวกับ `activeDataTab` ของแผง Data ที่มีอยู่แล้ว — สลับ render ระหว่าง `<SensorSelection>` เดิม (ไม่แตะ, ปุ่ม FolderPlus quick-assign ยังอยู่เหมือนเดิม) กับ `<FailureGroupsPanel>` ใหม่ — ปุ่ม "Add Special Sensor" ใน footer โชว์เฉพาะแท็บ Sensor
    - ปุ่ม **"Create Failure Group"** เดิม (spawn หน้าต่าง FG พร้อม handshake ยาวเกือบ 90 บรรทัด) **ถูกแทนที่ทั้งหมด**ด้วยปุ่ม "Failure Groups" ที่แค่ `expandPanel('sensors')` + `setActiveSensorTab('failure-groups')` — ลบโค้ด spawn/handshake/`tauri://destroyed` listener เดิมทิ้งหมด
    - เพิ่ม `<FailureGroupsSummaryModal>` render แบบ conditional (`showFgSummary`) ต่อท้าย root return
    - ลบ `emit('failure-group-updated')` ออกจาก `persistFailureGroupState` เพราะไม่มีหน้าต่าง FG ให้ฟังอีกต่อไปแล้ว (เดิมไว้แจ้ง FG window ที่เปิดค้างอยู่ให้ reload state)
  - **ลบไฟล์ `src/components/windows/FailureGroupCreation.tsx` ทิ้งทั้งไฟล์ (1074 บรรทัด)** + ลบ export ออกจาก `src/components/windows/index.ts` + ลบ import/branch `windowType === 'failure-group'` ออกจาก `src/main.tsx`
  - **`src/components/upload/DataUploadPage.tsx`**: ลบ branch `route === 'failure-group' || route === 'predictive-model'` ทั้งก้อน (~90 บรรทัด — เดิม spawn หน้าต่าง FG แบบ **destroy `main` ก่อน** ไม่ใช่ coexisting-window เหมือนปุ่มใน Dashboard ซึ่งเป็นรายละเอียดที่ต้องอ่านโค้ดจริงถึงจะรู้ ไม่ใช่แค่เดาจาก pattern อื่น) แทนที่ด้วยการเรียก `onDataReady(...)` ตรงๆ เสมอ ไม่ว่า `lastRoute` จะเป็นอะไร — Dashboard เป็นคนตัดสินใจเองว่าจะแลนด์ที่แท็บไหน/auto-spawn PM ไหม ผ่าน logic ที่เพิ่งเพิ่มด้านบน — ลบ import `WebviewWindow`/`emit` ที่ไม่ได้ใช้แล้วออกด้วย (`tsc` เตือน unused)
  - **`src/hooks/useSubWindowMenu.ts`**: แก้ doc comment ที่อ้างถึง FG (ตอนนี้มีแค่ PM ที่ใช้ hook นี้)
  - **`src/components/windows/PredictiveModelBuild.tsx`**: แก้ comment 2 จุดที่อ้างถึง FG ให้ตรงความจริง (listener `predictive-model-shake` ตอนนี้ไม่มีใคร emit แล้วเพราะ FG หายไป — เป็น dead code ที่ไม่เป็นอันตราย ตั้งใจ**ไม่ลบโค้ดจริงออก**เพราะต้องตรวจสอบ `containerRef` ทุกจุดที่ใช้ในไฟล์ 3889 บรรทัดก่อน ถือว่าเกินขอบเขตงานนี้)
  - **`src/__tests__/DataUploadPage.test.tsx`**: แก้ comment ที่อ้างถึง listener ที่ลบไปแล้ว (เทส I43 เองไม่กระทบเพราะมันจงใจทำให้ `loadWorkspaceData` ค้างไม่ resolve เลยไม่มีทางไปถึง branch ที่ลบ)
  - **`docs/BACKLOG.md`**: mark ข้อ 1 (FG↔Dashboard sync ทางเดียว) กับข้อ 2 (ASSIGNED GROUP single-select) เป็น **resolved-by-removal** — ปัญหาทั้งคู่เป็นเรื่องเฉพาะของไฟล์ FG เดิมที่หายไปแล้ว ไม่ได้ renumber ข้อที่เหลือ (กันพัง cross-reference ที่ entry อื่นในเอกสารนี้ชี้ไปที่ "ข้อ 5")
  - Verify: `npx tsc --noEmit` ผ่านสะอาดทุกรอบที่แก้ + เทสต์ทั้งโปรเจกต์ **97/97 ผ่าน** (รวม `DataUploadPage.test.tsx` แยกอีกรอบ 53/53) ไม่มี regression + `grep -rn "FailureGroupCreation"` ทั่ว `src/` ยืนยันว่าเหลือแต่คอมเมนต์อธิบายประวัติเท่านั้น ไม่มี reference ที่ทำงานจริงหลงเหลือ — **ยังไม่ได้ manual test เลย เป็น flow ใหญ่ที่สุดที่เคยแก้ในเซสชันนี้ ต้องเทสละเอียด**: (1) เปิด workspace ใหม่ ไปแท็บ Failure Groups ต้องไม่มี error, สร้างกลุ่มเปล่า, เพิ่ม sensor เข้ากลุ่มผ่านตัวค้นหา, แก้ concept/model type/notes, ติ๊ก Ready (2) กด Build Model ต้องเปิด PM window ได้จริง ตั้งค่า target/predictor ได้ปกติ (3) ปิด PM แล้วเปิดใหม่จาก workspace เดิม (ผ่าน Recent) ต้อง auto-spawn PM กลับมาเลยไม่ค้างที่ Dashboard เฉยๆ (4) ปิดตอนอยู่แท็บ Failure Groups แล้วเปิด workspace เดิมใหม่ ต้องแลนด์ที่แท็บ Failure Groups อัตโนมัติ (5) กดปุ่ม Summary ต้องเห็น modal ตารางถูกต้องตรงกับข้อมูลจริง กด Build จากในนั้นได้ (6) ปุ่ม FolderPlus quick-assign เดิมในแท็บ Sensor ต้องยังทำงานปกติไม่กระทบ (7) ทดสอบ workspace เก่าที่เคยมี `lastRoute: 'failure-group'` ค้างจากก่อนแก้ (ถ้ามี) ต้องไม่พัง แค่แลนด์ที่แท็บ Failure Groups แทนที่จะพยายามเปิดหน้าต่างที่ไม่มีอยู่แล้ว

- **🆕 2026-08-06 — ปรับ UI ฟีเจอร์ Failure Groups tab หลังผู้ใช้ manual test จริง (ส่งภาพหน้าจอที่ใช้งานได้แล้วมาให้ดู — ฟีเจอร์ใหญ่ก่อนหน้าทำงานถูกต้อง!)**: ผู้ใช้ให้ feedback 3 ข้อพร้อมเหตุผลชัดเจน สั่งแก้ตรงๆไม่ต้องถามก่อน:
  - **1) ลบปุ่ม "Failure Groups" (เดิมคือ "Create Failure Group") ที่ท้ายคอลัมน์ขวาออกทั้งปุ่ม**: ผู้ใช้ชี้ว่าซ้ำซ้อน — กดที่ตัวแท็บด้านบนได้อยู่แล้ว ไม่จำเป็นต้องมีปุ่มลัดอีกจุด — ลบทั้ง `.save-continue-section` div ออกจาก `Dashboard.tsx` เลย (ไม่ใช่แค่ซ่อน)
  - **2) ปุ่ม Summary ด้านบนแท็บก็เอาออกด้วย**: ผู้ใช้ชี้ว่าโชว์ข้อมูลซ้ำกับที่อยู่ในแท็บ manage อยู่แล้ว ไม่รู้จะแยก view ทำไม — **ลบทั้งฟีเจอร์ summary modal ทิ้ง** ไม่ใช่แค่ปุ่ม (ตามรูปแบบเดิมที่เคยเรียนรู้จากตอนลบปุ่ม CSV round-trip — "เอาออก" หมายถึง code ที่เกี่ยวข้องทั้งหมด): ลบไฟล์ `src/components/dashboard/FailureGroupsSummaryModal.tsx` ทิ้งทั้งไฟล์ (เพิ่งสร้างเมื่อกี้ในเซสชันนี้เอง), ลบ `showFgSummary` state + import + conditional render ออกจาก `Dashboard.tsx`, ลบ prop `onOpenSummary` ออกจาก `FailureGroupsPanel.tsx` (ทั้ง interface และปุ่ม Summary ใน header)
  - **3) เปลี่ยนคำ "Ready"/"Pending" เป็น "Complete"/"Incomplete" + ทำให้กด badge สถานะได้ตรงๆ**: ผู้ใช้ถามว่า "ทำไมกดเปลี่ยนสถานะไม่ได้" — เดิม badge สถานะที่แถวเซนเซอร์ (ตอนยังไม่ขยาย row) เป็นแค่ `<span>` แสดงผลอย่างเดียว ต้องกดขยาย row ก่อนถึงจะเจอ checkbox toggle ได้ข้างใน — **แก้ให้ badge เองกดได้เลย**: เปลี่ยนจาก `<span className="fg-status-pill...">` เป็น `<button className="fg-status-pill..." onClick={e => { e.stopPropagation(); onUpdateRow(row.id, 'status', !row.status); }}>` (มี `stopPropagation` กันไม่ให้ไป trigger การขยาย/ย่อ row ที่ครอบอยู่) — เปลี่ยนคำใน `FailureGroupsPanel.tsx` ทั้ง badge และ checkbox label ในตัว inline editor จาก Ready/Pending → Complete/Incomplete ให้ตรงกัน, เปลี่ยนคำในสถิติแถบบนสุดจาก "X% ready" → "X% complete" ด้วยเพื่อความสอดคล้อง
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (มี unused-import warning 1 จุดจาก `ArrowRight` ที่ปุ่มที่ลบไปเคยใช้ — ลบ import ออกด้วยแล้ว) + เทสต์ทั้งโปรเจกต์ **97/97 ผ่าน** ไม่มี regression + grep ยืนยันไม่มี `showFgSummary`/`FailureGroupsSummaryModal`/`onOpenSummary` เหลือค้างในโค้ดเลย — **ยังไม่ได้ manual test รอบนี้** รอผู้ใช้ลอง: แท็บ Failure Groups ต้องไม่มีปุ่ม "Failure Groups"/Summary ค้างอยู่แล้ว, กด badge สถานะที่แถวเซนเซอร์ (ไม่ต้องขยาย row ก่อน) ต้องสลับ Complete/Incomplete ได้ทันที, % complete บนแถบสถิติต้องอัปเดตตามด้วย

- **🆕 2026-08-06 — 🐛 พบสาเหตุ (ยังไม่แก้ รอผู้ใช้ confirm repro ก่อน): สร้าง Failure Group แล้วปิดโปรแกรมเปิดใหม่ ข้อมูลหาย**: ผู้ใช้ถามว่า "ไม่ได้จำค่าไว้ หรือจำไว้แต่ code ทำงานผิดพลาด" — สอบสวนโค้ดจริงแล้วสรุปว่า **เป็น race condition ระหว่างการเขียนไฟล์แบบ async กับการปิดโปรแกรม ไม่ใช่ "ไม่ได้บันทึกเลย"**:
  - `persistFailureGroupState` (`Dashboard.tsx`) เรียก `updateWorkspaceData(...)` แบบ**ไม่ await** — ฟังก์ชันนี้เป็น chain หลายจังหวะ (`loadWorkspaceData` อ่านไฟล์ → patch → `saveWorkspaceData` เช็ค folder + เขียนไฟล์ + อัปเดต recent-workspaces list — แต่ละจุดเป็น async IPC round-trip ไป Rust)
  - ปุ่มปิดหน้าต่าง (X) ใน [`TitleBar.tsx:166`](src/components/TitleBar.tsx) เรียก `appWindow.close()` ตรงๆ ไม่รอ pending write ใดๆ เลย + ฝั่ง Rust ([`lib.rs:2625-2661`](src-tauri/src/lib.rs)) มี exit-guard ที่พอเห็นว่าไม่มีหน้าต่างที่มองเห็นได้เหลือ จะสั่ง `app_handle.exit(0)` ทันที (มี fallback `std::process::exit(0)` บังคับอีกทีถ้า 500ms แล้วยังไม่จบ) — **ไม่มีขั้นตอนไหนรอ JS เขียนไฟล์ที่ค้างอยู่ให้เสร็จก่อนเลย**
  - ถ้าผู้ใช้กด Create แล้วปิดโปรแกรมทันที มีโอกาสสูงที่ process จะถูกฆ่าก่อนเขียนไฟล์เสร็จ — เป็นปัญหาเชิงระบบที่กระทบทุก action ที่ผ่าน `updateWorkspaceData`/`saveWorkspaceData` ไม่ได้จำกัดแค่ failure group แต่สังเกตเห็นชัดที่นี่เพราะมักเป็น action สุดท้ายก่อนปิดโปรแกรม
  - **ระวัง**: จุดที่เกี่ยวกับการปิดหน้าต่างเคยพังมาแล้ว 2 ครั้งในเซสชันนี้ (จาก `onCloseRequested` ที่ทำให้ปิดโปรแกรมไม่ได้เลย) ห้ามใช้แนวทางบล็อกการปิดหน้าต่างโดยตรงอีก — ถามผู้ใช้กลับไปว่ากด Create แล้วปิดทันทีหรือเปล่า เพื่อ confirm theory ก่อนเสนอทางแก้ แต่ยังไม่ได้คำตอบ (ผู้ใช้เปลี่ยนไปสั่งงานอื่นต่อ — ลบปุ่ม Save As) **ยังไม่ได้ implement อะไรเลย รอ confirm + ออกแบบทางแก้ที่ปลอดภัยก่อน**

- **🆕 2026-08-06 — ลบฟีเจอร์ "Save As" (สร้างสำเนา workspace ชื่อใหม่) ทิ้งทั้งหมด**: ผู้ใช้ส่งภาพชี้ปุ่ม disk icon บน titlebar บอก "ไม่ได้ใช้ เอาออกเลย รวมถึง code ที่เกี่ยวข้องด้วย" — ไล่โค้ดก่อนแล้วพบว่า `SaveAsWindow.tsx` เป็น component เดียวที่ใช้ร่วมกันระหว่าง 2 โหมด: `mode=save-as` (สร้างสำเนา — ที่จะลบ) กับ `mode=rename` (เปลี่ยนชื่อ workspace เดิม — **ต้องเก็บไว้** เพราะเป็น mechanism เดียวที่ใช้ rename จากเมนูของหน้าต่าง Predictive Model) — ต้องแยกให้ชัดว่าจุดไหนลบได้จุดไหนต้องเก็บ:
  - **ลบ**: `TitleBar.tsx` (ปุ่ม disk icon + prop `onSaveAs`), `App.tsx` (`handleManualSaveAs` + `onSaveAs` ที่ส่งเข้า `useAppMenu`/`<TitleBar>`), `useAppMenu.ts` (`onSaveAs` ใน interface + menu item "Save As…" ในเมนู File หลัก), `Dashboard.tsx` (`saveWorkspaceAs` ออกจาก `DashboardRef` + `useImperativeHandle`, ทั้ง effect `setupSaveAsListeners` ที่ฟัง `request-save-as-data`/`save-as-submit`, รวมถึง `localNameRef`/`buildStateRef` ที่มีไว้ใช้เฉพาะจุดนี้), `useSubWindowMenu.ts` (`doSaveAs` + menu item "Save As…" ในเมนู File ของหน้าต่าง Predictive Model — ลบเพราะเป็นฟีเจอร์เดียวกัน ทิ้งไว้ครึ่งเดียวจะขัดแย้งกันเอง)
  - **เก็บไว้ไม่แตะ**: `doRename`/menu item "Rename Workspace" ใน `useSubWindowMenu.ts`, `SaveAsWindow.tsx` ทั้งไฟล์ (ยังต้องใช้สำหรับ mode=rename) — เพิ่มคอมเมนต์อธิบายว่า branch `mode=save-as` ภายในไฟล์นี้กลายเป็น dead code แล้ว (ไม่มี caller ไหนส่ง `mode=save-as` อีกต่อไป ยืนยันด้วย grep) แต่ตั้งใจไม่แตะ logic ข้างในเพราะเสี่ยงทำ rename (ฟีเจอร์ที่ยังใช้อยู่) พังโดยไม่จำเป็น
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (เจอ `buildStateRef` ค้างอีกจุดที่ sync เข้า ref ตอนแรกลืมลบ — แก้แล้ว) + เทสต์ทั้งโปรเจกต์ **97/97 ผ่าน** ไม่มี regression + grep ยืนยันไม่มี `saveWorkspaceAs`/`onSaveAs`/`save-as-submit`/`doSaveAs` เหลือค้างที่ไหนอีก (เหลือแค่ comment อธิบายและ dead branch ที่ตั้งใจเก็บไว้ใน `SaveAsWindow.tsx`) — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: titlebar ต้องไม่มีปุ่ม disk icon แล้ว, เมนู File หลักต้องไม่มี "Save As…" แล้ว, ฟีเจอร์ Rename Workspace (ทั้งจาก titlebar เดิม inline-edit และจากเมนู Predictive Model) ต้องยังทำงานปกติไม่กระทบ

- **🆕 2026-08-06 — ตรวจสอบ sync ระหว่างแท็บ Sensor กับแท็บ Failure Groups (ไม่พบบั๊ก) + git push ครั้งแรกของเซสชันนี้ + อัปเดต README/CLAUDE.md/เอกสารนี้เอง**:
  - **ตรวจ sync 2 แท็บ**: ผู้ใช้กังวลว่า assign/remove sensor จากแท็บนึงจะไม่ไปอัปเดตอีกแท็บ (ประสบการณ์เดิมจากตอนที่ FG เป็นหน้าต่างแยกที่เคย sync ยาก) — ไล่โค้ดแล้วยืนยันว่า **sync กันแน่นอน เพราะทั้งสองแท็บอ่าน `fgGroups`/`fgRows` state ก้อนเดียวกันใน `Dashboard.tsx`** (บรรทัด ~205-208) ไม่มี copy แยก — ต่างจากปัญหาเดิมที่เป็นคนละ React tree คนละหน้าต่าง ไม่พบบั๊กจากการตรวจโค้ด (ยังไม่ได้ manual test เองเพราะ Tauri app ไม่เปิดผ่าน browser preview ได้)
  - **Git audit + commit + push ครั้งแรก**: ผู้ใช้ขอให้ตรวจไฟล์ก่อน push — เจอว่า `.gitignore` ถูกต้อง (ครอบคลุมจุดที่เคยแก้ไปก่อนหน้า), ไม่มี secret/credential หลุด, แต่**ยังไม่มี commit อะไรเลยตั้งแต่ 14 ก.ค.** (`git log` ล่าสุดคือ `cb6d105`) งานทั้งหมดสะสมอยู่ใน working tree เฉยๆ — repo มี 2 remotes (`origin` = org, `personal` = ของผู้ใช้เอง) ถามแล้วผู้ใช้เลือก push ไป `personal` — commit เป็นก้อนเดียว (`6dfb269`) รวมทุกอย่างสะสมมาตั้งแต่ก่อน 16 ก.ค. ถึงงานวันนี้ (ผู้ใช้ขอให้โชว์ commit message ก่อน commit จริง) — **ติดปัญหา git identity ไม่เคยตั้งค่าบนเครื่องนี้เลย** (`Author identity unknown`) ไม่ได้แก้ git config เองตาม Git Safety Protocol ให้ผู้ใช้รัน `git config --global user.name/user.email` เอง แล้ว commit ต่อสำเร็จ — push ไป `personal main` สำเร็จ (branch ใหม่บน repo ว่างเปล่า)
  - **README.md ล้าสมัยมาก**: ผู้ใช้สังเกตว่าไม่เคยอัปเดต README เลยทั้งที่ทำงานมานาน — ตรวจแล้วพบว่าจริง (`git log` ล่าสุดของไฟล์นี้คือ 2 ก.ค., ไม่ได้อยู่ใน commit ที่เพิ่ง push ด้วย) เนื้อหาอ้าง `cd Exploring-data-with-csv` (ชื่อ directory เก่า), component ชื่อเก่า (`ImportScreen`), ไม่มี Failure Groups/Predictive Model/Alarm Setpoints/multi-chart-type เลย — **เขียนใหม่ทั้งไฟล์** อ้างอิงจาก `CLAUDE.md` + `package.json` จริง (scripts/deps ตรวจแล้วตรง) ครอบคลุม features ปัจจุบัน, sub-window list ที่ถูกต้อง (3 ไม่ใช่ 4), setup/run/test commands, project structure ย่อ
  - **`CLAUDE.md` มี 1 บรรทัดที่ผิดไปแล้ว**: ส่วน "Windows & capabilities" ยังอ้างถึง sub-window `failure-group` ที่ถูกลบไปแล้ว — เพิ่มบรรทัดแก้ไขต่อท้ายอธิบายการเปลี่ยนแปลง (ไม่ได้ลบของเดิมทิ้ง เพิ่มหมายเหตุต่อแทน เพื่อให้เห็น diff ของความเข้าใจเก่า→ใหม่)
  - **🔧 พบว่าเอกสารนี้เอง (`PROJECT_HANDOVER.md`) มีส่วน reference (§1/§3/§5/§7/§8) ที่ค้างมาตั้งแต่ 16 ก.ค. และผิดไปแล้วหลายจุด** — ทั้งที่หัวไฟล์บอกว่า "อัปเดตอัตโนมัติทุกครั้งที่ทำงานเสร็จ" แต่ที่ผ่านมามีแต่การ**เพิ่ม entry ใหม่ต่อท้ายเป็น changelog** ไม่เคยย้อนกลับไปแก้ส่วน reference/overview ด้านบนให้ตรงเลย ทำให้ session ใหม่ที่อ่านเอกสารนี้ตั้งแต่ต้นจะได้ข้อมูลผิด (เช่น "ยังไม่มีอะไร commit เลย" ทั้งที่ commit ไปแล้ว, "4 sub-window" ทั้งที่เหลือ 3, README "ยังไม่ได้แก้" ทั้งที่เพิ่งแก้) — **แก้ไขรอบนี้**: อัปเดตวันที่ที่หัวไฟล์ + เพิ่มคำอธิบายชัดเจนว่า §1-§9 คือ reference (ต้องตรงปัจจุบันเสมอ) ส่วน §6.1 เป็นต้นไปคือ history log (ไม่ต้องตรงปัจจุบัน แค่ตรง ณ วันที่บันทึก), แก้ §1 (overview พูดถึง FG ที่ย้ายเข้า Dashboard แล้ว), §3 (sub-window list + FailureGroupsPanel.tsx), §5 (entry-point routing ตัด `failure-group` ออก), §7 (เขียนใหม่ทั้งหมดเป็นสถานะ git จริง), §8 ข้อ 1 และ 3 (mark resolved) — **บทเรียนสำหรับ session ถัดไป**: ทุกครั้งที่ทำงานเสร็จและจะ log เข้าเอกสารนี้ ต้องเช็คด้วยว่า entry ใหม่ทำให้ส่วน reference (§1-§9) ข้อไหนล้าสมัยไปด้วยไหม ไม่ใช่แค่เพิ่ม entry ท้ายไฟล์เฉยๆ
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (README/CLAUDE.md/เอกสารนี้เป็น markdown ล้วน ไม่กระทบ build) — **✅ commit + push แล้ว**: `5815b04` push ไป `personal main` สำเร็จ

- **🆕 2026-08-06 — `git pull` เจอ commit ใหม่จากอีกเครื่อง**: ผู้ใช้สั่ง `git pull` — เจอ `c560884` ("Redesign Add Special Sensor: click-driven builder, fix silent calc failures") บน `personal/main` ที่เครื่องนี้ไม่มี (ยืนยันด้วย `git fetch --all` แล้วเทียบ `git log` ของแต่ละ remote ก่อน pull) — เป็นงานที่ทำจากอีกเครื่องแล้ว push เข้ามาตามที่ตั้งใจไว้ (เหตุผลทั้งหมดของการตั้ง `personal` remote) — pull แบบ fast-forward สะอาด ไม่มี conflict (local ไม่มี commit ที่ diverge) เปลี่ยน 10 ไฟล์ ~1200 บรรทัด รวมถึงแก้ `AddSensorWindow.tsx`/`SensorTooling.tsx` ใหม่ทั้งหมด (redesign UI แบบ click-driven แทน dropdown เดิม) + เพิ่ม `src/config/operations.ts` (operation registry แบบ single source of truth ใหม่) + `useCalculationEngine.ts` (hook ใหม่) — `tsc --noEmit` + เทสต์ 97/97 ผ่านหลัง pull ไม่มี regression

- **🆕 2026-08-06 — ลบ 3 operation ออกจาก Add Special Sensor: "Subtract the rest from one", "Divide one by the rest" (ทั้งคู่คือ multi-op แบบมี base sensor), และ "Product"**: ผู้ใช้ถามให้อธิบายการทำงานของปุ่มเหล่านี้ก่อน (เจอจาก UI ที่เพิ่ง pull มาจากอีกเครื่อง) แล้วสั่งให้ลบทิ้งทั้งหมด "มันติดมากับของเก่าซึ่งไม่ได้ใช้แล้ว" — งานนี้ซับซ้อนกว่าที่คิดตอนแรกเพราะ `subtract`/`divide` (multi-op พร้อม base sensor) กับ `efficiency_pct` (Efficiency % — **ไม่ได้ให้ลบ**) ใช้ infrastructure เดียวกัน (`baseSensor` state, `pickingBase` UI, `BASE_OP_IDS`/`NEEDS_BASE_SENSOR` list) ต้องแยกเอาเฉพาะส่วนที่เกี่ยวกับ subtract/divide-with-base ออก เก็บกลไก base-sensor ทั้งหมดไว้ให้ efficiency_pct ใช้ต่อ:
  - **`src/config/operations.ts`**: ลบ entry `subtract`(requiresBase)/`divide`(requiresBase)/`product` ออกจาก `multi` array — เหลือ `sum`/`mean`/`median`/`temp_spread`/`abs_diff`/`efficiency_pct`
  - **`src/types.ts`**: `MultiOperationType` จาก `'sum'|'mean'|'median'|'product'|'subtract'|'divide'` → เหลือ `'sum'|'mean'|'median'` — ลบ field `multiOp.baseSensor?: string` ออกจาก `SensorOperationConfig` ด้วย (พิสูจน์แล้วว่า field นี้ไม่มีจุดไหนใช้อีกเลยหลังลบ เพราะ `efficiency_pct` เป็น formula-based ไม่ผ่าน `multiOp` เลย)
  - **`src/hooks/useCalculationEngine.ts`**: `LEGACY_MULTI_IDS` เหลือ `{sum,mean,median}`, `NEEDS_BASE_SENSOR` เหลือ `{efficiency_pct}` เท่านั้น (เดิมมี subtract/divide ปนอยู่ด้วย) — ลบ `needsBase`/`baseSensor` ออกจาก `multiOp` config ที่ build ในโหมด legacy (พิสูจน์แล้วว่า `needsBase` จะเป็น false เสมอหลังแก้ เพราะไม่มี legacy op ไหนต้องการ base sensor อีกแล้ว) — **เก็บ `baseSensor` state/`setBaseSensor` ไว้ทั้งหมด** เพราะ efficiency_pct ยังใช้อยู่
  - **`src/components/windows/SensorTooling.tsx`**: `AGG_OP_IDS` ลบ `"product"` ออก (เหลือ sum/mean/median/temp_spread/abs_diff — คุม section "Combine all"), `BASE_OP_IDS` เหลือ `["efficiency_pct"]` (คุม section "Compare one against the rest" — ตอนนี้เหลือปุ่มเดียว) — ลบ branch แสดงข้อความ preview แบบ raw subtract/divide (`${baseName} ${op.symbol} (${others})`) ที่กลายเป็น dead code ออก เหลือแค่ branch efficiency_pct ตรงๆ — **ไม่แตะ** กลไก `pickingBase`/star-icon/click-to-mark-base ที่ Target Sensors chips เลย เพราะ efficiency_pct ยังต้องใช้
  - **`src/types/calculationEngine.ts`**: เก็บ field `requiresBase?: boolean` ไว้ (ยังใช้กับ efficiency_pct)
  - **`src-tauri/src/operation_registry.rs`**: ลบ `product` ออกจาก `build_multi_ops()`, ลบฟังก์ชัน `execute_base_op`/`is_base_op` ทั้งคู่ทิ้ง (ไม่มีใครเรียกอีกแล้ว), ลบ `"product"`/`"subtract"`/`"divide"` ออกจาก `multi_op_name` match
  - **`src-tauri/src/lib.rs`**: `MultiOperation` struct ลบ field `base_sensor` ออก, `calculate_new_sensor` command ลบ branch `if operation_registry::is_base_op(...) {...} else {...}` ทิ้ง เหลือแค่ logic เดิมของ else (sum/mean/median loop) ทำงานตรงๆ ไม่มีเงื่อนไขแยกแล้ว
  - **`src-tauri/src/chart_query.rs`** (bounded preview pipeline ที่ทำงานคู่ขนานกับ `calculate_new_sensor` — พบว่ามี logic subtract/divide/product implementation แยกต่างหากอีกชุดที่นี่ ต้องแก้คู่กัน): ลบ field `base_sensor` ออกจาก `MultiOp` struct, ลบ variant `Product`/`Subtract(Option<usize>)`/`Divide(Option<usize>)` ออกจาก enum `MultiKind` เพิ่ม variant `Unknown` แทนสำหรับ op ที่ไม่รู้จัก (ผลลัพธ์ null ทั้งคอลัมน์ เหมือนพฤติกรรมเดิม), ลบ match arm ที่เกี่ยวข้องใน parsing logic และใน `combine_multi()` — **เจอ test ที่ต้องแก้ตามด้วย**: `multi_op_subtract_with_base` (เทสต์ subtract-with-base โดยเฉพาะ) ลบทิ้งทั้งเทสต์, helper `multi_op(op, base)` ตัดพารามิเตอร์ `base` ออกเพราะไม่มีใครเรียกแบบมีค่าอีกแล้ว (เดิมมีแค่ 2 จุดที่เคยส่ง `Some("B")`/`Some("MISSING")` ซึ่งอยู่ในเทสต์ที่ลบไปแล้ว)
  - Verify: `npx tsc --noEmit` ผ่านสะอาด + เทสต์ frontend **97/97 ผ่าน** + `cargo check` ผ่านสะอาด (ครั้งแรกพัง เพราะลืมแก้ test helper ใน `chart_query.rs` ที่ยัง reference field `base_sensor` ที่ลบไปแล้ว — แก้แล้ว) + `cargo test` **58/58 ผ่าน** (+ 8 integration test ของ predictive model) ไม่มี regression + grep ยืนยันไม่มี `is_base_op`/`execute_base_op`/`base_sensor`/`"product"` เหลือค้างในฝั่ง Rust เลย ฝั่ง frontend เหลือแต่ของ `efficiency_pct` ที่ตั้งใจเก็บไว้ — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: เปิด Add Special Sensor เลือก 2+ sensor ต้องไม่เห็นปุ่ม "Subtract the rest from one"/"Divide one by the rest"/"Product" อีกแล้ว, ปุ่ม "Efficiency %" ต้องยังทำงานปกติ (คลิก sensor เพื่อ mark เป็น base ได้เหมือนเดิม), sum/mean/median/spread/abs-diff ยังทำงานถูกต้องเหมือนเดิม — **ยังไม่ได้ commit/push รอบนี้**

- **🆕 2026-08-06 — เพิ่ม unit ต่อท้ายค่าใน hover tooltip ของ Line chart**: ผู้ใช้ส่งภาพชี้ tooltip ตอน hover กราฟ (โชว์แค่ `11TE1210.PV: 68.700` ไม่มีหน่วย) ขอให้ดึง unit จาก master data มาโชว์ด้วย รวมถึง special sensor (คำนวณเองผ่าน Add Special Sensor) ก็ต้องมี unit ด้วย:
  - ตรวจก่อนพบว่า **ข้อมูลพร้อมอยู่แล้ว** ไม่ต้องเพิ่ม field ใหม่เลย — `Dashboard.tsx` มี `extraSensorMetadata` state (เก็บ unit/description/component ของ special sensor ที่ผู้ใช้กรอกตอนสร้างจาก `AddSensorWindow.tsx`) merge เข้ากับ `sensorMetadataProp` (จาก mapping CSV) เป็น `sensorMetadata` ตัวเดียวที่ทุก panel ใช้อยู่แล้ว (บรรทัด ~139-145) — **ปัญหาจริงคือแค่ `sensorMetadata` ไม่เคยถูกส่งต่อไปถึง `LineChart.tsx` เลย** (`ChartTypes.ts`/`Chart.tsx` ไม่มี prop นี้มาก่อน)
  - **`src/components/charts/ChartTypes.ts`**: เพิ่ม `sensorMetadata?: SensorMetadata[] | null` เข้า `ChartProps`
  - **`src/components/charts/Chart.tsx`**: forward prop ต่อให้ `<LineChart>` (เฉพาะ Line — Scatter/Pair Plot มี tooltip ของตัวเองแยกต่างหาก ยังไม่ได้ทำ ผู้ใช้ไม่ได้ขอ)
  - **`src/components/charts/LineChart.tsx`**: ใช้ `useSensorMetaMap`/`normalizeSensorTag` (hook เดียวกับที่ `SensorSelection.tsx`/`Dashboard.tsx` ใช้อยู่แล้ว เพื่อให้กฎ normalize tag ไม่เพี้ยนจากที่อื่น) สร้าง lookup map แล้วต่อท้ายค่าใน tooltip formatter: `${p.seriesName}: ${displayValue}${unit ? ' ' + unit : ''}` — ไม่มี unit ก็ไม่แสดงอะไรต่อท้าย (ไม่ใช่ "undefined") — เพิ่ม `sensorMetaMap` เข้า deps array ของ `option` useMemo ด้วย (ไม่งั้น unit จะไม่ sync ตอน metadata เปลี่ยน)
  - **`Dashboard.tsx`**: ส่ง prop `sensorMetadata={sensorMetadata}` เข้า `<Chart>` ที่จุด render กราฟ Line (ตัวแปร `sensorMetadata` ในนี้คือตัวที่ merge extra เข้าไปแล้ว ไม่ใช่ prop ดิบจาก mapping CSV เฉยๆ)
  - Verify: `npx tsc --noEmit` ผ่านสะอาด + เทสต์ **97/97 ผ่าน** ไม่มี regression — **ยังไม่ได้ manual test** รอผู้ใช้ลอง hover กราฟดูว่า sensor ที่มี unit ใน mapping CSV โชว์หน่วยต่อท้ายค่าไหม แล้วลองสร้าง special sensor ผ่าน Add Special Sensor ที่กรอก unit ไว้ ต้องโชว์ unit นั้นในกราฟด้วยเหมือนกัน — **ยังไม่ได้ commit/push รอบนี้เช่นกัน (รวมกับงานลบ 3 operation ด้านบน ยังไม่ commit)**

- **🆕 2026-08-06 — ปรับ UI ย่อย 2 จุด**: (1) ขนาดหน้าต่าง Add Special Sensor จาก 800×700 → **1000×800** (ลองที่ 1200×1000 ก่อนแล้วผู้ใช้บอกใหญ่ไป) แก้ที่ `Dashboard.tsx` จุด `new WebviewWindow('add-sensor', {...})` — หน้าต่างนี้**ไม่มีการจำขนาดที่ผู้ใช้ปรับเอง** เปิดใหม่ได้ขนาด default เสมอ (ยังปรับขนาดเองได้ ไม่ได้ล็อก `resizable`) (2) ข้อความใต้ Target Sensors ตอนเลือก Efficiency % จาก "Click a sensor above to mark it as the **starting value**." → "...as the **input**." (ผู้ใช้ชี้ว่าคำเดิมเข้าใจยาก — คำว่า "starting value" เป็นของเก่าที่เหลือจากตอนมี subtract/divide ซึ่งลบไปแล้ว ตอนนี้เหลือแค่ Efficiency % ใช้ เลยเปลี่ยนให้ตรงกับคำว่า input/output ที่ปุ่มนั้นใช้อยู่แล้ว)

- **🆕 2026-08-06 — 📋 Audit ทั้งโปรเจกต์เรื่อง "ปิดโปรแกรมแล้วข้อมูลหาย" + เขียนแผน `docs/PERSISTENCE_PLAN.md`**: ผู้ใช้ถามว่ามีวิธีแก้ปัญหา special sensor หายตอน resume ไหม แล้วสั่งให้ค้นหาแนวทางเพิ่มเติมนอกจากที่เคยคุยกันไว้ + เขียนเป็นแผน แล้วสั่งเพิ่มระหว่างทางให้ไล่ทั้งโปรเจกต์ว่ามีส่วนไหนเป็นปัญหาแบบเดียวกันอีก — **ยังไม่ได้แก้โค้ดอะไรเลย เป็นงานสำรวจ+วางแผนล้วนๆ**:
  - **ผลการ audit — แยกได้ 2 ประเภทที่วิธีแก้คนละเรื่องกัน**:
    - **Class A (ข้อมูลจริงหายจาก RAM)**: grep `columns.push`/`headers.push`/`state_lock = Some` ทั่ว `lib.rs` แล้วยืนยันว่า**มีแค่ special sensor รายการเดียว** — จุดที่แก้ไข `ColumnarData` ตอน runtime มีแค่ 2 จุดคือ `calculate_new_sensor` (`lib.rs:1761-1762`) กับ `evaluate_formula` (`lib.rs:2113-2114`) ซึ่งทั้งคู่คือการสร้าง special sensor → **ขอบเขตปัญหาจำกัดชัดเจน ไม่มีฟีเจอร์อื่นที่เขียนข้อมูลลง RAM แล้วไม่ลงดิสก์**
    - **Class B (ค่า config หาย เพราะไม่เคยเขียนลง `WorkspaceState`)**: เจอ **6 จุด** — `sensorColors` (สีเส้นต่อ sensor, `Dashboard.tsx:368`), `sensorAxisRange` (Y-axis pin, `:371`), `xPin`/`yPin` ของ Scatter (`ScatterChart.tsx:116-117` — หายตอนสลับ chart type ด้วย เป็นบั๊กประเภทเดียวกับ `scatterAxes` ที่เพิ่งแก้), `clusters` ของ Pair Plot (`PairPlotChart.tsx:167` — lasso ที่ผู้ใช้ลากเองเสียไป), แท็บที่เปิดค้าง, ปุ่ม Y/M/W/D/H ที่เลือก — ส่วนผลลัพธ์ fit ของ PM ไม่นับเป็นบั๊กเพราะคอมเมนต์ที่ `PredictiveModelBuild.tsx:641-644` ระบุว่าจงใจไม่ persist กัน JSON บวม
    - ตรวจแล้วว่า **14 field ที่ persist อยู่ทำงานถูกต้องหมด** (รวม `outputDir` ที่เช็คแยกเพราะไม่อยู่ใน PM slice — พบว่าเขียนผ่าน `updateWorkspaceData` ที่ `PredictiveModelBuild.tsx:1685` ต่างหาก ถูกต้องแล้ว)
  - **แนวทางแก้ Class A — เพิ่มจาก 2 เป็น 5 ทางเลือก** (3 อันใหม่มาจากการ search): (1) เขียนกลับลง CSV ต้นฉบับ ❌ ขัดกฎ `CLAUDE.md` เอง + เสี่ยงทำไฟล์ผู้ใช้พัง (2) **recipe replay** ⭐ (3) 🆕 snapshot ทั้ง dataset เป็น Arrow IPC/Parquet ใน `$APPDATA` — resume เร็วขึ้นมากแต่เปลืองดิสก์ + ต้องทำ cache invalidation (4) 🆕 sidecar เก็บเฉพาะคอลัมน์ที่คำนวณ — ❌ มี failure mode "แถวเลื่อนแบบเงียบๆ" ถ้า CSV ต้นฉบับเปลี่ยน อันตรายกว่าปัญหาเดิม (5) 🆕 เปลี่ยนเป็น DuckDB/SQLite — ❌ rewrite ใหญ่เกินเหตุ (แต่ตรงกับ "step 3" ที่ `CLAUDE.md` วางไว้ เก็บไว้พิจารณาตอน RAM ไม่พอจริง)
  - **ข้อเสนอ: ทางเลือก 2 (recipe replay)** — เก็บสูตร/config ลง workspace JSON แล้วคำนวณใหม่หลัง `load_csv` ทุกครั้ง เหตุผลชี้ขาดคือ**ต้นทุนคำนวณใหม่น่าจะน้อยมากเทียบกับการ re-parse CSV ที่ทำอยู่แล้วทุกครั้ง** (เป็น row-wise op ฝั่ง Rust วนแถวเดียวจบ) — แต่**ระบุไว้ชัดว่าเป็นการประเมินจากการอ่านโค้ด ยังไม่ได้วัดจริง** เลยใส่ Phase 0 ในแผนให้วัดก่อนลงมือ ถ้าช้ากว่าคาดค่อยพลิกไปทางเลือก 3
  - **พบว่าข้อมูลใน BACKLOG ข้อ 7 ล้าสมัยไปแล้ว 1 จุด**: เดิมบอกว่าต้องเขียน master data กลับลง mapping CSV — แต่ `extraSensorMetadata` ถูกเพิ่มเข้า `WorkspaceState` ทีหลังจากที่เขียน entry นั้น ทำให้ master data persist ถูกต้องอยู่แล้ว **ตัดงานส่วนนั้นออกได้เลย เหลือแค่ replay ค่าจริง** — ใส่หมายเหตุแก้ไว้ใน BACKLOG แล้ว + ชี้ไปที่แผนใหม่
  - **จุดที่ต้องระวังตอน implement (เจอจากการไล่โค้ด)**: `AddSensorWindow.tsx:98` เรียก `load_csv` ซ้ำอีกจุด (retry path) ซึ่งก็ล้าง special sensor ทิ้งเหมือนกัน — ต้อง replay ตรงนั้นด้วย ไม่งั้นจะเจอบั๊ก "เปิดหน้า Add Special Sensor แล้ว sensor ที่เพิ่งสร้างหาย"
  - **สถานะ: รอผู้ใช้ตอบ 3 คำถามท้ายเอกสาร** (เอาทางเลือก 2 ไหม / จะทำ Class B พร้อมกันไหม / ถ้าทำ Class B เอาทั้ง 4 จุดหรือเฉพาะสีเส้น+axis pin)

- **🆕 2026-08-06 — แก้ Class B ทั้ง 4 จุด (จาก `docs/PERSISTENCE_PLAN.md`): สีเส้น, Y-axis pin ของ Line, axis pin ของ Scatter, ค่า shortcut Y/M/W/D/H**: ผู้ใช้สั่งทำ B1/B2/B3/"B5" (เข้าใจตรงกันแล้วว่า "B5" ที่ผู้ใช้พูดถึงคือค่า shortcut Y-M-W-D-H ในเอกสารเรียกว่า B6 — ไม่ใช่ B5 ที่หมายถึงแท็บที่เปิดค้าง ยังไม่ได้ทำ B4/B5-ตามเอกสารเลย) ก่อนเริ่มเช็คตามที่ผู้ใช้ขอว่า "time period" ที่เลือกไว้จริง persist อยู่แล้วไหม — ยืนยันว่า `filters.timestampStart/End` persist ถูกต้องอยู่แล้ว ส่วนที่หายคือแค่ค่าที่พิมพ์ไว้ในกล่อง shortcut เท่านั้น (`relativeAmount`/`relativeUnit` ไม่ใช่ตัว filter ที่ใช้งานจริง):
  - **`types.ts`**: เพิ่ม 4 field ใหม่ใน `WorkspaceState` — `sensorColors?`, `sensorAxisRange?`, `scatterAxisPins?: ScatterAxisPins`, `relativeTimeRange?: { amount, unit }` — สร้าง type export ใหม่ `ScatterAxisPin`/`ScatterAxisPins` (แยกจาก `scatterAxes` เดิมที่เก็บแค่ "sensor ไหนอยู่บนแกนไหน" — ตัวนี้เก็บ "แกนนั้น min/max เท่าไหร่")
  - **`Dashboard.tsx`**: B1/B2 แค่เปลี่ยน `useState` init จาก `{}` เป็น `initialState?.xxx ?? {}` แล้วต่อเข้า `buildWorkspaceState()` — ง่ายเพราะ setter เดิมไม่ต้องแก้เลย B3 (Scatter axis pin) ซับซ้อนกว่าเพราะ state เดิมอยู่ใน `ScatterChart.tsx` ไม่ใช่ Dashboard ต้อง lift ขึ้นมาแบบเดียวกับที่เคยทำกับ `scatterAxes`/`scatterX`/`scatterY` ก่อนหน้านี้: เพิ่ม state `scatterAxisPins` + handler `handleScatterAxisPinsChange` ใน Dashboard, ส่ง prop ผ่าน `Chart.tsx` ไปยัง `ScatterChart.tsx` B6 (relativeAmount/relativeUnit) ย้ายตำแหน่ง declare ขึ้นมาก่อน `buildWorkspaceState` (เจอ TS error "used before declaration" แบบเดียวกับตอนแก้ Build Model logic ก่อนหน้านี้ในเซสชันนี้ — รูปแบบบั๊กเดิมที่เคยเจอมาแล้ว แก้แบบเดิมคือย้าย state ขึ้นมาแทนที่จะย้าย `buildWorkspaceState` ลง)
  - **`ChartTypes.ts`/`Chart.tsx`**: เพิ่ม prop `scatterAxisPins`/`onScatterAxisPinsChange` ส่งต่อเป็น pass-through เหมือน `scatterX`/`scatterY`/`onScatterAxesChange` เดิมทุกจุด
  - **`ScatterChart.tsx`**: `xPin`/`yPin` local state seed จาก `persistedPins?.x`/`persistedPins?.y` แทน `null` เปล่าๆ (lazy init เหมือน `scatterX`/`scatterY`) เพิ่ม effect ใหม่ยิง `onScatterAxisPinsChange({x: xPin ?? undefined, y: yPin ?? undefined})` ทุกครั้งที่ pin เปลี่ยน (apply/clear) — ไม่แตะ logic การคำนวณ/validate pin เดิมเลยสักบรรทัด แค่เปลี่ยนจุดกำเนิดค่าเริ่มต้นกับเพิ่มจุดส่งค่าออก
  - Verify: `npx tsc --noEmit` ผ่านสะอาด (เจอ error "used before declaration" 1 รอบระหว่างทาง แก้แล้ว) + เทสต์ **97/97 ผ่าน** ไม่มี regression — grep ยืนยัน 4 field ใหม่ต่อสายครบทุกจุดที่ควรมี (`types.ts` → `Dashboard.tsx` → ไฟล์ chart ที่เกี่ยวข้อง) — **ยังไม่ได้ manual test** รอผู้ใช้ลอง: ตั้งสีเส้น + pin Y-axis ของ Line chart, ตั้ง axis pin ของ Scatter (ปุ่มไม้บรรทัด), พิมพ์ค่า shortcut Y-M-W-D-H ที่ไม่ใช่ default (เช่น "3" + "W") → ปิดโปรแกรม เปิด workspace เดิมใหม่ → ทั้ง 4 อย่างต้องกลับมาตามที่ตั้งไว้ รวมถึงลองสลับ chart type ไปมาด้วยว่า Scatter axis pin ไม่หายเหมือนเดิม — **ยังไม่ได้ commit/push** (รวมกับงานที่ค้างจากรอบก่อนๆในเซสชันนี้ที่ก็ยังไม่ commit เหมือนกัน)

- **🆕 2026-08-10 — ⚡ เขียน unit test ให้ครบทั้งโปรเจกต์**: คำสั่งจากผู้ใช้คือ "เริ่มจากที่แนะนำ (operation_registry.rs + useCalculationEngine.ts) แต่หลังจากนั้นต้องเขียน test ให้ครบทั้งหมด" — เป็นงานขอบเขตใหญ่ ไล่ทำทุกไฟล์ source ที่ยังไม่มีเทสต์เลยในโปรเจกต์ ทั้ง frontend และ Rust แบบต่อเนื่องจนครบ ไม่มีคำสั่งอื่นจากผู้ใช้ระหว่างทางนอกจากตอบคำถามเดียวว่าจะเทส `Dashboard.tsx` (orchestrator 1,908 บรรทัด) ลึกแค่ไหน → ผู้ใช้เลือก "เทสให้ครอบคลุมที่สุดเท่าที่ทำได้" แล้วใช้มาตรฐานเดียวกันนี้กับไฟล์ใหญ่อื่นๆ ต่อ (เช่น `PredictiveModelBuild.tsx` 3,896 บรรทัด) โดยไม่ถามซ้ำ:
  - **Frontend**: จาก baseline 97 เทสต์ (5 ไฟล์) → **586 เทสต์ (45 ไฟล์)** ครอบคลุมทุกไฟล์ที่เคยไม่มีเทสต์เลย — hooks ทั้งหมด (`useCalculationEngine`, `useSensorMetaMap`, `useThemeMode`, `useTablePage`, `useCoalescedDraw`, `useFormulaEditor`, `useAppMenu`, `useSubWindowMenu`, `usePMReport`), utils (`alarmLevels`, `dateFormat`), `workspaceManager.ts`, `errorReporter.ts`, chart component ทั้งชุด (`Chart`, `LineChart`, `ScatterChart`/`PairPlotCell`/`PairPlotChart` — mock `regl-scatterplot` แทนใช้ WebGL จริงใน jsdom, `ResponsiveECharts`), panel ลูกของ Dashboard ทั้งหมด (`DataTable`, `ColorPlatePicker`, `FilterPanel`, `FailureGroupsPanel`, `SensorSelection`), `Dashboard.tsx` เอง (mock child component ทุกตัวแล้วเทส wiring/state logic ล้วนๆ), หน้าต่างลูกทั้งหมดรวม `PredictiveModelBuild.tsx` (ไฟล์ใหญ่สุดในโปรเจกต์), `App.tsx` + `TitleBar` + `ErrorBoundary` + `ErrorToasts` + upload sub-component ทั้ง 6 ตัว
  - **Rust**: เพิ่ม 42 เทสต์ใหม่ใน `lib.rs` — path validation ทั้งชุด (`sanitize_filename_component`, `validate_read_path`, `excel_safe`, `validate_save_dir`, `validate_user_write_path` — ฟังก์ชัน security-critical ป้องกัน path traversal/CSV formula injection ที่ไม่เคยมีเทสต์มาก่อนเลยทั้งที่ doc comment ระบุชัดว่าเป็นจุดป้องกันความปลอดภัย), `check_formula_limits` (DoS guard กันสูตรยาว/ลึกเกิน), `ResolvedFilter` (ตรรกะ filter ที่ทุก data command ของ dashboard ใช้ร่วมกัน) — จาก baseline 98 unit + 8 integration → **139 unit + 8 integration**
  - **พยายามเทสต์ command ที่ใช้ `State<AppState>` โดยตรงด้วย** (`load_csv`, `get_all_sensors`, `calculate_new_sensor` ฯลฯ) **แต่ทำไม่สำเร็จ**: tauri 2.9.3 ไม่มี `State::from(&T)` ให้ใช้ตรงๆ ต้องผ่าน `tauri::test::mock_builder()` เท่านั้น ซึ่งต้องเปิด feature `test` ของ crate `tauri` เป็น dev-dependency — พอลองแล้วเจอ **Windows Application Control policy บล็อกการรัน build-script binary ที่ compile ใหม่ (os error 4551)** เป็นข้อจำกัดของเครื่องนี้ ไม่ใช่โค้ดผิด — **revert กลับหมดแล้ว** ทั้ง `Cargo.toml` (ลบ `features = ["test"]` ออก กลับไปมีแค่ `tempfile = "3"` ใน `[dev-dependencies]`) และไฟล์ `lib.rs` (ลบ `session_command_tests` module ที่เขียนไว้ทิ้งไปทั้งหมด) ยืนยันว่ากลับสู่สภาพ compile/test ผ่านสะอาดแล้วด้วย `cargo test --lib` (139 passed) — เก็บไว้เป็นข้อมูลสำหรับใครจะลองใหม่ในอนาคต (ต้องหาเครื่องที่ policy ไม่บล็อก หรือขอ allowlist จาก IT)
  - **แพทเทิร์น/กับดักที่เจอซ้ำๆ ระหว่างเขียนเทสต์รอบนี้** (มีประโยชน์สำหรับเทสต์ไฟล์ใหม่ในอนาคต): โปรเจกต์นี้ใช้ `.toBeTruthy()`/`.toBeNull()`/`.toEqual()` **ไม่มี** jest-dom matcher (`toBeInTheDocument` ใช้ไม่ได้); `.at(-1)` compile ไม่ผ่านเพราะ `tsconfig.json` target เป็น ES2020 (`lib: ["ES2020",...]`) ต้องใช้ `arr[arr.length-1]` หรือ helper `last()` แทนทุกครั้ง; `vi.mock` auto-hoist ตัวแปรที่ขึ้นต้นด้วย `mock` ใช้ไม่ได้ชัวร์ 100% กรณีโมดูลที่ mock มี side-effect รันตอน import (เช่น `initErrorReporter()` ที่ import ตอน `App.tsx` โหลด) ต้องใช้ `vi.hoisted()` แทนไม่งั้นเจอ TDZ `ReferenceError`; `Array.prototype.map(someFn)` ที่ `someFn` มี default param ตัวที่ 2 จะพังเงียบๆ เพราะ `map` ส่ง `(item, index, array)` เข้าไปทับ default เสมอ; jsdom ไม่มี layout engine จริง (`clientWidth`/`clientHeight` เป็น 0 เสมอ) component ที่เช็ค `if (width === 0) return` ต้อง stub `Object.defineProperty` เอง
  - Verify: `npx tsc --noEmit` ผ่านสะอาดทุกไฟล์ตลอดเซสชัน + `npx vitest run` **586/586 ผ่าน** (45 ไฟล์) + `cargo test --lib` **139/139 ผ่าน** (1 ignored ของเดิม) + `cargo test --test predictive_model_tests` **8/8 ผ่าน** — ไม่มี regression ตลอดทั้งเซสชัน — **งานทั้งหมดยังไม่ได้ commit/push** (เซสชันนี้เขียนเทสต์ล้วนๆ ไม่ได้แตะ production code เลย ยกเว้นบรรทัด `Cargo.toml`/`lib.rs` ที่ทดลองแล้ว revert คืนกลับไปแล้ว) — รวมกับงาน Class B persistence และงานลบ 3 operation จากรอบก่อนหน้าที่ก็ยังไม่ commit เหมือนกัน ตอนนี้มีงานค้างสะสมหลายรอบแล้วรอ commit/push เป็นก้อนใหญ่ก้อนเดียว หรือแยก commit ตาม logical change ก็ได้ รอผู้ใช้สั่ง

- **🆕 2026-08-11 — 🔍 Audit ไฟล์ test เก่า + เพิ่มกฎ "เขียนเทสต์คู่กับทุกการแก้โค้ด" + commit/push + build .exe + CHANGELOG.md**: ผู้ใช้กังวลว่าไฟล์ test 5 ไฟล์ที่มีมาก่อนเซสชันเขียนเทสต์ครั้งใหญ่ (`DataUploadPage.test.tsx`, `useChartData.test.ts`, `useDataUpload.test.ts`, `useMappingData.test.ts`, `useScatterSample.test.ts`) อาจล้าสมัยเพราะ UI เปลี่ยนไปหลายรอบโดยไม่มีใครแก้เทสต์ตาม:
  - **ผล audit**: อ่านทั้ง 5 ไฟล์ test เทียบทีละบรรทัดกับซอร์สโค้ดปัจจุบัน (component/hook ที่มันเทสต์) — **ทั้ง 5 ไฟล์ยังตรงกับพฤติกรรมปัจจุบันทุกจุด** ไม่มีจุดไหนล้าสมัย (field name, invoke command, UI flow ทุกจุดตรงกันหมด รวมถึง step 0/1/2 onboarding flow ที่ซับซ้อนสุดใน `DataUploadPage.tsx`) — รัน `npx vitest run` ยืนยัน 586/586 ผ่านจริง ไม่ใช่ผ่านเพราะเทสต์ไม่ครอบคลุมพอจะจับความเปลี่ยนแปลง
  - **เพิ่มกฎถาวรใน `CLAUDE.md`** section ใหม่ "Testing discipline": ทุกครั้งที่เขียนโค้ดใหม่หรือแก้โค้ดเดิม ต้องเขียน/แก้เทสต์ในรอบเดียวกันเสมอโดยไม่ต้องรอสั่ง รวมถึงเตือนให้ grep หา field/command/ข้อความ UI เก่าที่เปลี่ยนชื่อในไฟล์ test ก่อนถือว่างานเสร็จ — เพิ่มเป็นข้อใน Anti-patterns list ด้วย
  - **Commit งานที่ค้างจากเซสชันก่อนหน้าทั้งหมด** (แยก 2 commit ตาม logical change): `738f2a9` ลบ operation Product/Subtract/Divide + persist Class B (สี/Y-axis pin/scatter axis pin/relative time range), `238604c` ชุดเทสต์ 586 รายการ + docs + กฎ testing discipline
  - **Push**: `git push origin main` ล้มเหลว — บัญชี git บนเครื่องนี้ไม่มีสิทธิ์เขียนเข้า `origin` (Alpha-Com-Thailand/soothsayer-wizard-app, 403) ลองสลับไป push `personal` แทน แต่ auto-mode classifier ของ Claude Code บล็อกไว้เอง (มองว่าเป็นการพยายามเลี่ยงหลังโดนปฏิเสธรอบแรก) — ถามผู้ใช้ผ่าน AskUserQuestion แล้วผู้ใช้เลือก push ไปที่ `personal` (toeyps/soothsayer-wizard-app) — **push สำเร็จ** `c560884..238604c` — **`origin` ยังไม่ sync ค้างอยู่ 4 commit ตามหลัง `personal`** (ต้องขอสิทธิ์เขียนเพิ่มถ้าจะ sync origin ในอนาคต)
  - **เขียน `CHANGELOG.md` ใหม่** (ไม่เคยมีมาก่อน) — สรุปทุกอย่างที่ต่างจากเวอร์ชันที่เคย build/แจกจริงล่าสุดคือ **0.1.6** (0.2.1 เคยตั้งเลขไว้ในโค้ดมาสักพักแต่ไม่เคย build เป็น installer จริงมาก่อน) ดึงจาก `git log 0.1.6..HEAD` ทั้ง 10 commit จัดกลุ่มเป็น Breaking change / ฟีเจอร์ใหม่ / Bug fixes / Performance / Test coverage / เอกสาร / ฟีเจอร์ที่ถูกลบ เขียนเป็นภาษาไทยเน้นอาการที่ผู้ใช้เห็นจริง ไม่ใช่แค่ชื่อฟังก์ชัน — commit แยกเป็น `b2127f4`
  - **เพิ่มกฎถาวรใน `CLAUDE.md`** section ใหม่ "Release checklist": ทุกครั้งที่ build เป็น .exe/installer ต้องเขียน entry ใหม่ใน `CHANGELOG.md` ในรอบเดียวกันเสมอ (ผู้ใช้สั่งตรงๆ ว่า "ต้องทำแบบนี้ทุกรอบถ้าหากว่าจะ build เป็น app") พร้อมขั้นตอน bump version ในไฟล์ที่เกี่ยวข้อง (`package.json`/`Cargo.toml`/`tauri.conf.json`) และ tag release หลัง build สำเร็จ
  - **Build .exe**: รัน `scripts\build-installer-windows.ps1` (prerequisite ครบ, sidecar Python มีอยู่แล้วข้าม rebuild) — **รอบแรกล้มเหลว** เพราะสั่งด้วย `2>&1 | Tee-Object` ใน PowerShell 5.1 ซึ่งแปลง stderr ธรรมดาจาก `node.exe` ("Info Looking up installed tauri packages...") ให้กลายเป็น terminating error (สคริปต์เปิด `$PSNativeCommandUseErrorActionPreference=$true` ไว้) ไม่ใช่บั๊กจริงของ build — **รันใหม่โดยไม่ redirect stderr** (ใช้ `Start-Transcript` แทน) แล้วผ่านสำเร็จ: `npm run build` (tsc+vite) 15.85s → Rust release compile 1m45s → NSIS+MSI installer รวม ~3 นาที (warm cache, sidecar ไม่ต้อง rebuild) ได้ผลลัพธ์:
    - `src-tauri\target\x86_64-pc-windows-msvc\release\bundle\nsis\Wizard_0.2.1_x64-setup.exe` (57.9 MB, แนะนำให้ใช้ตัวนี้)
    - `src-tauri\target\x86_64-pc-windows-msvc\release\bundle\msi\Wizard_0.2.1_x64_en-US.msi` (58.9 MB)
    - build ไม่ได้เซ็นลายเซ็นดิจิทัล (unsigned) — Windows SmartScreen จะเตือนตอนเปิดครั้งแรก ต้องกด "More info" → "Run anyway"
  - Tag `v0.2.1` แล้ว push ไปที่ `personal` remote (`git tag -a v0.2.1 -m "v0.2.1 — see CHANGELOG.md"` + `git push personal v0.2.1`) เพื่อให้เป็น anchor สำหรับ diff ของ CHANGELOG.md รอบถัดไป
  - บันทึกทั้ง 2 กฎ (testing discipline, changelog-on-build) ลง Claude memory ส่วนตัวด้วยแล้ว เพื่อให้ติดตัวไปทุกโปรเจกต์ ไม่ใช่แค่ในนี้

- **🆕 2026-08-11 — ⚙️ Windows build เอาแค่ .exe (ไม่เอา .msi)**: ผู้ใช้ขอให้ build รอบหน้าได้แค่ไฟล์เดียว — เพิ่ม `"bundle": { "targets": ["nsis"] }` ใน `src-tauri/tauri.windows.conf.json` (ไม่แตะ `tauri.conf.json` หลักที่ macOS build ยังต้องใช้ `"all"` เพื่อได้ `dmg`/`app`) verify ด้วยการ build จริงอีกรอบ ยืนยันว่าได้ `Wizard_0.2.1_x64-setup.exe` ไฟล์เดียว ไม่มี `.msi` ปนมา — commit `dbc30e8` + จดกฎไว้ใน `CLAUDE.md` ว่าเป็นการตั้งค่าที่ตั้งใจ ห้ามลบทิ้งเฉยๆ (`6ffafdb`) — ระหว่างทางเจอไฟล์ .exe รอบแรกโดนแอนตี้ไวรัสลบ (ผู้ใช้บอกว่าจะไป allow เอง ไม่ใช่ปัญหาโค้ด) ไม่ใช่ config ผิด — push ทั้งสอง commit ไปที่ `personal` แล้ว

- **🆕 2026-08-11 — 📋 เช็ค Notion task list + คุย pair plot redesign (Notion task "pairplot re design and refactor")**: ผู้ใช้ขอดู task ที่ยังไม่เสร็จใน Notion ก่อนเริ่มงาน แล้วเลือกทำ "pairplot re design and refactor" ต่อ:
  - **Notion**: database "wizard application plan improvement" มี 10 รายการยังไม่เสร็จ (2 waiting re design, 3 wait for advisor, 5 Not started) — สรุปให้ผู้ใช้ดูครบ พร้อมชี้ว่ารายการ "ปิดโปรแกรมเปิดใหม่แล้ว special sensor หาย" ตรงกับ Class A ที่มีแผนแก้ไว้แล้วใน `docs/PERSISTENCE_PLAN.md`
  - **อธิบายโค้ด pair plot ปัจจุบัน**: อ่าน `PairPlotChart.tsx`/`PairPlotCell.tsx`/`useCoalescedDraw.ts` ให้ผู้ใช้ฟังทั้งระบบ (grid n×(n+1), regl-scatterplot แยก instance ต่อ cell, lasso→multi-cluster, ไม่มี persistence ของ cluster เลย)
  - **แก้บั๊กสีจุด/แท่งไม่ตรงกัน** (`PairPlotChart.tsx`): scatter cell ใช้ `POINT_COLOR` ฮาร์ดโค้ด `[0.39,0.58,0.98,0.55]` (≈`#6394fa`) ส่วน diagonal histogram ใช้ `#6366f1` (indigo-500 ซึ่งเป็นสี accent จริงของแอป ใช้อยู่แล้วใน `LINE_CHART_COLORS`/`App.css`/`PredictiveModelBuild.tsx`) — รวมเป็นค่าเดียว `ACCENT_HEX='#6366f1'` ให้ทั้งคู่ derive จากตัวเดียวกัน
  - **สร้าง mockup ออกแบบใหม่**: publish Artifact เสนอ 2 การเปลี่ยนแปลงเท่านั้น (ตามที่ผู้ใช้บอกว่า "ยังไม่มี idea เลย" ขอให้ช่วยดีไซน์ให้): (1) เพิ่มปุ่ม pan/zoom คู่กับ lasso เดิม ไม่ใช่แทนที่ (2) ครึ่งสามเหลี่ยมล่างที่ว่างเปล่าอยู่ตอนนี้ (`k < i` ใน `PairPlotChart.tsx` ไม่ render อะไรเลย) เปลี่ยนเป็น correlation heatmap (Pearson r, สีตามเครื่องหมาย/ขนาด) ไม่เพิ่มการคำนวณข้อมูลใหม่ ใช้ sample เดิมที่มีอยู่แล้ว — ผู้ใช้ดูแล้วชอบ ("ดูดีมากๆเลย") ยังไม่ได้ implement จริง เป็นแค่ mockup ให้ตัดสินใจ
  - **แก้บั๊กสี background ไม่ตรงกัน** (บั๊กจริง เจอจากภาพหน้าจอที่ผู้ใช้ส่งมาโชว์แอปจริง): scatter cell's container div ไม่เคยตั้ง background เอง เลย fallback ไปที่ `--bg-primary` (`#0a0a0b` ในธีมมืด) ส่วน diagonal histogram hardcode `#000`/`#fff` ตรงๆ — สองค่าใกล้กันจนแยกด้วยตาลำบากแต่ต่างกันจริง เห็นชัดในภาพหน้าจอที่ผู้ใช้ส่ง (cell histogram สีพื้นหลังต่างจาก cell ข้างๆ)
  - **แก้ทั้ง 2 บั๊กสีด้วยวิธีเดียวกัน — สร้าง single source of truth**: ไฟล์ใหม่ `src/components/charts/pairPlotColors.ts` เก็บ `ACCENT_HEX`, `hexToRgba`/`rgbaToHex`, `CANVAS_BG_HEX`/`CANVAS_BG` (ดำ/ขาวสนิท) ตัวเดียว ให้ทั้ง `PairPlotCell.tsx` (canvas clear color + container div background) และ `PairPlotChart.tsx` (DiagonalHistogram's fill + background) import มาใช้ร่วมกันทั้งหมด ไม่มีค่าคงที่ซ้ำซ้อนที่หลุด sync กันได้อีก
  - **เพิ่มเทสต์คู่กับทุกจุดที่แก้** (ตามกฎ testing discipline): `PairPlotChart.test.tsx` เช็คว่า histogram fill กับ scatter pointColor มาจาก hex เดียวกัน + เช็ค background ของ histogram cell เป็น `rgb(0,0,0)`; `PairPlotCell.test.tsx` เพิ่มเทสต์ใหม่เช็ค container background ตรงกับ `CANVAS_BG_HEX` ทั้ง dark/light theme
  - Verify: `npx tsc --noEmit` สะอาดทุกรอบ + เทสต์ทั้งโปรเจกต์ **588/588 ผ่าน** ไม่มี regression — **ยังไม่ได้ commit** งานแก้สีทั้งหมดนี้ (ไฟล์ที่ค้าง: `PairPlotCell.tsx`, `PairPlotChart.tsx`, `pairPlotColors.ts` ใหม่, เทสต์ 2 ไฟล์) รอผู้ใช้สั่ง commit/push — pair plot redesign ตัวจริง (correlation heatmap + pan/zoom) ยังไม่ได้เริ่ม implement รอผู้ใช้ตอบ feedback ต่อ mockup ก่อน
  - **⚠️ กฎ auto-update เอกสารนี้หลุดไปจริงระหว่างรอบสีทั้ง 2 fix ข้างบน** — ผู้ใช้ถามตรงๆ ว่า "ยังเขียนอยู่ใช่ไหม" แล้วเช็คพบว่าไม่ได้แก้เอกสารนี้เลยตั้งแต่ commit `6ffafdb` ทั้งที่มี 2 code fix เกิดขึ้นแล้ว — สาเหตุ: ช่วงคุยแบบ multi-turn ไปมาเร็วๆ (อธิบายโค้ด → ทำ mockup → ฟัง feedback → แก้ → ฟัง feedback → แก้) ไม่มีจุด "จบงานใหญ่" ที่ชัดเจนแบบตอนเขียนเทสต์ยาวๆ ทำให้ไม่ได้ pause เช็คเอกสารระหว่างทาง — **แก้ไปแล้ว**: ตกลงกับผู้ใช้ว่าต่อจากนี้ทุก response ที่มีการแก้โค้ดในโปรเจกต์นี้ต้องบอกตรงๆ ว่าอัปเดตเอกสารนี้ในรอบเดียวกันหรือยัง (เหมือนที่รายงานผล tsc/vitest ทุกครั้งอยู่แล้ว) ไม่ใช่ทำเงียบๆ อยู่เบื้องหลัง — ผู้ใช้เช็คเองได้ทุกเมื่อด้วย `git status --short docs/PROJECT_HANDOVER.md` ถ้าไฟล์โค้ดอื่นแก้แต่ไฟล์นี้ไม่ขึ้น `M` แปลว่าหลุดอีกแล้ว — บันทึกรายละเอียดไว้ใน Claude memory ส่วนตัวด้วยแล้ว (`feedback_auto_update_handover.md`) กันลืมข้ามเซสชัน

- **🆕 2026-08-11 — 🎨 Implement pair plot redesign จริง (correlation heatmap + pan/zoom tool)**: ผู้ใช้ชี้ว่า "เอาตามที่คุณ design เลย" ที่พูดไปก่อนหน้าคือการอนุมัติให้ implement mockup ไปแล้ว ไม่ใช่แค่ให้แก้บั๊กสี — เข้าใจผิดไปว่าแค่ fix บั๊กที่ผู้ใช้ชี้ก็พอ แก้ความเข้าใจแล้ว implement 2 อย่างจาก mockup จริง:
  - **ไฟล์ใหม่ `src/components/charts/pairPlotStats.ts`**: `pearsonCorrelation(xs, ys)` — คำนวณ Pearson r แบบ pairwise-complete (ใช้เฉพาะคู่ที่ทั้งสอง sensor มีค่า finite ตรงกัน ตรงกับที่ `PairPlotCell` เองก็กรองแถวแบบเดียวกันตอน push เข้า WebGL) คืนค่า `null` ถ้าคู่ข้อมูลน้อยกว่า 2 หรือ series ใดมี variance เป็น 0 (ค่าคงที่ตลอด — correlation ไม่มีนิยาม)
  - **Lower-triangle correlation heatmap**: `PairPlotChart.tsx` เดิม branch `k < i` ไม่ render อะไรเลย (comment "skip — empty grid space") ตอนนี้ render `CorrelationCell` ใหม่ — mirror sensor pair เดียวกับ scatter cell ด้านบน ไม่ fetch ข้อมูลใหม่เลย ใช้ `sensorVals` เดิมที่มีอยู่แล้วสำหรับ diagonal histogram คำนวณ correlation ทั้งหมดครั้งเดียวผ่าน `useMemo` (`correlations` map คีย์ `"i-k"`) พื้นหลัง cell tint ตามเครื่องหมาย/ขนาดของ r (บวก = `ACCENT_HEX` indigo, ลบ = `NEGATIVE_HEX='#f43f5e'` rose ใหม่ที่เพิ่มใน `pairPlotColors.ts`, alpha แปรตาม `|r|`) แสดงข้อความ `+0.82`/`-0.61`/`n/a`
  - **Pan/zoom tool คู่กับ lasso**: เพิ่ม state `tool: 'lasso' | 'pan'` ใน `PairPlotChart` แทนที่ปุ่ม Lasso เดิมที่ hardcode `active`/`aria-pressed="true"` ตลอด — ตอนนี้ toolbox มี 2 ปุ่ม toggle กันเอง (เพิ่มไอคอน `Move` จาก lucide-react) ปุ่มเดียวกันนี้ซ้ำในหน้าต่าง enlarged-view dialog ด้วย (เดิม dialog มีแค่ indicator "Lasso (always on)" ตายตัว) — prop `tool="lasso"` ที่ hardcode ไว้ 3 จุด (scatter cell ครึ่งบน, time-series คอลัมน์ขวา, cell ใน dialog) เปลี่ยนเป็น `tool={tool}` ทั้งหมด `PairPlotCell` เดิมรองรับ prop นี้อยู่แล้ว (มี effect `sc.set({mouseMode: tool==='lasso'?'lasso':'panZoom'})`) เลยแค่ต้อง wire state จาก parent ลงมา ไม่ต้องแก้ `PairPlotCell.tsx` เลย
  - **เพิ่มเทสต์ครบตามกฎ testing discipline**: ไฟล์ใหม่ `pairPlotStats.test.ts` (8 เทสต์ — perfect +1/-1, near-zero, NaN/Infinity filtering, <2 pairs → null, zero-variance → null, mismatched-length array); `PairPlotChart.test.tsx` เพิ่ม 5 เทสต์ (correlation cell แสดง `+1.00`/`n/a` ถูกต้อง, toggle เริ่มที่ lasso, คลิก Pan แล้ว `tool` prop ของทุก child cell เปลี่ยนเป็น `pan`, สลับกลับ lasso ได้) — เจอ 2 จุดต้องแก้ระหว่างเขียนเทสต์: fixture "uncorrelated" เดิมสุ่มได้ r=0.696 (ไม่ใช่ค่าที่ตั้งใจ) เปลี่ยนเป็น permutation ที่คำนวณแล้วได้ r≈-0.086 จริง, และเทสต์ toggle ต้อง clear `cellCalls.length=0` ก่อนคลิกเหมือน pattern ที่มีอยู่แล้วในเทสต์ Reset (ไม่งั้น assertion เจอ call เก่าจาก initial mount ปนอยู่)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **601/601 ผ่าน** (+13 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (ไฟล์ที่ค้างเพิ่มจากรอบก่อน: `pairPlotStats.ts`/`pairPlotStats.test.ts` ใหม่, `PairPlotChart.tsx` แก้เพิ่ม, `PairPlotChart.test.tsx` แก้เพิ่ม, `pairPlotColors.ts` แก้เพิ่ม `NEGATIVE_HEX`/`hexToRgbaCss`) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` เลย รอผู้ใช้ลองเปิดจริง

- **🆕 2026-08-11 — 🏷️ แก้ label ชื่อ sensor ที่หายไปเกือบทั้ง matrix**: ผู้ใช้ชี้ว่ามองไม่ออกว่าแต่ละ cell (ทั้ง scatter และ correlation ที่เพิ่งเพิ่ม) เป็นคู่ sensor ตัวไหน — ไล่โค้ดพบว่าเป็นบั๊กจริงที่มีมาแต่เดิม ไม่ใช่แค่เรื่องความสวยงาม:
  - **root cause**: `showXLabel={i === 0}` (แสดงชื่อคอลัมน์แค่แถวบนสุด) และ `showYLabel={false}` (**ไม่เคยแสดงชื่อแถวเลยสักที่**) ในทุก scatter cell + time cell ของ `PairPlotChart.tsx` ส่วน `DiagonalHistogram` ก็โชว์ชื่อตัวเองแค่ตอน `isTopRow` (แถว 0 เท่านั้น) — สรุปคือทุกแถวที่ไม่ใช่แถวแรกไม่มี label ชื่อ sensor ให้เห็นเลยสักจุดในทั้งแถว (มีแต่ต้องนับตำแหน่งคอลัมน์เทียบ header บนสุดเอาเอง) ยืนยันว่า `AXIS_PAD`/`CELL_PAD` เว้นพื้นที่ไว้สำหรับ label เสมออยู่แล้วไม่ว่าจะโชว์หรือไม่ (ตาม docstring เดิม) — เปิดใช้ label ทุกจุดจึงไม่กระทบ layout เลย แค่เติมข้อความลงพื้นที่ว่างที่เตรียมไว้แล้ว
  - **แก้**: `DiagonalHistogram` ลบ prop `isTopRow` ทิ้ง โชว์ชื่อ sensor ของตัวเองเสมอทุกแถว; scatter cell + time cell เปลี่ยน `showXLabel`/`showYLabel` เป็น `true` เสมอ (ไม่ผูกกับ index แถวอีกต่อไป); `CorrelationCell` (ที่เพิ่งสร้างรอบก่อน) เพิ่ม label บน (sensorX) + label ซ้ายหมุน -90° (sensorY) เข้าไปด้วย ใช้ style เดียวกับที่ `PairPlotCell.tsx` ใช้ render label ของตัวเอง (font-size 10, `var(--text-primary)`) เพื่อให้ดูเป็นชุดเดียวกันทั้ง matrix
  - **เพิ่มเทสต์ 3 รายการใน `PairPlotChart.test.tsx`**: เช็คว่าทุก scatter/time cell ได้ `showXLabel`/`showYLabel` เป็น `true` เสมอไม่ว่าจะแถวไหน (regression guard), เช็คว่าแถวที่ไม่ใช่แถวแรกก็โชว์ชื่อ sensor ของ diagonal ตัวเอง (ก่อนแก้จะไม่โชว์เลย), เช็คว่า correlation cell แสดงชื่อ sensor ทั้งคู่ (X และ Y) ของคู่ที่มันแทน
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **604/604 ผ่าน** (+3 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (รวมกับไฟล์ที่ค้างจากรอบ correlation heatmap/pan-zoom ก่อนหน้า) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` เช่นเดิม รอผู้ใช้ลองเปิดจริงเมื่อพร้อม

- **🆕 2026-08-11 — 🌈 เปลี่ยนสี correlation heatmap เป็น diverging-by-magnitude (แดง→เหลือง→เขียว)**: ผู้ใช้ขอสีใหม่ตรงๆ — "-1 เขียว 0 แดง 1 เขียว ระหว่างทางไล่ gradient" คือ mapping ตามขนาด `|r|` ไม่ใช่ตามเครื่องหมายแบบเดิม (เดิม fix รอบก่อนใช้ indigo=บวก/rose=ลบ ตามเครื่องหมาย):
  - **`pairPlotColors.ts`**: ลบ `NEGATIVE_HEX` ทิ้ง (ไม่ได้ใช้แล้วเพราะ scheme ใหม่ไม่สนเครื่องหมาย) เพิ่ม `correlationGradientHex(t)` — รับ `t` (ตั้งใจให้ป้อน `|r|`) คืน hex สีที่ interpolate จาก 3 stop: แดง `#ef4444` → เหลือง/amber `#f59e0b` → เขียว `#10b981` (ทั้ง 3 สีนี้มีอยู่แล้วใน `LINE_CHART_COLORS` ของแอป ไม่ได้เลือกใหม่มั่วๆ) ใช้ 3 stop แทนที่จะ lerp ตรงจากแดงไปเขียว 2 สี เพราะถ้า lerp ตรงในพื้นที่ RGB ตรงกลางจะออกมาเป็นสีน้ำตาล/เขียวหม่นๆ ไม่ใช่สีที่ดูตั้งใจ
  - **`CorrelationCell` (`PairPlotChart.tsx`)**: เปลี่ยนจาก tint ตามเครื่องหมาย (`r<0 ? rose : indigo`) เป็น `correlationGradientHex(Math.abs(r))` ตัวอักษร r-value เปลี่ยนจากสีเดียวกับ tint เป็น `var(--text-primary)` เสมอ (อ่านง่ายกว่าบนพื้นหลังไล่สี 3 แบบ ไม่ต้องคำนวณ contrast แยกตามสี) ปรับ alpha ของพื้นหลังขึ้นเล็กน้อย (0.22 + |r|×0.55 จากเดิม 0.14+|r|×0.5) ให้สีเห็นชัดขึ้น
  - **เพิ่มไฟล์เทสต์ใหม่ `pairPlotColors.test.ts`** (13 เทสต์) — คลุมทุกฟังก์ชัน pure ที่ไฟล์นี้เคยไม่มีเทสต์ตรงๆ มาก่อนเลย (`hexToRgba`, `rgbaToHex`, `hexToRgbaCss`, `CANVAS_BG`/`CANVAS_BG_HEX`) บวกเทสต์ `correlationGradientHex` ครบทุก stop (t=0 แดงเป๊ะ, t=0.5 เหลืองเป๊ะ, t=1 เขียวเป๊ะ, ค่ากลางๆ interpolate ถูกจุด, ค่านอกช่วง [0,1] clamp ถูก) — ค่า hex ที่ใช้ assert ในเทสต์ interpolation คำนวณเป๊ะๆ ด้วยการรัน implementation จริงผ่าน `node -e` ก่อน ไม่ได้เดาหรือคำนวณด้วย Python (ลอง Python ก่อนแล้วเจอว่า built-in `round()` ปัดเศษ .5 แบบ banker's rounding ต่างจาก JS `Math.round()` ที่ปัดขึ้นเสมอ ได้ผลไม่ตรงกัน 1 ค่า เลยเปลี่ยนมารันของจริงแทน)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **617/617 ผ่าน** (+13 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (รวมกับไฟล์ pairplot ที่ค้างสะสมมาหลายรอบก่อนหน้าแล้ว) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` เช่นเดิม รอผู้ใช้ลองเปิดจริง

- **🆕 2026-08-11 — 🏷️ ปรับ label กลับเป็นแบบ "กรอบนอกสุดเท่านั้น" (outer frame only) แทนที่จะทุก cell**: ผู้ใช้เห็น mockup ที่ทำให้ดูก่อน (Artifact) แล้วเลือกแบบ "outer frame only" — ยืนยันว่าถูกทาง ("ใช่ๆ เอาแค่ส่วน outer frame ตามภาพที่ออกแบบมาใหม่") ให้ implement จริง — เป็นการ**ปรับจากรอบก่อน** (ที่ทำให้ทุก cell self-label เอง) ไม่ใช่ revert กลับบั๊กเดิม เพราะ correlation cell ยังได้ label อยู่ ต่างจากก่อนแก้บั๊กครั้งแรกที่ correlation cell ไม่มี label เลยสักตัว:
  - **`DiagonalHistogram`**: เอา `isTopRow` prop กลับมาใส่ใหม่ (เพิ่งลบไปตอนแก้บั๊กรอบก่อน) — โชว์ชื่อ sensor เฉพาะแถว 0 เท่านั้น เพราะแถว 0 คือ column header ของคอลัมน์นั้นอยู่แล้ว แถวอื่นไม่ต้องซ้ำ
  - **scatter cell + time cell**: `showXLabel={i === 0}` (กลับไปเหมือนเดิมก่อนแก้บั๊กรอบที่แล้ว), `showYLabel={false}` เสมอ — เหตุผลคือ scatter/time cell ไม่เคยอยู่คอลัมน์ 0 เลย (คอลัมน์ 0 มีแต่ diagonal กับ correlation cell เท่านั้น) เลยไม่ต้องมี Y label ให้มันเลย
  - **`CorrelationCell`**: ตัดการโชว์ top label (sensorX) ออกทั้งหมด เพราะ correlation cell ไม่เคยอยู่แถว 0 (i≥1 เสมอเพราะ k<i ต้องการ i≥1) เพิ่ม prop ใหม่ `showYLabel: boolean` ให้ parent ส่งเป็น `k === 0` — เฉพาะ correlation cell ที่อยู่คอลัมน์ซ้ายสุดของแถวนั้นๆ ถึงจะโชว์ชื่อแถว (sensorY) ตัวอื่นในแถวเดียวกัน (คอลัมน์ 1, 2, ...) ไม่โชว์อะไรเลย ตัด prop `sensorX` ที่ไม่ได้ใช้แล้วออกจาก component ทั้งหมด (เคยรับมาแต่ตอนนี้ไม่ต้องใช้)
  - **enlarged-view dialog**: ใส่ `isTopRow={true}` กลับเข้าไปที่ `DiagonalHistogram` call (โชว์ label เสมอในหน้าต่าง zoom แบบเดี่ยว ไม่เกี่ยวกับกฎ outer-frame ของ matrix ใหญ่)
  - **แก้เทสต์ 3 รายการที่เขียนไว้รอบก่อนให้ตรงกับพฤติกรรมใหม่** (เดิมเทสต์ assert ว่า "ทุก cell ต้องโชว์เสมอ" ซึ่งตอนนี้ผิดแล้ว) + **เพิ่มเทสต์ใหม่ 1 รายการ** ด้วย fixture 3 sensor เพื่อพิสูจน์ชัดๆ ว่า correlation cell ที่ไม่ได้อยู่คอลัมน์ซ้ายสุด (interior) ไม่โชว์ label อะไรเลยจริง — ใช้วิธี query cell ตาม `gridRow`/`gridColumn` ของ `.pair-regl-cell-slot` โดยตรง (เจอปัญหาระหว่างเขียนเทสต์แรกที่พยายาม parse ข้อความด้วย regex ซับซ้อนเกินจำเป็น เปลี่ยนมาใช้ position-based query ที่ตรงและอ่านง่ายกว่า)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **618/618 ผ่าน** (net +1 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` เช่นเดิม รอผู้ใช้ลองเปิดจริง — ยังค้างคำถามเรื่อง **จำกัดจำนวน sensor สูงสุดที่เลือกได้ใน pairplot** (แนะนำ 4-5 ตัวเพราะแต่ละ scatter/time cell คือ WebGL context แยกกัน browser จำกัดที่ประมาณ 16 context พร้อมกัน เกินแล้วจะเริ่มโดนบีบ context ทิ้งแบบไม่มี error เตือน เพราะ `PairPlotCell.tsx` ไม่ได้ดัก `webglcontextlost` แบบที่ `ScatterChart.tsx` ทำไว้) รอผู้ใช้ยืนยันตัวเลขก่อนถึงจะ implement

- **🆕 2026-08-11 — 🐛 แก้ label ถูกตัดสั้น + 🔒 จำกัด pairplot ไม่เกิน 4 sensor**: ผู้ใช้ส่งภาพหน้าจอแอปจริงมาชี้ 2 เรื่อง — (1) label แกน Y ของ correlation cell ถูกตัดสั้นจนอ่านไม่ออก เช่น "11TE1602.PV" กลายเป็น "11TE1..." (2) ยืนยันเลข cap ที่ 4 sensor ตามที่แนะนำไว้ก่อนหน้า:
  - **บั๊ก label ถูกตัด — root cause จริง**: `CorrelationCell` วาง `<span>` (rotated -90°) ไว้ใน flex container กว้างแค่ `CELL_PAD.left` (36px) พร้อมใส่ `overflow:hidden` + `textOverflow:ellipsis` บน span เอง — ปัญหาคือ `transform` ใน CSS **ไม่กระตุ้น reflow** ดังนั้น browser จะ layout ข้อความแบบปกติ (แนวนอน) ในพื้นที่ 36px ก่อน ตัดคำให้พอดี 36px (~4 ตัวอักษร) **แล้วค่อยหมุน** ผลคือ label ที่หมุนแล้วสั้นตามความกว้างที่ถูกตัดไปก่อนหมุน ไม่ใช่ตามความสูงจริงของ cell ที่มีพื้นที่เหลือเฟือ — แก้โดยตัด `overflow`/`textOverflow` ออกจาก span ปล่อยให้ span มีความกว้างตามเนื้อหาจริง (ก่อนหมุน) แล้วให้ `.pair-regl-cell-slot` (CSS class เดิมที่มี `overflow:hidden` อยู่แล้วเป็น safety clip ระดับนอกสุด) เป็นตัวจำกัดแทน ซึ่งกว้างกว่า 36px มาก (เท่าความสูงจริงของ cell)
  - **จำกัด pairplot ที่ 4 sensor**: เพิ่ม `MAX_PAIR_PLOT_SENSORS = 4` เป็น single source of truth ที่ `ChartTypes.ts` (export ผ่าน barrel `charts/index.ts`) ให้ทั้ง `Dashboard.tsx` และ `PairPlotChart.tsx` import ใช้ร่วมกัน (ไม่ใช่ hardcode ค่าซ้ำ 2 จุดแบบที่เคยพลาดมาก่อนกับเรื่องสี):
    - **`Dashboard.tsx`**: เพิ่ม `canPairPlot` แยกจาก `canScatter` เดิม (Scatter ไม่ต้องจำกัดเพราะเลือกแค่ 2 sensor มาพล็อตเสมอ ไม่ได้เปิด WebGL context ตามจำนวน sensor ที่เลือกทั้งหมด) ปุ่ม Pair Plot ใช้ `canPairPlot` แทน + title message แยกกรณี "น้อยไป" กับ "เยอะไป" ขยาย useEffect เดิม (bounce กลับ line เมื่อ sensor ต่ำกว่า 2) ให้ครอบคลุมกรณี sensor เกิน 4 ตอนอยู่ใน pair mode ด้วย
    - **`PairPlotChart.tsx`**: เพิ่ม guard `n > MAX_PAIR_PLOT_SENSORS` คู่กับ guard `n < 2` เดิม เป็น defense-in-depth เพราะ `useEffect` ของ Dashboard รันหลัง render เสมอ มีเฟรมนึงที่ยังไม่ทัน bounce กลับที่ component นี้อาจได้รับ sensor เกิน 4 ตัวอยู่ (เช่นตอน restore workspace ที่ persist `chartType:'pair'` พร้อม selection เกิน)
  - **เพิ่มเทสต์ครบทุกจุดที่แก้**: `PairPlotChart.test.tsx` +3 เทสต์ (placeholder ตอนเกิน cap, render ปกติที่ 4 พอดี, label ยาวไม่ถูกตัด — เช็ค `style.overflow`/`style.textOverflow` ต้องไม่ใช่ `hidden`/`ellipsis`); `Dashboard.test.tsx` +2 เทสต์ (Pair Plot ปิดแต่ Scatter ยังเปิดอยู่เมื่อเกิน 4, bounce กลับ line เมื่อ selection โตเกิน cap ระหว่างอยู่ pair mode) — ต้องเพิ่มปุ่ม mock ใหม่ `select-4-tags`/`select-5-tags` ใน `SensorSelection` mock ด้วย เจอปัญหาระหว่างรันเทสต์ที่ mock ของ `../components/charts` ใน `Dashboard.test.tsx` ไม่มี `MAX_PAIR_PLOT_SENSORS` export (ทำให้ทุกเทสต์ทั้งไฟล์ error รวดเดียว 31 ตัว) แก้โดยเพิ่มเข้าไปใน mock object
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **623/623 ผ่าน** (+5 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (สะสมมาหลายรอบแล้วในเซสชันนี้ทั้งหมดเกี่ยวกับ pairplot redesign) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` เช่นเดิม รอผู้ใช้ลองเปิดจริง

- **🆕 2026-08-11 — 📋 ผู้ใช้สั่งงาน 5 ข้อพร้อมกันจากภาพหน้าจอ**: ให้ทำ 4 ข้อแรกก่อนแล้วค่อย design ข้อ 5 ต่อ (ยังไม่ implement ข้อ 5 — เป็นแค่ข้อเสนอ):
  1. **Scrollbar สี Selected Sensor ไม่ตรง theme**: container ที่ scroll (`Dashboard.tsx` renderDataContent) ไม่เคยมี class ใดๆ เลย ใช้ default browser scrollbar (สีอ่อนของ Windows) ตรงๆ — แก้โดยชุบชีวิต class `.custom-scrollbar` ที่มีอยู่ใน `App.css` แต่**ไม่เคยถูกใช้จริงในไฟล์ component ไหนเลย** (dead CSS) ปรับให้รองรับ vertical scroll (`width` ไม่ใช่แค่ `height`) และเปลี่ยนสี hardcode (`#475569` ฯลฯ) เป็น CSS var ที่ theme-aware (`var(--border-strong)`/`var(--text-faint)`) แล้ว apply `className="custom-scrollbar"` ให้ container จริง
  2. **Pair Plot cap UX เปลี่ยนจาก "bounce ไป line" เป็น "บล็อกไปเลย"**: ผู้ใช้ไม่เอา behavior เดิม (เลือกเกิน 4 แล้วเด้งไป line chart อัตโนมัติ) เปลี่ยนเป็น:
     - **บล็อกที่จุดเลือก sensor**: `SensorSelection.tsx` เพิ่ม prop `maxSelectable?: number` — `Dashboard.tsx` ส่ง `chartType==='pair' ? MAX_PAIR_PLOT_SENSORS : undefined` เข้าไป `handleSensorToggle` เช็คก่อน: ถ้ากำลังจะ "เพิ่ม" sensor ใหม่ (ไม่ใช่ถอด) และถึง cap แล้ว → no-op ทันที ไม่ปล่อยผ่าน `onSensorChange` เลย พร้อม visual affordance (checkbox `disabled` + row `opacity:0.45`/`cursor:not-allowed` + title tooltip อธิบาย) ให้ผู้ใช้เห็นว่าทำไมกดแล้วไม่มีอะไรเกิดขึ้น
     - **ปุ่ม Pair Plot ตอนอยู่ line/scatter แล้วเลือกเกิน 4**: เปลี่ยนจาก native `disabled` (กดไม่ได้เลย) เป็นปุ่มที่กดได้จริงแต่โชว์หน้าตา "disabled" ผ่าน class ใหม่ `.chart-type-btn.blocked` (CSS เหมือน `:disabled` ทุกอย่างแต่ยังคลิกได้จริง) — คลิกแล้วเรียก `handlePairPlotClick` เช็คก่อนว่าเกิน `MAX_PAIR_PLOT_SENSORS` ไหม ถ้าเกินเรียก `message()` จาก `@tauri-apps/plugin-dialog` (native dialog, pattern เดียวกับ About dialog ที่มีอยู่แล้วใน `App.tsx`) แจ้งเตือนตรงๆ ว่าเกินเท่าไหร่ ให้ deselect ก่อน แล้ว `return` ไม่สลับ chart type — ปุ่มยัง native `disabled` อยู่เฉพาะกรณี < 2 sensor (ไม่เปลี่ยนพฤติกรรมเดิมจุดนั้น)
     - **ลบ auto-bounce effect ที่เคยเพิ่มไว้เมื่อรอบก่อน** (bounce จาก pair → line ตอน selection โตเกิน cap ระหว่างอยู่ pair mode) ทิ้งไปเลย เพราะตอนนี้ selection ไม่มีทางโตเกิน cap ได้อีกแล้วจากการเลือกปกติ (บล็อกไว้ที่ต้นทางแล้ว) เหลือแค่ path เดียวที่ยังหลุดได้คือ workspace restore ที่ persist ค่าเก่าไว้ก่อนมี cap — ปล่อยให้ `PairPlotChart.tsx`'s guard เดิม (ที่ทำไว้รอบก่อนหน้า) โชว์ข้อความ inline แทนแทนที่จะบังคับสลับหน้าจอ ตรงกับ philosophy "ห้ามกดไปเลย ไม่ใช่เด้งหนี" ที่ผู้ใช้ต้องการ
  3. **ลบ "Row ID" ออกจาก tooltip ของ scatter/pairplot**: `ScatterChart.tsx`/`PairPlotChart.tsx` ทั้งคู่มี `<div className="scatter-regl-tooltip-title">Row {rowIdx}</div>` เป็นบรรทัดแรกของ tooltip ที่ไม่มีใครใช้ — ลบทิ้งทั้งคู่ ค่า `rowIdx`/`origIdx` เองยังเก็บไว้ใน state ตามเดิม (ใช้งานจริงในโค้ด lasso/cluster mapping จุดอื่น) แค่ไม่ render ให้เห็นในตัว tooltip อีกต่อไป
  - **เพิ่ม/แก้เทสต์ทุกจุด**: `ScatterChart.test.tsx` 2 เทสต์, `PairPlotChart.test.tsx` 2 เทสต์ (ปรับให้ assert ว่า `.scatter-regl-tooltip-title` ไม่มีอยู่แทนที่จะเช็คข้อความ "Row 0"), `Dashboard.test.tsx` แก้ 2 เทสต์เดิม (จาก native-disabled → blocked-แต่คลิกได้-โชว์ dialog, และจาก "bounce กลับ line" → "ไม่ bounce เลย") + เพิ่ม mock `message` เข้า `@tauri-apps/plugin-dialog` mock ของไฟล์นี้ (เดิมมีแค่ `save`), `SensorSelection.test.tsx` เพิ่ม describe block ใหม่ 5 เทสต์คลุม `maxSelectable` ทั้งกรณี บล็อกตอนเลือกใหม่/ไม่บล็อกตอนถอด/checkbox disabled จริง/ยังเลือกได้ตอนไม่ถึง cap/ไม่มีผลเลยตอน undefined
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **628/628 ผ่าน** (+5 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` รอผู้ใช้ลองเปิดจริง
  - **ข้อ 5 (redesign pairplot ให้มี description โดยไม่ clutter) ยังไม่ implement** — เป็นแค่การพูดคุยหาแนวทางกับผู้ใช้ ยังไม่มี mockup/โค้ดจริง ดู entry ถัดไปถ้ามี

- **🆕 2026-08-11 — 🏷️ Implement hover tooltip โชว์ sensor description ใน pairplot (ข้อ 5)**: ทำ mockup 2 รอบ — รอบแรกเสนอ legend/key แถบเหนือ matrix ผู้ใช้ปฏิเสธ ("ไม่ชอบเท่าไหร่" → ถามต่อผ่าน AskUserQuestion ได้คำตอบชัดว่า "Legend/key แบบนี้ ไม่เวิร์ค") รอบสองเสนอ hover tooltip แบบไม่มี UI เพิ่มเลย (zero footprint ตอนไม่ hover) ผู้ใช้ชอบ สั่ง implement ทันที:
  - **`Chart.tsx`**: เพิ่ม `sensorMetadata={sensorMetadata}` เข้า `<PairPlotChart>` ที่ **ไม่เคยส่งมาก่อนเลย** (ต่างจาก `LineChart` ที่ได้ prop นี้อยู่แล้วนานแล้ว) — นี่คือ root cause ที่ทำให้ pairplot ไม่มีทางรู้จัก description ของ sensor เลยตั้งแต่ต้น
  - **`PairPlotChart.tsx`**: รับ `sensorMetadata` เพิ่มใน props, ใช้ `useSensorMetaMap`/`normalizeSensorTag` (hook เดียวกับที่ `LineChart.tsx`/`SensorSelection.tsx`/`Dashboard.tsx` ใช้อยู่แล้ว ตามธรรมเนียมเดิมของโปรเจกต์ ไม่ต้อง normalize tag ใหม่เอง) สร้าง `sensorMetaMap` แล้ว helper `getDescription(tag)` คืนค่า `undefined` ถ้าไม่มี metadata หรือ description ว่างเปล่า (ไม่โชว์ tooltip เปล่าๆ)
  - **Component ใหม่ `AxisLabel`**: รวม logic label ที่กระจายอยู่ 3 จุด (SVG text ใน `DiagonalHistogram`, SVG text ใน `PairPlotCell`, HTML span ใน `CorrelationCell`) ให้เหลือจุดเดียวที่ `PairPlotChart.tsx` ควบคุมเอง เป็น HTML overlay ลอยอยู่เหนือแต่ละ cell — ปิดการ render label เดิมของ `DiagonalHistogram`/`PairPlotCell` ในส่วน matrix ทั้งหมด (`isTopRow`/`showXLabel` เป็น `false` เสมอตอนนี้ — **หน้าต่าง enlarged-view dialog ไม่แตะ ยังใช้ label เดิมของมันเองเหมือนเดิม** เพราะ mockup ที่ผู้ใช้ดูมีแค่ matrix ไม่รวม dialog) ใช้ pure CSS `:hover` ล้วนๆ ไม่มี JS state/position tracking เลย
  - **กับดักที่เจอระหว่างเขียน (สำคัญ)**: ตอนแรก copy style จาก scatter cell's เดิมมาใส่ `AxisLabel` แบบรวมๆ ใส่ `overflow:hidden`+`textOverflow:ellipsis` ให้ทั้ง 2 orientation (`top` กับ `left`) — **นี่คือการนำบั๊กตัดข้อความสั้นที่เพิ่งแก้ไปเมื่อหลายรอบก่อนกลับมาใหม่** เพราะ `left` orientation ต้องหมุน -90° ซึ่ง `overflow`/`ellipsis` จะไปตัดข้อความ**ก่อน**หมุน ทำให้สั้นลงเหลือ ~4 ตัวอักษรเหมือนเดิม — เทสต์ที่เขียนไว้จากรอบก่อน (`the row header is not truncated...`) จับได้ทันทีตอนรัน สรุปคือ: ellipsis โอเคสำหรับ `top` (ไม่หมุน) แต่ห้ามใช้กับ `left` (หมุน) เด็ดขาด แก้แล้วโดยแยก conditional ตาม orientation
  - **เพิ่ม CSS ใหม่**: `.pair-regl-axis-label.hoverable` (dotted underline บอกว่า hover ได้), `.pair-regl-axis-tooltip` + `-tag`/`-desc` (bubble ที่ opacity:0/visibility:hidden ตอนปกติ โผล่ตอน `:hover` ผ่าน CSS transition ล้วนๆ) ตำแหน่ง tooltip ต่างกันตาม orientation (`top` โผล่ด้านล่าง label, `left` โผล่ด้านขวา label)
  - **เพิ่ม/แก้เทสต์ครบ**: แก้ 2 เทสต์เดิมที่ query ข้อความแบบกำกวมเกินไป (query เจอทั้ง top-label กับ left-label ของ sensor เดียวกัน เพราะใช้ component เดียวกันแล้ว) เปลี่ยนมา query แยกด้วย `style.transform` (มีแปลว่า orientation=left) เพิ่ม describe block ใหม่ 5 เทสต์คลุม hover-tooltip โดยตรง (มี description → hoverable + เนื้อหาถูกต้อง, case-insensitive matching ผ่าน `normalizeSensorTag`, description ว่างเปล่า → ไม่ hoverable, ไม่มี `sensorMetadata` เลย → ไม่มี label ไหน hoverable, "Time" ไม่ใช่ sensor จริงเลยไม่มี tooltip) และเพิ่มเทสต์ regression ใน `Chart.test.tsx` ยืนยันว่า `sensorMetadata` ถูกส่งต่อไปยัง `PairPlotChart` จริง
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **634/634 ผ่าน** (+6 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (สะสมงาน pairplot ทั้งเซสชันนี้ยังไม่ commit เลยสักครั้ง) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` รอผู้ใช้ลองเปิดจริง โดยเฉพาะจุด hover tooltip ที่ต้องลองด้วยเมาส์จริงเพราะเทสต์ automated เช็คได้แค่ DOM/CSS class ไม่เช็ค visual position จริง

- **🆕 2026-08-11 — 🐛 ผู้ใช้ลองเปิดแอปจริงแล้วเจอ 2 บั๊ก — แก้ทั้งคู่**: ส่งภาพหน้าจอมาว่า (1) hover แล้ว tooltip ไม่ขึ้นเลย เหมือนยังไม่ได้ implement (2) กราฟใน pairplot cell มีขอบดำซ้าย-ขวาเยอะมาก ไม่เต็ม cell เหมือนใน mockup — ทั้งสองเป็นบั๊กจริง ไม่ใช่ user error:
  - **บั๊ก 1 — tooltip ไม่ขึ้นเลย**: root cause คือ `Dashboard.tsx` มี `<Chart>` อยู่ 2 จุด (branch `chartType==='line'` กับ branch scatter/pair) — จุดแรกส่ง `sensorMetadata` เข้าไปอยู่แล้วนานแล้ว แต่ **จุดที่สอง (scatter/pair) ไม่เคยส่ง `sensorMetadata` เข้าไปเลย** ทั้งที่ `Chart.tsx`→`PairPlotChart.tsx` เพิ่งแก้ให้รับ prop นี้ไปเมื่อรอบก่อน — ผลคือ `sensorMetadata` เป็น `undefined` เสมอตอนถึง `PairPlotChart` ไม่ว่าจะแก้โค้ดข้างในถูกแค่ไหนก็ตาม ทำให้ `getDescription()` คืน `undefined` เสมอ ไม่มี label ไหนได้ class `hoverable` เลยสักตัว — แก้แค่บรรทัดเดียว เพิ่ม `sensorMetadata={sensorMetadata}` เข้า `<Chart>` จุดที่สอง
  - **บั๊ก 2 — ขอบดำซ้าย-ขวาใน pairplot cell**: ไล่โค้ดเจอว่า `regl-scatterplot` (library ที่ใช้วาด) มี option `aspectRatio` default เป็น `1.0` เสมอ (สมมติว่า data domain เป็นสี่เหลี่ยมจัตุรัส) — ถ้า canvas ไม่ใช่สี่เหลี่ยมจัตุรัส (ซึ่ง pairplot cell ส่วนใหญ่กว้างกว่าสูง) lib จะ **letterbox** (เว้นขอบดำ) เพื่อรักษาสัดส่วน 1:1 ของ data แทนที่จะยืดเต็ม canvas — โค้ดเรา normalize ทั้ง x/y เป็น `[-1,1]` เท่ากันอยู่แล้ว (ไม่มีความหมายทางกายภาพที่ต้องรักษาสัดส่วนจริง) เลยไม่มีเหตุผลต้อง letterbox เลย แก้โดยส่ง `aspectRatio: innerDims.width / innerDims.height` (สัดส่วนจริงของ canvas) เข้าไปตอน `createScatterplot()` ทำให้กราฟยืดเต็ม cell แทน
  - **เจอบั๊กเดียวกันใน `ScatterChart.tsx` ด้วย** (Scatter ธรรมดา ไม่ใช่แค่ pairplot) เพราะ setup regl-scatterplot เหมือนกันทุกจุด (ไม่ได้แชร์โค้ดกัน คนละไฟล์ copy pattern เดียวกัน) — ผู้ใช้ไม่ได้บ่นเรื่องนี้ แต่เป็นบั๊กเดียวกันแน่นอนเลยแก้ไปพร้อมกันเลยไม่ปล่อยครึ่งๆ กลางๆ
  - **เพิ่ม/แก้เทสต์**: `PairPlotCell.test.tsx`/`ScatterChart.test.tsx` เพิ่ม assertion `aspectRatio` เข้าไปในเทสต์ที่เช็ค `createScatterplot()` options อยู่แล้ว (ไม่ได้เพิ่มเทสต์ใหม่ แค่ขยาย assertion เดิม), `Dashboard.test.tsx` เพิ่มเทสต์ใหม่ 1 ตัวเช็คว่า `sensorMetadata` ถูกส่งเข้า `chartProps` ทั้งตอนอยู่ Scatter และ Pair Plot mode (ไม่ใช่แค่ Line)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **635/635 ผ่าน** (+1 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** — รอผู้ใช้ลองเปิดจริงอีกรอบยืนยันว่าทั้ง 2 บั๊กหายจริง

- **🆕 2026-08-11 — 🐛 บั๊กที่ 3 จากการทดสอบจริง: hover ใน enlarged-view (magnifier) dialog ไม่ขึ้น tooltip เลย**: ผู้ใช้สังเกตว่า cell เล็กใน matrix hover ขึ้น tooltip ปกติดี แต่พอกด magnifier ขยายเป็นภาพใหญ่แล้ว hover กลับไม่ขึ้นอะไรเลย:
  - **Root cause**: tooltip ตัวเดียวกัน (`.scatter-regl-tooltip`, `PairPlotChart.tsx`) ถูกใช้ร่วมกันทั้ง matrix cell ปกติและ `PairPlotCell` ตัวที่อยู่ใน enlarged dialog (ทั้งคู่ใช้ `onHover={setHover}` เดียวกัน) — แต่ tooltip render เป็น sibling ของ dialog modal ไม่ได้ nest อยู่ข้างใน และตั้ง `zIndex: 50` (inline style) ไว้ ในขณะที่ `.pair-regl-expand-backdrop` (ฉากหลัง dialog) ใช้ `z-index: 150` — ทั้งคู่เป็น `position: fixed` เลยแข่งกันที่ระดับเดียวกัน (top-level stacking context) ผลคือ backdrop (150) วาดทับ tooltip (50) เสมอ **hover ยังทำงานถูกต้อง 100% (ข้อมูล/ตำแหน่งถูกคำนวณและ set state จริง) แค่วาดไปโผล่หลังม่านมองไม่เห็นเท่านั้น** ไม่ใช่บั๊ก event/logic ตามที่คาดตอนแรก
  - **แก้**: เปลี่ยน `zIndex: 50` → `zIndex: 200` (สูงกว่า backdrop's 150 แน่นอน และตรงกับ convention เดิมของโปรเจกต์ที่ overlay ระดับบนสุดหลายจุดใน `App.css` ก็ใช้ 200 อยู่แล้ว) พร้อมคอมเมนต์อธิบายเหตุผลไว้กันคนมาลดค่ากลับ
  - **เพิ่มเทสต์ใหม่**: `PairPlotChart.test.tsx` — เทสต์ regression ใหม่ 1 ตัว เปิด magnifier mode, คลิก overlay เปิด enlarged dialog จริง, hover ใน mocked cell ตัวที่อยู่ใน dialog (ตัวสุดท้ายในบรรดา `hover-A-B` button ที่ซ้ำกันเพราะ mock render ทั้ง matrix cell และ dialog cell), assert ว่า tooltip ที่ได้มี `zIndex` มากกว่า 150 จริง
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **636/636 ผ่าน** (+1 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (สะสมงาน pairplot ทั้งเซสชันนี้ยังไม่ commit เลยสักครั้ง ตอนนี้ค้างมาหลายรอบมากแล้ว) — ยังไม่ได้ manual test ผ่าน `npm run tauri dev` รอผู้ใช้ลองเปิดจริงยืนยันว่า hover ใน magnifier dialog ขึ้น tooltip แล้ว

- **🆕 2026-08-13 — ⚡ แก้ warning "chunks larger than 500 kB" ตอน build (`vite build`)**: ก่อนแก้ ทั้งแอปถูก bundle เป็น JS ไฟล์เดียว **3.4 MB minified (1.1 MB gzip)** — โหลด/parse ทั้งก้อนตั้งแต่เปิดโปรแกรมแม้จะยังไม่เข้าเห็น Dashboard เลยก็ตาม:
  - **`App.tsx`**: เปลี่ยน `Dashboard` จาก static import เป็น `React.lazy(() => import('./components/dashboard').then(m => ({ default: m.Dashboard })))` ห่อด้วย `<Suspense fallback={null}>` — เหตุผลคือ `Dashboard` ลากเข้า `echarts`/`echarts-for-react` และ `regl-scatterplot` (2 dependency ที่หนักที่สุดของแอป) มาด้วยเสมอ ทั้งที่หน้าแรกสุด (`DataUploadPage`) ไม่ต้องใช้เลย — ตอนนี้ 2 lib นี้จะโหลดเฉพาะตอนเปิด workspace จริงแล้วเท่านั้น (**สำคัญ**: ต้อง import ผ่าน path เดียวกับที่ `App.test.tsx` mock ไว้คือ `'./components/dashboard'` (index.ts) ไม่ใช่ `'./components/dashboard/Dashboard'` ตรงๆ — ลองแบบหลังไปทีแรกแล้วเทสต์พัง เพราะ `vi.mock` อิงตาม module specifier เป๊ะๆ ไม่รู้ว่า path ไหนๆ ก็ resolve ไปที่ไฟล์เดียวกัน)
  - **`usePMReport.tsx`**: เปลี่ยน `import { pdf } from '@react-pdf/renderer'` และ `import { PMReportTemplate } from '../components/reports/PMReportTemplate'` (ทั้งคู่เคย static import อยู่บนสุดไฟล์) เป็น dynamic `import()` ข้างในฟังก์ชัน `exportPDF` เท่านั้น — `@react-pdf/renderer` เป็น dependency ที่หนักที่สุดในแอป (**1.46 MB minified แยกก้อนเดียว**) แต่ใช้เฉพาะตอนกด export PDF ของ PM Report ซึ่งเป็น action ที่ใช้ไม่บ่อย ไม่มีเหตุผลต้องอยู่ใน bundle หลักเลย
  - **`vite.config.ts`**: เพิ่ม `build.rollupOptions.output.manualChunks` แยก `echarts`+`echarts-for-react` และ `regl-scatterplot` ออกเป็น vendor chunk ของตัวเอง (ไม่ได้ลดขนาดรวม แต่แยกให้ cache ได้อิสระจากโค้ดแอปเอง และ parse แบบขนานกันได้)
  - **ผลลัพธ์หลัง build**: จากไฟล์เดียว 3.4 MB → แตกเป็นหลายก้อน — `index` (แอปหลัก, 420 KB), `regl-scatterplot` (224 KB), `echarts` (1.14 MB, ยังโหลดตอนเปิด Dashboard เหมือนเดิมเพราะเป็น core feature), **`react-pdf.browser` (1.46 MB) ย้ายไปโหลดแบบ lazy ทั้งก้อน ไม่ต้องใช้ตอนเปิดโปรแกรมอีกต่อไป**, `PMReportTemplate` (12.6 KB, lazy คู่กัน) — **warning ยังเหลืออยู่สำหรับ `echarts` และ `react-pdf.browser` เพราะทั้งคู่ยังมีขนาด >500 kB โดยตัวมันเองจริงๆ** (เป็น full charting engine / full PDF engine ไม่ใช่ bug การจัดการ chunk) จะลดต่อได้อีกต้องเปลี่ยนไปใช้ modular import ของ echarts (`echarts/core` + register เฉพาะ chart type ที่ใช้จริง) ซึ่งมีความเสี่ยงสูงกว่ามาก (ต้องไล่ทุกจุดที่ใช้ echarts ทั้งแอปให้ register ครบ ไม่งั้นกราฟบางแบบจะ render ไม่ออกเงียบๆ) — ยังไม่ได้ทำในรอบนี้ ถือว่า trade-off ที่ยอมรับได้เพราะ chunk ที่ใหญ่ที่สุด (react-pdf) ไม่ได้อยู่ใน initial load อีกแล้ว
  - **เจอระหว่างทาง**: ตรวจ `package.json` พบ dependency ที่ import แล้ว 0 จุดในทั้ง repo (ไม่ได้แค่ tree-shake ออกจาก bundle เฉยๆ แต่ไม่มี code อ้างถึงเลย) 6 ตัว — `echarts-gl`, `pako`, `clsx`, `tailwind-merge`, `react-is`, `@tauri-apps/plugin-opener` — **ยังไม่ได้ลบออก** เพราะการลบ/downgrade dependency เป็น action ที่ควรถามผู้ใช้ก่อนเสมอ (อาจมีเหตุผลที่ไม่ปรากฏในโค้ด เช่น peer dependency ของ lib อื่น) แจ้งผู้ใช้ไว้แล้วในแชท รอการตัดสินใจ
  - **แก้เทสต์ที่พังจากการเปลี่ยน `Dashboard` เป็น lazy**: `App.test.tsx` — 4 เทสต์ที่คลิก `trigger-data-ready` แล้ว assert หา `trigger-back` (ปุ่มของ mocked Dashboard) แบบ sync ทันที พังหมด เพราะ `React.lazy`'s dynamic `import()` เป็น microtask เสมอแม้จะ mock module ไว้แล้วก็ตาม — แก้โดยเปลี่ยนจาก `screen.getByText('trigger-back')` เป็น `await screen.findByText('trigger-back')` (หรือรอมันก่อนแล้วค่อยกด) ทุกจุดที่ต้องพึ่ง Dashboard mount แล้ว — เทสต์อื่นๆ ที่ assert เฉพาะ state ของ `App` เอง (เช่น `hasWorkspace`, TitleBar props) ไม่กระทบเพราะไม่ต้องรอ Dashboard mount จริง — `usePMReport.test.ts` ไม่ต้องแก้อะไรเลยเพราะ `vi.mock('@react-pdf/renderer', ...)` ดักได้ทั้ง static และ dynamic import อยู่แล้วโดยธรรมชาติของ vitest
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **636/636 ผ่าน** ไม่มี regression + รัน `vite build` จริงยืนยันตัวเลข chunk ด้านบน — **ยังไม่ได้ commit**

- **🆕 2026-08-13 — 🗑️ ลบ feature "Export as PDF Report" ทั้งหมด (UI + code + dependency)**: ผู้ใช้เห็นว่า `react-pdf.browser` เป็น chunk ที่ไม่คุ้นเลย เช็คแล้วพบว่ามันคือฟีเจอร์ export PDF ใน **หน้าต่าง Predictive Model Build** (แยกหน้าต่างจาก Dashboard หลัก เปิดจากปุ่ม Build Model ในแท็บ Failure Groups) ไม่ใช่ Dashboard — ผู้ใช้ยืนยันว่าไม่เคยใช้ฟีเจอร์นี้แน่ๆ สั่งให้ลบทิ้งทั้งหมด (เก็บ "Export as PNG" ไว้ตามเดิมเพราะเป็นคนละฟีเจอร์ที่ใช้ dropdown เดียวกัน):
  - **ลบไฟล์ทั้งไฟล์**: `src/components/reports/PMReportTemplate.tsx`, `src/components/reports/pmReportTheme.ts` (ทั้งคู่ใช้แค่กับ react-pdf เท่านั้น ไม่มีจุดอื่น import)
  - **`pmReportTypes.ts`**: ลบ interface ที่เป็น PDF-report-data ล้วนๆ ออกหมด (`PMReportData`, `ReportSensorRef`, `ReportSensorStats`, `ReportClusterRange`, `ReportChartImage`) พร้อม import `WorkspaceSensorFilter` ที่ตามมาด้วย — **เก็บ** `STIFFNESS_OPTIONS`/`STIFFNESS_DEFAULT`/`stiffnessLabel`/`snapStiffness` ไว้เพราะเป็นคนละเรื่อง (ใช้กับ dropdown ตั้งค่า Relationship model บนหน้า PM เอง ไม่เกี่ยวกับ PDF) แค่ดันมาอยู่ไฟล์เดียวกันแต่แรกเฉยๆ
  - **`usePMReport.tsx`**: ลบ `exportPDF` ทั้งฟังก์ชันออกจาก hook, เหลือแค่ `exportPNG`
  - **`PredictiveModelBuild.tsx`**: ลบปุ่มเมนู "Export as PDF Report" ออกจาก dropdown (เหลือแค่ "Export as PNG"), ลบฟังก์ชัน `buildReportData` ทั้งก้อน (~215 บรรทัด — เคยไล่ capture ECharts เป็น PNG แล้วประกอบเป็น `PMReportData` object ให้ template ใช้) พร้อม `buildReportDataRef`/`useEffect` คู่กัน, ลบ `subModelsPromiseRef` (มีไว้ให้ PDF export รอ background sub-model fits เสร็จก่อนเท่านั้น ไม่มีจุดอื่นอ่านค่านี้เลย พอ PDF หายไปมันก็ write-only ทันที) และปรับ `handleRelationshipApply` กลับเป็น fire-and-forget ธรรมดาไม่ต้องเก็บ promise ไว้แล้ว — ลบ import `echarts` ทิ้งด้วยเพราะหลังลบ `buildReportData` แล้วไม่มีจุดไหนในไฟล์นี้เรียก `echarts.*` อีกเลย
  - **ลบ `@react-pdf/renderer` ออกจาก `package.json`** (dependency ตัวเดียวที่มีไว้เพื่อ feature นี้โดยเฉพาะ) แล้วรัน `npm install` ใหม่ — ดึง transitive package ตามออกไปด้วย **59 ตัว** (fontkit, yoga-layout ฯลฯ)
  - **ผลลัพธ์ build**: chunk `react-pdf.browser` (1.46 MB) กับ `PMReportTemplate` (12.6 KB) หายไปจาก build output ทั้งคู่ — เหลือ warning "chunk >500kB" แค่จุดเดียวคือ `echarts` (1.14 MB) ซึ่งเป็นขนาดจริงของ full charting engine ไม่ใช่ปัญหาการจัด chunk (ดู entry ก่อนหน้าสำหรับรายละเอียดตัวเลือก modular-echarts ที่ยังไม่ทำ)
  - **แก้เทสต์**: ลบ `describe('exportPDF', ...)` ทั้ง block ออกจาก `usePMReport.test.ts` (พร้อม mock `@react-pdf/renderer`/`PMReportTemplate` ที่ไม่ใช้แล้ว), ลบ `exportPDF` ออกจาก mock ของ `usePMReport` ใน `PredictiveModelBuild.test.tsx` — ไม่มีเทสต์ไหนอ้างอิงปุ่ม/เมนู PDF โดยตรงมาก่อนอยู่แล้ว เลยไม่ต้องแก้จุดอื่น
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **634/634 ผ่าน** (-2 จากการลบเทสต์ exportPDF, ไม่มี regression อื่น) + `npm install` สำเร็จ (215 packages) + `vite build` ยืนยัน chunk react-pdf หายจริง — **ยังไม่ได้ commit**

- **🆕 2026-08-13 — 🧹 ลบ dependency ที่ไม่ได้ใช้จริงอีก 6 ตัวออกจาก `package.json`**: ต่อจาก entry ก่อนหน้าที่แจ้งว่าเจอ dependency import 0 จุดในทั้ง repo — ผู้ใช้สั่งลบทันที ("ถ้าไม่ได้ใช้จริงๆลบออกได้เลย"):
  - **เช็คก่อนลบ**: ไล่ `package-lock.json` ทุก package ว่ามีใคร declare 6 ตัวนี้เป็น `peerDependencies` ไหม (ถ้ามีแล้วลบจะทำให้ npm install เตือน unmet peer dep) — **ไม่มีเลยสักตัว** ปลอดภัยที่จะลบ
  - **`@tauri-apps/plugin-opener`**: ตัวนี้ต้องเช็คพิเศษเพราะเป็น Tauri plugin (npm side กับ Rust side แยกกันคนละระบบ) — เช็ค `src-tauri/Cargo.toml`/`lib.rs`/`capabilities/default.json` แล้วพบว่า plugin นี้ **ยังถูกใช้งานจริงฝั่ง Rust** (`tauri_plugin_opener::init()` + capability `opener:default`) — แต่ npm package (`@tauri-apps/plugin-opener`) ไม่เคยถูก JS import เลยสักจุด เพราะฟีเจอร์เปิดลิงก์ภายนอกทำงานผ่าน Rust ล้วนๆ (หรือ webview intercept `<a target="_blank">` อัตโนมัติ) ไม่เคยต้องเรียกผ่าน JS API เลย — **ลบเฉพาะ npm package ออก ไม่แตะ Rust/Cargo.toml/capabilities เลย ฟีเจอร์เปิดลิงก์ยังทำงานเหมือนเดิม 100%**
  - **ตัวอื่นอีก 5 ตัว** (`echarts-gl`, `pako`, `clsx`, `tailwind-merge`, `react-is`) — ไม่มีความซับซ้อนแบบ Tauri plugin ลบตรงๆ ได้เลย
  - รัน `npm install` ใหม่ — ดึง transitive package ตามออกไปอีก **8 ตัว**
  - **ผลลัพธ์ `package.json`**: จาก 21 production + 16 dev (37 ตัว) เหลือ **14 production + 16 dev = 30 direct dependencies** — ทุกตัวที่เหลือยืนยันแล้วว่ามีจุด import จริงในโค้ด
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **634/634 ผ่าน** ไม่มี regression + `vite build` ให้ output ขนาดเท่าเดิมทุก byte (ยืนยันว่า 6 ตัวนี้ไม่เคยถูก bundle อยู่แล้ว เป็นแค่ dead weight ใน `node_modules`/install size ไม่ใช่ปัญหา bundle size) — **ยังไม่ได้ commit** (รวมกับงาน code-splitting + ลบ PDF feature จากรอบก่อนหน้าที่ยังไม่ commit เหมือนกัน)

- **🆕 2026-08-13 — ✅ ทำ 4 งานสถานะ "In progress" ใน Notion พร้อมกัน**: ผู้ใช้ให้ list งาน Notion แล้วเปลี่ยนงานที่ 5 ("sync button and selected time period") จาก Not started → In progress เอง แล้วสั่งให้ทำทั้ง 4 งาน in-progress พร้อมกัน — เจอ 2 งานที่ชื่อกำกวมเกินกว่าจะเดา เลยถาม `AskUserQuestion` ก่อนเริ่ม ("add criteria sensor in scatter plot" = เพิ่ม sensor ที่ 3 ระบายสีจุดแบบ gradient เหมือน clustering criteria ใน Predictive Model; "sync button..." = ปุ่ม ✓ Apply relative range กับตัวเลข/หน่วยที่เลือกไม่ sync กับวันที่จริงที่แสดง) ได้คำตอบชัดแล้วเริ่มทำทีละงาน:
  1. **บั๊ก "pairplot description ถูกตัดถ้า text ยาวเกินไป"**: root cause คือ `.pair-regl-cell-slot` มี `overflow:hidden` (จำเป็นสำหรับ clip WebGL canvas) แต่ tooltip ของ `AxisLabel` เป็น descendant ของมันด้วย พอ description ยาวเกินความกว้างเซลล์แคบๆ ก็โดน clip ไปด้วย — แก้โดยย้าย `overflow:hidden` ลงไปที่ `.pair-regl-cell` (ชั้นในสุด ขนาดเท่าเดิมเป๊ะ ไม่กระทบการ render กราฟเลย) แทน `.pair-regl-cell-slot` (ชั้นนอก) ทำให้ tooltip ซึ่งเป็น sibling ของ `.pair-regl-cell` มีที่ล้นออกไปได้ — ต้องแก้ `CorrelationCell` เพิ่มเติมด้วยเพราะเดิม render `AxisLabel` (row header) ซ้อนอยู่ *ข้างใน* `.pair-regl-cell` ของมันเอง (ไม่เหมือน cell ประเภทอื่นที่ AxisLabel เป็น sibling) เลยต้องปรับให้เป็น sibling เหมือนกันหมด ถึงจะหลุดพ้น clip ได้จริง — เพิ่ม `max-width:220px` + `white-space:normal` ให้ tooltip ด้วย (เดิม `nowrap` ไม่มี max-width เลยจะยาวเป็นเส้นเดียวไม่จบ)
  2. **feature "add description in scatter plot"**: เพิ่ม hover-tooltip แสดง description ให้ ScatterChart เหมือนที่ pairplot มีอยู่แล้ว — **refactor**: ดึง `AxisLabel` component ออกจาก `PairPlotChart.tsx` เป็นไฟล์ใหม่ `src/components/charts/AxisLabel.tsx` (generalize ให้รับ `style`/`rotate`/`tooltipSide` แทนการผูกกับ `CELL_PAD` ของ pairplot ตรงๆ) แล้วให้ทั้ง `PairPlotChart.tsx` และ `ScatterChart.tsx` import ใช้ร่วมกัน — เจอบั๊ก wiring gap แบบเดียวกับที่เคยเจอกับ pairplot มาก่อน: `Chart.tsx` ไม่เคยส่ง `sensorMetadata` เข้า `<ScatterChart>` เลย (ส่งให้ Line/PairPlot อย่างเดียว) แก้แล้ว — X/Y axis title เปลี่ยนจาก SVG `<text>` เป็น HTML overlay (จำเป็นเพื่อให้ hover ได้)
  3. **บั๊ก "sync button and selected time period"**: root cause คือปุ่มหน่วย (Y/M/W/D/H) ใน widget "Apply relative range" ไฮไลต์ตาม `relativeUnit === u` เฉยๆ ซึ่งเป็นแค่ค่าที่เลือกไว้ใน picker ไม่ใช่ค่าที่ถูก apply จริงกับวันที่ที่แสดงอยู่ — พอผู้ใช้แก้ Start/End เองด้วยมือ ปุ่มก็ยังค้างไฮไลต์หลอกว่า preset นั้นยัง active อยู่ — แก้โดยเพิ่ม state ใหม่ `relativeRangeApplied` (true เฉพาะทันทีหลังกด ✓, false ทันทีที่แก้ Start/End/unit/amount เองด้วยมือ) แล้วเปลี่ยนเงื่อนไขไฮไลต์เป็น `relativeRangeApplied && relativeUnit === u`
  4. **feature "add criteria sensor in scatter plot"**: เพิ่ม dropdown ที่ 3 "Colour by…" ให้เลือก sensor ตัวที่ 3 มาระบายสีจุดใน Scatter Plot แบบ gradient ต่อเนื่อง (ไม่ใช่ bucket แบบ clustering ใน Predictive Model เพราะไม่มี UI ตั้งค่าจำนวน/ช่วง cluster ในหน้านี้) — ใช้กลไก native ของ `regl-scatterplot` เอง: `colorBy: 'valueB'` (ช่องข้อมูลต่อเนื่อง แยกจาก `valueA` ที่ PairPlotCell ใช้กับ cluster สี discrete) + `pointColor: [gradient stops]` ให้ library เป็นคน interpolate สีเองตาม normalized value ของแต่ละจุด — สี gradient ใช้โทน sky→indigo→violet→fuchsia→rose (คนละชุดกับ red-amber-green ของ correlation heatmap ใน pairplot เพื่อไม่ให้สื่อความหมายมั่ว) ค่า criteria ที่หายไปในบางแถว fallback เป็น 0.5 (กึ่งกลาง gradient) แทนที่จะดรอปจุดทิ้ง (กัน point count ไม่ตรงกับตอนไม่มี criteria) — เพิ่ม legend bar เล็กๆ โชว์ min/max ของ sensor ที่เลือกเมื่อ active — **ยังไม่ persist** ข้าม session/workspace reload (เหมือนทุก state อื่นใน ScatterChart ที่ไม่ได้ seed จาก `persisted*` prop จะรีเซ็ตทุกครั้งที่สลับออกจาก Scatter แล้วกลับมา) — ถ้าต้องการให้จำค่าไว้ข้าม session ต้องขยายงานเพิ่ม (touch `WorkspaceState`/`Dashboard.tsx` แบบเดียวกับที่เคยทำให้ xPin/yPin) ยังไม่ได้ทำในรอบนี้เพราะไม่ได้อยู่ในขอบเขตที่ขอมา
  - **เพิ่ม/แก้เทสต์ครบทุกจุด**: `PairPlotChart.test.tsx` (แก้ 1 เทสต์เดิมที่ query ผ่าน `.pair-regl-cell` ให้เปลี่ยนเป็น `.pair-regl-cell-slot` ตามโครงสร้างใหม่ + เทสต์ regression ใหม่ยืนยันว่า tooltip ไม่ได้อยู่ใต้ `.pair-regl-cell` อีกต่อไป), `ScatterChart.test.tsx` (เทสต์ hover-tooltip ใหม่ 3 ตัว + เทสต์ criteria-sensor coloring ใหม่ 6 ตัว ครอบคลุม colorBy toggle, valueB normalization + null fallback, legend แสดง/ซ่อน, revert เมื่อ deselect), `Chart.test.tsx` (เทสต์ regression sensorMetadata ส่งถึง ScatterChart), `Dashboard.test.tsx` (เทสต์ใหม่ 6 ตัวคลุม `relativeRangeApplied` ทุกทาง: ก่อน apply, หลัง apply, เคลียร์ตอนแก้ start/end/unit/amount เอง, re-apply ใหม่)
  - **เจอปัญหา TS target ระหว่างเขียนเทสต์**: ใช้ `Array.prototype.at(-1)` ไปทีแรก แต่ `tsconfig.json` ตั้ง `target: ES2020` (ไม่มี `.at()` ซึ่งเป็น ES2022) — vitest รันผ่านเพราะ esbuild/Node ไม่แคร์ lib target แต่ `tsc --noEmit` fail จริง แก้โดยเปลี่ยนกลับไปใช้ pattern `arr[arr.length-1]` (helper `last()`) ที่ใช้อยู่แล้วในไฟล์เทสต์อื่นๆ ของโปรเจกต์นี้
  - **หมายเหตุ**: task ทั้ง 4 ใน Notion ยัง**ไม่ได้เปลี่ยนเป็น Done** — รอผู้ใช้ทดสอบจริงผ่าน `npm run tauri dev` ก่อนตามธรรมเนียมเดิมของ session นี้ (implement → user ทดสอบ → user confirm ถึงจะปิดงาน)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **651/651 ผ่าน** (+17 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (สะสมมาหลายรอบแล้วในเซสชันนี้)

- **🆕 2026-08-13 — 🐛🎨 ผู้ใช้ทดสอบ feature ที่เพิ่งทำ (screenshot) แล้วสั่ง 2 อย่าง: ลบ lasso-select ออกจาก Scatter Plot ทั้งหมด + เปลี่ยน criteria-sensor coloring จาก gradient เป็น value-range multi-select**:
  1. **ลบ lasso-select ออกจาก `ScatterChart.tsx` ทั้งหมด** ("function นี้ไม่ได้ใช้ ลบที่เกี่ยวข้องออกทั้งหมด" — เห็นจาก screenshot ว่ามีปุ่ม "1 point selected" + CSV export + Clear ค้างอยู่ด้านล่างกราฟ): ลบ `tool`/`selectedIndices` state, `select`/`deselect` subscription, `handleClear`/`handleExportCsv`, selection panel JSX (`.scatter-regl-panel`), ปุ่ม Lasso + ปุ่ม Eraser (Clear) ออกจาก toolbox — **เหลือแค่ Pan/Zoom เป็นพฤติกรรม default ถาวรของ library ไม่ต้องมีปุ่มสลับอีกต่อไป** (ไม่มี tool อื่นให้สลับไปแล้ว) เหลือ toolbox แค่ 2 ปุ่ม: Reset view, Axis pin — **เช็คก่อนลบ CSS**: `.scatter-regl-tool`/`.scatter-regl-btn`/`.scatter-regl-btn-icon`/`.scatter-regl-actions` ถูกใช้ร่วมกับ `PairPlotChart.tsx` และ `AddSensorWindow.tsx` ด้วย **ห้ามลบ** — ลบเฉพาะ `.scatter-regl-panel`/`.scatter-regl-panel b` ที่ใช้เฉพาะ selection panel เท่านั้น — **ไม่แตะ PairPlotChart/PairPlotCell เลย** เพราะ lasso+cluster ของ pairplot เป็นฟีเจอร์คนละตัวที่ยังใช้งานอยู่จริง (ผู้ใช้พูดถึงเฉพาะ "ใน scatterplot" ในข้อความ)
  2. **Redesign criteria-sensor coloring จาก continuous gradient → discrete value-range multi-select ("load bands")**: ผู้ใช้ขอชัดเจนว่าอยากเลือก "ช่วงค่า" ได้ เช่น speed 4000-5000, 5000-6000 และเลือกได้หลายช่วงพร้อมกันเพื่อดู load ของเครื่องจักร — เดิม gradient ต่อเนื่องตอบโจทย์นี้ไม่ได้เลย (ไม่มีทางระบุขอบเขตช่วงที่ต้องการเจาะจงดู) แก้ทั้งกลไก:
     - **State ใหม่**: `criteriaRanges: Array<{id,min,max,enabled}>` — เลือก sensor อย่างเดียวยังไม่ทำอะไร (`colorBy` ยังเป็น `null`) จนกว่าจะมี range ที่ enabled อย่างน้อย 1 ช่วง ต่างจากเดิมที่เลือก sensor ปุ๊บ gradient โชว์ทันที
     - **เปลี่ยนกลไก regl-scatterplot**: จาก `colorBy:'valueB'` (continuous) → `colorBy:'valueA'` (discrete/categorical — ตัวเดียวกับที่ `PairPlotCell` ใช้กับ lasso cluster) — จุดที่ตรงกับ range ที่ enabled ตัวแรกที่แมตช์ (ตามลำดับที่เพิ่ม) ได้ category = index+1, จุดที่ไม่ตรง range ไหนเลย (หรือ sensor ไม่มีค่า) ได้ category 0 → สีจาง `CRITERIA_UNMATCHED_COLOR` (alpha 0.12) ให้ยังเห็น overall shape ของข้อมูลอยู่แต่ band ที่เลือกเด่นชัดสุด — สี palette ของแต่ละ range ใช้ `RANGE_PALETTE` ชุดเดียวกับ `CLUSTER_PALETTE` ใน `PairPlotChart.tsx` (amber/emerald/fuchsia/rose/sky/orange/lime/violet) เพื่อความสอดคล้องกันทั้งแอป
     - **UI ใหม่**: แทนที่ gradient legend bar เดิม ด้วย panel ที่มี: hint โชว์ min/max จริงของ sensor ที่เลือก (ช่วยผู้ใช้รู้ว่าควรพิมพ์ตัวเลขประมาณไหน), chip ต่อ 1 range (checkbox เปิด/ปิด + สี swatch + label min–max + ปุ่มลบ), ช่อง min/max + ปุ่ม "+ Add" สำหรับเพิ่ม range ใหม่ (mirror pattern เดียวกับ axis-scale editor ที่มีอยู่แล้วในไฟล์นี้) — validation: min ต้อง < max ไม่งั้นโชว์ error inline
     - **Multi-select ทำงานยังไง**: uncheck range ใดๆ ไม่ได้ลบทิ้ง แค่เอาออกจากการระบายสี (ยังอยู่ใน list ให้เปิดกลับได้) — เปิดพร้อมกันได้หลาย range แต่ละ range ได้สีของตัวเอง เห็นเทียบกันได้ในกราฟเดียว ตรงตามที่ขอ ("เพื่อดู load")
  - **เพิ่ม/แก้เทสต์ครบ**: `ScatterChart.test.tsx` — ลบเทสต์ lasso/selection/CSV export ทั้งหมด (3 เทสต์: selection panel, tool toggle mouseMode, CSV export) + แก้เทสต์ subscribe ให้ยืนยันว่า **ไม่มี** `select`/`deselect` subscription อีกต่อไป, เขียน `describe('criteria-sensor colouring (value-range...)')` ใหม่ทั้งบล็อก (10 เทสต์): เลือก sensor อย่างเดียวไม่ทำงาน, เพิ่ม range แล้ว colorBy เปิด + categorize ถูกต้องต่อจุด, หลาย range พร้อมกัน, uncheck ไม่ลบ, ลบผ่านปุ่ม X, validation error min>=max, hint min/max, revert เมื่อเคลียร์ sensor, drop ranges เมื่อ sensor หลุดจาก selection แล้ว re-select ต้องไม่มี range เก่าเหลือ
  - **เจอบั๊กเทสต์ของตัวเอง 2 จุดระหว่างเขียน**: (1) เขียน helper `flush()` แต่ไม่ได้เรียกใช้จริง (inline logic แทนทุกที่) — `tsc --noEmit` จับได้ว่า unused function ลบทิ้ง (2) พิมพ์ผิดในเทสต์เอง (`expect(screen.getByText('100.00–100.00'))` ทั้งที่ range ที่เพิ่มคือ 50–100) แก้เป็น `'50.00–100.00'`
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **652/652 ผ่าน** (+1 จากรอบก่อน — ลบ 3 เทสต์เก่า ลบ+เพิ่มใหม่สุทธิ) ไม่มี regression — **ยังไม่ได้ commit**

- **🆕 2026-08-13 — ⚡ Performance review ตามคำขอ + แก้จุดเสี่ยงที่เจอ (ResizeObserver ไม่มี debounce)**: ผู้ใช้ขอให้เช็ค code-splitting ที่ทำไปก่อนหน้า + รีวิวจุดที่เสี่ยงทำให้แอปช้าลงอีก (บ่นว่าช้าอยู่แล้วกับข้อมูลขนาดใหญ่):
  - **เช็ค code-splitting**: รัน `vite build` ซ้ำ ยืนยัน chunk breakdown เหมือนเดิมทุกจุด ไม่มีอะไรเปลี่ยน
  - **สรุปสิ่งที่ดีอยู่แล้ว** (ไม่ต้องแก้): Rust reservoir-sample คุม scatter/pairplot ไม่เกิน 200K/50K points (O(n) single-pass, มี hard ceiling 2M กันคนเรียกผิด), line chart cap 4,000 points + LTTB downsampling + ปิด animation อัตโนมัติเมื่อข้อมูลใหญ่, table server-side pagination ไม่เกิน 1,000 แถว/หน้า, pairplot จำกัด 4 sensor (WebGL context ไม่เกิน 10), ทุก data-fetch hook debounce 200-250ms + race-guard (fetchIdRef pattern เดียวกันทุกที่)
  - **เจอความเสี่ยงจริง 1 จุด**: `ScatterChart.tsx` และ `PairPlotCell.tsx` ใช้ `ResizeObserver` โดย**ไม่มี debounce เลย** — ทุก tick ของการลาก resize แผง (split.js) ทำให้ effect ที่สร้าง WebGL instance (`[innerDims.width, innerDims.height, ...]` เป็น dependency) ทำลาย+สร้าง context ใหม่ทันที (compile shader + upload buffer ใหม่ทั้งหมด) — pairplot มีได้ถึง 10 instance พร้อมกัน (4 sensor cap) เลยเสี่ยงกระตุกหนักกว่า scatter เดี่ยวๆ ตอนลาก resize ขณะมีข้อมูลใกล้ cap
  - **แก้**: ทั้ง 2 ไฟล์ — เก็บพฤติกรรมเดิมไว้สำหรับ mount ครั้งแรก (`update()` เรียกทันที ไม่ดีเลย์ ให้ layout initial ถูกต้องทันที) แต่ ResizeObserver callback หลังจากนั้นห่อด้วย debounce 150ms (`clearTimeout` + `setTimeout` แบบง่ายๆ ไม่ได้เพิ่ม dependency ใหม่หรือทำ shared hook เพราะมีแค่ 2 จุดใช้ ไม่คุ้มจะสร้าง abstraction) — ผลคือลาก resize ต่อเนื่องจะจ่าย cost การสร้าง context ใหม่แค่ครั้งเดียวตอนหยุดลาก (หรือหยุดนิ่ง 150ms) แทนที่จะจ่ายทุก tick ระหว่างทาง
  - **เพิ่มเทสต์ทั้ง 2 ไฟล์**: `ScatterChart.test.tsx`/`PairPlotCell.test.tsx` — ปรับ `MockResizeObserver` ให้เก็บ callback ที่ constructor รับมาไว้ในตัวแปร module-level (`lastResizeCallback`) แทนที่จะทิ้งไปเฉยๆ เหมือนเดิม แล้วเขียนเทสต์ regression ใหม่ (`vi.useFakeTimers()`) จำลองการลาก resize เร็วๆ 3 ครั้งติดกัน (ห่างกัน 50ms) ยืนยันว่า `createScatterplot` ไม่ถูกเรียกซ้ำเลยจนกว่าจะผ่าน debounce window (150ms) หลัง tick สุดท้าย แล้วถูกเรียกแค่ **ครั้งเดียว** ไม่ใช่ 3 ครั้ง — เพิ่ม `vi.useRealTimers()` ใน `afterEach` ทั้ง 2 ไฟล์กันปัญหา fake timers รั่วข้ามเทสต์อื่น
  - **ฟีเจอร์ criteria-range ที่เพิ่งทำ**: ยืนยันว่าไม่ได้เพิ่มความเสี่ยงใหม่ที่มีนัยสำคัญ — ใช้ data pipeline ที่ bound ไว้อยู่แล้วเหมือนเดิม การติ๊ก/ถอดติ๊ก range ทำให้ต้อง re-upload x/y ใหม่จริง (เพราะ `regl-scatterplot`'s `draw()` บังคับส่ง x/y ทุกครั้ง ไม่มีทาง update สีอย่างเดียวได้ตาม type definition ของ lib) แต่เป็น trigger แบบคลิกครั้งเดียว ไม่ใช่ continuous เหมือน resize เลยไม่ถือเป็นปัญหา
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **654/654 ผ่าน** (+2 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (สะสมมาหลายรอบมากแล้วในเซสชันนี้ ยังไม่มีครั้งไหน commit เลยตั้งแต่ push ครั้งแรกของวันนี้)

- **🆕 2026-08-13 — 🐛 ผู้ใช้ manual test แล้วเจอ 2 ปัญหา: ช่อง min/max แคบเกิน + criteria-sensor state หายเมื่อสลับ chart type (บั๊กเดิมที่เคยแก้แล้วกลับมาใหม่)**:
  1. **ช่อง min/max แคบเกิน**: เดิม inline `style={{width: 52}}` ซึ่งจริงๆ **แคบกว่า** ค่า default ของ class `.scatter-regl-axis-editor-input` เองด้วยซ้ำ (64px) เพราะ inline style ทับ class เสมอ — ตัวเลขยาวๆ อย่าง "8454.00" เลยแสดงไม่ครบ แก้เป็น 84px
  2. **criteria-sensor + ranges หายเมื่อสลับ chart type แล้วกลับมา**: root cause คือตอนสร้างฟีเจอร์นี้ (2 entry ก่อนหน้า) ใช้ `useState` ธรรมดาสำหรับ `criteriaSensor`/`criteriaRanges` ทั้งที่ `ScatterChart.tsx` มี pattern แก้ปัญหานี้อยู่แล้ว 2 บรรทัดถัดจากมันเป๊ะๆ — `scatterX`/`scatterY`/`xPin`/`yPin` ถูก seed จาก `persisted*` prop + push ขึ้น Dashboard ผ่าน callback เพราะ `Chart.tsx` unmount `ScatterChart` ทั้งตัวทุกครั้งที่ chartType ไม่ใช่ 'scatter' (คอมเมนต์เดิมในโค้ดก็อธิบายเหตุผลนี้ไว้ชัดอยู่แล้ว) — ฟีเจอร์ criteria-range ใหม่ไม่ได้ทำตาม pattern เดียวกัน เลยโดนบั๊กเดิมซ้ำ ผู้ใช้ต้องจับได้เองจาก manual test
     - **แก้ end-to-end แบบเดียวกับ xPin/yPin ทุกจุด**: เพิ่ม `ScatterCriteriaRange`/`ScatterCriteria` interface ใน `types.ts` + field `scatterCriteria?: ScatterCriteria` ใน `WorkspaceState` → เพิ่ม `scatterCriteria`/`onScatterCriteriaChange` ใน `ChartProps` (`ChartTypes.ts`) → ส่งผ่าน `Chart.tsx` → `ScatterChart.tsx` seed `criteriaSensor`/`criteriaRanges` จาก `persistedCriteria` prop ตอน mount (`useState(() => persistedCriteria?.sensor ?? '')`) แล้ว push ขึ้นทุกครั้งที่เปลี่ยนผ่าน `useEffect` ใหม่ (mirror `onScatterAxisPinsChange` effect เป๊ะๆ) → `Dashboard.tsx` เพิ่ม state `scatterCriteria` + handler + ใส่ใน `buildWorkspaceState()`/autosave dependency array + ส่งเข้า `<Chart>`
     - **ย้าย `CriteriaRange` interface ที่เคย local ใน `ScatterChart.tsx`** ไปเป็น `ScatterCriteriaRange` ใน `types.ts` (shared) เพราะตอนนี้ `Dashboard.tsx`/`WorkspaceState` ต้องรู้จัก shape นี้ด้วย
  - **ผู้ใช้ขอให้จดไว้เป็นกฎ**: "เวลาให้เพิ่ม function อะไรซักอย่างต้อง recheck การทำงานกับของเดิมด้วย" — บันทึกลง Claude memory ส่วนตัวแล้ว (`feedback_recheck_existing_patterns_on_new_features`) เพื่อให้ติดตัวไปทุกโปรเจกต์ ไม่ใช่แค่ในนี้ — สรุปคือ: ก่อนถือว่าฟีเจอร์ใหม่เสร็จ ต้องเช็คว่า component เดียวกันมี pattern แก้ปัญหาคล้ายกันอยู่แล้วหรือไม่ (persistence, debounce, validation shape ฯลฯ) แล้ว apply ให้ state ใหม่ด้วย ไม่ใช่แค่ทำ state ใหม่แยกลอยๆ
  - **เพิ่มเทสต์ครบ**: `ScatterChart.test.tsx` — describe block ใหม่ `persistence (regression: ...)` 2 เทสต์ (seed จาก prop ตอน mount, `onScatterCriteriaChange` fire ทุกครั้งที่ sensor/range เปลี่ยน), `Chart.test.tsx` ขยายเทสต์เดิมให้ครอบคลุม `scatterCriteria`/`onScatterCriteriaChange` ด้วย, `Dashboard.test.tsx` เทสต์ใหม่ 1 ตัวยืนยัน `initialState.scatterCriteria` ไหลไปถึง `chartProps` จริง
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **657/657 ผ่าน** (+3 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (ค้างมาทั้งวันตั้งแต่ push ครั้งแรก)

- **🆕 2026-08-13 — 🏷️ ปุ่ม "Reset" ใต้กราฟ ย้ายตำแหน่ง + เปลี่ยนชื่อให้สื่อความหมาย**: ผู้ใช้ถามว่าปุ่ม Reset (ที่อยู่หลัง AGGREGATION dropdown ในภาพ) reset อะไรบ้าง — เช็คโค้ดแล้วตอบว่า reset แค่ **ช่วงเวลา (Time Period)** อย่างเดียว ไม่แตะ Aggregation เลย แต่ตำแหน่งเดิม (อยู่หลัง AGGREGATION) ทำให้เข้าใจผิดได้ว่าอาจ reset aggregation ด้วย — ผู้ใช้เลยขอให้ย้ายปุ่มไปอยู่หลัง TIME RANGE (ไม่ใช่หลัง AGGREGATION) และเปลี่ยนชื่อปุ่มให้บอกชัดว่า reset อะไร:
  - **ย้าย JSX**: ปุ่ม Reset (เดิมเป็น sibling ตัวสุดท้ายใน `.chart-tab-content` ต่อจาก group AGGREGATION) ย้ายมาเป็น sibling ตรงกลาง คือหลัง group TIME RANGE แต่ก่อน group AGGREGATION — เป็นแค่ reorder JSX ไม่แตะ CSS เลย (`.reset-range-btn` ไม่มี absolute positioning หรือ margin-left:auto ที่อิงตำแหน่งเดิม เป็น flex item ธรรมดา)
  - **เปลี่ยนชื่อ**: "Reset" → **"Reset Period"** สื่อชัดว่า reset ช่วงเวลา ไม่ใช่ทั้งแถบ พร้อมเพิ่ม `title` tooltip อธิบายเพิ่มว่า "does not change Aggregation"
  - **แก้ bug ที่เจอระหว่างตอบคำถามเดิม** (คุยไว้ก่อนหน้านี้ในเทิร์นก่อน แต่ยังไม่ได้แก้จนกว่าจะมาถึงรอบนี้): ปุ่มนี้เรียก `handleFiltersChange` ตรงๆ ไม่ผ่าน path เดียวกับตอนแก้ Start/End เอง เลยไม่เคลียร์ `relativeRangeApplied` — กด Reset Period แล้วปุ่มหน่วย (เช่น M) อาจค้างไฮไลต์อยู่ทั้งที่วันที่ไม่ตรงกับ preset นั้นแล้ว เพิ่ม `setRelativeRangeApplied(false)` เข้าไปใน onClick ให้ตรงกับพฤติกรรมที่ถูกต้อง (ตรงตามกฎ "recheck existing patterns" ที่เพิ่งบันทึกไว้เมื่อกี้พอดี — จุดนี้คือตัวอย่างจริงของมัน)
  - **เพิ่มเทสต์**: `Dashboard.test.tsx` — describe block ใหม่ `"Reset Period" button (regression: ...)` 4 เทสต์: ชื่อปุ่มต้องเป็น "Reset Period" ไม่ใช่ "Reset" เฉยๆ, ตำแหน่งใน DOM ต้องอยู่ก่อน AGGREGATION label (เช็คด้วย `compareDocumentPosition`), กดแล้ว reset เฉพาะ timestamp_start/end (เป็น `null` ตาม `filters.timestampStart || null`) โดย Aggregation dropdown ไม่เปลี่ยนค่า, กดแล้วปุ่มหน่วยที่เคยไฮไลต์ต้องดับ
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **661/661 ผ่าน** (+4 จากรอบก่อน) ไม่มี regression — **ยังไม่ได้ commit** (ค้างมาทั้งวันตั้งแต่ push ครั้งแรก)

- **🆕 2026-08-13 — 🐛 บั๊กที่ 3 (ซ้อนกันมา) ในวิดเจ็ต TIME RANGE: กดปุ่มหน่วย (Y/M/W/D/H) แล้วเหมือนไม่มีอะไรเกิดขึ้นเลย ต้องกด ✓ Apply ก่อนถึงจะ "ใช้งานได้"**: ผู้ใช้ขอ AskUserQuestion ให้เลือกอธิบาย sequence เอง ได้คำอธิบายมาว่า: เลือกหน่วยจาก calendar เอง (ไม่มีปัญหา) สลับกับใช้ shortcut หน่วย (เช่น 3M) ไปเรื่อยๆ แล้วจะกด Y/M/W/D/H ไม่ติด ต้องกด ✓ ก่อนถึงจะเลือกได้ — ไล่โค้ดแล้วเจอว่าเป็นผลข้างเคียงของการแก้บั๊กรอบก่อนหน้านี้เอง (entry ก่อนหน้า 2 entry — "sync button and selected time period"):
  - **root cause**: การแก้รอบก่อนผูก visual feedback ของปุ่มหน่วยไว้กับ `relativeRangeApplied` ล้วนๆ (`background: relativeRangeApplied && relativeUnit===u ? accent : transparent`) — ผลคือทันทีที่ `relativeRangeApplied` เป็น false (เช่น หลังแก้ปฏิทินเอง) **ปุ่มหน่วยทุกปุ่มจะดูเหมือนกันหมด ไม่ว่าจะกดปุ่มไหนก็ตาม** เพราะเงื่อนไข active ต้องมี `relativeRangeApplied` เป็น true เสมอ — ทำให้ดูเหมือนปุ่มไม่ตอบสนอง (แต่จริงๆ `relativeUnit` state เปลี่ยนอยู่เบื้องหลังปกติทุกครั้งที่กด แค่ไม่มี feedback ให้เห็นเฉยๆ) กด ✓ Apply ครั้งเดียวถึงจะเห็น highlight โผล่ขึ้นมา (ของหน่วยที่ค้างเลือกไว้ล่าสุด) ทำให้เข้าใจผิดว่า "ต้องกด Apply ก่อนถึงจะเลือกปุ่มได้"
  - **แก้โดยแยก 2 concept ที่ปนกันออกจากกัน**: `isSelected` (`relativeUnit === u` — true ทันทีทุกครั้งที่กด ไม่ขึ้นกับ Apply เลย) กับ `isActive` (`isSelected && relativeRangeApplied` — คำกล่าวอ้างที่แรงกว่า ว่าตรงกับวันที่ที่แสดงอยู่จริง) — ตอนนี้มี 3 สถานะภาพชัดเจน: ยังไม่เลือก (plain), เลือกแล้วแต่ยังไม่ apply/ไม่ตรงกับวันที่ที่โชว์ (โครงเส้นขอบสี accent + ตัวอักษรสี accent แต่พื้นหลังใส), เลือกและ apply ตรงกับวันที่จริง (พื้นหลัง solid accent + ตัวอักษรขาว) — D (หน่วย default ตอนเปิดแอป) เลยโชว์เป็นสถานะ "เลือกแล้ว" (โครงเส้น) ตั้งแต่ render แรกโดยไม่ต้องกดอะไรเลย ตรงตามความเป็นจริง — อัปเดต `title` tooltip ให้สื่อ 3 สถานะด้วยเหมือนกัน ("Selected — click ✓ Apply to use..." สำหรับสถานะกลาง)
  - **แก้เทสต์**: rename describe block เดิม `unit-button "applied" indicator sync` → `unit-button selected/applied indicator (2-tier: outline = picked, solid = picked AND matches the shown dates)` เขียนใหม่ทั้งหมด 8 เทสต์ครอบคลุมทั้ง 3 สถานะ (plain/outline/solid) รวมเทสต์ regression ตรงจุดที่ผู้ใช้เจอเป๊ะๆ ("moves the outline to the newly-clicked unit... — this is the exact click the user reported as unresponsive") — เปลี่ยนวิธี query ปุ่มจาก `screen.getByTitle('Days')` เป็น `screen.getByRole('button', {name: 'D'})` เพราะ title ไม่ใช่ค่าคงที่อีกต่อไป (เปลี่ยนตามสถานะ) — ต้องแก้เทสต์ `Reset Period` เดิม 1 จุดด้วยเหตุผลเดียวกัน — ระหว่างเขียนเทสต์เจอ jsdom normalize `'#fff'` เป็น `'rgb(255, 255, 255)'` เวลาอ่านกลับจาก `.style.color` เลยต้อง assert ด้วยรูปแบบหลัง
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **662/662 ผ่าน** (+1 จากรอบก่อน — ลบเทสต์เก่า 7 เพิ่มใหม่ 8 สุทธิ) ไม่มี regression — **ยังไม่ได้ commit** (ค้างมาทั้งวันตั้งแต่ push ครั้งแรก วันนี้เป็นวันที่แก้ widget นี้ 3 รอบติดกันในเซสชันเดียว — เป็นตัวอย่างที่ดีว่าทำไมกฎ "recheck existing patterns" ถึงสำคัญ)

- **🆕 2026-08-13 — 🎨 DESIGN เท่านั้น (ยังไม่ implement) — "Highlight periods" บนกราฟตาม timestamp**: task เดียวที่เหลือใน Notion สถานะ "In progress" ตอนนี้ — requirement เขียนไว้ในชื่อ task เอง (ตัวเพจว่างเปล่า ไม่มี description แยก): *"add highlight on graph by time stamp ต้องเลือกได้มากกว่า 1 period เปลี่ยนสีของ highlight ได้ด้วย เอา control function ไปไว้กับ tab เดียวกับพวก selected sensor filter"* — คุยออกแบบกับผู้ใช้ผ่าน Artifact mockup (คุยกันคนละ session ต่อได้ ดู URL ด้านล่าง) ผู้ใช้ approve แนวทางแล้วแต่**ยังไม่ได้เริ่มโค้ดเลยสักบรรทัด** งานนี้ทำต่อได้ทันทีในรอบหน้า:
  - **Mockup**: `https://claude.ai/code/artifact/367869b9-b311-4538-ad6a-a9e671030d8b` (private ของผู้ใช้เอง เปิดดูได้ทุกเมื่อ ไม่ต้องมี context เซสชันนี้) — มี 2 ส่วน: ส่วนที่ 1 ออกแบบหน้าตาสำหรับ Line chart (approved แล้ว "ok"), ส่วนที่ 2 ออกแบบว่าจะรวมกับ criteria-range (Scatter) และ lasso-cluster (Pair Plot) ที่มีอยู่แล้วยังไง (ผู้ใช้ยังไม่ได้ตอบรับส่วนที่ 2 ชัดเจนว่า "ok" หรือยัง ให้เช็คกับผู้ใช้อีกทีก่อนเริ่ม implement ส่วน Scatter/Pair Plot)
  - **ดีไซน์ที่ตกลงกันไว้ (Line chart)**:
    - เพิ่มแท็บใหม่ **"Highlights"** ในแท็บบาร์ฝั่งขวาเดียวกับ Selected Sensor / Data Insight / Filter (`activeDataTab` ใน `Dashboard.tsx` ตอนนี้เป็น union `'insight' | 'selected' | 'filter'` — ต้องขยายเป็น `| 'highlights'`)
    - ในแท็บ: ฟอร์มเพิ่ม highlight ใหม่ (Start/End datetime + label ข้อความสั้นๆ + เลือกสีจาก swatch แถวเดียว) ตามด้วย list ของ highlight ที่เพิ่มไว้แล้ว (checkbox เปิด/ปิด + สี dot + label + ช่วงวันที่ + ปุ่มลบ) — mirror pattern เดียวกับ criteria-range chip ที่เพิ่งทำใน `ScatterChart.tsx` (`+ Add` form + chip list + toggle ไม่ลบ)
    - สี default 5 สี ใช้ชุดเดียวกับ `RANGE_PALETTE`/`CLUSTER_PALETTE` ที่มีอยู่แล้ว (amber/emerald/rose/sky/violet) เพื่อให้เป็นภาษาเดียวกันทั้งแอป ไม่ต้องคิดพาเลตใหม่
    - บนกราฟ (LineChart.tsx, ECharts): render เป็น **`markArea`** (คนละ mechanism กับ `markLine`/`ChartMarkLine` ที่มีอยู่แล้วสำหรับเส้นแนวนอน alarm setpoint — ยังไม่เคยใช้ `markArea` ในโปรเจกต์นี้เลย ต้องเพิ่ม prop ใหม่ เช่น `timeHighlights` ใน `ChartProps`/`ChartTypes.ts` แยกจาก `markLines`)
    - highlight list เป็น **global ข้าม chart type** ไม่ผูกกับ Line/Scatter/Pair Plot อันใดอันหนึ่ง (เพราะเป็นแค่ช่วง timestamp ไม่ใช่ค่า sensor) — persist ต้องอยู่ระดับ `Dashboard.tsx`/`WorkspaceState` ไม่ใช่ local ใน chart component ใดๆ (เรียนรู้จากบั๊ก scatterCriteria ที่เพิ่งแก้ไปเมื่อกี้ — ต้องคิดเรื่อง persistence ตั้งแต่แรกรอบนี้ ไม่ใช่มาแก้ทีหลัง)
  - **ดีไซน์ที่เสนอไว้ (Scatter/Pair Plot) — รอ confirm**: แทนที่จะแย่ง `colorBy` กับ criteria-range (Scatter ใช้ `valueA` อยู่แล้ว) หรือ lasso-cluster (Pair Plot ใช้ `valueA` ต่อ cell อยู่แล้ว) ให้ time-highlight ใช้ **`opacityBy: valueB`** แทน (คนละ channel กับสี ไม่ชนกัน) — จุดที่ timestamp อยู่ในช่วงที่ติ๊กไว้ = opacity เต็ม, จุดนอกช่วง = จางลง (เช่น 0.1) โดยสี (จาก criteria-range หรือ lasso-cluster) ไม่เปลี่ยนเลย — ไม่ติ๊ก highlight เลย = ไม่มีอะไรเปลี่ยนจากที่เป็นอยู่ตอนนี้ 100% (เหมือน pattern "zero footprint until used" ที่ใช้มาตลอดทั้ง session)

  > **หมายเหตุ (เพิ่มทีหลัง 2026-08-16):** ดีไซน์ Scatter/Pair Plot ด้านบนไม่ได้ถูก implement ตามที่ร่างไว้ในที่สุด — ทิศทางที่ผู้ใช้ตัดสินใจจริงคือ **ตัด scope ทั้ง criteria-range (Scatter) และ Pair Plot ออกจาก Highlights ไปเลย**: Line ใช้ทั้ง highlight (band/line-colour) ได้, Scatter ใช้ได้แค่ highlight (แสดงเป็นวงแหวนคงที่ ไม่มีทางเลือก display mode), Pair Plot ไม่ใช้อะไรจาก Highlights เลย (คงไว้แค่ lasso-cluster ของตัวเอง) — `ScatterCriteriaRange`/`ScatterCriteria` ถูกลบออกจาก `types.ts` ทั้งหมดในรอบหลังจากนี้ ดูรายละเอียดที่ entry งานถัดๆ ไปหรือ git log ช่วง 2026-08-15/16
  - **ยังไม่ได้ทำอะไรในโค้ดเลยสักไฟล์** สำหรับ entry นี้ — เป็น design-only entry เพื่อให้ session ถัดไป (เครื่องอื่น) เข้าใจ context ต่อได้ทันทีโดยไม่ต้องมี chat history

- **🆕 2026-08-16 — 🗑️⚡ ลบแท็บ "Data Insight" (ตาราง raw/aggregated) ออกจาก Dashboard — ผู้ใช้ไม่ได้ใช้งานจริง, แก้ performance-review finding ไปในตัว**: ระหว่างรีวิว performance รอบสองของ session นี้ เจอว่า `useTablePage` ยิง backend query ทุกครั้งที่ filter/sampling เปลี่ยน **แม้แท็บ Data Insight จะไม่ได้เปิดอยู่เลยก็ตาม** (ไม่มี gate ใดๆ ผูกกับ `activeDataTab === 'insight'`) — ผู้ใช้ตอบว่า "ไม่ได้ใช้อะ เอาออกเลยก็ได้นะ...แค่ผมคนเดียวไม่ได้ใช้งานจริงๆ" พร้อมขอให้บันทึกไว้ที่นี่เผื่อเอากลับมาใช้ในอนาคต:
  - **ตัดออกจาก `Dashboard.tsx` เท่านั้น — ไม่ลบไฟล์ต้นทาง**: `DataTable.tsx` และ `hooks/useTablePage.ts` ยังอยู่ครบ ไม่ได้แตะเลย (ยืนยันแล้วว่าทั้งสองไฟล์ไม่มีจุดอื่นใน repo import ใช้เลยนอกจาก `Dashboard.tsx` เดิม) — เอาออกแค่จุดที่ "เสียบสาย" เข้ากับ Dashboard:
    - `import DataTable`, `import { useTablePage }` — ลบ
    - `useTablePage(...)` call + `tablePageIndex`/`setTablePageIndex`/`tableQueryKey` state ทั้งก้อน — ลบ (คือตัวที่เป็นต้นเหตุของ query สิ้นเปลืองที่เจอ)
    - `tableRows` memo (project ข้อมูล table page → `CsvRecord[]` สำหรับ `DataTable`) — ลบ (ใช้แค่จุดเดียว)
    - `activeDataTab` union type: `'insight' | 'selected' | 'filter' | 'highlights'` → เหลือ `'selected' | 'filter' | 'highlights'` — ปุ่มแท็บ "Data Insight" หายไปเองเพราะ render จาก array ของ union type นี้
    - `<DataTable .../>` branch ใน content ternary — ลบ
    - `const loading = viewLoading || tableLoading` → `const loading = viewLoading` (ไม่มี table query ให้รวมแล้ว)
  - **ไม่เสีย feature อื่นไปด้วย (เช็คก่อนลบ ไม่ใช่ลบมั่ว)**: `tableTotalRows` (ใช้ทั้งกับ badge "X Rows" ที่โชว์ตลอดไม่ว่าอยู่แท็บไหน และ `exportDisabled` ของปุ่ม Export CSV) เดิมอ่านจาก `tablePage?.total_rows` — เปลี่ยนมาอ่านจาก **`view?.total_rows`** แทน (`ChartViewData.total_rows`, `types/commands.ts:155` — doc comment ในไฟล์บอกตรงๆ ว่า "the badge count" คือถูกออกแบบมาให้ใช้จุดนี้อยู่แล้วตั้งแต่แรก) — `view` มาจาก `useChartData` ที่ยิงอยู่แล้วเสมอสำหรับ Line/Scatter chart เอง ไม่ต้องเพิ่ม query ใหม่เลย ผลคือ badge/export ทำงานเหมือนเดิมทุกอย่างโดยไม่ต้องพึ่ง `useTablePage` อีกต่อไป
  - **ปุ่ม "Export dataset" (CSV) เดิมซ่อนอยู่หลังเงื่อนไข `activeDataTab === 'insight'`** — ถ้าลบแท็บเฉยๆ จะเสียฟีเจอร์ export ไปด้วยทั้งที่เป็นคนละเรื่องกัน (export ไม่เคยพึ่งพา `DataTable`/`useTablePage` จริง ยิง `export_chart_csv` ตรงไปที่ Rust) — ย้ายปุ่มออกมาเป็น tab-independent (โชว์ตลอดเหมือนกับ badge "X Rows" ที่อยู่ข้างๆ) แทนที่จะลบทิ้งไปเงียบๆ
  - **วิธีเอากลับมาใช้ในอนาคต**: ไล่ revert 5 จุดข้างบนย้อนกลับ (`git log`/`git diff` หา commit ของรอบนี้เพื่อดู diff เต็ม) — ไฟล์ `DataTable.tsx`/`useTablePage.ts`/เทสต์ของทั้งคู่ (`DataTable.test.tsx`, `useTablePage.test.ts`) ยังอยู่ครบไม่ต้องเขียนใหม่ แค่เสียบสายกลับเข้า `Dashboard.tsx` ตามเดิม
  - **แก้เทสต์**: `Dashboard.test.tsx` — ลบ mock `DataTable`/`useTablePage` (`dataTableProps`, `mockUseTablePage`) ที่ไม่ได้ใช้แล้ว, ลบ `fireEvent.click(screen.getByText('Data Insight'))` ออกจาก 2 เทสต์ในกลุ่ม "CSV export" (ปุ่มโชว์ตลอดแล้ว ไม่ต้องคลิกแท็บก่อน), แก้ชื่อเทสต์ 1 จุดที่พูดถึง "Data Insight" ในคำอธิบาย — จำนวนเทสต์สุทธิไม่เปลี่ยน (716/716 ทั้งก่อนและหลัง เพราะไม่มี `it()` ไหนถูกลบทิ้งจริง แค่แก้เนื้อใน)
  - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **716/716 ผ่าน** ไม่มี regression — **ยังไม่ได้ commit**

  > **อัปเดตทันที (ยังวันเดียวกัน, ต่อจาก entry ด้านบน):** ผู้ใช้ดูปุ่ม "Export dataset" ที่เพิ่งย้ายมา visible ตลอด แล้วบอกว่า **"ไม่ได้ใช้เหมือนกัน" — เอาออกไปเลยด้วย** (ไม่ได้ขอย้ายกลับไปซ่อน แต่ตัดทิ้งไปเลยทั้งปุ่ม):
  > - ลบปุ่ม + `onClick` handler (เรียก `export_chart_csv` ผ่าน `save()` dialog) ออกจาก `Dashboard.tsx` ทั้งก้อน, ลบ `exportDisabled` const ที่ใช้แค่จุดเดียว (badge "X Rows" ไม่ได้พึ่งมัน), ลบ import ที่ไม่ใช้แล้ว: `invoke` (`@tauri-apps/api/core` — ไม่มีจุดอื่นเรียกในไฟล์นี้เลย), `save` (`@tauri-apps/plugin-dialog` — เหลือแค่ `message` ที่ยังใช้อยู่), `Download` icon (`lucide-react`)
  > - **(ตอนแรก) เหมือน Data Insight — ไม่แตะ Rust**: ตอนแรกเก็บ command `export_chart_csv` (`lib.rs`) ไว้เฉยๆ ไม่ลบ/unregister ออกจาก `invoke_handler` — **แต่ผู้ใช้ตามมาแก้ทันทีในข้อความถัดไปว่าให้ลบฝั่ง Rust ด้วย** เพราะเหตุผลคนละแบบจาก Data Insight: "จะไม่มีการ export raw data แน่นอน...ก่อนใช้โปรแกรมคุณต้องมี raw data อยู่แล้ว" — ต่างจาก Data Insight ที่ผู้ใช้บอกแค่ "ไม่ได้ใช้" (เผื่ออนาคต), อันนี้คือ "ไม่มีวันจะใช้" (ตัดสินใจถาวรกว่า) เลยลบให้สุดทั้ง 2 ฝั่งแทนที่จะเก็บไว้เผื่อ — ดู entry ถัดไปสำหรับรายละเอียดการลบฝั่ง Rust แบบเต็ม
  > - **แก้เทสต์**: `Dashboard.test.tsx` — ลบ `describe('CSV export', ...)` ทั้ง block (2 เทสต์), ลบ mock `save`/`mockSave` (`@tauri-apps/plugin-dialog`) ที่ไม่ได้ใช้แล้ว (เหลือแค่ mock `message` ไว้)
  > - Verify: `npx tsc --noEmit` สะอาด + เทสต์ทั้งโปรเจกต์ **714/714 ผ่าน** (-2 จาก 716, ลบเทสต์ CSV export ทิ้งจริง ไม่มี regression อื่น) — **ยังไม่ได้ commit**

- **🆕 2026-08-16 — 🗑️ ลบ `export_chart_csv` ออกจากฝั่ง Rust ทั้งหมดด้วย (ต่อจาก entry ก่อนหน้า) — ต่างจาก Data Insight ตรงที่ลบถาวร ไม่เก็บไว้เผื่อกลับมาใช้**: ผู้ใช้ยืนยันชัดว่า raw-data export ไม่มีวันจำเป็น (มี raw data อยู่แล้วตั้งแต่ก่อน import เข้าโปรแกรม) เลยลบให้ครบทุกชั้น ไม่ใช่แค่ถอดสาย:
  - **`src-tauri/src/lib.rs`**: ลบ `fn export_chart_csv` (คำสั่ง Tauri command ทั้งฟังก์ชัน) + เอาออกจากรายการ `invoke_handler![...]` + แก้ doc comment ของ `validate_user_write_path` ที่เคยพูดถึง `export_chart_csv` เป็นหนึ่งใน caller (เหลือแค่ `write_user_file`)
  - **`src-tauri/src/chart_query.rs`**: ลบ `pub fn export_csv` (ตัว logic เขียน CSV จริง, เรียกจาก `export_chart_csv` เท่านั้น) + เทสต์ของมันอีก 2 ตัว (`export_writes_csv_with_excel_safety`, `export_aggregated_rows`) — **ไล่ลบ dead code ต่อเนื่อง** ที่เหลือจากการลบนี้: `fn csv_field` (helper quote-CSV-field ใช้แค่ใน `export_csv`), `use crate::{excel_safe, ...}` (ไม่มีจุดเรียกอีกแล้วในไฟล์นี้ — `excel_safe` ตัวจริงใน `lib.rs` ยังอยู่ เพราะมี caller อื่น), `use std::io::Write` (เฉพาะ `export_csv` ใช้ trait bound นี้) — แก้ doc comment บนสุดของไฟล์ (module doc) ที่เคยพูดถึง "CSV written straight to disk" ด้วย
  - **`src/types/commands.ts`**: ลบ entry `export_chart_csv` ออกจาก `TauriCommands` (contract-first ตาม `CLAUDE.md` — ต้องลบคู่กับฝั่ง Rust เสมอ) + แก้ doc comment ของ `DashboardDataFilter` ที่เคยลิสต์ `export_chart_csv` เป็นหนึ่งใน consumer
  - **ไม่ได้ลบ**: `validate_user_write_path` (ยังมี `write_user_file` เรียกอยู่), `excel_safe` ใน `lib.rs` (มี caller อื่นที่ไม่เกี่ยวกับ export นี้)
  - Verify: `cargo check --lib` สะอาด (ไม่มี warning unused-import/unused-fn เลยแม้แต่ตัวเดียว) + `cargo test --lib` **137 ผ่าน, 1 ignored, 0 พัง** + ฝั่ง frontend `npx tsc --noEmit` สะอาด + `npx vitest run` **714/714 ผ่าน** (จำนวนเทสต์ frontend ไม่เปลี่ยนจาก entry ก่อนหน้าเพราะเทสต์ frontend ของฟีเจอร์นี้ถูกลบไปแล้วในรอบนั้น) — **ยังไม่ได้ commit**

- **🆕 2026-08-17 — 📄 เอกสารเท่านั้น (ไม่แตะโค้ด app) — Redesign multi-agent orchestration pipeline ให้ "ใช้งานได้จริง"**: ผู้ใช้ขอให้อ่าน `multi_agent_orchestration_design.md` + `.claude/agents/*.md` แล้ววิเคราะห์ว่าทำไม pipeline นี้ไม่เคยถูกใช้เลย (สังเกตเอง: "ตอนนี้ไม่ได้ใช้งานเลย") วินิจฉัยแล้วแก้ตามที่เสนอ:
  - **สิ่งที่พบ**: pipeline นี้เคยถูกใช้จริง 2 ครั้งตอนต้นโปรเจกต์เท่านั้น (Feature 1: Data Upload Page redesign, Feature 3: Predictive Model Rust port — ดู `docs/task.md` ที่ tag agent ไว้ครบทุก phase) แล้วหยุดใช้ไปเลยตั้งแต่ 2026-05-07 — งานทั้งหมดที่ทำกันมา 3+ เดือนหลัง (คือทั้ง engagement นี้) ทำผ่านเซสชันหลักตรงๆ ล้วนๆ ไม่เคย spawn pm-agent อีกเลย
  - **root cause ที่วินิจฉัย**: (1) design doc เดิมเขียนไว้ตอน ~2026-03 โดยอิงกลไก `claude --profile` + `scripts/orchestrate.sh` (bash spawn `claude` process แยก) ซึ่ง**ไม่มีจริงใน harness ที่ใช้งานอยู่** (ไม่มี `--profile` flag ไม่มี CLI ให้ spawn ผ่าน bash แบบนั้น) — `.claude/agents/*.md` ที่มีอยู่จริงถูกปรับให้ใช้ Agent tool ถูกกลไกแล้วก็จริง แต่ `scripts/orchestrate.sh` ไม่เคยถูกสร้างขึ้นจริงเลย (2) รูปแบบงานจริงของโปรเจกต์นี้ (bug report สั้นๆ + screenshot, ขอ confirm ก่อนแก้เสมอ, แก้ข้ามหลายโซนในไฟล์เดียวกันเป็นเรื่องปกติ) ไม่เข้ากับ waterfall-style PM→worker→QA ที่ pipeline นี้ออกแบบไว้ (3) เจอหลักฐานจุดอ่อนจริงจากตอนที่เคยใช้: ท้าย `docs/task.md` มี note ค้างว่า `fe-logic-agent`/`rust-agent` เปลี่ยน signature ของ `apply_mapping` (Rust) ระหว่างทำ Feature 1 จนทำให้ `src-tauri/tests/csv_tests.rs` คอมไพล์ไม่ผ่าน — ถูก flag ว่า "out of scope for Feature 3" แล้วปล่อยพังค้างไว้ เพราะ zone ownership เดิมตัด `src-tauri/tests/` ออกจาก rust-agent ให้ qa-agent เป็นเจ้าของแต่ผู้เดียว แต่ qa-agent ก็ไม่เคยถูกเรียกใช้อีกเลยหลัง Feature 1/3 (ไฟล์ `csv_tests.rs` หายไปจาก repo แล้วตอนนี้ แต่ไม่ชัดว่าใครแก้ถูกทางหรือลบทิ้งไปดื้อๆ)
  - **แก้ 3 จุด (ตามที่ user confirm ว่า "แก้ตามนี้ได้เลย")**:
    1. เขียน `multi_agent_orchestration_design.md` ใหม่ทั้งไฟล์ — ตัด Option B/C (`--profile`/bash orchestration) ที่ใช้ไม่ได้จริงทิ้ง เหลือกลไกจริงเดียว (เซสชันหลัก spawn `pm-agent` ผ่าน Agent tool → `pm-agent` มีสิทธิ์ใช้ Agent tool เองด้วย ซ้อน spawn worker ต่อได้) เพิ่ม section "เมื่อไหร่ควรใช้ pipeline นี้ — และเมื่อไหร่ไม่ควร" ไว้บนสุด (เกณฑ์: feature ก้อนใหญ่ contract Rust↔TS ชัด แยก phase เป็นธรรมชาติ เหมือน Feature 1/3 — ถ้าลังเลว่าจะ spawn ดีไหม แปลว่าไม่ควร) พร้อม section "Lesson from Feature 1/3" บันทึกเหตุการณ์ `apply_mapping`/`csv_tests.rs` ไว้ตรงๆ
    2. ผ่อน zone ownership ของ 3 worker agent (`fe-ui-agent`, `fe-logic-agent`, `rust-agent`) — เพิ่มสิทธิ์เขียนเทสต์ของโค้ดที่ตัวเองแก้ได้เลยในรอบเดียวกัน (`src/__tests__/`/inline `#[cfg(test)]`/`src-tauri/tests/` เฉพาะไฟล์ที่ตัวเองกระทบ) แทนที่จะยกให้ qa-agent ทั้งหมด — ตรงตาม pattern "write tests with every change" ที่เป็นนิสัยจริงของโปรเจกต์นี้อยู่แล้ว `rust-agent` โดยเฉพาะเพิ่มกฎบังคับ: ถ้าเปลี่ยน signature ต้อง grep `src-tauri/tests/` หา caller แล้วแก้เองก่อนปิดงาน (fix ตรงจุดที่เคยพังจริง)
    3. `qa-agent` เปลี่ยนบทบาทจาก "เขียนเทสต์ทุกไฟล์คนเดียว" เป็น **cross-cutting integration sweep รอบสุดท้าย** (เทสต์ที่ต้องข้าม zone เท่านั้น + รัน full suite ซ้ำ + เช็คว่า HANDOFF ของ worker อื่นโกหกไหม) ส่วน `pm-agent` เพิ่ม "Final Verification" บังคับก่อนรายงานผู้ใช้ (รัน `tsc --noEmit` + `cargo check --lib` + full test suite เอง ไม่ใช่แค่เชื่อ HANDOFF ของ worker แต่ละตัว) และรับ requirement เป็นข้อความสั้นๆ ในพรอมต์ได้เลยโดยไม่ต้องรอให้มีคนเขียน `docs/requirements.md` ไว้ก่อน (จุดคอขวดเดิมที่ทำให้ไม่มีใครเริ่มใช้ pipeline นี้เลย)
    4. sync `CLAUDE.md`'s "Agent Roles & File Ownership" table ให้ตรงกับของจริง เพิ่มเงื่อนไข "ใช้เฉพาะงานก้อนใหญ่ๆ" อ้างอิงไปยัง design doc ไว้บนสุดของ section
  - **ยืนยันแล้วว่า Agent tool มองเห็น agent ทั้ง 5 ตัวจริงในเซสชันปัจจุบัน** (project path นิ่งแล้ว ไม่ติดปัญหา path-change-mid-session ที่เคยบันทึกไว้ใน §8.9 เดิม — ควรถือว่า §8.9 ข้อนั้นล้าสมัยแล้ว) แต่**ยังไม่เคยลองรันจริงตาม design ที่แก้ใหม่นี้สักครั้ง** — รอ feature ก้อนใหญ่ก้อนถัดไปที่เข้าเกณฑ์เพื่อพิสูจน์ว่าใช้งานได้จริงตามที่ออกแบบ
  - ไฟล์ที่แก้: `multi_agent_orchestration_design.md` (เขียนใหม่ทั้งไฟล์), `.claude/agents/{pm,fe-ui,fe-logic,rust,qa}-agent.md` (ทั้ง 5), `CLAUDE.md` (section "Agent Roles & File Ownership") — ไม่แตะโค้ด app เลยสักไฟล์ ไม่ต้อง `tsc`/`vitest`/`cargo test` เพราะเป็นเอกสารล้วน

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — 📄 เอกสารเท่านั้น — ออกแบบ "Notion task sync" process ใหม่ + เขียนเป็น CLAUDE.md rule**: ผู้ใช้อธิบาย process ที่อยากได้เป็น 3 ข้อ (สรุป: งานเสร็จแล้วต้อง sync เข้า Notion, งาน design บางงานต้อง push ขึ้น Figma จริงให้แก้เองได้, ถ้าสั่งงานที่ไม่มีใน Notion list ให้เพิ่มเข้าไป ส่วน status เปลี่ยนเมื่อผู้ใช้บอกเท่านั้น) แล้วถามว่าควรเป็น "instruction หรือ skill":
  - **ตอบคำถามหลัก**: เลือก **CLAUDE.md instruction + memory** ไม่ใช่ skill เพราะ (1) พฤติกรรมที่ขอคือ "ทำทุกครั้งที่งานเสร็จ ไม่ต้องสั่ง" ไม่ใช่ workflow ที่เรียกเฉพาะจุดแบบ skill (2) โปรเจกต์นี้มี precedent เดิม 2 อย่างที่ implement แบบเดียวกันอยู่แล้ว (auto-update `PROJECT_HANDOVER.md`, changelog ทุก build) ทั้งคู่เป็น CLAUDE.md rule + memory ไม่ใช่ skill (3) ไม่มี `.claude/skills/` อยู่ในโปรเจกต์หรือเครื่องเลยตอนนี้ (เช็คแล้ว) — ทำ skill ต้องสร้าง infra ใหม่โดยไม่ได้ประโยชน์เพิ่ม
  - **ตรวจ schema จริงของ Notion database "wizard application plan improvement"** (`collection://39e959a6-c718-8039-b30b-000bbea5ca96`) ก่อนออกแบบ: `Status` มี 5 ค่าจริง ไม่ใช่แค่ 2 — `Not started` → `In progress` / `wait for advisor` / `waiting re design` → `Done` — เจอว่า **`waiting re design` มีอยู่แล้วพอดีกับ use case "ส่งดีไซน์ขึ้น Figma ให้ผู้ใช้แก้เอง"** ไม่ต้องคิดค่าใหม่ — ตรวจ Figma MCP ที่ต่อไว้ด้วยว่า push ดีไซน์จริงได้ (`create_new_file`/`use_figma`/`upload_assets`) ไม่ใช่แค่อ่านอย่างเดียว
  - **เจอ tension จริงในสิ่งที่ผู้ใช้พิมพ์มา**: ข้อ 1 บอก "งานเสร็จแล้วเปลี่ยนเป็น done ได้เลย" ข้อ 3 บอก "เปลี่ยนเมื่อผู้ใช้สั่งเท่านั้น หลังเช็คแอปจริงแล้ว" — ถามผู้ใช้ตรงๆ ผ่าน `AskUserQuestion` แทนที่จะเดา เพราะพลาดแล้วจะแปลว่า Notion item ถูก mark ผิดไปแล้วหลายรายการ **ผู้ใช้ยืนยัน: รอ user confirm ก่อนเสมอ ห้าม auto-mark Done จาก code+tests ผ่านอย่างเดียว**
  - **เขียนกฎลง `CLAUDE.md` section ใหม่ "Notion task tracking"** (ต่อจาก "Testing discipline" ก่อน "Security constraints"): sync ทุกงานที่เสร็จเข้า Notion (update ถ้ามีอยู่แล้ว/สร้างใหม่ถ้าไม่มี), ห้าม mark Done เองเด็ดขาด, sub-rule design→Figma (push ไฟล์ Figma จริง, ตั้ง status `waiting re design`, อ่านกลับตอนผู้ใช้แก้เสร็จก่อน implement) — ระบุชัดว่าคนละเรื่องกับ `docs/figma/*.svg` เดิม (ที่ไม่ sync กับโค้ดตามกฎเก่า)
  - **บันทึก memory ใหม่** `feedback_notion_task_sync.md` (type: feedback) documenting ทั้ง 3 ข้อ, การแก้ tension, และผลโหวตของผู้ใช้ — link ไปยัง `feedback_recheck_existing_patterns_on_new_features` (เหตุผลที่ไม่เชื่อ automated test อย่างเดียว) และ `feedback_no_figma_svg_sync` (แยกให้ชัดว่าคนละ scenario) เพิ่ม entry ใน `MEMORY.md` index
  - ไฟล์ที่แก้: `CLAUDE.md` (section ใหม่ "Notion task tracking"), `docs/PROJECT_HANDOVER.md` (banner + entry นี้), memory files (`feedback_notion_task_sync.md` ใหม่ + `MEMORY.md` index) — ไม่แตะโค้ด app เลย ไม่ต้อง verify ด้วย tsc/vitest/cargo test

  > **อัปเดตทันที (ยังวันเดียวกัน, ต่อจาก entry ด้านบน):** ผู้ใช้ยืนยัน 3 ข้อพร้อมเพิ่ม 2 จุดสำคัญ: (1) **"push code to github" ต้องเป็นส่วนหนึ่งของ checklist หลังงานเสร็จด้วย** ไม่ใช่แค่ sync Notion เฉยๆ — เดิมการ push เคยเป็น action ที่ต้องรอผู้ใช้สั่งทุกครั้ง ("push to git เลย") ตอนนี้ยกระดับเป็น **unasked ทุกครั้งที่งานเสร็จ** (2) **ข้อ 3 (Figma) ใช้เฉพาะงาน design ใหม่ทั้งหมดเท่านั้น** — ถ้าเป็นแค่แก้บั๊กหรือปรับเล็กน้อยของดีไซน์เดิม ใช้ Artifact พอ ไม่ต้อง push ขึ้น Figma (กัน over-apply กฎเดิมที่เขียนไว้กว้างเกินไป)
  > - เปลี่ยนชื่อ section ใน `CLAUDE.md` จาก "Notion task tracking" เป็น **"Post-task checklist — Notion sync + git push"** เพิ่ม step 5 บังคับ commit+push ไปที่ `personal` remote เท่านั้น (อ้างอิงกฎเดิมเรื่อง remote) ในรอบเดียวกับ Notion sync — แก้ step 4 (Figma) ให้ระบุชัดว่า "เฉพาะ design ใหม่ทั้งหมด" ส่วนงานปรับเล็กน้อยใช้ Artifact
  > - อัปเดต memory `feedback_notion_task_sync.md` ให้รวม git-push เข้าไปในคำอธิบาย + เพิ่มคำอธิบาย Figma-vs-Artifact — อัปเดต `feedback_push_target_personal_only.md` เดิมด้วย (เปลี่ยนจาก "push เมื่อถูกสั่งเท่านั้น" เป็น "push อัตโนมัติทุกงานที่เสร็จ" — remote restriction เดิมไม่เปลี่ยน) — อัปเดต `MEMORY.md` index ให้ตรง
  > - ยังเป็นเอกสารล้วน ไม่แตะโค้ด app — **จากรอบนี้เป็นต้นไป การ commit/push จะเกิดขึ้นอัตโนมัติทุกครั้งที่งานเสร็จโดยไม่ต้องรอให้ผู้ใช้สั่ง "push" อีกต่อไป** (เว้นแต่ผู้ใช้บอกให้หยุด/เปลี่ยนพฤติกรรมนี้ภายหลัง)

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — 📄 เอกสารเท่านั้น — ออกแบบ version-bump scheme สำหรับ build .exe**: ผู้ใช้ถามต่อ "เวลา build .exe จะเป็น version อะไร แล้ว note ต้องบอกอะไรบ้าง เป็น skill หรือ instruction ดี":
  - **ตอบคำถามหลัก**: instruction เหมือนเดิม — แต่รอบนี้พิเศษกว่า 2 ครั้งก่อน (multi-agent, Notion sync) ตรงที่**ไม่ใช่กลไกใหม่เลย** section "Release checklist" มีอยู่แล้วใน `CLAUDE.md` (เขียนไว้ตั้งแต่ก่อนหน้านี้ตอน `feedback_changelog_on_every_build`) แค่ยังขาดกฎ "bump เลขไหนกันแน่" — งานนี้เลยเป็นการต่อยอด section เดิม ไม่ใช่สร้างใหม่
  - **เจอจุดที่ path เพี้ยนไปแล้วระหว่างตรวจ**: Release checklist ยังอ้าง `CHANGELOG.md` ที่ root แต่ไฟล์ย้ายไป `docs/CHANGELOG.md` ตั้งแต่ entry ก่อนหน้า (ตอน redesign multi-agent) — แก้ path ให้ตรงทั้งใน `CLAUDE.md` และ relative link ใน `docs/CHANGELOG.md` เอง (เดิมลิงก์ `[CLAUDE.md](CLAUDE.md)` จากในโฟลเดอร์ `docs/` ชี้ผิดที่ ต้องเป็น `../CLAUDE.md`)
  - **ออกแบบ semver scheme** (มีแค่ 0.2.1 ที่เคย build จริงเป็นตัวอย่างเดียว ไม่มี pattern เดิมให้ยึด): ตัดสิน bump จาก category ที่รุนแรงที่สุดที่สะสมมาตั้งแต่ tag ล่าสุด (ใช้ category set เดียวกับที่ changelog ใช้อยู่แล้ว: Breaking/Features/Bug fixes/Performance/Tests/Docs/Removed) — Breaking change หรือ Feature ใหม่ → bump **MINOR** (reset patch), มีแค่ Bug fix/Perf/Tests/Docs/Removed แบบไม่ breaking → bump **PATCH** เท่านั้น, **ไม่ bump MAJOR อัตโนมัติแม้มี breaking change** — MAJOR อยู่ที่ 0 จนกว่าผู้ใช้จะประกาศเองว่าถึงจุด "1.0 พร้อมใช้จริงจัง" (ไม่ใช่สิ่งที่เดาจาก changeset ได้) ทุก build ที่แจกจริงต้อง bump อย่างน้อย PATCH เสมอ กันเลขซ้ำกันข้าม build ที่เคยแจกไปคนละรอบ
  - เขียนกฎนี้ลง `CLAUDE.md` § Release checklist โดยตรง (เพิ่มตารางตัดสิน bump เป็น step 1 ใหม่ ผูกเข้ากับ step 2 เดิม (เขียน note) ให้ใช้ commit range เดียวกัน) — ผู้ใช้ตอบ "ok เขียนทิ้งไว้ในนั้นเลย" ไม่มีจุดไหนต้องถามเพิ่ม (semver เป็น convention มาตรฐาน ไม่มี tradeoff ให้เลือก)
  - **อัปเดต memory** `feedback_changelog_on_every_build.md` ให้รวม version-bump table + path ใหม่เข้าไปในคำอธิบาย, อัปเดต `MEMORY.md` index ให้ตรง
  - ไฟล์ที่แก้: `CLAUDE.md` (§ Release checklist), `docs/CHANGELOG.md` (แก้ relative link), `docs/PROJECT_HANDOVER.md` (banner + entry นี้), memory (`feedback_changelog_on_every_build.md` + `MEMORY.md` index) — ไม่แตะโค้ด app เลย ไม่ต้อง verify ด้วย tsc/vitest/cargo test — commit+push ตาม "Post-task checklist" ที่เพิ่งเขียนไว้เอง (ทดสอบ rule ใหม่ต่อเนื่องเป็นรอบที่ 2)

  > **อัปเดตทันที (ยังวันเดียวกัน, ต่อจาก entry ด้านบน):** ผู้ใช้ชี้จุดสำคัญที่กฎเดิมพลาดไป — **"มันนานมากๆกว่าผมจะ build เป็น app ซักที เพราะฉะนั้นตอนเขียน note ของ version อาจจะต้องไปดูใน project handover ว่าแต่ละ version ทำอะไรไว้บ้าง"**:
  > - **root cause ของปัญหาที่ผู้ใช้ชี้**: กฎเดิม (step 2 ของ Release checklist) บอกให้ pull จาก `git log <last-shipped-tag>..HEAD` เป็นหลัก แต่ commit ของโปรเจกต์นี้มักรวมงานหลายวัน/หลายจุดไม่เกี่ยวกันไว้ commit เดียว (เห็นได้จาก commit ที่เพิ่ง push ไปเองในเซสชันนี้ เช่น "Pair plot redesign follow-ups, PDF export removal, and dashboard fixes" ที่จริงๆ รวมงานทั้งวันไว้) เพราะ commit ไม่ได้เกิดหลังทุกงานเสร็จ (จนกฎ push-ทุกงานเสร็จเพิ่งถูกตั้งเมื่อกี้นี้เอง) ต่างจาก `docs/PROJECT_HANDOVER.md` ที่มี entry แยกทุกงานพร้อม context เต็ม
  > - **แก้ Release checklist**: เปลี่ยน primary source ของทั้ง step 1 (ตัดสิน version bump) และ step 2 (เขียน note) จาก `git log` เป็น **อ่าน `docs/PROJECT_HANDOVER.md`'s dated `🆕 YYYY-MM-DD` entries ทุกอันหลังวันที่ของ version ที่เคย shipped ล่าสุด** — ใช้ `git log` แค่เป็น completeness cross-check (เช็คว่ามีอะไรหลุดไปโดยไม่มี entry คู่กันไหม ถ้าเจอให้ถามผู้ใช้แทนที่จะเดา) เพิ่มกฎด้วยว่า**งานที่ revert/superseded ไปแล้วในวันเดียวกัน (มีบันทึกไว้ใน handover ชัดว่า revert) ไม่ต้องมี line ใน changelog** — เอาแค่ net effect ที่ shipped จริงเท่านั้น
  > - อัปเดต memory `feedback_changelog_on_every_build.md` ให้ตรงกับกฎใหม่
  > - ไฟล์ที่แก้: `CLAUDE.md` (§ Release checklist step 1-2), `docs/PROJECT_HANDOVER.md` (banner + entry นี้), memory (`feedback_changelog_on_every_build.md`) — เอกสารล้วน ไม่แตะโค้ด app — commit+push ตาม Post-task checklist ต่อเนื่องเป็นรอบที่ 3

- **🆕 2026-08-17 — 🎨 DESIGN เท่านั้น (ยังไม่ implement) — "Tag Point" ฟีเจอร์เทียบค่า data point หลายจุดพร้อมกัน**: งานใหม่ (ไม่ได้มาจาก Notion เดิม) ผู้ใช้ขอเพิ่ม task ใน Notion + ออกแบบ UI พร้อมกัน — requirement: "tag point data function เพื่อเป็นการแสดงผลค่าของ data point 2 จุด หรือมากกว่าพร้อมๆกัน"
  - **เพิ่มเข้า Notion**: task "tag point data function" (`https://app.notion.com/p/3bf959a6c7188182b0dde98d717cfef1`) status `In progress` ระหว่างออกแบบ (ตาม Post-task checklist ใหม่ — ไม่มีอยู่ใน Notion เดิมเลย ต้องสร้างใหม่)
  - **Mockup**: `https://claude.ai/code/artifact/8241f01f-271b-48bd-94c6-d9f5cdcb4d42` (interactive prototype คลิกได้จริงสำหรับ Line/Scatter) — republish 2 รอบในเซสชันนี้ (รอบแรกมี compare-table ด้านล่าง, รอบสองตัดตารางออกตามที่ผู้ใช้ขอ + เพิ่ม Scatter/Pair Plot ตามที่ขอเพิ่ม)
  - **ดีไซน์ที่ตกลงกันไว้**:
    - **Control เป็น toolbox icon บนตัวกราฟ ไม่ใช่ tab** — ตัดสินใจเองตอนออกแบบ (Notion note แรกที่เขียนไว้ตอนเพิ่ม task เข้าใจผิดว่าจะเหมือน Highlights ซึ่งเป็น tab แต่พอออกแบบจริงเห็นว่า Tag Point เป็นการดูค่าเดี๋ยวนี้ชั่วคราว ไม่ใช่ config ที่ต้อง save ใกล้เคียงปุ่ม Ruler/axis-pin ที่ `ScatterChart.tsx` มีอยู่แล้วมากกว่า — อัปเดต Notion note ให้ตรงกับดีไซน์จริงแล้ว)
    - **Line chart**: คลิกไอคอน 🏷️ มุมขวาบนกราฟเข้าโหมด tag แล้วคลิกบนเส้นเพื่อปักหมุด — แต่ละหมุด badge เลข + สี (พาเลตเดิม amber/sky/emerald/rose/violet) พร้อม callout ลอยโชว์เวลา+ค่าทุก sensor ที่จุดนั้น จุดที่ 2 ขึ้นไปโชว์ Δ เทียบกับจุดแรกในตัว callout เอง — **ตัดตารางเปรียบเทียบด้านล่างออกตามที่ผู้ใช้ขอ** (รอบแรกมี ผู้ใช้บอกไม่ต้องการ)
    - **Scatter plot**: ไอคอนเดียวกันอยู่ใน toolbox เดิมข้างปุ่ม Ruler — tag เป็นวงแหวนรอบจุด (ไม่แตะสีเติมของ dot) ใช้ร่วมกับ criteria-range coloring ที่มีอยู่แล้วได้เลย (แนวทาง "แยกช่องทางภาพ" เดียวกับที่คุยกันไว้ตอนออกแบบ Highlights-on-Scatter)
    - **Pair Plot**: 1 หมุด = 1 แถวข้อมูลจริง โผล่วงแหวนสีเดียวกันในทุก cell ของ matrix พร้อมกัน (คนละเรื่องกับ lasso-cluster ที่ยังทำงานแยกอิสระต่อ cell) control เป็นปุ่มเดียวที่ toolbar บนสุดของทั้งตาราง ไม่ใช่ต่อ cell — mockup ส่วนนี้เป็น**ตัวอย่างสถิตแสดง concept เท่านั้น** (Line/Scatter คลิกได้จริง แต่ Pair Plot ยังไม่ทำ interactive เต็ม รอ confirm ทิศทางก่อน)
  - **ยังไม่ตัดสินใจ — รอ confirm ก่อน implement**: persist หมุดข้ามเซสชันไหม (เหมือน Highlights) หรือชั่วคราว, จำนวนหมุดสูงสุด (mockup จำกัด 5 ตามพาเลต), จะทำ Pair Plot ให้คลิกได้จริงเหมือน Line/Scatter ไหม
  - **ยังไม่ได้ทำอะไรในโค้ดแอปเลยสักไฟล์** — design-only entry เหมือน pattern ของ Highlights ก่อนหน้า
  - ไฟล์ที่แก้: ไม่มีไฟล์โค้ดในโปรเจกต์ (Notion page ใหม่ + Artifact เท่านั้น), `docs/PROJECT_HANDOVER.md` (banner + entry นี้) — commit+push ตาม Post-task checklist

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — ⚡ Implement ฟีเจอร์ "Tag Point" เต็มรูปแบบ (Line + Scatter — Pair Plot ยังไม่ทำ)**: ผู้ใช้ตอบ "implement ได้เลย" หลัง iterate mockup จนลงตัว (ตัดตาราง, เพิ่ม Scatter/Pair Plot, ทดสอบปุ่มลบ tag) — ทำตาม scope ที่ mockup คุยกันไว้ (Line/Scatter interactive, Pair Plot ข้าม เพราะยังไม่ confirm):
  - **`types.ts`**: เพิ่ม `LineTaggedPoint { id, timestamp, color }` + `WorkspaceState.lineTaggedPoints?` — **จงใจระบุด้วย timestamp ไม่ใช่ index** (index จะเลื่อนทุกครั้งที่ aggregation/time-range เปลี่ยน) re-resolve ด้วย `nearestXIndex` ตัวเดียวกับที่ `TimeHighlight` ใช้อยู่แล้ว
  - **`pairPlotColors.ts`**: ย้าย `RANGE_PALETTE` (เดิมอยู่ใน `ScatterChart.tsx`) มาไว้ที่นี่แทน (ไฟล์ dependency-free) + เพิ่ม `TAG_BADGES` (①-⑧) และ `MAX_TAGGED_POINTS` (=8, ตามจำนวนสี) — เหตุผล: `LineChart.tsx` ต้องใช้ palette เดียวกัน ถ้า import จาก `ScatterChart.tsx` ตรงๆ จะดึง `regl-scatterplot` ทั้งโมดูลเข้ามาโดยไม่จำเป็น (import แค่ค่าคงที่ แต่ ES module ต้อง evaluate ทั้งไฟล์) — `ScatterChart.tsx` เหลือแค่ `export { RANGE_PALETTE }` re-export กัน `Dashboard.tsx`/`charts/index.ts` ที่ import จากที่เดิมพัง
  - **`ResponsiveECharts.tsx`**: เพิ่ม prop `onChartReady?: (instance) => void` ยิงครั้งเดียวหลัง mount — จำเป็นเพราะต้องเรียก `instance.getZr()`/`convertFromPixel`/`containPixel` ที่ `onEvents` เดิมเอื้อมไม่ถึง
  - **`LineChart.tsx`**: คลิก zrender canvas ตรงๆ (ไม่ใช่ ECharts series click ผ่าน `onEvents`) เพราะ `isLargeData` mode ตั้ง `silent: true` บน line series ไว้แล้ว (กัน hit-test cost) — click ผ่าน series จะไม่ทำงานเลยกับข้อมูลที่ฟีเจอร์นี้มีประโยชน์ที่สุด `convertFromPixel` แปลง pixel → index เอง ปักหมุดด้วย native ECharts **`markPoint`** (คนละ mechanism จาก `markLine`/`markArea` ที่มีอยู่แล้ว) แปะกับ series แรกเท่านั้น (pattern เดียวกับ `markArea`) — label แสดง badge เลข (①②③ unicode) + เวลา + ค่าทุก sensor + Δ (จุดที่ 2 ขึ้นไป) **คลิกจุดที่ tag ไว้แล้วซ้ำ = ลบ (toggle)** แทนที่จะต้องคลิก badge แม่นๆ (ตามที่ mockup ออกแบบไว้ตอนแรก แต่พบว่า hit-test badge ที่แม่นยำทำยากกว่า/เสี่ยงกว่าการเช็คจาก data โดยตรง) — persist ผ่าน `lineTaggedPoints` prop ตาม pattern `scatterAxisPins`/`scatterCriteria` เป๊ะ (seed จาก persisted prop ตอน mount, push กลับผ่าน `onLineTaggedPointsChange` effect)
  - **`ScatterChart.tsx`**: ไอคอน 🏷️ เพิ่มเข้า toolbox เดิมข้าง Ruler — คลิกใช้ `hoveredIdx` ที่มีอยู่แล้ว (จาก `pointover`/`pointout`) ไม่ต้องคำนวณ nearest-point เอง — ring/badge/callout เป็น DOM overlay (ไม่ใช่ WebGL) วางตำแหน่งด้วย `sc.getScreenPosition()` เหมือน tooltip เดิม **จงใจไม่ persist** (ต่างจาก Line) เพราะ Scatter sample มาจาก reservoir sampling (`get_scatter_sample`) — จุดที่ tag ไว้ไม่มี identity ที่ stable ข้าม refetch เลย (แม้แต่ภายใน session เดียวกัน สลับ X/Y sensor ก็ทำให้ index หมายถึงคนละจุด) เคลียร์ tag ทั้งหมดอัตโนมัติเมื่อ `data`/`scatterX`/`scatterY` เปลี่ยน (ไม่ใช่ pin/highlight/resize เพราะพวกนั้นไม่กระทบ point set)
  - **`ChartTypes.ts`/`Chart.tsx`/`Dashboard.tsx`**: thread `lineTaggedPoints`/`onLineTaggedPointsChange` ผ่าน Chart ไปแค่ branch Line เท่านั้น (Scatter ไม่รับ prop นี้เลย เพราะเก็บ stateในตัวเอง) — Dashboard state + `buildWorkspaceState` wiring ตาม pattern `scatterAxisPins` เป๊ะ
  - **`App.css`**: เพิ่ม `.line-chart-toolbox`/`.line-chart-tool` (mirror `.scatter-regl-toolbox`/`.scatter-regl-tool` แต่แยก class ไม่ทับของเดิม กัน risk กับเทสต์ที่มีอยู่), `.scatter-tag-ring`/`.scatter-tag-badge`/`.scatter-tag-callout` สำหรับ Scatter overlay ใหม่
  - **เทสต์**: เพิ่มครบทุกไฟล์ที่แก้ (`ResponsiveECharts.test.tsx` — onChartReady; `LineChart.test.tsx` — toggle, tag/untag, Δ, cap 8, seed จาก prop, containPixel guard, clear-all, ต้องห่อ `zrClickHandler` ด้วย `act()` เพราะ state update นอก React event system; `ScatterChart.test.tsx` — เหมือนกันฝั่ง scatter + เทสต์ reset เมื่อ data/axis เปลี่ยน; `Chart.test.tsx`/`Dashboard.test.tsx` — pass-through + persistence) — **บั๊กที่เจอระหว่างเขียนเทสต์**: regex `getByTitle(/Compare 2\+ points/)` case-sensitive ไม่ match ข้อความจริง (ตัวเล็ก "compare") ต้องเติม `/i` flag
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **740/740 ผ่าน** (ก่อนหน้า 740 เช่นกัน — งานนี้เพิ่มเทสต์ใหม่ ~30 ตัวแต่ก็ลบ/รวมไม่มี สุทธิเท่าเดิมเพราะยังไม่นับรวมให้แม่นตรงนี้ ดู `git diff --stat` ของจริงถ้าต้องการตัวเลขที่แม่นกว่า) ไม่มี regression — **ยังไม่ได้ทดสอบบนแอปจริงเลย** (`npm run tauri dev`) ต้องลองก่อนถือว่า "เสร็จจริง": คลิกปักหมุด, คลิกซ้ำเพื่อลบ, สลับ chart type แล้วกลับมาดู Line persist ไหม, สลับ X/Y บน Scatter แล้ว tag หายจริงไหม, ลองกับ dataset ใหญ่ (isLargeData path)
  - **Notion**: task "tag point data function" อัปเดต Note แล้ว **status ยังคง `In progress`** ไม่ mark Done จนกว่าผู้ใช้ทดสอบแอปจริงแล้วยืนยัน (ตาม Post-task checklist rule)

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — 🐛 แก้บั๊กร้ายแรง: Tag Point คลิกไม่ติดเลยบน Line chart**: ผู้ใช้ทดสอบบนแอปจริงตามที่ขอให้ลอง (ครั้งแรกที่ implement เสร็จ) แล้วส่ง screenshot มา "ใช้ไม่ได้เลย ไม่ได้จริงๆ" — ปุ่ม tag เปิดอยู่ (active สีฟ้า) hint text โชว์ แต่คลิกกราฟแล้วไม่มีอะไรเกิดขึ้นเลย (กล่องที่เห็นใน screenshot คือ native hover tooltip เดิม ไม่ใช่ tag callout ใหม่ — สังเกตจาก bullet จุดกลมสีตามเส้นซึ่งเป็น signature ของ tooltip formatter เดิม ไม่ใช่ markPoint label ของฟีเจอร์ใหม่)
  - **Root cause**: `ResponsiveECharts.tsx`'s `onChartReady` (ที่เขียนไว้ตอน implement) ดัก instance ผ่าน `useEffect` ที่ยิงทันทีหลัง mount แล้วเรียก `chartRef.current?.getEchartsInstance()` เอง — แต่ `echarts-for-react` (v3.0.5, อ่าน source จริงใน `node_modules` เพื่อ confirm) init แบบ **async**: `componentDidMount` สร้าง instance **ชั่วคราว** ก่อน รอ event `'finished'` ของ render แรก แล้ว **dispose ตัวชั่วคราวทิ้ง สร้างใหม่อีกตัว** (เพื่อวัด width/height จริงแล้วค่อย init ด้วยขนาดที่ถูกต้อง) — instance ตัว "จริง" ที่อยู่ยาวคือตัวที่สอง ไม่ใช่ตัวแรก effect ของเราที่ยิงทันทีหลัง mount (synchronous) เลยไปจับตัวชั่วคราว (หรือยังไม่มีตัวไหนเลยด้วยซ้ำ) — พอตัวจริงถูกสร้างทีหลัง `chartInstance` state ใน `LineChart.tsx` ก็ค้างเป็น `null`/instance ผิดตัวตลอดไป ทำให้ effect ที่ผูก `zr.on('click', ...)` ไม่เคยได้แนบกับ instance ที่ถูกต้องเลย — คลิกกราฟเลยไม่มีอะไรเกิดขึ้น
  - **ทำไมเทสต์ไม่จับบั๊กนี้**: mock ของ `echarts-for-react` ใน `ResponsiveECharts.test.tsx` เรียบง่ายเกินไป — คืน instance เดียวกันทันทีแบบ synchronous ผ่าน `useImperativeHandle` ไม่ได้จำลอง async temp-instance-then-dispose-then-recreate ที่ library จริงทำ เทสต์เดิมเลยผ่านทั้งที่โค้ดจริงพัง — เป็นตัวอย่าง mock-vs-reality gap ที่เทสต์ยืนยันไม่ได้ว่า integration ใช้งานได้จริง (แค่ยืนยันว่า "logic ภายในถูกต้อง ถ้า onChartReady ถูกเรียกด้วย instance ที่ถูกต้อง" ซึ่งเป็นสมมติฐานที่ผิดในความเป็นจริง)
  - **แก้**: `echarts-for-react`'s `EChartsReact`/`EChartsReactCore` **รองรับ prop `onChartReady` อยู่แล้วโดยตรง** (เห็นจาก source จริง `node_modules/echarts-for-react/esm/core.js` — เรียกหลัง async init dance เสร็จ ด้วย instance จริงตัวสุดท้าย) — ลบ `useEffect` ที่เขียนเองทิ้งทั้งหมด แล้ว **ส่ง `onChartReady` prop ต่อตรงๆ ไปที่ `<ReactECharts onChartReady={onChartReady} />`** แทน ให้ library จัดการ timing ให้เองทั้งหมด (ง่ายกว่าโค้ดเดิมด้วย ไม่ต้องมี effect ของตัวเองเลย)
  - **แก้เทสต์**: `ResponsiveECharts.test.tsx` — ลบเทสต์เดิม 2 ตัวที่ผิดสมมติฐาน (คาดว่า component เรียก `onChartReady` เอง) เปลี่ยนเป็นเทสต์ว่า `onChartReady` prop ถูก forward ไปที่ `<ReactECharts>` ตรงๆ (เหมือน pattern การเทสต์ `onEvents` ที่มีอยู่แล้ว) พร้อม comment อธิบาย root cause เต็มๆ กันคนมาแก้กลับผิดอีกในอนาคต — `LineChart.test.tsx`/`ScatterChart.test.tsx` ไม่ต้องแก้เลย เพราะ mock `ResponsiveECharts` ทั้งก้อนอยู่แล้ว (เทสต์พวกนั้นตรวจ "ถ้า onChartReady ถูกเรียก LineChart ทำงานถูกไหม" ซึ่งยังถูกต้องอยู่ — บั๊กอยู่ใน `ResponsiveECharts.tsx` เอง ไม่ใช่ `LineChart.tsx`)
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **740/740 ผ่าน** ไม่มี regression — **ยังไม่ได้ทดสอบซ้ำบนแอปจริง** ต้องรอผู้ใช้ลองใหม่อีกรอบก่อนถือว่าใช้งานได้จริง — Scatter chart ไม่ได้ใช้ mechanism นี้เลย (ใช้ native DOM click listener บน canvas ตรงๆ ผูกกับ effect เดียวกับที่สร้าง regl-scatterplot instance ซึ่งไม่มีปัญหา async แบบนี้) — ถ้า Scatter ก็ใช้ไม่ได้ด้วยจะเป็นบั๊กคนละจุด ต้องขอให้ผู้ใช้เช็คแยกกันด้วย
  - ไฟล์ที่แก้: `ResponsiveECharts.tsx` (แก้กลไกหลัก), `ResponsiveECharts.test.tsx` (แก้เทสต์), `docs/PROJECT_HANDOVER.md` (banner + entry นี้)

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — 🐛 แก้บั๊กที่ 2 (คนละจุด): Line chart ยังคลิกไม่ติดแม้แก้ onChartReady แล้ว — เจอผ่านการสร้าง test harness จริงนอกแอป**: ผู้ใช้ restart `npm run tauri dev` ตามที่ขอ + ทดสอบ Scatter (ใช้ได้แล้ว ✅) แต่ Line ยัง "ใช้ไม่ได้เลย" — ขอให้ "เช็คระเอียดๆถึงการเทสด้วย" (ตรวจสอบละเอียดรวมถึงวิธีเทสของ Claude เองด้วย)
  - **ปัญหาที่เจอ**: อ่านโค้ดซ้ำหลายรอบ (zrender event names, ECharts instance API, echarts-for-react lifecycle) ทุกจุด "ถูกต้องตามสเปก" ในทางทฤษฎี หาบั๊กจาก static analysis ต่อไม่ได้แล้ว — **แอปนี้เปิดใน Browser preview ไม่ได้เลย** (TitleBar.tsx เรียก Tauri API ตรงๆ พังตั้งแต่ root) เลยไม่มีทางรัน integration test ของจริงกับ Tauri app ได้
  - **วิธีแก้ปัญหา**: สร้าง **standalone HTML test harness** ที่ไม่พึ่ง React/Tauri เลย — ใช้ `echarts.min.js` ตัวจริงจาก `node_modules/echarts/dist/` ตรงๆ สร้างกราฟ category-axis เหมือน LineChart จริง แนบ `zr.on('click', ...)` เหมือนโค้ดจริงทุกจุด แล้วรันผ่าน **local Python http.server + Browser pane** (`file://` เปิดได้แค่ static snapshot คลิกไม่ได้ ต้องเสิร์ฟผ่าน http:// ถึงจะ interactive ได้เต็มรูปแบบ) จำลองคลิกด้วย `dispatchEvent(MouseEvent)` ตรงบน canvas แล้ว log ผลลัพธ์ของทุก API variant ที่เป็นไปได้ลงหน้าเว็บ
  - **ผลที่เจอ (ยืนยันด้วยข้อมูลจริง ไม่ใช่เดา)**: `chart.convertFromPixel({xAxisIndex: 0}, pixel)` **คืนค่า `NaN` เสมอ** สำหรับ category axis (ทั้งที่เป็น pattern ที่เจอบ่อยสุดตาม tutorial ทั่วไป) — โค้ดเดิมมี guard `if (rawIdx == null || !isFinite(rawIdx)) return;` ซึ่ง `!isFinite(NaN)` เป็น `true` เสมอ **ทำให้ return ออกจาก handler ทุกครั้งก่อนจะปักหมุดได้เลย** — ทดสอบ variant อื่นในหน้าเดียวกัน: `convertFromPixel({seriesIndex: 0}, pixel)` และ `convertFromPixel('grid', pixel)` **คืนค่าถูกต้อง** เป็น `[categoryIndex, value]` (array ไม่ใช่ number เดี่ยวๆ)
  - **แก้**: เปลี่ยน finder จาก `{xAxisIndex: 0}` เป็น `{seriesIndex: 0}` ใน `LineChart.tsx`'s click handler แล้วอ่าน `result[0]` แทนการใช้ผลลัพธ์ตรงๆ (เพราะตอนนี้เป็น array) — เขียน comment อธิบาย root cause ไว้ในโค้ดเต็มๆ กันคนแก้กลับไปใช้ `xAxisIndex` อีกในอนาคตเพราะมันดู "ถูกต้อง" กว่าตาม intuition
  - **แก้เทสต์ให้ตรงกับความจริง ไม่ใช่แค่ให้ผ่าน**: mock เดิม (`mockConvertFromPixel = vi.fn(() => 0)`) คืนค่าเป็น number เดี่ยวๆ ตรงกับสมมติฐานเดิมที่ผิด — เปลี่ยน mock ให้คืน `[index, value]` แบบ array เหมือนของจริง (ไม่งั้นเทสต์จะยัง "ผ่าน" ได้ทั้งที่ทดสอบ code path ที่ไม่ตรงกับพฤติกรรมจริงของ echarts) เพิ่มเทสต์ regression ใหม่ 1 ตัวยืนยันตรงๆ ว่าเรียก `convertFromPixel` ด้วย `{seriesIndex: 0}` ไม่ใช่ `{xAxisIndex: 0}`
  - **บทเรียน**: เทสต์แบบ mock-heavy (มี mock หลายชั้น: `ResponsiveECharts` mock ทั้งก้อนใน `LineChart.test.tsx`, `echarts-for-react` mock ใน `ResponsiveECharts.test.tsx`) verify แค่ "internal logic ถูกต้อง ถ้าสมมติฐานเรื่อง external API ถูกต้อง" — แต่ไม่เคยพิสูจน์ว่าสมมติฐานนั้นตรงกับพฤติกรรมจริงของ library เลย เป็น gap เดียวกับบั๊กรอบก่อน (onChartReady timing) แค่คนละจุดในโค้ด — เมื่อ mock ไม่ได้ผลและ end-to-end ก็ทำไม่ได้ (Tauri) ทางออกคือสร้าง **minimal standalone reproduction ด้วย library จริง** แยกออกมาทดสอบเดี่ยวๆ นอกบริบทแอปทั้งหมด
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **741/741 ผ่าน** (+1 จากเทสต์ regression ใหม่) ไม่มี regression — **ยังไม่ได้ทดสอบซ้ำบนแอปจริง** ต้องรอผู้ใช้ลองใหม่อีกรอบ (ครั้งนี้ทั้ง Line และ Scatter ที่เคยยืนยันว่าใช้ได้แล้ว ควรลองซ้ำให้แน่ใจว่าไม่มีอะไรพังเพิ่ม)
  - ไฟล์ที่แก้: `LineChart.tsx` (finder + comment), `LineChart.test.tsx` (mock shape + เทสต์ regression ใหม่), `docs/PROJECT_HANDOVER.md` (banner + entry นี้) — ไฟล์ test harness (`echarts_click_test.html`) อยู่ใน scratchpad เท่านั้น ไม่ได้เข้า repo

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — ✅ Tag Point ยืนยันใช้งานได้จริงแล้ว + 2 UX refinement**: ผู้ใช้ทดสอบซ้ำแล้วยืนยัน "scatterplot ใช้ได้ แต่ line plot ใช้ไม่ได้" ตอนแรก (บั๊กที่ 2 ยังไม่ได้ push ตอนนั้น) — พอ push แก้แล้วลองใหม่ ยืนยันว่าใช้ได้ทั้งคู่ แล้วขอปรับ 2 จุด:
  - **(1) ตัด Δ (delta เทียบกับหมุดแรก) ออกทั้ง Line และ Scatter** — ผู้ใช้บอกตรงๆ ไม่ได้ใช้ อยากเห็นแค่ค่าที่กดพอ:
    - `LineChart.tsx`: ลบ logic คำนวณ+แสดง `Δ vs ①: ...` ออกจาก markPoint label formatter ทั้งหมด (โค้ดที่หา `baseTag`/`baseIdx`/`deltaParts` ตัดทิ้ง)
    - `ScatterChart.tsx`: ลบ `delta` field ออกจาก `tagOverlays` useMemo (ไม่คำนวณ `baseTag`/`baseX`/`baseY` อีกต่อไป) + ลบ JSX block `{t.delta && (...)}` ออกจาก callout
    - `App.css`: ลบ CSS `.scatter-tag-callout-delta` ที่ไม่ได้ใช้แล้ว
  - **(2) แก้ text card ของ Line chart โดนตัดขอบ** (เห็นชัดใน screenshot ที่ 2 — การ์ดมุมขวาบนถูก clip ทั้งขอบบนและขอบขวา มองไม่เห็นข้อความครบ): ECharts hard-clip label ที่ล้นขอบ canvas (ไม่ reflow เหมือน DOM tooltip) — แก้โดยคำนวณ `position`/`align` ของแต่ละ tag **แบบ dynamic ตามตำแหน่งจริงของหมุดนั้น** แทนที่จะ fix เป็น `position:'top', align:'left'` ตายตัว:
    - แนวนอน: ถ้า index อยู่ใน 65% ขวาสุดของกราฟ → `align:'right'` (การ์ดยื่นไปทางซ้ายแทน หนีขอบขวา) ถ้าอยู่ใน 10% ซ้ายสุด → `align:'left'` นอกนั้น `align:'center'` เหมือนเดิม
    - แนวตั้ง: เทียบค่าที่จุดนั้นกับ min/max ของ sensor แรก (sensor ที่ marker ปักอยู่) ถ้าอยู่ใน 25% บนสุดของช่วงข้อมูล → `position:'bottom'` (การ์ดโผล่ใต้จุดแทน หนีขอบบน) ไม่งั้น `position:'top'` เหมือนเดิม — เป็นการประมาณจาก data range ไม่ใช่ pixel จริง (แกน Y มี auto-fit padding เล็กน้อย) แต่คลาดเคลื่อนแค่ใกล้ๆ ขอบเท่านั้นซึ่งเป็นจุดที่ต้องการแก้พอดี
    - Scatter chart **ไม่ได้แก้จุดนี้** เพราะผู้ใช้ระบุว่าปัญหาอยู่ที่ "ภาพที่ 2 ส่วนของ lineplot" เท่านั้น ไม่ได้รายงานว่า Scatter โดนตัดขอบด้วย
  - **แก้เทสต์**: ลบเทสต์ Δ เดิมทั้ง `LineChart.test.tsx`/`ScatterChart.test.tsx` เปลี่ยนเป็นยืนยันว่าไม่มี "Δ" ในข้อความแล้ว เพิ่มเทสต์ใหม่ 2 ตัวใน `LineChart.test.tsx` ยืนยัน `label.align`/`label.position` เปลี่ยนถูกต้องตามตำแหน่งหมุด (ซ้ายสุด/ค่าต่ำสุด → `align:'left', position:'top'`, ขวาสุด/ค่าสูงสุด → `align:'right', position:'bottom'`, กลางกราฟ → `align:'center', position:'top'`)
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **744/744 ผ่าน** ไม่มี regression — **ยังไม่ได้ทดสอบจริงบนแอป** โดยเฉพาะจุด (2) ที่ยังไม่เคย verify ด้วยตาจริงว่า flip ถูกทิศจริงไหม (คำนวณจาก data-range heuristic ไม่ใช่ pixel จริง อาจต้องปรับ threshold 65%/10%/25% ถ้าเห็นว่ายังไม่พอดี)
  - ไฟล์ที่แก้: `LineChart.tsx`, `ScatterChart.tsx`, `App.css`, `LineChart.test.tsx`, `ScatterChart.test.tsx`, `docs/PROJECT_HANDOVER.md` (banner + entry นี้)

- **🆕 2026-08-17 (ต่อในวันเดียวกัน) — 🔧 Tag Point (Line): เลิก persist ลงไฟล์ workspace ตามที่ผู้ใช้ขอ**: ผู้ใช้บอกตรงๆ "function tag ไม่ต้อง save ไว้เพื่อใช้ตอนปิดเปิดใหม่ function นี้จะไม่ถูกจำเวลาปิดโปรแกรม แก้ไขหน่อย ส่วนอื่นยังเป็นเหมือนเดิม":
  - **ตีความ**: "ส่วนอื่นยังเป็นเหมือนเดิม" หมายถึง**ยังต้อง survive การสลับ chart type ไป-กลับในเซสชันเดียวกัน** (Dashboard ยังเก็บ state ไว้ใน memory เหมือนเดิม ไม่ย้อนกลับไปเป็น LineChart-local state) — สิ่งเดียวที่ตัดออกคือการเขียนลงไฟล์ workspace (JSON) ที่โหลดกลับมาตอนเปิดแอปใหม่
  - **`types.ts`**: ลบ `lineTaggedPoints?: LineTaggedPoint[];` ออกจาก `WorkspaceState` interface ทั้งหมด (ไม่ใช่แค่เลิกอ่าน — ลบ field ออกจาก schema ไปเลย กัน field ผีที่ดูเหมือนใช้งานได้แต่จริงๆ ไม่มีความหมาย) — คง `LineTaggedPoint` interface ไว้เหมือนเดิม (ยังใช้เป็น prop type ปกติ) อัปเดต docstring อธิบายเหตุผลที่ไม่อยู่ใน `WorkspaceState`
  - **`Dashboard.tsx`**: `useState<LineTaggedPoint[]>(initialState?.lineTaggedPoints ?? [])` → `useState<LineTaggedPoint[]>([])` (เริ่มว่างเสมอ ไม่อ่านจาก `initialState` อีกต่อไป) ลบ `lineTaggedPoints` ออกจาก `buildWorkspaceState()`'s return object และ dependency array — **ไม่แตะ**: `handleLineTaggedPointsChange`, การส่ง prop เข้า `<Chart>`, หรือโค้ดฝั่ง `LineChart.tsx`/`ChartTypes.ts`/`Chart.tsx` เลยสักบรรทัด (ตรงตามที่ขอว่า "ส่วนอื่นยังเป็นเหมือนเดิม" — กลไก cross-chart-switch-in-session ยังทำงานเหมือนเดิมทุกอย่าง เพราะ Dashboard ยัง hold state ไว้ตลอดที่แอปยังรันอยู่ แค่ไม่เขียนลงดิสก์)
  - **แก้เทสต์**: `Dashboard.test.tsx`'s describe block เขียนใหม่ทั้งก้อน — ลบเทสต์ "seeds จาก initialState" (ทำไม่ได้แล้วเพราะ type ไม่มี field นี้ให้ seed) เปลี่ยนเป็นเทสต์ "เริ่มว่างเสมอไม่ว่า initialState จะเป็นอะไร" + แก้เทสต์ autosave ให้ยืนยันว่า **`saved.lineTaggedPoints` ต้องเป็น `undefined`** (แทนที่จะเช็คว่า persist ถูกต้อง) — เป็นเทสต์ regression กันย้อนกลับไปใส่ persistence กลับเข้ามาโดยไม่ตั้งใจในอนาคต
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **743/743 ผ่าน** (-1 จาก 744 — รวมเทสต์ "seeds จาก initialState" กับ "default ว่าง" เป็นเทสต์เดียวเพราะตอนนี้พฤติกรรมเดียวกัน ไม่ใช่ลบ coverage) ไม่มี regression — **ยังไม่ได้ทดสอบจริงบนแอป** ต้องลองปิด-เปิดแอปใหม่จริงๆ เพื่อยืนยันว่า tag หายจริงตามที่ต้องการ (และยังต้องเช็คว่าสลับ Line → Scatter → กลับ Line ในเซสชันเดียวกัน tag ยังอยู่เหมือนเดิม)
  - ไฟล์ที่แก้: `types.ts`, `Dashboard.tsx`, `Dashboard.test.tsx`, `docs/PROJECT_HANDOVER.md` (banner + entry นี้)

- **🆕 2026-08-18 — ⚡ งานใหญ่: ออกแบบ + implement ระบบ Failure Group / Predictive Model ใหม่ทั้งหมด (ตอบทุก task ใน Notion ที่เกี่ยวกับ FG)**: ผู้ใช้ list task ที่เกี่ยวกับ FG ใน Notion มาให้ทั้งหมด พร้อม requirement ใหม่ยาว — ต้องมี FG ID/description/recommendation, add model ต้องระบุ detail ก่อน add sensor (แต่ละ kind ต้องการ sensor ไม่เหมือนกัน — Individual 1 ตัว, Relationship 1 target + ≥1 predictor, Clustering 2 ตัว X/Y + criteria สensor เสริม), ทุก model ต้องมี category (Performance/Condition) ผูกกับ component (derive จาก target sensor), ปุ่ม "Create FG" ควรเปลี่ยนเป็น "Build Model" เพราะ add FG จริงๆทำที่ Dashboard แล้ว — ให้ออกแบบก่อน implement
  - **ขั้นตอนออกแบบ**: อ่านโค้ดปัจจุบันทั้งหมด (`types.ts`, `Dashboard.tsx`'s FG block, `FailureGroupsPanel.tsx`, `PredictiveModelBuild.tsx`'s hydration/persist effect) เพื่อยืนยัน root cause ของ 2 ปัญหาที่ผู้ใช้ complain ไว้ใน Notion: **(1)** "1 model อยู่หลาย FG ต้อง train ซ้ำ" — root cause คือ `PredictiveModelStateSlice` เดิมเป็น global slot เดียวใน `WorkspaceState` ไม่ได้ผูกกับ model ไหนเฉพาะ สลับ target เมื่อไหร่ config เก่าหาย **(2)** "สร้างชื่อ FG ซ้ำได้" — ยืนยันว่าไม่มี duplicate-name check เลยสักจุดในโค้ดจริง — ทำ interactive mockup (Artifact, republish 2 รอบ) ให้ผู้ใช้เล่นก่อน ผู้ใช้ตอบคำถามเปิด 4 ข้อ + เพิ่ม requirement "add model name": FG ID ใช้เลขเดิมรันต่อ (ไม่ใช่ field ใหม่, เป็นแค่ display string `FG-{no}`), Build Model เป็น**หน้าต่างแยก**ไม่ใช่ modal (เผื่อเปิด 2 กลุ่มพร้อมกันเทียบกัน), Dashboard tab เดิมเป็นแค่**preview อ่านอย่างเดียว** (แก้ไขทุกอย่างในหน้าต่าง Build Model เท่านั้น — กันซ้ำกับ pattern ที่เคยถูก revert มาก่อน คือปุ่ม/modal สรุปซ้ำข้อมูลที่มีอยู่แล้ว), migrate ข้อมูลเก่าเป็น 3 kind เลย (เดา kind จาก free-text `modelType` เดิม แทนที่จะ default เป็น individual หมดแล้วให้ผู้ใช้ตั้งใหม่เอง) — ผู้ใช้ตอบ **"implement เลย เดี๋ยวผมบอกเองถ้ามีอะไรเพิ่มหรือแก้ไข"** จึง implement ตรงตาม design ทันทีโดยไม่รอ confirm เพิ่ม (คำถามเดียวที่ยังไม่ตอบ — legacy model ควร default category อะไร — ตัดสินใจเองเป็น `null`/unset แทนการเดา เพราะไม่มี source field ให้ map และ "ไม่ตั้งค่าที่ชัดเจน" ดีกว่า "เดาผิดแบบเงียบๆ")
  - **`types.ts`**: `FailureGroup` เพิ่ม `description?`/`recommendation?` — `FailureSensorRow` เดิม mark `@deprecated` เก็บไว้แค่ให้ migration shim อ่าน (ไม่ลบทิ้งเพราะยังมีประโยชน์เป็น type สำหรับข้อมูลเก่า) — เพิ่ม `ModelKind`/`ModelCategory` type ใหม่ — **`FailureModel` (ใหม่) extends `PredictiveModelStateSlice` เดิมทั้งก้อน** (แทนที่จะ list field ซ้ำ) บวก `id/groupNo/name/kind/category/notes/status/xSensor/ySensor` — เหตุผลที่ extend แทนแยก: fold PM config (relModelName/relStiffness/clusterRanges/ฯลฯ) เข้าไปในตัว model โดยตรงคือ fix หลักของปัญหา (1) — ทุก model self-contained เรื่อง config ไม่มี global slot ให้ชนกันอีก — `component` **ไม่เก็บเป็น field** (derive สดจาก `SensorMetadata.component` ของ target sensor ทุกครั้งที่ต้องใช้ เพราะ sensor metadata มาจาก CSV mapping ที่ re-derive ทุก session ไม่ใช่ persisted data) — `FailureGroupStateSlice.rows` เปลี่ยนชื่อเป็น `models: FailureModel[]` — `WorkspaceState.predictiveModelState` mark `@deprecated` (เก็บไว้แค่ให้ migration shim อ่านค่าเก่า ไม่มีโค้ดใหม่เขียนเข้าไปอีก) + เพิ่ม `lastPmModelId?` (จำว่า PM window ล่าสุดเปิดโมเดลไหนไว้ ใช้ resume ตอนเปิด workspace ซ้ำ แทนที่ `predictiveModelState.targetSensor` เดิม)
  - **`workspaceManager.ts`**: เพิ่ม migration shim (`migrateFailureGroupState`) เรียกอัตโนมัติใน `loadWorkspaceData` ทุกครั้ง (จุดเดียวที่ทุกหน้าต่างอ่าน workspace ผ่าน — ยืนยันแล้วว่า Dashboard/PredictiveModelBuild/DataUploadPage ทุกจุดเรียกผ่านฟังก์ชันนี้จุดเดียว) — no-op ทันทีถ้า `failureGroupState.models` มีอยู่แล้ว (migrate ไปแล้ว) — แปลง legacy `rows` → `models`: เดา `kind` จาก free-text `modelType` เดิม (มีคำว่า "relationship"/"cluster" → kind ตรงนั้น ไม่งั้น individual), `category: null` เสมอ, **carry PM config ของ global slot เดิมไปให้แค่ model เดียวที่ตรงกับ `predictiveModelState.targetSensor` เท่านั้น** ตัวอื่นได้ default สดใหม่ (กันไม่ให้ config เก่าที่ไม่เกี่ยวข้องเลอะไปโมเดลอื่นที่ไม่เคยถูกเปิดจริง)
  - **`FailureGroupsPanel.tsx`**: เขียนใหม่ทั้งไฟล์ให้เป็น**preview การ์ดล้วน** (ตัด per-row sensor picker/concept-sensor/model-type/notes/build-model-button ทั้งหมดออก — ย้ายไป Build Model window แทน) — การ์ดต่อกลุ่ม: ชื่อ + badge `FG-{no}` + preview description บรรทัดเดียว (ตัวเอียง "No description yet" ถ้าว่าง) + จำนวน model/completion + "Build model →" hint (โผล่ตอน hover) — คลิกที่การ์ด (ยกเว้นปุ่ม rename/delete หรือ input ตอนแก้ชื่อ) เรียก `onOpenBuildModel(groupNo)` — เพิ่ม **duplicate-name check แบบ case-insensitive** ทั้งตอนสร้างใหม่และ rename (error message แบบ inline แดง ไม่ใช่ `alert()`)
  - **`SensorSelection.tsx`**: อัปเดต prop `fgRows`→`fgModels`, membership check เปลี่ยนไปดูจาก individual-kind model แทน row + เพิ่ม duplicate-name check แบบเดียวกับ `FailureGroupsPanel.tsx` ในฟอร์ม "New group name" ของ per-sensor quick-assign (เดิมไม่มี validation เลย)
  - **`BuildModelWindow.tsx` (ไฟล์ใหม่)**: หน้าต่างแยกใหม่ทั้งหมด (label `build-model-{groupNo}` — **ตั้งใจไม่ทำ singleton แบบ PM** เพราะผู้ใช้ขอเปิดหลายกลุ่มพร้อมกันได้) — layout ซ้าย/ขวา: ซ้ายแก้ description/recommendation ของกลุ่ม (debounce save 250ms แบบเดียวกับ PM), ขวาเป็น list การ์ดโมเดล + ฟอร์ม "+ Add Model" ที่ปรับ field ตาม kind ที่เลือก (Individual = sensor เดียว, Relationship = target + predictor chips ≥1, Clustering = X/Y sensor + criteria เสริม + cluster-range ต่อช่วงถ้าเปิด criteria) + auto-readout component (derive จาก target/Y sensor สด ไม่เก็บ persist) — validate ปิดปุ่ม Create จนกว่าจะครบตาม kind — แต่ละโมเดลมีปุ่ม Remove (confirm ก่อน) กับ Train (`emit('launch-predictive-model', {modelId})` ให้ Dashboard เป็นคนเปิด PM จริง เพราะ Dashboard เท่านั้นที่มี raw sensor data ที่ PM ต้องใช้)
  - **สถาปัตยกรรม cross-window sync (ส่วนที่ซับซ้อนที่สุดของงานนี้)**: เพราะตอนนี้มี 3 หน้าต่างที่แก้ `failureGroupState` ได้พร้อมกัน (Dashboard เอง, Build Model หลายวินโดว์, PM) — ทุกจุดเขียนผ่าน `updateWorkspaceData` (read-modify-write) แล้ว **broadcast event `failure-group-state-changed`** ให้ทุกหน้าต่างที่เปิดอยู่รีเฟรช in-memory state ของตัวเอง (Dashboard/BuildModelWindow ต่างก็ `listen` event นี้) — เหตุผลที่ต้องทำขนาดนี้: Dashboard เดิมมี full-overwrite autosave (`buildWorkspaceState()` → `saveWorkspaceData`) ที่ยิงจาก in-memory `fgGroups`/`fgModels` ของตัวเอง — ถ้าไม่ sync ตรงนี้ Build Model window เขียนอะไรลงดิสก์ไป autosave รอบถัดไปของ Dashboard จะเขียนทับข้อมูลเก่าที่ค้างใน memory กลับไปทันที (data loss เงียบๆ) — แก้ด้วย listener นี้แทนที่จะเปลี่ยน autosave mechanism ทั้งระบบ (เสี่ยงกระทบ state อื่นที่ Dashboard เป็นเจ้าของแต่ผู้เดียวอยู่แล้ว)
  - **`PredictiveModelBuild.tsx`**: เปลี่ยนจากอ่าน/เขียน `predictiveModelState` (global slot) เป็นอ่าน/เขียนเข้า `failureGroupState.models` ตรงตัวที่ id ตรงกับ `modelId` ที่ได้รับตอน hydrate (payload ใหม่มี `modelId` เพิ่ม) — เพราะ `FailureModel extends PredictiveModelStateSlice` การ hydrate/persist เกือบไม่ต้องแก้ logic ภายในเลย แค่เปลี่ยนจุดอ่าน/เขียนจาก global slot เป็น `.find(m => m.id === modelId)`/`.map(...)` บน array — broadcast `failure-group-state-changed` หลัง persist ทุกครั้งเหมือนกัน — **PM ยังเป็น singleton window เหมือนเดิม** (ไม่เปลี่ยน แม้ Build Model จะ multi-instance) เพราะ requirement "เปิด 2 tab พร้อมกัน" หมายถึง Build Model ไม่ใช่ PM
  - **`Dashboard.tsx`**: state เปลี่ยนจาก `fgRows`→`fgModels`, ลบ `toggleGroupCollapse`/`addBlankRowToGroup`/`updateFgRow`/`removeFgRowById`/`handleBuildModel` ทั้งหมด (ย้ายไป `BuildModelWindow.tsx`) เพิ่ม `spawnBuildModel(groupNo)` (เปิดหน้าต่างใหม่ label `build-model-{groupNo}`) + listener `launch-predictive-model` (โหลด model จากดิสก์สดๆ ด้วย `loadWorkspaceData` แล้วค่อย spawn PM — ไม่ใช้ in-memory state เพราะอาจ stale) + listener `failure-group-state-changed` (sync กลับตามที่อธิบายด้านบน) — เพิ่ม duplicate-name guard ในทุก path ที่สร้าง/rename กลุ่ม (`createGroupForSensor`/`renameGroup`/`createEmptyGroup`) เป็น defense-in-depth (UI เองก็ guard อยู่แล้วแต่กันไว้เผื่อ caller อื่น)
  - **`main.tsx`/`components/windows/index.ts`**: เพิ่ม route `?window=build-model` → `BuildModelWindow`
  - **เทสต์**: อัปเดตทุกไฟล์ที่แก้ (`Dashboard.test.tsx`, `SensorSelection.test.tsx`, `FailureGroupsPanel.test.tsx` เขียนใหม่ทั้งไฟล์ตาม props ใหม่, `PredictiveModelBuild.test.tsx` แก้ hydration/persistence describe blocks) + ไฟล์ใหม่ 2 ไฟล์ (`BuildModelWindow.test.tsx` — 13 เทสต์ครอบคลุม form validation ทุก kind/persist/broadcast/train/remove, `workspaceManager.test.ts` เพิ่ม describe block migration shim 6 เทสต์)
  - Verify: `npx tsc --noEmit` สะอาด + `npx vitest run` **761/761 ผ่าน** ทั้ง repo ไม่มี regression — **ยังไม่ได้ทดสอบบนแอปจริงเลย** (`npm run tauri dev`) เพราะฟีเจอร์นี้พึ่งพา native multi-window spawning (`WebviewWindow`) หนักมาก ทดสอบผ่าน Browser preview ไม่ได้เลยตั้งแต่ต้น (เหมือนที่เคยเจอกับ Tag Point) — ผู้ใช้บอกไว้ตั้งแต่ต้นแล้วว่า "ถ้าไม่เล่นของจริงก่อน มันมองไม่ออกว่าต้องเพิ่มหรือแก้อะไร" คาดว่าจะมี feedback รอบถัดไปหลังทดสอบจริง
  - Notion: ยังไม่ mark task ไหน Done — รอผู้ใช้ทดสอบแอปจริงยืนยันก่อนตามกฎ Post-task checklist
  - ไฟล์ที่แก้: `types.ts`, `workspaceManager.ts`, `Dashboard.tsx`, `FailureGroupsPanel.tsx`, `SensorSelection.tsx`, `PredictiveModelBuild.tsx`, `main.tsx`, `components/windows/index.ts`, `App.css` (`.fg-open-hint` hover reveal) — ไฟล์ใหม่: `BuildModelWindow.tsx` — เทสต์: `Dashboard.test.tsx`, `SensorSelection.test.tsx`, `FailureGroupsPanel.test.tsx`, `PredictiveModelBuild.test.tsx`, `workspaceManager.test.ts`, `BuildModelWindow.test.tsx` (ใหม่) — `docs/PROJECT_HANDOVER.md` (banner + entry นี้)
