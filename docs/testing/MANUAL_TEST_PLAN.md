# Wizard — Manual Test Plan

> 243 ข้อ · เขียนไว้ 2026-09-07 หลังทำ Manage Special Sensors (ลบ + แก้ไข) เสร็จ · อัปเดต 2026-09-09 (rename special sensor, บังคับกรอกครบ 4 ช่อง, Group by Model Type, ย้ายปุ่ม Build Model) · **อัปเดต 2026-09-21 ให้ตรงกับ v0.5.0** — เพิ่มหมวด MULTI (หลายโปรเจกต์) และ VIS (กวาดหน้าตาหลังล้าง CSS), เปลี่ยนเมนู Zoom, Running Condition Filter, ปุ่ม Finish, ช่อง predictor แบบค้นหา/จัดกลุ่ม, Component เป็น dropdown, และ**ตัดข้อของฟีเจอร์ที่ถูกเอาออก** (Preview / Save Model / Confirm Save / Report PNG / Saving overlay / แถบเลื่อน dataZoom) · **อัปเดต 2026-09-23** — Running Condition Filter เพิ่ม AND/OR (BMW-11) และ per-model Workspace/Custom override (PM-21 ใหม่) · **อัปเดต 2026-09-24 (Feature 4 Phase 2)** — Build Model / Failure Groups จัดเป็น**หนึ่งแถวต่อ sensor** และ Category ตั้งที่หัว sensor ครั้งเดียว (BMW-3 → BMW-15, BMW-12 → BMW-16, BMW-7 → BMW-17, FG-3 → FG-8 และข้อใหม่ BMW-19/20/21/22, FG-9) · **อัปเดต 2026-09-24 (Feature 4 Phase 3)** — Running Condition บังคับตั้งก่อน Build/Finish (BMW-11 → BMW-18, PM-14 → PM-22, ขยาย BMW-17 และข้อใหม่ BMW-23/24, PM-23) · **อัปเดต 2026-09-24 (Feature 4 Phase 4)** — Training time เป็น**หลายช่วงเวลา (periods)** แทน Time start/end ช่วงเดียว (PM-8 → PM-26, PM-21 → PM-27, ข้อใหม่ BMW-25, PER-10; BMW-18/PM-18/PER-9 ปรับข้อความ) · **อัปเดต 2026-09-24 (แก้บั๊ก QA)** — เพิ่ม FG-10, BMW-26, PM-28/29/30
>
> **ต้องทดสอบบน installer ที่ติดตั้งแล้ว ไม่ใช่ `npm run tauri dev`** — CSP และ path ของ Python sidecar เป็นคนละกลไกกันระหว่าง dev กับ build จริง บั๊กหลายตัวที่เจอมาเห็นเฉพาะในตัวติดตั้ง
>
> ผลที่กรอกไว้ในไฟล์ HTML เก็บตาม id ของแต่ละข้อ — ข้อที่ **เปลี่ยนความหมายมากจึงตั้ง id ใหม่ / ตัดออก** (PM-2, PM-9, PM-10, PM-10b, PM-11, PM-11c, BMW-11, PM-14, PM-8, PM-21) ถ้าเคยกรอกผลรอบก่อนไว้ให้กด "ล้างผลทั้งหมด" ก่อนเริ่มรอบนี้
>
> เครื่องหมาย: 🆕 = ของที่เพิ่งทำ ยังไม่เคยมีคนกดจริง · 👀 = บั๊กที่เพิ่งแก้ หรือรายงานที่ยังไม่เคยตามไปดู · 🐍 = path เดียวที่เรียก Python sidecar

## สารบัญ

- [PREP — เตรียมก่อนเริ่ม](#prep) (5 ข้อ)
- [APP — เปิดโปรแกรม & โครงหน้าต่าง](#app) (6 ข้อ)
- [IMP — หน้า Import (3 ขั้น)](#imp) (18 ข้อ)
- [LAY — โครง Dashboard](#lay) (5 ข้อ)
- [SEN — แผง Sensor](#sen) (11 ข้อ)
- [SEL — แท็บ Selected Sensor](#sel) (8 ข้อ)
- [FIL — แท็บ Filter](#fil) (5 ข้อ)
- [HL — แท็บ Highlights](#hl) (7 ข้อ)
- [TIME — ช่วงเวลา & Aggregation](#time) (6 ข้อ)
- [LINE — Line chart](#line) (9 ข้อ)
- [SCAT — Scatter plot](#scat) (8 ข้อ)
- [PAIR — Pair Plot](#pair) (10 ข้อ)
- [SPC — Add Special Sensor — แท็บ Create](#spc) (14 ข้อ)
- [MNG — Manage — ลบ special sensor](#mng) (15 ข้อ)
- [EDT — Manage — แก้ไข special sensor](#edt) (27 ข้อ)
- [FG — แท็บ Failure Groups](#fg) (8 ข้อ)
- [BMW — หน้าต่าง Build Model](#bmw) (18 ข้อ)
- [PM — Predictive Model Build](#pm) (14 ข้อ)
- [MULTI — เปิดมากกว่า 1 โปรเจกต์ (ข้อมูลต้องไม่ปนกัน)](#multi) (7 ข้อ)
- [PER — ปิดเปิดโปรแกรม & การบันทึก](#per) (8 ข้อ)
- [ERR — ความผิดพลาด & ขอบ](#err) (6 ข้อ)
- [PERF — ประสิทธิภาพ](#perf) (6 ข้อ)
- [VIS — กวาดหน้าตา (หลังล้าง CSS ที่ไม่ได้ใช้ และลบโค้ดตาย)](#vis) (3 ข้อ)

---

## PREP

### เตรียมก่อนเริ่ม

_ทำให้ครบก่อน ไม่งั้นข้อหลัง ๆ จะทดสอบไม่ได้จริง_

- [ ] **PREP-1 — Build installer จาก commit ล่าสุด**
  - `npm run installer:win`
  - ดูว่าไฟล์ที่ได้ชื่อ `Wizard_0.5.0_x64-setup.exe` (หรือเวอร์ชันที่ bump ใหม่) ที่ `src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/`
  - **คาดหวัง:** build ผ่านไม่มี error — ถ้าจะ bump version ต้องแก้ให้ตรงกัน 4 ที่ (package.json / package-lock.json / Cargo.toml / tauri.conf.json) และเขียน section ใหม่ใน `docs/release/CHANGELOG.md` ก่อน build ทุกครั้ง แล้วติด tag `vX.Y.Z` หลัง build สำเร็จ

- [ ] **PREP-2 — ถอนตัวเก่าออกก่อนแล้วติดตั้งใหม่**
  - ถอนการติดตั้ง Wizard ตัวเดิมออกจาก Windows
  - ติดตั้งตัวใหม่
  - เปิดโปรแกรม
  - **คาดหวัง:** เปิดขึ้นมาได้ ไม่มีจอดำ ไม่มี error dialog — **ต้องทดสอบบน installer ไม่ใช่ `npm run tauri dev`** เพราะ CSP กับ path ของ Python sidecar ต่างกันคนละกลไก

- [ ] **PREP-3 — workspace เดิมยังอยู่ครบหลังติดตั้งทับ**
  - ดูรายการ Recent Workspaces
  - **คาดหวัง:** workspace เก่าอยู่ครบ — identifier `com.prompt-solution.tauri-app` ใน tauri.conf.json คือสิ่งที่กำหนด path ของ $APPDATA ถ้าหายแปลว่ามีคนไปแก้

- [ ] **PREP-4 — เตรียมไฟล์ทดสอบให้ครบ 5 แบบ**
  - CSV ข้อมูลขนาดปกติ (ไม่กี่ MB)
  - CSV ข้อมูลขนาดใหญ่จริง (หลายร้อย MB ขึ้นไป)
  - CSV หลายไฟล์ที่ต้อง merge กัน
  - CSV mapping ที่มีคอลัมน์ ALARM_L / ALARM_LL / ALARM_H / ALARM_HH
  - ไฟล์เสีย: ไม่มีคอลัมน์ timestamp / timestamp format แปลก / ไฟล์ว่าง
  - **คาดหวัง:** มีครบทั้ง 5 แบบวางไว้ที่เดียวกัน

- [ ] **PREP-5 — เตรียม workspace 2 โปรเจกต์ที่ข้อมูลต่างกันชัดเจน (สำหรับหมวด MULTI)** 🆕
  - สร้างโปรเจกต์ A และ B จาก CSV คนละชุด ให้ชื่อ sensor / component ของสองโปรเจกต์ต่างกันให้เห็นชัด (ถ้าใช้ mapping ก็ให้ description ต่างกัน)
  - ใน A สร้าง special sensor อย่างน้อย 1 ตัว (ตั้ง Component และ Description เฉพาะตัว) และสร้าง Failure Group + โมเดลอย่างน้อย 2 ตัว
  - ใน B สร้าง Failure Group + โมเดลคนละชุด (ชื่อกลุ่มไม่ซ้ำกับ A)
  - **คาดหวัง:** มี 2 โปรเจกต์ที่แยกออกได้ทันทีด้วยตาว่าอันไหนเป็นของโปรเจกต์ไหน — **เคยพลาดเพราะทดสอบด้วยโปรเจกต์เดียวมาตลอด บั๊กข้อมูลปนกันจึงไม่เคยโผล่**

---

## APP

### เปิดโปรแกรม & โครงหน้าต่าง

- [ ] **APP-1 — เปิดโปรแกรมแล้วเข้าหน้า Import เสมอ**
  - เปิดโปรแกรม
  - **คาดหวัง:** เข้าหน้า Import ทุกครั้ง ไม่ auto-resume workspace ล่าสุด (เป็นพฤติกรรมที่ตั้งใจ)

- [ ] **APP-2 — Title bar — ปุ่มหน้าต่าง**
  - กด minimize / maximize / close
  - ลากแถบบนย้ายหน้าต่าง
  - double-click แถบบน
  - **คาดหวัง:** ครบทุกปุ่มทำงาน, ลากย้ายได้, double-click = maximize/restore (หน้าต่างนี้ decorations ปิด ใช้ title bar ที่เขียนเอง)

- [ ] **APP-3 — เมนู File**
  - File → New Workspace (หรือ Ctrl+N)
  - File → Close Workspace
  - File → Rename Workspace
  - File → Quit
  - **คาดหวัง:** New/Close พากลับหน้า Import, Rename เปิดช่องแก้ชื่อที่ title bar, Quit ปิดโปรแกรม — Rename/Close ควรใช้ไม่ได้ตอนยังไม่เปิด workspace

- [ ] **APP-4 — Help → About Wizard**
  - เปิด About
  - **คาดหวัง:** เลขเวอร์ชันตรงกับ installer ที่เพิ่งติดตั้ง (อ่านจาก `getVersion()` ตอน runtime ไม่ได้ hardcode)

- [ ] **APP-5 — เมนู Edit ใช้ได้ในช่องกรอกข้อความ**
  - คลิกช่องกรอกชื่อโปรเจกต์
  - Ctrl+C / Ctrl+V / Ctrl+A / undo
  - **คาดหวัง:** copy-paste ทำงานปกติ

- [ ] **APP-6 — ย่อ/ขยายหน้าต่างแล้ว layout ไม่พัง**
  - ย่อหน้าต่างให้เล็กสุด
  - ขยายเต็มจอ
  - ลากปรับขนาดช้า ๆ
  - **คาดหวัง:** กราฟ resize ตาม ไม่มีแถบเลื่อนแนวนอนโผล่ ไม่มีแผงซ้อนทับกัน

---

## IMP

### หน้า Import (3 ขั้น)

_Step 0 = เลือกว่าจะสร้างใหม่หรือเปิดของเดิม · Step 1 = ตั้งชื่อโปรเจกต์ · Step 2 = ใส่ข้อมูล_

- [ ] **IMP-1 — Step 0 — หน้าเลือกทางเข้า**
  - ดูหน้าแรก
  - **คาดหวัง:** มีทั้งปุ่มสร้างโปรเจกต์ใหม่ และรายการ Recent Workspaces — ตอนอยู่ step 0 ต้องไม่มี sidebar รายการ workspace ซ้ำอีกชุด

- [ ] **IMP-2 — รายการ Recent Workspaces**
  - ดูชื่อ / วันเวลาที่แก้ล่าสุด
  - ดูลำดับ
  - **คาดหวัง:** เรียงตัวที่แก้ล่าสุดขึ้นก่อน ชื่อและเวลาตรงกับที่เพิ่งทำจริง

- [ ] **IMP-3 — เมนู 3 จุด ของแต่ละ workspace → Delete**
  - กดเมนูของ workspace ที่ไม่ใช้แล้ว
  - กด Delete
  - กดที่อื่นเพื่อปิดเมนู
  - **คาดหวัง:** ลบแล้วหายจากรายการทันที, กดที่อื่นแล้วเมนูปิด, ไม่ลบตัวอื่นพลาด

- [ ] **IMP-4 — เปิด workspace เดิมแล้วเข้าหน้าที่ค้างไว้**
  - เปิด workspace ที่ปิดค้างไว้ตอนอยู่หน้า Dashboard
  - ทำซ้ำกับตัวที่ปิดค้างตอนอยู่แท็บ Failure Groups
  - **คาดหวัง:** เข้าหน้าเดิมที่ค้างไว้ (`lastRoute`) ไม่ใช่เด้งกลับหน้าแรกเสมอ

- [ ] **IMP-5 — workspace ที่ไฟล์ CSV ต้นทางถูกย้าย/ลบไปแล้ว**
  - ย้ายไฟล์ CSV ต้นทางออกจากที่เดิม
  - เปิด workspace นั้น
  - **คาดหวัง:** ขึ้นข้อความบอกว่าหาไฟล์ไม่เจอแบบอ่านรู้เรื่อง ไม่ crash ไม่จอขาว

- [ ] **IMP-6 — Step 1 — สร้างโปรเจกต์**
  - ปล่อยชื่อว่างไว้แล้วดูปุ่ม Continue
  - พิมพ์ชื่อ + description
  - กด Back
  - **คาดหวัง:** Continue ใช้ไม่ได้จนกว่าจะมีชื่อ, Back กลับ step 0 ได้

- [ ] **IMP-7 — ตั้งชื่อโปรเจกต์ซ้ำกับที่มีอยู่**
  - ตั้งชื่อให้ตรงกับ workspace เดิมเป๊ะ ๆ
  - ทำต่อจนจบ
  - **คาดหวัง:** ดูว่าเตือนหรือปล่อยผ่าน — ถ้าปล่อยผ่านแล้วเปิดผิดตัวทีหลัง ให้บันทึกไว้

- [ ] **IMP-8 — Step indicator กดย้อนกลับได้อย่างเดียว**
  - อยู่ step 2 แล้วกดวงกลม step 1
  - อยู่ step 1 แล้วลองกดวงกลม step 2
  - **คาดหวัง:** ย้อนกลับได้ แต่กดข้ามไปข้างหน้าไม่ได้

- [ ] **IMP-9 — Step 2 — อัปโหลด CSV ข้อมูล 1 ไฟล์**
  - เลือกไฟล์ CSV ขนาดปกติ
  - **คาดหวัง:** ขึ้นชื่อไฟล์ + สรุปจำนวนแถว/คอลัมน์

- [ ] **IMP-10 — อัปโหลดหลายไฟล์แล้ว merge**
  - เลือก CSV หลายไฟล์ที่มีคอลัมน์เหมือนกัน
  - **คาดหวัง:** รวมเป็นชุดเดียว จำนวนแถวเท่ากับผลรวม, คอลัมน์ timestamp ถูกจับเป็นตัวเดียว

- [ ] **IMP-11 — Data validation summary**
  - ดูสรุปหลังอัปโหลด
  - **คาดหวัง:** บอกจำนวนแถว, คอลัมน์, และแถวที่ timestamp parse ไม่ได้ — ลองกับไฟล์ที่จงใจมี timestamp เสียแล้วดูว่าตัวเลขตรง

- [ ] **IMP-12 — อัปโหลด mapping CSV**
  - เลือกไฟล์ mapping
  - **คาดหวัง:** แสดงว่าจับคู่ tag ได้กี่ตัว ไม่ได้กี่ตัว

- [ ] **IMP-13 — จับคู่ tag ที่ระบบจับไม่ได้ ด้วยมือ**
  - หา tag ที่จับคู่ไม่ได้
  - เลือกคู่เอง
  - **คาดหวัง:** เลือกได้และผลไปโผล่ที่ description/unit/component ของ sensor นั้นใน Dashboard

- [ ] **IMP-14 — Alarm setpoint เข้ามาจาก mapping**
  - ใช้ mapping ที่มี ALARM_L / ALARM_LL / ALARM_H / ALARM_HH
  - เข้า Dashboard แล้วเปิด alarm ของ sensor นั้น
  - **คาดหวัง:** เส้น alarm ขึ้นตรงค่าที่อยู่ใน mapping — sensor ที่ไม่มีค่าในคอลัมน์นั้นต้อง**ไม่ขึ้นเส้นที่ 0**

- [ ] **IMP-15 — ไฟล์ผิดรูปแบบ**
  - ลองไฟล์ที่ไม่มีคอลัมน์ timestamp
  - ลองไฟล์ว่าง
  - ลองไฟล์ที่ไม่ใช่ CSV
  - **คาดหวัง:** ทุกกรณีขึ้นข้อความบอกสาเหตุ ไม่ crash และยังกดทำอย่างอื่นต่อได้

- [ ] **IMP-16 — ไฟล์ใหญ่จริง**
  - อัปโหลด CSV ขนาดหลายร้อย MB
  - **คาดหวัง:** มีสัญญาณว่ากำลังทำงาน (ไม่ค้างแบบเงียบ), จบแล้วเข้า Dashboard ได้, จับเวลาไว้ด้วยว่านานเท่าไร

- [ ] **IMP-17 — กด Continue เข้า Dashboard**
  - กด Continue ที่ step 2
  - **คาดหวัง:** เข้า Dashboard พร้อมชื่อ workspace ที่ตั้งไว้บน title bar

- [ ] **IMP-18 — เปลี่ยนชื่อ workspace ในรายการ Recent (inline)** 🆕
  - ที่รายการ Recent ด้านซ้าย เปลี่ยนชื่อ workspace ตัวหนึ่ง
  - เปิด workspace นั้นเข้าไป
  - **คาดหวัง:** ชื่อใหม่ขึ้นในรายการทันที และตรงกับที่แสดงบน title bar ตอนเปิด (หน้าต่าง "Save As / Rename" แยกถูกลบไปแล้ว — เปลี่ยนชื่อทำได้ที่นี่ที่เดียวนอกเหนือจาก File → Rename Workspace)

- [ ] **IMP-19 — แถบเตือนสีเหลืองหลัง parse (ก่อนหน้านี้ backend ส่งมาแต่ไม่เคยแสดงเลย, แก้แล้ว 2026-09-22)** 🆕
  - อัปโหลด CSV ที่มีคอลัมน์ชื่อซ้ำกันข้ามไฟล์ (2 ไฟล์ขึ้นไป) หรือ timestamp ซ้ำข้ามไฟล์ แล้ว Parse files
  - **คาดหวัง:** ใต้ปุ่ม Parse files ขึ้นกล่องข้อความสีเหลือง (ไม่ใช่สีแดงแบบ error) บอกรายละเอียด เช่น "Duplicate column '...' found in file 1, file 2" — parse ยังสำเร็จ กด Continue ต่อได้ปกติ

- [ ] **IMP-20 — ไฟล์ที่ใช้ปี พ.ศ. ถูกแปลงเป็น ค.ศ. อัตโนมัติ + 29 ก.พ. ที่เคยหายกลับมา (แก้แล้ว 2026-09-22)** 🆕
  - อัปโหลดไฟล์ที่คอลัมน์ timestamp เป็นปี พ.ศ. (เช่น `2566-05-13 01:00:00...`, มีข้อมูลคาบเกี่ยว 29 กุมภาพันธ์ของปีอธิกสุรทิน พ.ศ. เช่น 2567)
  - ดูแถบเตือนหลัง parse แล้วเข้า Dashboard ดูกราฟ ทั้งโหมด Raw และ Aggregate
  - **คาดหวัง:** แถบเตือนบอกจำนวนแถวที่แปลงปี พ.ศ.→ค.ศ. — ในกราฟ **ปีที่แสดงต้องเป็น ค.ศ.ทั้งหมด** (เช่น 2566 → 2023) และต้อง**ตรงกันทั้ง Raw กับ Aggregate** (เดิมถ้าแก้ไม่ครบจะเห็นปีไม่ตรงกันระหว่างสองโหมด) — แถวของวันที่ 29 กุมภาพันธ์ต้องมีข้อมูลอยู่ ไม่หายไป (เดิมหายทั้งวันเพราะปีที่อ่านเป็น ค.ศ. ตรงๆ ไม่ใช่ปีอธิกสุรทิน)

---

## LAY

### โครง Dashboard

- [ ] **LAY-1 — มีครบ 3 แผง**
  - ดูหน้า Dashboard
  - **คาดหวัง:** แผงกราฟ (Sensor Readings), แผงข้อมูลด้านล่าง (Selected Sensor / Filter / Highlights), แผง Sensor ด้านขวา (Sensor / Failure Groups)

- [ ] **LAY-2 — ซ่อน/แสดงแผง**
  - กดไอคอนตาขีดฆ่าของแต่ละแผง
  - เอากลับมา
  - **คาดหวัง:** ซ่อนได้ทั้ง 3 แผง และเอากลับมาได้ครบ

- [ ] **LAY-3 — ลากปรับขนาดแผง**
  - ลากเส้นแบ่งระหว่างแผง
  - ปิดโปรแกรมแล้วเปิดใหม่
  - **คาดหวัง:** ขนาดที่ลากไว้ยังอยู่หลังเปิดใหม่

- [ ] **LAY-4 — ปุ่มลูกศรกลับหน้า Import**
  - กดลูกศรซ้ายมุมบนของแผงกราฟ
  - **คาดหวัง:** กลับหน้า Import และงานที่ทำไว้ถูกบันทึกแล้ว (เปิดกลับเข้ามาต้องครบ)

- [ ] **LAY-5 — badge จำนวนจุด**
  - ดู badge บนหัวแผงกราฟตอน Line
  - ดูตอน Scatter
  - **คาดหวัง:** Line แสดง `X / Y pts (downsampled)` หรือ `N Points`; Scatter แสดง `X / Y pts (sampled)` — ตัวเลขต้องขยับตามช่วงเวลาที่กรอง

---

## SEN

### แผง Sensor

- [ ] **SEN-1 — ค้นหา sensor**
  - พิมพ์ tag
  - พิมพ์คำใน description
  - พิมพ์ชื่อ component
  - พิมพ์หน่วย
  - **คาดหวัง:** ค้นเจอทั้ง 4 แบบ

- [ ] **SEN-2 — จัดกลุ่มตาม component**
  - พับ/กางกลุ่ม
  - **คาดหวัง:** พับกางได้ sensor ที่ไม่มี component อยู่ใต้ Uncategorized

- [ ] **SEN-3 — ติ๊กเลือก sensor**
  - ติ๊กทีละตัว 3-4 ตัว
  - ติ๊กออก
  - **คาดหวัง:** กราฟกับแท็บ Selected Sensor อัปเดตตาม และตัวเลขในหัวแท็บ `Sensor (N)` ตรง

- [ ] **SEN-4 — ปุ่ม Alarm setpoints**
  - กดไอคอน alarm ของ sensor ที่มีค่า setpoint
  - กดปิด
  - **คาดหวัง:** เส้น LL/L/H/HH ขึ้น/หายบนกราฟ พร้อม label

- [ ] **SEN-5 — เปิด popup Add to failure group**
  - กดไอคอนเพิ่มเข้ากลุ่มของ sensor
  - **คาดหวัง:** popup เปิด แสดงรายการกลุ่มทั้งหมด

- [ ] **SEN-6 — popup ไฮไลต์แถวกลุ่มที่ sensor เป็นสมาชิกอยู่** 👀
  - เพิ่ม sensor เข้ากลุ่มหนึ่ง
  - เปิด popup อีกครั้ง
  - **คาดหวัง:** แถวของกลุ่มนั้นมีพื้นหลังต่างจากแถวอื่น — มองปราดเดียวรู้

- [ ] **SEN-7 — ติ๊กชนิดโมเดลต่อกลุ่ม**
  - ใน popup ติ๊ก individual / relationship / clustering
  - **คาดหวัง:** ติ๊กได้หลายชนิดในกลุ่มเดียว และ sensor หนึ่งอยู่ได้หลายกลุ่มพร้อมกัน

- [ ] **SEN-8 — เอา sensor ออกจากกลุ่มสุดท้ายแล้วต้องหายไปเลย** 👀
  - ให้ sensor อยู่กลุ่มเดียว
  - ติ๊กออกจากกลุ่มนั้น
  - ไปดูที่ Not in Group
  - **คาดหวัง:** **หายไปเลย ไม่ไปโผล่ใต้ Not in Group** (จุดนี้เคยผิดและเพิ่งแก้)

- [ ] **SEN-9 — สร้างกลุ่มใหม่จากใน popup**
  - พิมพ์ชื่อในช่อง New group name แล้วสร้าง
  - **คาดหวัง:** กลุ่มใหม่ถูกสร้างและ sensor ตัวนั้นเข้ากลุ่มทันที

- [ ] **SEN-10 — เปลี่ยนชื่อ / ลบกลุ่ม**
  - เปลี่ยนชื่อกลุ่ม
  - ลบกลุ่มที่มีสมาชิก
  - **คาดหวัง:** ชื่อเปลี่ยนทุกที่ที่อ้างถึง, ลบกลุ่มแล้วสมาชิกไม่ค้างเป็นผี — ตั้งชื่อซ้ำกับกลุ่มเดิมควรถูกกัน

- [ ] **SEN-11 — ปุ่ม Add Special Sensor เปิดหน้าต่างลูก**
  - กดปุ่มล่างสุดของแผง Sensor
  - **คาดหวัง:** หน้าต่างใหม่เปิดมากลางจอ กดปุ่ม X ปิดได้

---

## SEL

### แท็บ Selected Sensor

- [ ] **SEL-1 — รายการแสดงข้อมูลครบ**
  - ดูแต่ละแถว
  - **คาดหวัง:** มี description เป็นบรรทัดหลัก และ `tag • unit` เป็นบรรทัดรอง สีตัวหนังสือตรงกับสีเส้นบนกราฟ

- [ ] **SEL-2 — ซ่อน/แสดงเส้นด้วย checkbox**
  - ติ๊ก checkbox ออก
  - ติ๊กกลับ
  - **คาดหวัง:** เส้นหาย/กลับมา และแถวจางลงตอนถูกซ่อน

- [ ] **SEL-3 — เปิด/ปิดเส้นแล้วกราฟต้องไม่กระพริบเปลี่ยนสี** 👀
  - เลือก sensor ไว้หลายตัว
  - ติ๊กเปิด-ปิดสลับไปมาเร็ว ๆ หลายรอบ
  - จ้องดูตอนเส้นกำลังหาย
  - **คาดหวัง:** **เส้นที่เหลือต้องคงสีเดิม ห้ามมีจังหวะที่เส้นเปลี่ยนเป็นอีกสีก่อนหาย** (บั๊กที่เพิ่งแก้ — เดิมสีมาจากลำดับ พอเอาตัวหนึ่งออกตัวหลังเลื่อน slot)

- [ ] **SEL-4 — เปลี่ยนสีเส้น**
  - กดวงสี → เลือกสีใหม่
  - ปิดเปิดโปรแกรม
  - **คาดหวัง:** สีเปลี่ยนทันทีทั้งกราฟและ swatch และยังอยู่หลังเปิดใหม่

- [ ] **SEL-5 — ตรึงช่วงแกน Y ต่อ sensor**
  - ตั้ง min/max ของ sensor หนึ่ง
  - ล้างค่าออก
  - **คาดหวัง:** แกนของเส้นนั้นถูกตรึงตามที่ตั้ง และล้างแล้วกลับไป auto

- [ ] **SEL-6 — เอา sensor ออกจากกราฟทีละตัว**
  - กดปุ่ม Remove from plot
  - **คาดหวัง:** หายจากกราฟและจากรายการ, ในแผง Sensor ติ๊กหลุดตาม

- [ ] **SEL-7 — Clear all**
  - กด Clear all
  - **คาดหวัง:** เอาออกหมด กราฟว่าง ขึ้นข้อความ "No sensors plotted yet"

- [ ] **SEL-8 — สลับไป Scatter / Pair Plot แล้ว control เปลี่ยน**
  - สลับ chart type
  - **คาดหวัง:** ปุ่มซ่อน/สี/แกน หายไป (เพราะใช้ไม่ได้กับ 2 โหมดนี้) และขึ้นกล่องน้ำเงินอธิบายวิธีใช้แทน

---

## FIL

### แท็บ Filter

_แผงนี้ใช้ draft state — แก้/เพิ่ม/ลบแถว filter ยังไม่มีผลกับกราฟจนกว่าจะกด **Apply Filter** เป็นการตั้งใจออกแบบ (กันไม่ให้ยิง query รัวทุกครั้งที่พิมพ์) ไม่ใช่บั๊ก — อย่าลืมกดปุ่มนี้ก่อนเช็คผลทุกข้อในหมวดนี้_

- [ ] **FIL-1 — เพิ่ม filter ทีละแบบ**
  - less than
  - greater than
  - between (min/max)
  - equals
  - กด Apply Filter
  - **คาดหวัง:** ทั้ง 4 แบบกรองได้จริงหลังกด Apply จำนวนจุดบน badge ลดลงตาม

- [ ] **FIL-2 — หลาย filter พร้อมกัน**
  - ใส่ filter 2-3 ตัวคนละ sensor
  - กด Apply Filter
  - **คาดหวัง:** รวมกันแบบ AND (ต้องผ่านทุกเงื่อนไข)

- [ ] **FIL-3 — ลบ filter**
  - กดปุ่มลบของแถว filter
  - กด Apply Filter
  - **คาดหวัง:** ข้อมูลกลับมา — **ต้องกด Apply Filter ก่อนถึงจะมีผล การลบแถวเฉย ๆ ยังไม่คืนค่าให้ทันที (เป็น draft state ที่ตั้งใจออกแบบไว้)**

- [ ] **FIL-4 — filter มีผลกับทุกโหมดกราฟ**
  - ตั้ง filter ไว้
  - สลับ Line → Scatter → Pair Plot
  - **คาดหวัง:** ทุกโหมดเห็นข้อมูลที่ถูกกรองชุดเดียวกัน

- [ ] **FIL-5 — filter บน sensor ที่ถูกเอาออกจากกราฟ**
  - ตั้ง filter บน sensor A
  - เอา A ออกจากกราฟ
  - **คาดหวัง:** ดูว่า filter หายตามหรือค้างอยู่ — บันทึกพฤติกรรมจริงไว้

---

## HL

### แท็บ Highlights

_By time ใช้กับ Line/Scatter · By value ใช้กับ Scatter เท่านั้น · Pair Plot ไม่อ่านแท็บนี้เลย (ตั้งใจ)_

- [ ] **HL-1 — By time — เพิ่มช่วงเวลา**
  - เพิ่มช่วง
  - ใส่ label
  - เปลี่ยนสี
  - **คาดหวัง:** แถบสีขึ้นบนกราฟตรงช่วงที่กำหนด

- [ ] **HL-2 — By time — toggle / rename / ลบ**
  - ปิด-เปิดช่วง
  - เปลี่ยนชื่อ
  - ลบ
  - **คาดหวัง:** ทำได้ครบและกราฟตามทันที

- [ ] **HL-3 — เพิ่มหลายช่วงพร้อมกัน**
  - เพิ่ม 3 ช่วงคนละสี
  - **คาดหวัง:** ขึ้นครบทุกช่วง ไม่ทับกันมั่ว

- [ ] **HL-4 — โหมดแสดงเส้นของ highlight**
  - สลับ line display mode
  - **คาดหวัง:** รูปแบบการแสดงเปลี่ยนตาม

- [ ] **HL-5 — By value (Scatter)**
  - สลับไป Scatter
  - เลือก sensor ที่จะไล่สี
  - เพิ่มช่วง min/max + สี
  - **คาดหวัง:** จุดในช่วงนั้นเปลี่ยนสีตาม

- [ ] **HL-6 — By value — toggle / ลบ / เปลี่ยนสี**
  - ปิด-เปิดช่วง
  - เปลี่ยนสี
  - ลบ
  - **คาดหวัง:** ทำได้ครบ

- [ ] **HL-7 — Pair Plot ไม่ถูก highlight กระทบ**
  - ตั้ง highlight ไว้
  - สลับไป Pair Plot
  - **คาดหวัง:** Pair Plot ไม่เปลี่ยนสีตาม (ตั้งใจให้ใช้ lasso cluster ของตัวเองแทน)

---

## TIME

### ช่วงเวลา & Aggregation

- [ ] **TIME-1 — เลือกช่วงเวลาด้วย Start / End**
  - ใส่วันเริ่ม-วันจบ
  - ดูไอคอนปฏิทินท้ายช่อง แล้วกดที่ไอคอน
  - **คาดหวัง:** กราฟตัดเหลือเฉพาะช่วงนั้น จำนวนจุดลดลง — ไอคอนปฏิทิน**สีขาว ชิดขอบขวาของช่อง เห็นชัด**และกดแล้วเปิดตัวเลือกวันที่ได้ (เคยมองไม่เห็นเลยเพราะใช้ไอคอนของเบราว์เซอร์)

- [ ] **TIME-2 — Relative range ยึดจากวันสุดท้ายของข้อมูล ไม่ใช่วันนี้ (แก้แล้ว 2026-09-22)** 🆕
  - ใช้ข้อมูลย้อนหลัง (ปีล่าสุดของข้อมูลไม่ใช่ปีนี้) ใส่จำนวน + เลือกหน่วย (Y/M/W/D/H)
  - กดปุ่ม ✓ Apply
  - **คาดหวัง:** ต้องกด Apply ก่อนถึงมีผล — ก่อนกด tooltip ควรบอกว่า "Selected — click ✓ Apply" — และช่วงที่ได้ต้องนับถอยหลังจาก**วันสุดท้ายของข้อมูล** (ดู label "Data: ..." ที่ TIME-8) **ไม่ใช่วันที่ปัจจุบันของเครื่อง** — เดิมนับจากวันนี้ ทำให้ข้อมูลย้อนหลังกด 1M/6M/1Y แล้วได้ 0 จุดเสมอถ้าปีข้อมูลไม่ตรงปีปัจจุบัน

- [ ] **TIME-3 — Reset Period**
  - กด Reset Period
  - **คาดหวัง:** ช่วงเวลากลับไปเต็มช่วงข้อมูล (ตรงกับ label "Data: ...") แต่ **Aggregation ต้องไม่ถูกรีเซ็ตไปด้วย**

- [ ] **TIME-4 — Aggregation ครบ 6 แบบ**
  - สลับ Raw / Avg / Max / Min / First / Last
  - **คาดหวัง:** เส้นเปลี่ยนรูปตามวิธี aggregate และ badge บนหัวกราฟเปลี่ยนตาม (เช่น `AVG (1h)`)

- [ ] **TIME-5 — ระยะห่างบนแกนเวลาต้องสัมพันธ์กับเวลาจริง (แก้แล้ว 2026-09-16)** 🆕
  - ตั้งช่วงกว้าง 10 นาที ดูระยะห่างระหว่างจุด
  - ตั้งช่วงกว้าง 10 ชั่วโมง ดูอีกครั้ง
  - ใช้ข้อมูลที่มีช่วงว่าง (เครื่องหยุด) คั่นกลาง
  - **คาดหวัง:** **ระยะห่างต้องไม่เท่ากัน** และช่วงที่ไม่มีข้อมูลต้องเห็นเป็นช่องว่างจริงบนแกน — แกน x ถูกเปลี่ยนจาก category เป็น time axis แล้ว (เดิมทุกจุดห่างเท่ากันไม่ว่าเวลาจะห่างแค่ไหน) ⚠ ข้อแลกเปลี่ยนที่รู้แล้ว: กราฟอาจรู้สึกช้ากว่าเดิมเล็กน้อย (ดู PERF-6)

- [ ] **TIME-6 — สร้าง project ใหม่ ครั้งแรกที่เข้า Dashboard ต้องเห็นข้อมูล 6 เดือนล่าสุดของ dataset เป็นค่าเริ่มต้น (แก้แล้ว 2026-09-22)** 🆕
  - Import CSV ใหม่ (project ที่ไม่เคยเปิดมาก่อน) เข้า Dashboard ครั้งแรก
  - ดูช่อง Start/End และปุ่มหน่วยที่ไฮไลต์
  - **คาดหวัง:** ช่วงเวลาตั้งต้นเป็น **6 เดือนล่าสุดของข้อมูลจริง** (ไม่ใช่ข้อมูลทั้งหมด และไม่ใช่ 6 เดือนล่าสุดจากวันนี้) ปุ่ม **M** ไฮไลต์แบบ "กำลังใช้งาน" (พื้นสีทึบ) ช่อง amount แสดง `6` — เดิม (ก่อนแก้) กราฟแสดงข้อมูลทั้งหมดของ dataset เป็นค่าเริ่มต้นเสมอ

- [ ] **TIME-7 — Label "Data: ..." บอกช่วงข้อมูลทั้งหมดเสมอ** 🆕
  - เปิด workspace ที่มีข้อมูล ดูข้อความ "Data: ..." ข้าง Reset Period
  - ลองตั้ง time filter แคบๆ ดูว่า label เปลี่ยนไหม
  - **คาดหวัง:** label แสดง timestamp แรก-สุดท้ายของ**ทั้ง dataset** ตรงกับไฟล์ต้นฉบับ และ**ไม่เปลี่ยนตาม filter ที่ตั้งไว้** (ต่างจากตัวเลข "N Points" ที่เปลี่ยนตาม filter)

- [ ] **TIME-8 — ค่าเริ่มต้น 6 เดือนไม่บังคับทับ workspace ที่เคยตั้ง/ล้างช่วงเวลาไว้แล้ว** 🆕
  - เปิด workspace ที่เคยกด Reset Period ไว้ (เคยดูข้อมูลทั้งหมด) ปิดโปรแกรมแล้วเปิดใหม่ กลับมาที่ workspace เดิม
  - **คาดหวัง:** ยังคงแสดงข้อมูลทั้งหมดเหมือนตอนปิด **ไม่ถูกดึงกลับไปเป็น 6 เดือนล่าสุด** — ค่าเริ่มต้น 6 เดือนใช้แค่ครั้งแรกสุดที่ project ยังไม่เคยตั้งช่วงเวลาอะไรเลยเท่านั้น

---

## LINE

### Line chart

- [ ] **LINE-1 — เส้นขึ้นครบและสีตรงกับ swatch**
  - เลือก 5-6 sensor
  - **คาดหวัง:** ครบทุกเส้น สีตรงกับที่แสดงในแท็บ Selected Sensor

- [ ] **LINE-2 — Tooltip**
  - เอาเมาส์ชี้บนกราฟ
  - **คาดหวัง:** แสดงเวลาและค่าของทุกเส้น ณ จุดนั้น — **ค่าต้องเป็นตัวเลขจริงพร้อมหน่วย** (เช่น `6425.308 KW`) ไม่ใช่ตัวเลข timestamp ดิบต่อท้าย (เคยเห็น `1756872000000,6425.3 KW` หลังเปลี่ยนเป็นแกนเวลา แก้แล้ว)

- [ ] **LINE-3 — Zoom — เมนู "Horizontal zoom / Zoom out" (แทนแถบเลื่อนเดิม)** 🆕
  - ดูใต้กราฟ: **ต้องไม่มีแถบเลื่อน (slider) แล้ว**
  - กดไอคอนแว่นขยายที่มุมบนขวาของกราฟ (ข้างไอคอน Tag) ให้เมนูเปิด
  - เลือก "Horizontal zoom" แล้วลากคลุมช่วงเวลาบนกราฟ ปล่อยเมาส์
  - ทำซ้ำเพื่อซูมลึกขึ้นอีกชั้น
  - เลือก "Zoom out"
  - **คาดหวัง:** เมนู**ไม่ถูกตัดขอบ**อ่านครบทั้งสองข้อความ, ขณะลากมีกรอบสีฟ้าโชว์ช่วงที่เลือก, **ปล่อยแล้วกราฟซูมเข้าช่วงนั้นจริง** (เคยลากได้แต่ปล่อยแล้วไม่ซูม), Zoom out ถอยกลับทีละขั้น ลื่นไหล ข้อมูลไม่เพี้ยน

- [ ] **LINE-3b — ขยาย Time Range หลังซูมไว้ กราฟต้องไม่ค้างช่วงเก่า** 🆕
  - ซูมเข้าไปช่วงแคบ ๆ
  - ขยายช่วงเวลาที่ Time Range ให้กว้างขึ้นมาก (เช่น 1 เดือน → 4 เดือน)
  - **คาดหวัง:** กราฟกลับมาแสดงเต็มช่วงใหม่ทั้งช่วง ไม่ค้างที่ช่วงที่ซูมไว้จนเห็นแค่ส่วนเดียว (เคยเป็นบั๊ก: แถบเลื่อนสั้นกว่าที่ควร ทำให้ดูเหมือนข้อมูลขาดหาย)

- [ ] **LINE-4 — ไม่มี legend บนตัวกราฟ — ซ่อน/แสดงเส้นทำที่แท็บ Selected Sensor เท่านั้น**
  - ดูใต้/รอบกราฟ Line ว่ามี legend ของ ECharts ให้กดหรือไม่
  - ไปที่แท็บ Selected Sensor แล้วติ๊ก checkbox ออก (ดู SEL-2)
  - **คาดหวัง:** **ยืนยันแล้วในโค้ด — ไม่มี ECharts legend อยู่บนกราฟเลย** (เอาออกไปแล้ว เพราะชื่อ sensor อยู่ในแท็บ Selected Sensor อยู่แล้ว) การซ่อน/แสดงเส้นทำได้ทางเดียวคือ checkbox ในแท็บนั้น — ถ้าเห็น legend โผล่มาบนกราฟจริง ๆ ถือเป็นเรื่องแปลกให้รายงาน

- [ ] **LINE-5 — เส้น alarm setpoint**
  - เปิด alarm ของ 2-3 sensor
  - **คาดหวัง:** เส้นขึ้นพร้อม label LL / L / H / HH และไม่บังเส้นข้อมูลจนอ่านไม่ออก

- [ ] **LINE-6 — Tagged points**
  - กดไอคอน Tag ที่มุมกราฟก่อน (ต้องเปิดโหมดนี้ก่อน คลิกกราฟเฉย ๆ โดยไม่เปิดโหมดจะไม่ทำอะไรเลย)
  - คลิกจุดบนเส้นเพื่อปัก tag
  - ปักหลายจุด
  - คลิกจุดที่ปักไว้แล้วซ้ำอีกที
  - กด Clear all tags
  - **คาดหวัง:** เปิดโหมดแล้วคลิกถึงปักได้ (ทดสอบซ้ำหลังเปลี่ยนเป็นแกนเวลา — เคยคลิกไม่ติดเลย แก้แล้ว), คลิกจุดที่ปักไว้ซ้ำ = เอาออก, Clear all ล้างหมดทีเดียว — tag พวกนี้ตั้งใจไม่ให้อยู่ข้ามการปิดโปรแกรม

- [ ] **LINE-7 — Time highlight บนเส้น**
  - ตั้ง highlight by time ไว้
  - **คาดหวัง:** แถบขึ้นตรงช่วงเวลาที่ถูกต้อง ไม่เลื่อนเวลาผิด timezone

- [ ] **LINE-8 — Downsample badge**
  - ดูข้อมูลเต็มช่วง
  - ย่อช่วงเวลาให้แคบลงเรื่อย ๆ
  - **คาดหวัง:** พอจุดน้อยกว่าเพดานแล้ว badge ต้องเลิกบอกว่า downsampled

---

## SCAT

### Scatter plot

_โหมดนี้คือตัวที่เคยพังทั้งหมดใน 0.4.0 เพราะ CSP — ให้ดูละเอียด_

- [ ] **SCAT-1 — ปุ่ม Scatter กดไม่ได้ตอนเลือก sensor ไม่ถึง 2 ตัว**
  - เลือก sensor แค่ 1 ตัว
  - **คาดหวัง:** ปุ่มถูก disable และ tooltip บอกว่า "Select at least 2 sensors"

- [ ] **SCAT-2 — จุดขึ้นจริงบนกราฟ** 👀
  - เลือก 2+ sensor แล้วกด Scatter
  - **คาดหวัง:** **เห็นจุดจริง ไม่ใช่พื้นที่ว่าง** — ถ้าว่างเปล่าคือ CSP `unsafe-eval` หายไปอีกแล้ว (regl คอมไพล์ draw command ตอน runtime)

- [ ] **SCAT-3 — เลือกแกน X / Y — dropdown แสดง description แทน tag เปล่าๆ แล้ว (แก้ 2026-09-22)** 🆕
  - ดู dropdown X และ Y เหนือกราฟ (มีป้าย "X"/"Y" เล็กๆ แยกอยู่ข้างนอกกล่อง)
  - เปลี่ยนค่าใน dropdown ทั้งสอง
  - **คาดหวัง:** แต่ละตัวเลือกใน dropdown เขียนเป็น "description (tag)" (ตัวที่ไม่มี description ในไฟล์ mapping ก็ยังเขียน tag เปล่าๆ ได้) **ไม่ใช่ "tag (X)"/"tag (Y)" แบบเดิม** — กล่อง dropdown กว้างพอที่ description จะไม่ถูกตัดขาด, กราฟเปลี่ยนตามที่เลือก และเลือกค่าที่ตั้งไว้ยังอยู่หลังปิดเปิดโปรแกรม

- [ ] **SCAT-4 — ตรึงสเกลแกน (ไอคอนไม้บรรทัด)**
  - ใส่ min/max ของแกน X
  - ใส่ของแกน Y
  - ล้างออก
  - **คาดหวัง:** สเกลถูกตรึงตามที่ใส่ และค่ายังอยู่หลังปิดเปิด

- [ ] **SCAT-5 — Reset view**
  - zoom/pan ไปไกล ๆ แล้วกด Reset view
  - **คาดหวัง:** กลับมามุมมองเริ่มต้น

- [ ] **SCAT-6 — Tagged points — แก้ให้ตรงกับของจริงแล้ว (ข้อเดิมพูดถึง "ลบ row id" ที่ไม่มีอยู่จริงในโค้ด)**
  - กดไอคอน Tag ที่มุมกราฟก่อน (ต้องเปิดโหมดนี้ก่อน)
  - คลิกจุดบนกราฟเพื่อปัก tag ให้ครบจำนวนสีในพาเลท (ลองปักเกินจำนวนสีที่มีดูด้วย)
  - คลิกจุดที่ปักไว้แล้วซ้ำอีกที
  - กด Clear all tags
  - **คาดหวัง:** เปิดโหมดแล้วคลิกถึงปักได้, คลิกจุดที่ปักไว้ซ้ำ = เอาออก, Clear all ล้างหมดทีเดียว — **ปักเกินจำนวนสีในพาเลทแล้วต้องไม่ปักเพิ่ม (เงียบ ๆ ไม่มี error) ไม่ใช่ปักทับสีซ้ำหรือ error** — ไม่มีช่องกรอก "row id" ให้ลบแบบข้อความ ทุกอย่างทำผ่านการคลิกจุดบนกราฟเท่านั้น

- [ ] **SCAT-7 — Highlight by value ระบายสีจุด**
  - ตั้งช่วงค่าใน Highlights → By value
  - **คาดหวัง:** จุดในช่วงเปลี่ยนสีถูกต้อง

- [ ] **SCAT-8 — สลับไป-กลับ Line ↔ Scatter หลายรอบ**
  - สลับสลับ 10 รอบ
  - **คาดหวัง:** ไม่มีจอดำ ไม่มี error ใน toast — ScatterChart ถูก unmount ทุกครั้งที่สลับ

---

## PAIR

### Pair Plot

- [ ] **PAIR-1 — เพดานจำนวน sensor**
  - เลือก sensor เกินเพดานแล้วกด Pair Plot
  - **คาดหวัง:** ขึ้น dialog อธิบายว่ารองรับได้กี่ตัวและตอนนี้เลือกไว้กี่ตัว ไม่ใช่เงียบ ๆ

- [ ] **PAIR-2 — ทุก cell วาดครบ** 👀
  - เลือก 4 sensor แล้วเปิด Pair Plot
  - **คาดหวัง:** **ทุกช่องในตารางมีจุดจริง ไม่มีช่องว่างเปล่า** (CSP อีกจุด)

- [ ] **PAIR-3 — Lasso select**
  - เลือกโหมด lasso
  - ลากล้อมจุดในช่องหนึ่ง
  - **คาดหวัง:** จุดที่ล้อมกลายเป็น cluster สีเดียวกันทุกช่อง

- [ ] **PAIR-4 — สร้างหลาย cluster**
  - lasso อีก 2 ครั้ง
  - **คาดหวัง:** ได้ 3 cluster คนละสี, เปลี่ยนสีและลบทีละอันได้

- [ ] **PAIR-5 — แก้ไขแล้ว — ไม่มีปุ่ม Enable/Disable ทั้งหมด (ไม่มีอยู่ในโค้ด) มีแค่ลบทีละ cluster**
  - สร้าง cluster ไว้ 3 อัน
  - ลบทีละอันจากปุ่มถังขยะที่แถวนั้น
  - ดูตัวเลข "N clusters · M rows" ที่หัวแผง
  - ลบจนหมดทุกอัน
  - **คาดหวัง:** **ไม่มีปุ่ม "เปิด/ปิดทั้งหมด" อยู่จริงในแอป** — แต่ละแถวมีแค่วงกลมสี (คลิกเปลี่ยนสีได้ ใช้ native color picker ของ OS ไม่ใช่ ColorPlatePicker แบบจุดอื่น) กับปุ่มถังขยะเท่านั้น ไม่มีปุ่มซ่อนชั่วคราว — ตัวเลขหัวแผงต้องลดตามทุกครั้งที่ลบ และพอลบหมดแล้ว**แผง cluster ทั้งกล่องต้องหายไปเลย** ไม่ใช่โชว์ "0 clusters" ค้างอยู่

- [ ] **PAIR-6 — ดูแถวที่ brush ไว้เป็นตาราง**
  - กด View brushed rows
  - **คาดหวัง:** ตารางเปิดขึ้นและแถวตรงกับจุดที่เลือก

- [ ] **PAIR-7 — ดาวน์โหลดแถวที่เห็นเป็น CSV**
  - กด Download visible rows as CSV
  - เปิดไฟล์ที่ได้
  - **คาดหวัง:** ไฟล์ถูกเขียนลงเครื่องจริงและข้อมูลตรง

- [ ] **PAIR-8 — Pan / zoom mode**
  - สลับไปโหมด pan/zoom แล้วลาก
  - **คาดหวัง:** เลื่อน/ซูมได้ ไม่ไปสร้าง cluster แทน

- [ ] **PAIR-9 — Reset view + drop clusters**
  - กดปุ่ม reset ทั้งสองแบบ
  - **คาดหวัง:** อันหนึ่งแค่ล้าง selection อีกอันทิ้ง cluster ทั้งหมด — ต่างกันจริง

- [ ] **PAIR-10 — เลือก sensor เยอะสุดที่เพดานอนุญาต**
  - เลือกให้เต็มเพดานแล้วเปิด Pair Plot
  - สลับไป-กลับหลายรอบ
  - **คาดหวัง:** ไม่มี error "(regl) context lost" ท่วมจอ

---

## SPC

### Add Special Sensor — แท็บ Create

- [ ] **SPC-1 — หน้าต่างเปิดและมี 2 แท็บ**
  - เปิดหน้าต่าง
  - **คาดหวัง:** หัวหน้าต่างเขียน "Special Sensors" และมีแท็บ Create กับ Manage

- [ ] **SPC-2 — Explorer ฝั่งซ้าย**
  - ค้นหา
  - ติ๊กเลือก sensor
  - ลากเส้นแบ่งกลาง
  - **คาดหวัง:** ค้นได้ ติ๊กได้ ลากปรับความกว้างได้ (split.js)

- [ ] **SPC-3 — Operation แบบ single**
  - เลือก sensor 1 ตัว
  - ลอง add / subtract / multiply / divide / power (ต้องใส่ค่า)
  - ลอง abs / sqrt / log10 / exp / ceil / floor (ไม่ต้องใส่ค่า)
  - **คาดหวัง:** คำนวณถูกทุกแบบ — เทียบค่าสัก 2-3 จุดกับที่คิดเอง

- [ ] **SPC-4 — Operation แบบ multi**
  - เลือก 2-3 sensor
  - ลอง sum / mean / median
  - ลอง temp_spread / abs_diff / efficiency_pct
  - **คาดหวัง:** ได้ผลถูกต้อง (3 ตัวหลังเป็น formula เบื้องหลัง จะไปโผล่ในแท็บ Manage เป็นชนิดสูตร)

- [ ] **SPC-5 — Formula mode**
  - พิมพ์ `$TagName * 2`
  - พิมพ์ `${Tag With Space} + 1`
  - ลองใช้ autocomplete
  - **คาดหวัง:** ทั้งสอง syntax ใช้ได้ และ autocomplete เสนอเฉพาะ sensor ที่ติ๊กไว้

- [ ] **SPC-6 — บังคับกรอกครบ 4 ช่องก่อนกด Add sensor ได้** 🆕
  - ตั้งสูตรหรือ operation ไว้ (ทำให้ปรากฏช่อง Name/Description/Unit/Component)
  - กรอกแค่บางช่อง เว้นที่เหลือว่างไว้ทีละช่อง แล้วดูปุ่ม Add sensor
  - กรอกครบทั้ง 4 ช่อง
  - **คาดหวัง:** ปุ่ม **Add sensor จางลงกดไม่ได้** ตราบใดที่ยังขาดช่องใดช่องหนึ่งจาก Name/Description/Unit/Component ข้อความใต้ปุ่มบอกชัดว่าขาดช่องไหนบ้าง (เช่น "Fill in a description, a unit, a component before adding.") และอัปเดตสดตามที่กรอก — พอครบทั้ง 4 ปุ่มถึงจะกดได้ **(เดิมเช็คแค่ Name และเช็คแบบ error หลังกดไปแล้ว ตอนนี้เช็คครบ 4 ช่องแบบ disable ปุ่มไว้ก่อน)**

- [ ] **SPC-7 — ใส่ description / unit / component (บังคับทั้ง 3 ช่อง) — Component เลือกจากรายการ**
  - ดูว่าทั้ง 4 label (Name/Description/Unit/Component) มีเครื่องหมาย * สีแดงกำกับ
  - เปิดช่อง Component
  - กรอกครบทั้ง 4 ช่องแล้วกด Add sensor
  - **คาดหวัง:** Component เป็น **dropdown เลือกจาก component ที่มีอยู่ใน workspace เท่านั้น** (พิมพ์เองไม่ได้ มีตัวเลือก "Uncategorized" ให้เลือกเอง) — สร้างสำเร็จ และค่าไปโผล่ในแท็บ Selected Sensor ของ Dashboard ครบทั้ง 3 ช่อง (Description/Unit/Component) และ sensor ใหม่ไปอยู่ใต้ component ที่เลือกจริงในแผง Sensor (ไม่หล่นไปอยู่ Uncategorized เงียบ ๆ)

- [ ] **SPC-8 — สร้างแล้วหน้าต่างไม่ปิด**
  - กด Add sensor
  - **คาดหวัง:** ขึ้น toast ✓, ฟอร์มถูกล้างพร้อมสร้างตัวถัดไป, หน้าต่างยังเปิดอยู่, กราฟฝั่ง Dashboard อัปเดตทันที

- [ ] **SPC-9 — สร้างตัวที่ 2 ต่อยอดจากตัวแรก**
  - สร้าง A จาก sensor ดิบ
  - สร้าง B โดยใช้ A เป็น input
  - **คาดหวัง:** A ถูกเลือกเป็น input ได้ทันทีโดยไม่ต้องปิดเปิดหน้าต่าง

- [ ] **SPC-10 — sensor ใหม่โผล่ในแผง Sensor ของ Dashboard**
  - ปิดหน้าต่าง แล้วดูแผง Sensor
  - **คาดหวัง:** มีอยู่ในรายการและค้นหาเจอ

- [ ] **SPC-11 — สูตรผิด**
  - พิมพ์สูตรที่ syntax เสีย
  - พิมพ์สูตรอ้าง sensor ที่ไม่มี
  - พิมพ์สูตรที่ซ้อนวงเล็บลึกมาก
  - **คาดหวัง:** ทุกกรณีขึ้น error อ่านรู้เรื่อง ไม่ crash หน้าต่าง

- [ ] **SPC-12 — ตั้งชื่อ special sensor ซ้ำกับคอลัมน์ดิบ**
  - ตั้งชื่อให้ตรงกับ tag ที่มีอยู่ใน CSV
  - **คาดหวัง:** ดูว่าเกิดอะไรขึ้น — คอลัมน์ดิบต้องไม่ถูกทับ (การสร้างตั้งใจให้ append ไม่ replace) จดพฤติกรรมจริงไว้

- [ ] **SPC-13 — ปุ่มเครื่องหมาย (+/−/×/÷) ต้องปิดจริงตอนเลือก shortcut ไว้** 🆕
  - เลือก sensor ตั้งแต่ 2 ตัวขึ้นไป
  - เลือก shortcut เช่น "Sum all" หรือ "Compare one against the rest"
  - ดูแถบ "Combine with operators" (เครื่องหมาย +/−/×/÷ ระหว่างแต่ละคู่ sensor)
  - ลองกดเครื่องหมายตัวใดตัวหนึ่ง
  - เลิกเลือก shortcut (กดซ้ำ) แล้วลองกดเครื่องหมายอีกที
  - **คาดหวัง:** ตอนเลือก shortcut ไว้ แถบเครื่องหมายต้องจางลง กดแล้ว**ไม่มีอะไรเกิดขึ้นเลย** (ไม่เปิด popover เลือกเครื่องหมายใหม่ ไม่เผลอไปยกเลิก shortcut) มีข้อความอธิบายว่า "A shortcut below is selected..." — พอเลิกเลือก shortcut แถบต้องสว่างขึ้นและกดได้ปกติทันที (ก่อนหน้านี้กดได้ตลอดแม้เลือก shortcut ไว้ ซึ่งเผลอไปยกเลิก shortcut แบบไม่มีอะไรโชว์ให้เห็น)

- [ ] **SPC-14 — แก้ไขตัวเลือก sensor ระหว่างตั้งค่า ไม่ทำให้ Description/Unit/Component หาย** 🆕
  - เลือก sensor ไว้ 1 ตัว
  - กรอก Description, Unit, Component ไว้ (ยังไม่กด Add sensor)
  - เพิ่มหรือลด sensor อีกตัวหนึ่งในการเลือก (เช่น ติ๊กเพิ่มอีกตัว หรือเอาตัวหนึ่งออกแต่ไม่ให้เหลือ 0 ตัว)
  - **คาดหวัง:** **ค่าที่กรอกไว้ทั้ง 3 ช่องต้องยังอยู่ครบ ไม่หายไปเงียบ ๆ** — ช่องพวกนี้อธิบาย sensor ใหม่ที่กำลังจะสร้าง ไม่ใช่ตัวที่เลือกไว้เป็น input จึงไม่ควรถูกล้างตามการปรับตัวเลือก (ช่องจะถูกล้างเฉพาะตอนเอา sensor ออกจนเหลือ 0 ตัวเท่านั้น — นั่นคือ "เริ่มใหม่")

---

## MNG

### Manage — ลบ special sensor

_ของใหม่ทั้งหมด ยังไม่เคยมีคนกดจริง_

- [ ] **MNG-1 — แท็บ Manage แสดงรายการครบ** 🆕
  - สร้าง special sensor ไว้ 3-4 ตัว ทั้งแบบสูตรและ operation
  - เปิดแท็บ Manage
  - **คาดหวัง:** มีครบทุกตัว, หัวแท็บบอกจำนวน `Manage (N)`, badge ƒ = สูตร / Σ = operation

- [ ] **MNG-2 — สูตร/operation อ่านออก** 🆕
  - ดูคอลัมน์ขวาของแต่ละแถว
  - **คาดหวัง:** สูตรแสดงตามที่พิมพ์, operation แสดงเป็น `sum(a, b)` หรือ `a + 10` ไม่ใช่ JSON ดิบ

- [ ] **MNG-3 — ช่องค้นหา** 🆕
  - พิมพ์ชื่อ
  - พิมพ์คำใน description
  - พิมพ์บางส่วนของสูตร
  - **คาดหวัง:** ค้นเจอทั้ง 3 แบบ และตัวเลข `x / y` มุมขวาถูกต้อง

- [ ] **MNG-4 — ลบตัวที่ไม่มีใครใช้** 🆕
  - หา special sensor ที่ไม่ได้ปักบนกราฟและไม่มีใครสร้างต่อ
  - กดถังขยะ
  - **คาดหวัง:** แถวหายทันทีโดยไม่ถาม และมีแถบ Undo โผล่ขึ้นมา

- [ ] **MNG-5 — กด Undo ภายใน 8 วินาที** 🆕
  - ลบแล้วรีบกด Undo
  - **คาดหวัง:** แถวกลับมาเหมือนเดิม และ**ฝั่ง Dashboard ต้องไม่เคยรู้เรื่องการลบเลย** (กราฟไม่กระพริบ)

- [ ] **MNG-6 — ปล่อยครบ 8 วินาที** 🆕
  - ลบแล้วนับ 1-10 ไม่ต้องแตะอะไร
  - ไปดูแผง Sensor ฝั่ง Dashboard
  - **คาดหวัง:** sensor หายจากแผง Sensor และจากกราฟ

- [ ] **MNG-7 — ลบตัวที่กำลังปักอยู่บนกราฟ** 🆕
  - ปัก special sensor ตัวหนึ่งบนกราฟ
  - ไปลบมันในแท็บ Manage
  - **คาดหวัง:** chip "on chart" ขึ้นที่แถว แต่ปุ่มลบยัง**กดได้** และพอลบจริงเส้นหายจากกราฟให้อัตโนมัติ

- [ ] **MNG-8 — ลบตัวที่มี special sensor อื่นสร้างต่อ → ต้องกดไม่ได้** 🆕
  - สร้าง A
  - สร้าง B โดยใช้ A
  - ไปลบ A
  - **คาดหวัง:** ปุ่มลบของ A ถูกปิด, มี chip 🔒 "Used by 1 sensor" กดแล้วกางบอกว่า B ใช้อยู่ และบอกให้ลบ B ก่อน

- [ ] **MNG-9 — ลบตัวที่โมเดลใน Failure Group ใช้อยู่ → ต้องกดไม่ได้** 🆕
  - เอา special sensor ไปเป็น target ของโมเดลหนึ่ง
  - ไปลบมัน
  - **คาดหวัง:** ปุ่มลบถูกปิด, chip บอกจำนวนโมเดล, กางแล้วเห็นชื่อโมเดล + ช่องที่อ้าง (เช่น "— target sensor")

- [ ] **MNG-10 — เช็คให้ครบทั้ง 7 ช่องของโมเดล** 🆕
  - ทดลองเอา special sensor ไปใส่ทีละช่อง: target sensor, predictor, X sensor, Y sensor, criteria sensor, scatter X sensor, PM filter
  - แต่ละครั้งไปดูว่าปุ่มลบถูกปิดไหม
  - **คาดหวัง:** **ทั้ง 7 ช่องต้องบล็อกการลบ** — ถ้าช่องไหนหลุด จะเกิดโมเดลที่ชี้ไป sensor ที่ไม่มีอยู่แบบเงียบ ๆ

- [ ] **MNG-11 — ลบสายโซ่จากปลายเข้าหาต้น** 🆕
  - สร้าง A → B → C
  - ลบ C
  - ดูปุ่มของ B
  - ลบ B
  - ดูปุ่มของ A
  - **คาดหวัง:** ลบได้ทีละตัวจากปลาย และตัวก่อนหน้าปลดล็อกทันทีที่ตัวถัดไปถูกลบ (ไม่ต้องปิดเปิดหน้าต่าง)

- [ ] **MNG-12 — ชื่อที่เป็นส่วนหนึ่งของอีกชื่อ** 🆕
  - สร้าง special sensor ชื่อ `test`
  - สร้างอีกตัวชื่อ `test extend`
  - สร้างตัวที่ 3 ที่ใช้สูตร `${test extend} + 1`
  - ไปดูปุ่มลบของ `test`
  - **คาดหวัง:** **`test` ต้องลบได้** (ไม่มีใครอ้างมันจริง) ส่วน `test extend` ต้องลบไม่ได้ — นี่คือเหตุผลที่ต้องแยก parser ไปฝั่ง Rust แทนการเช็คแบบ substring

- [ ] **MNG-13 — ปิดหน้าต่างขณะยังมี Undo ค้าง** 🆕
  - ลบ 1 ตัว
  - รีบกด Close ทันที
  - **คาดหวัง:** ถือว่ายืนยันการลบ — sensor หายจริงฝั่ง Dashboard

- [ ] **MNG-14 — ลบแล้วปิดเปิดโปรแกรม** 🆕
  - ลบ special sensor 1 ตัวให้ครบกระบวนการ
  - ปิดโปรแกรม เปิดใหม่ เปิด workspace เดิม
  - **คาดหวัง:** **ไม่กลับมาอีก** — ตัว recipe ถูกลบออกจาก workspace แล้ว จึงไม่ถูก replay ตอนเปิด

- [ ] **MNG-15 — หน้าจอตอนยังไม่มี special sensor เลย** 🆕
  - เปิด workspace ที่ไม่เคยสร้าง special sensor
  - เปิดแท็บ Manage
  - **คาดหวัง:** ขึ้นข้อความ "No special sensors yet" พร้อมบอกให้ไปสร้างที่แท็บ Create

---

## EDT

### Manage — แก้ไข special sensor

_ของใหม่ทั้งหมด และเป็นส่วนที่ซับซ้อนที่สุดที่ทำไป_

- [ ] **EDT-1 — กดดินสอเปิดฟอร์มแก้ไข** 🆕
  - กดไอคอนดินสอที่แถวหนึ่ง
  - กดซ้ำอีกที
  - **คาดหวัง:** ฟอร์มกางออกใต้แถวนั้น กดซ้ำแล้วปิด และเปิดได้ทีละแถวเท่านั้น

- [ ] **EDT-2 — เปลี่ยนชื่อ (rename) ได้แล้ว — กรณีพื้นฐานไม่มีอะไรอ้างถึง** 🆕
  - เปิดแก้ special sensor ที่ไม่มีตัวอื่นสร้างต่อและไม่มีโมเดลใช้อยู่
  - ลบชื่อในช่อง Name ออก พิมพ์ชื่อใหม่
  - Save changes
  - **คาดหวัง:** บันทึกสำเร็จ, แถวในรายการเปลี่ยนเป็นชื่อใหม่ทันที, ชื่อเดิมหายไปจากรายการ (ไม่ใช่มีทั้งชื่อเก่าและใหม่ค้างอยู่พร้อมกัน) — **เดิมช่องนี้ถูกล็อกแก้ไม่ได้เลย ตอนนี้เปิดให้แก้ได้แล้ว** (ดู EDT-19 ถึง EDT-26 ท้ายหมวดนี้สำหรับเคสที่ซับซ้อนกว่า)

- [ ] **EDT-3 — แก้สูตรแล้วค่าบนกราฟเปลี่ยนทันที** 🆕
  - ปัก special sensor ตัวหนึ่งบนกราฟ จำรูปเส้นไว้
  - แก้สูตรจาก `* 2` เป็น `* 5`
  - กด Save changes
  - **ห้ามติ๊ก sensor ออก-เข้า**
  - **คาดหวัง:** **เส้นบนกราฟเปลี่ยนเองภายในไม่กี่วินาที** โดยไม่ต้องไปแตะอะไรอีก (นี่คือสิ่งที่ `dataRevision` ทำ)

- [ ] **EDT-4 — แก้ description / unit / component** 🆕
  - แก้ทั้ง 3 ช่องแล้ว Save
  - ไปดูแท็บ Selected Sensor
  - **คาดหวัง:** ข้อความใหม่ขึ้นทั้งในแท็บ Manage และในรายการ Selected Sensor

- [ ] **EDT-4b — บังคับกรอกครบ 4 ช่องในฟอร์มแก้ไข (เหมือน Create) — Component เลือกจากรายการ** 🆕
  - เปิดแก้ special sensor ที่มีข้อมูลครบอยู่แล้ว
  - ลบ Name / Description / Unit ออกทีละช่องให้ว่าง
  - ดูช่อง Component
  - **คาดหวัง:** ปุ่ม **Save changes จางลงกดไม่ได้ทันที** พร้อมข้อความบอกว่าขาดช่องไหน — Component เป็น dropdown เลือกจากรายการที่มีอยู่ (รวม "Uncategorized") จึงเว้นว่างไม่ได้ ไม่มี fallback เงียบ ๆ อีกต่อไป

- [ ] **EDT-5 — แก้ operation — เปลี่ยน source sensors** 🆕
  - เปิดแก้ตัวที่เป็น operation
  - กด X ลบ chip ของ sensor หนึ่งออก
  - เลือกตัวใหม่จาก dropdown
  - Save
  - **คาดหวัง:** ค่าคำนวณใหม่ตาม input ชุดใหม่ และ dropdown ต้องไม่เสนอชื่อตัวมันเอง และไม่เสนอตัวที่เลือกไปแล้ว

- [ ] **EDT-6 — แก้ operation — เปลี่ยนชนิดและค่า** 🆕
  - เปลี่ยน operation จาก Add เป็น Multiply
  - เปลี่ยนตัวเลข Value
  - เปลี่ยนไปเป็น Square root
  - **คาดหวัง:** คำนวณถูก และพอเลือก op ที่ไม่ต้องใช้ค่า (sqrt/abs/...) ช่อง Value ต้องหายไปเอง

- [ ] **EDT-7 — จำนวน source sensor ต้องถูกต้อง** 🆕
  - operation แบบ single: ลองเพิ่มเป็น 2 ตัว
  - operation แบบ multi: ลองลดเหลือ 1 ตัว
  - **คาดหวัง:** ปุ่ม Save ถูกปิด และขึ้นข้อความบอกว่าต้องเลือกกี่ตัว

- [ ] **EDT-8 — ⭐ แก้ตัวต้นน้ำแล้วตัวปลายน้ำต้องเปลี่ยนตาม** 🆕
  - สร้าง A = `$TAG * 2`
  - สร้าง B = `${A} + 10`
  - สร้าง C = `${B} * 3`
  - ปักทั้ง A B C บนกราฟ จดค่าไว้
  - แก้ A เป็น `$TAG * 5`
  - Save
  - **คาดหวัง:** **ทั้ง B และ C ต้องเปลี่ยนค่าตามด้วย ไม่ใช่แค่ A** — toast ควรบอกว่า "recomputed 2 sensor(s) built on it". ถ้า B/C ไม่ขยับ = การ replay สายโซ่ไม่ทำงาน

- [ ] **EDT-9 — แก้ให้เกิดวงจร → ต้องถูกปฏิเสธ** 🆕
  - มี A → B → C อยู่แล้วจาก EDT-8
  - แก้ A ให้เป็น `${B} + 1`
  - Save
  - **คาดหวัง:** ขึ้น error สีแดงบอกว่าจะทำให้ขึ้นกับตัวเอง พร้อมบอกชื่อ B, ฟอร์มไม่ปิด, และ**ค่าของ A/B/C ต้องไม่ถูกแตะเลย**

- [ ] **EDT-10 — แก้ให้อ้างตัวเอง → ต้องถูกปฏิเสธ** 🆕
  - แก้ A ให้เป็น `${A} + 1`
  - Save
  - **คาดหวัง:** ถูกปฏิเสธเช่นกัน — ถ้าปล่อยผ่านค่าจะไต่ขึ้นทุกครั้งที่กด Save

- [ ] **EDT-11 — อ้างตัวที่อยู่ต้นน้ำได้ ไม่ใช่วงจร** 🆕
  - ในช่องสูตร (Formula) ของ C พิมพ์ทับข้อความเดิมด้วย `${A} + ${B}` โดยตรง (ไม่มีปุ่ม "รวม/combine" แยกต่างหาก — สูตรเป็นช่องข้อความที่พิมพ์ได้อิสระ)
  - Save
  - **คาดหวัง:** **ต้องผ่าน** — C อยู่ปลายน้ำอยู่แล้ว การอ้างขึ้นไปข้างบนไม่ใช่วงจร

- [ ] **EDT-12 — สูตรที่ไม่อ้าง sensor เลย** 🆕
  - แก้สูตรเป็น `42`
  - Save
  - **คาดหวัง:** ถูกปฏิเสธพร้อมบอกให้ใช้ `$Name` หรือ `${Name With Spaces}`

- [ ] **EDT-13 — สูตรอ้าง sensor ที่ไม่มีอยู่** 🆕
  - แก้สูตรให้อ้างชื่อมั่ว ๆ
  - Save
  - **คาดหวัง:** ขึ้น error ที่มีคำว่า "Could not recompute" และชื่อ sensor ที่พัง, ฟอร์มยังเปิดอยู่ให้แก้ต่อ

- [ ] **EDT-14 — ⭐ ปิดเปิดโปรแกรมแล้วต้องไม่มีคอลัมน์ซ้ำ** 🆕
  - แก้สูตรของ special sensor สัก 2 ตัว
  - ปิดโปรแกรมสนิท เปิดใหม่ เปิด workspace เดิม
  - ไปดูแผง Sensor ว่าชื่อนั้นมีกี่รายการ
  - ปักบนกราฟดูว่าค่าคือสูตรใหม่
  - **คาดหวัง:** **ต้องมีชื่อละ 1 รายการเท่านั้น และค่าคือสูตรใหม่** — นี่คือจุดที่ต้องแก้ Rust ให้ทับคอลัมน์แทนการต่อท้าย ถ้าเห็นชื่อซ้ำ 2 อัน แปลว่า `replace` ไม่ทำงาน

- [ ] **EDT-15 — แก้แล้ว Scatter / Pair Plot อัปเดตด้วย** 🆕
  - เปิด Scatter โดยใช้ special sensor เป็นแกนหนึ่ง
  - ไปแก้สูตรของมัน
  - กลับมาดู Scatter
  - **คาดหวัง:** จุดขยับตามสูตรใหม่โดยไม่ต้องสลับ chart type ไป-กลับ

- [ ] **EDT-16 — แก้ตัวที่ลบไม่ได้ (มีคนใช้อยู่)** 🆕
  - หา special sensor ที่ปุ่มลบถูกปิด
  - กดดินสอ
  - **คาดหวัง:** **แก้ได้ตามปกติ** — การมีคนใช้อยู่บล็อกแค่การลบ ไม่บล็อกการแก้ (เพราะมีการ recompute สายโซ่ให้)

- [ ] **EDT-17 — Cancel ไม่บันทึกอะไร** 🆕
  - แก้สูตรมั่ว ๆ แล้วกด Cancel
  - เปิดฟอร์มใหม่
  - **คาดหวัง:** ค่ากลับเป็นของเดิม ไม่มีอะไรถูกคำนวณใหม่

- [ ] **EDT-18 — แก้ special sensor ที่โมเดลใช้เป็น target อยู่** 🆕
  - เอา special sensor ไปเป็น target ของโมเดล
  - กลับมาแก้สูตรของมัน
  - ไปดูโมเดลนั้น
  - **คาดหวัง:** โมเดลยังชี้ที่ sensor เดิมได้ (ชื่อไม่เปลี่ยน) และถ้ามีการ preview/train ต้องใช้ค่าใหม่

- [ ] **EDT-19 — ⭐ Rename ตัวที่มี special sensor อื่นสร้างต่อ (สายโซ่)** 🆕
  - สร้าง A = `$TAG * 2`
  - สร้าง B = `${A} + 10`
  - ปักทั้ง A และ B บนกราฟ จดค่าไว้
  - เปิดแก้ A เปลี่ยนชื่อเป็น `A2` เท่านั้น (ไม่แตะสูตร)
  - Save changes
  - **คาดหวัง:** บันทึกสำเร็จ, แถวของ A เปลี่ยนชื่อเป็น A2 ทันที, **ค่าของ B ต้องไม่เปลี่ยนเลย** (ข้อมูลเบื้องหลังไม่ได้เปลี่ยน แค่ชื่ออ้างอิงเปลี่ยน), เปิดดูสูตรของ B ต้องอ่านว่า `${A2} + 10` — **ไม่ใช่ `${A} + 10` ที่ค้างชี้ไปชื่อที่ไม่มีอยู่แล้ว**

- [ ] **EDT-20 — Rename ตัวที่เป็น operation (ไม่ใช่ formula) ที่มีตัวอื่นสร้างต่อ** 🆕
  - สร้าง A ด้วย operation เช่น sum ของ 2 sensor
  - สร้าง B ที่มี A เป็นหนึ่งใน source sensors (ผ่านปุ่ม combine)
  - เปิดแก้ A เปลี่ยนชื่อเป็น `A2`
  - Save changes
  - **คาดหวัง:** เปิดแก้ B อีกที — ชิป source sensor ที่เคยเป็น A ต้องเปลี่ยนเป็น A2 อัตโนมัติ (ไม่ใช่แค่ formula-kind ที่ต้องอัปเดตตาม operation-kind ก็ต้องเหมือนกัน)

- [ ] **EDT-21 — Rename ชื่อซ้ำ → ต้องกดไม่ได้** 🆕
  - เปิดแก้ special sensor ตัวหนึ่ง
  - พิมพ์ชื่อให้ตรงกับ raw sensor ที่มาจาก CSV
  - ลองพิมพ์ชื่อให้ตรงกับ special sensor ตัวอื่นอีกที
  - ลองสลับตัวพิมพ์เล็กใหญ่ (เช่น `TAG1` → `tag1`)
  - **คาดหวัง:** ทุกกรณีขึ้นข้อความ "... is already in use by another sensor" สีแดง และปุ่ม **Save changes จางลงกดไม่ได้** — เช็คชื่อซ้ำแบบไม่สนตัวพิมพ์เล็กใหญ่ (เหมือนจุดอื่นในแอปทั้งหมด)

- [ ] **EDT-22 — Rename ชื่อว่าง → ต้องกดไม่ได้** 🆕
  - ลบชื่อในช่อง Name ออกให้ว่างเปล่า หรือมีแต่ space
  - **คาดหวัง:** ขึ้นข้อความ "Name is required" และปุ่ม Save changes กดไม่ได้

- [ ] **EDT-23 — ⭐ Rename แล้วของที่ผูกกับกราฟต้องตามไปครบ** 🆕
  - ปักบนกราฟ + ตั้งสีเส้นเอง + ตรึงแกน Y + เปิด alarm line (ถ้ามี setpoint) ของ special sensor ตัวหนึ่ง
  - ถ้าอยู่ใน Scatter ให้ตั้งเป็นแกน X หรือ Y ด้วย
  - ในแท็บ Highlights → By value ตั้งให้ไล่สีตาม sensor ตัวนี้
  - เปิดแก้แล้วเปลี่ยนชื่อ, Save
  - ไปเช็คทุกจุดที่ตั้งไว้ทีละจุด
  - **คาดหวัง:** **ทุกจุด (สีเส้น / แกน Y ที่ตรึง / alarm line / แกน scatter / highlight by value) ต้องยังผูกกับ sensor ตัวเดิมอยู่ครบ ภายใต้ชื่อใหม่** — ห้ามมีจุดไหนรีเซ็ตกลับ default หรือหายไปเงียบ ๆ

- [ ] **EDT-24 — ⭐ Rename แล้ว Failure Group model ต้องตามไปด้วย** 🆕
  - เอา special sensor ไปเป็น target ของโมเดล individual (หรือ predictor ของ relationship / X-Y ของ clustering)
  - เปิดแก้ special sensor นั้นแล้วเปลี่ยนชื่อ, Save
  - ไปดูโมเดลนั้นทั้งในแท็บ Failure Groups และในหน้าต่าง Build Model
  - **คาดหวัง:** โมเดลยังชี้ไปที่ sensor ตัวเดิม (แสดงชื่อใหม่) — **ไม่กลายเป็นช่องว่างเปล่า**

- [ ] **EDT-25 — Rename แล้วปิดเปิดโปรแกรม** 🆕
  - ทำ rename ให้ครบกระบวนการ (เปลี่ยนชื่อ + มีของผูกอยู่แบบ EDT-23/24)
  - ปิดโปรแกรมสนิท เปิดใหม่ เปิด workspace เดิม
  - **คาดหวัง:** ชื่อใหม่ยังอยู่ ไม่ย้อนกลับไปชื่อเดิม และทุกอย่างที่ผูกไว้ใน EDT-23/24 ยังอยู่ครบเหมือนก่อนปิดโปรแกรม

- [ ] **EDT-26 — บังคับกรอกครบ 4 ช่องในฟอร์มแก้ไข (เหมือน Create) — ดู EDT-4b ด้านบนด้วย** 🆕
  - เปิดแก้ special sensor ตัวหนึ่ง
  - ลบ Name หรือ Description หรือ Unit ออกทีละช่อง
  - **คาดหวัง:** ปุ่ม Save changes จางลงกดไม่ได้ทุกกรณีที่ขาดช่องใดช่องหนึ่งจาก 4 ช่อง (Name/Description/Unit/Component) — ไม่ใช่แค่ Component เท่านั้น

---

## FG

### แท็บ Failure Groups

- [ ] **FG-1 — สร้างกลุ่มเปล่า**
  - กดสร้างกลุ่มใหม่
  - **คาดหวัง:** กลุ่มถูกสร้างและขึ้นในรายการทันที

- [ ] **FG-2 — แก้รายละเอียดกลุ่ม**
  - แก้ Name
  - แก้ Description
  - แก้ Recommendation
  - **คาดหวัง:** บันทึกครบทั้ง 3 ช่องและยังอยู่หลังปิดเปิด

- [ ] **FG-8 — ลบกลุ่ม / ลบโมเดล (ปุ่มลบโมเดลอยู่ข้างในแถว sensor ที่กางออก)** 🆕
  - กางแถว sensor ในการ์ดกลุ่มแล้วลบโมเดลออกทีละตัว (ปุ่มถังขยะอยู่ในรายการโมเดลข้างใน ไม่ได้อยู่ที่หัวแถว)
  - ลบทั้งกลุ่ม
  - **คาดหวัง:** ลบโมเดลตัวไหนหายเฉพาะตัวนั้น โมเดลอื่นของ sensor เดียวกันยังอยู่ (ชิป I/R/C ของชนิดที่ถูกลบหายไป) — ลบตัวสุดท้ายของ sensor แล้วทั้งแถว sensor หายไป — ลบทั้งกลุ่มได้ และแผง Sensor ฝั่งขวาเห็นการเปลี่ยนแปลงตาม

- [ ] **FG-4 — สีประจำกลุ่ม**
  - ดูสีของแต่ละกลุ่ม
  - **คาดหวัง:** แต่ละกลุ่มสีต่างกันและสีเดียวกันทุกที่ที่กลุ่มนั้นโผล่

- [ ] **FG-5 — ข้อความตอนยังไม่มีอะไร**
  - ดู workspace ที่ยังไม่มีกลุ่ม/โมเดล
  - **คาดหวัง:** ขึ้น "No failure groups yet" / "No models yet"

- [ ] **FG-6 — ⚠️ กลับจากหน้า Failure Group มา Dashboard** 👀
  - เข้าแท็บ Failure Groups
  - กดปุ่มย้อนกลับ
  - **คาดหวัง:** ควรกลับมาที่ Dashboard — **มีรายงานค้างว่ามันเด้งกลับไปหน้าเริ่มโปรแกรม** ให้ยืนยันว่ายังเป็นอยู่ไหม

- [ ] **FG-7 — ปุ่ม Build Model**
  - กด Build Model จากแท็บ Failure Groups
  - กดซ้ำตอนหน้าต่างเปิดอยู่แล้ว
  - **คาดหวัง:** เปิดหน้าต่าง Build Model และเปิดได้ทีละหน้าต่างเท่านั้น (singleton) — กดซ้ำแล้วหน้าต่างเดิมถูกดึงขึ้นมาหน้าสุดและแสดงข้อมูลของ workspace ปัจจุบัน ไม่เปิดซ้อน

- [ ] **FG-9 — แท็บ Failure Groups จัดเป็นหนึ่งบรรทัดต่อ sensor (ไม่มีตัวเลือก Category)** 🆕
  - ให้ sensor ตัวเดียวมีโมเดลหลายชนิด (เช่น Individual + Relationship) ในกลุ่มเดียวกัน
  - ดูการ์ดของกลุ่มนั้น แล้วกดที่บรรทัด sensor เพื่อกาง/พับ
  - ดูตัวเลขสรุปด้านบนของแผง
  - **คาดหวัง:** sensor เดียวขึ้นบรรทัดเดียวพร้อมชิป I / R / C เฉพาะชนิดที่มีจริง (Clustering จัดตาม X sensor) — กางออกเห็นโมเดลแต่ละตัวพร้อมปุ่มลบ — ข้อความยาวตัดด้วย … บรรทัดเดียว — **ไม่มีปุ่ม Category ที่แผงนี้** (ตั้งที่หัว sensor ในหน้าต่าง Build Model) — ตัวเลขสรุปเป็น "N sensors · M models · G groups"

- [ ] **FG-10 — เปลี่ยนชื่อ / แก้รายละเอียด / สร้าง / ลบกลุ่มบน Dashboard ต้องไม่ทับ Category และช่วงเวลาที่ตั้งไว้ใน Build Model (แก้บั๊ก QA 2026-09-24)** 🆕
  - เปิด Build Model ตั้ง Category ที่หัว sensor และเพิ่ม Training periods ให้โมเดลแบบ Custom ไว้
  - กลับไป Dashboard แท็บ Failure Groups เปลี่ยนชื่อกลุ่ม → แก้คำอธิบาย → สร้างกลุ่มว่างใหม่ → ลบกลุ่มหนึ่ง
  - เปิด Build Model ซ้ำ (หรือปิดเปิด workspace)
  - **คาดหวัง:** Category และ Training periods ยังเป็นค่าที่ตั้งล่าสุด ไม่ย้อนกลับเป็นค่าเก่า และแบนเนอร์แจ้งเตือน category ไม่ขึ้นซ้ำ

---

## BMW

### หน้าต่าง Build Model

- [ ] **BMW-1 — เลือกกลุ่มได้หลายกลุ่ม**
  - ติ๊กให้โมเดลอยู่ 2 กลุ่มพร้อมกัน
  - **คาดหวัง:** ทำได้ (many-to-many) และไม่ไปสร้างโมเดลซ้ำอีกใบ

- [ ] **BMW-2 — Not in Group**
  - เอาโมเดลออกจากทุกกลุ่มจริง
  - **คาดหวัง:** ตกไปอยู่ Not in Group ซึ่งเป็นถังพิเศษหมายเลข 0 (ลบไม่ได้)

- [ ] **BMW-15 — ตั้งค่าโมเดลในแถว sensor: ฟิลด์ร่วมครั้งเดียว + แท็บต่อชนิด (ไม่มี Category ในฟอร์มแล้ว)** 🆕
  - กดที่แถว sensor ที่มีโมเดลมากกว่า 1 ชนิดเพื่อกางออก
  - ดูส่วนบน: Target sensor (ล็อก), Component (คำนวณจาก sensor อัตโนมัติ), Failure groups (อ่านอย่างเดียว)
  - สลับแท็บ Individual / Relationship / Clustering: Individual ตั้ง Model name — Relationship ตั้ง Model name + Predictor sensors — Clustering ตั้ง Model name + Y sensor / Criteria sensor + cluster ranges (X sensor ล็อก)
  - แก้ค่าในแท็บหนึ่งแล้วสลับไปอีกแท็บและสลับกลับ
  - กด Save changes ในแท็บที่แก้
  - **คาดหวัง:** ฟิลด์ร่วมขึ้นครั้งเดียวไม่ซ้ำต่อโมเดล — แท็บที่แก้ค้างไว้ยังไม่ Save ขึ้นป้าย "edited" และค่าที่พิมพ์ค้างไม่หายตอนสลับแท็บ — Save changes บันทึกเฉพาะโมเดลของแท็บนั้น (โมเดลอื่นของ sensor เดียวกันไม่ถูกแตะ) และแถวยังเปิดอยู่หลัง Save — ชนิดโมเดลเปลี่ยนที่นี่ไม่ได้ — **ไม่มีช่อง Category ในฟอร์มอีกแล้ว** (ดู BMW-20)

- [ ] **BMW-4 — sync สองทางกับ Dashboard (ทั้งสองทิศทาง)** 🆕
  - เปิดหน้าต่าง Build Model ค้างไว้
  - **ไปแก้กลุ่มที่ Dashboard** (เช่น ติ๊ก sensor เข้า/ออกจากกลุ่ม, สร้างกลุ่มใหม่, ลบโมเดล) โดยไม่ปิดหน้าต่าง Build Model
  - ดูหน้าต่าง Build Model
  - แล้วลองแก้บางอย่างที่ Build Model (เช่น กดป้าย Incomplete) แล้วดูแผง Failure Groups ที่ Dashboard
  - **คาดหวัง:** **ทั้งสองฝั่งอัปเดตทันทีโดยไม่ต้องปิดเปิดหน้าต่าง** ไม่มีฝั่งไหนเขียนทับของอีกฝั่ง (เดิม Dashboard ไม่ส่งการเปลี่ยนแปลงไปให้ Build Model จึงเห็นข้อมูลเก่าจนกว่าจะเปิดใหม่)

- [ ] **BMW-5 — สลับ workspace ขณะหน้าต่างเปิดอยู่**
  - เปิด Build Model **และ Add Special Sensor** ค้างไว้
  - กลับหน้า Import แล้วเปิด workspace อื่น
  - **คาดหวัง:** ทั้งสองหน้าต่างปิดเองอัตโนมัติ ไม่ค้างชี้ข้อมูลของ workspace เก่า (เดิมปิดเฉพาะ Build Model — Add Special Sensor ค้างแล้วส่ง sensor ของโปรเจกต์เก่าเข้าโปรเจกต์ใหม่)

- [ ] **BMW-6 — ปุ่ม "Group by Model Type" (ตัวเลือกที่ 3 ต่อจาก Failure Group / Component)** 🆕
  - ให้มีโมเดลอย่างน้อย 1 ตัวของแต่ละชนิด Individual / Relationship / Clustering
  - กดปุ่ม "Group by Model Type" มุมขวาบน
  - **คาดหวัง:** เห็น section แยกตาม **Individual → Relationship → Clustering เรียงลำดับตายตัวเสมอ** (ไม่ใช่เรียงตามตัวอักษรหรือลำดับที่สร้าง) — kind ไหนไม่มีโมเดลเลยต้องไม่มี section ว่างโผล่ขึ้นมา และแต่ละแถวยังมี FG chip ของตัวเอง (เหมือนตอน "Group by Component")

- [ ] **BMW-17 — ปุ่ม "Build Model →" ต่อจาก Save changes + ปิดจนกว่ากรอกครบ และ sensor ต้องมี Category ก่อน** 🆕
  - เปิดแก้โมเดลที่ข้อมูลยังไม่ครบ (เช่น Relationship ที่ยังไม่เลือก predictor สักตัว)
  - เปิดแถว sensor ที่ยังไม่ได้เลือก Category ที่หัวแถว
  - ดูตำแหน่ง สถานะ และข้อความข้างปุ่ม Build Model / Save changes
  - **คาดหวัง:** ปุ่ม **"Build Model →" อยู่ต่อจาก "Save changes" ที่มุมล่างขวาของแท็บ** และต้อง **จางลงกดไม่ได้เหมือนปุ่ม Save changes เป๊ะ ๆ** พร้อม tooltip/ข้อความบอกเหตุผล — ถ้ายังไม่มี Category ต้องขึ้น **"Pick a category on the sensor header."** (ข้อความเหมือนกันทุกที่ ทั้งข้อความข้างปุ่ม tooltip ป้ายสถานะ และปุ่ม Finish) และปุ่มทั้งสองกดไม่ได้จนกว่าจะเลือกที่หัว sensor — **ถ้ายังดูสีเข้มเหมือนกดได้ทั้งที่ข้อมูลยังไม่ครบ ให้รายงานทันที** — **ถ้าข้อมูลครบและมี Category แล้ว แต่ workspace ยังไม่ได้ตั้ง Running Condition (หรือยังไม่ได้กด "No condition") ปุ่ม Build Model ต้องจางกดไม่ได้ พร้อมเหตุผล "Set a running condition first, or choose "No condition — use all rows"." ทั้งใน tooltip และข้อความข้างปุ่ม ส่วนปุ่ม Save changes ยังกดได้** (ดู BMW-23)

- [ ] **BMW-8 — กด Build Model แล้วค่าที่เพิ่งแก้ (ยังไม่ได้กด Save) ต้องถูกบันทึกด้วย** 🆕
  - เปิดแก้โมเดลตัวหนึ่งที่ข้อมูลครบอยู่แล้ว
  - แก้ field บางอย่าง (เช่น Model name) โดย**ไม่กด Save changes**
  - กด "Build Model →" ทันที
  - กด Back กลับมาหน้า Overview แล้วเปิดแก้โมเดลเดิมอีกครั้ง
  - **คาดหวัง:** ค่าที่เพิ่งแก้ไว้ต้องถูกบันทึกแล้ว (ปุ่ม Build Model จะ commit ฟอร์มให้อัตโนมัติก่อน navigate ไปหน้า train เสมอ) — **ไม่ใช่ค่าเก่าที่ยังไม่ได้ Save** ซึ่งจะทำให้ train ด้วยข้อมูลผิดแบบเงียบ ๆ

- [ ] **BMW-9 — ปุ่ม "Add predictors…" เปิด popup ค้นหาได้และจัดกลุ่มตาม Component (เดิมเป็น dropdown แทรกในหน้า เปลี่ยนเป็น popup 2026-09-22 — ดู BMW-13)**
  - เปิดแก้โมเดล Relationship ที่หน้า Overview
  - คลิกปุ่ม "Add predictors…" ที่ช่อง Predictor sensors
  - พิมพ์ค้นหาด้วย tag และด้วยคำใน description ใน popup
  - เลือก predictor 2-3 ตัว กด OK แล้วดูชิปที่ขึ้นนอก popup กด x บนชิปเพื่อเอาออกทีละตัว
  - **คาดหวัง:** popup แบ่งรายการเป็นกลุ่มตามชื่อ Component (เรียงตามตัวอักษร ตัวที่ไม่มี component ไปอยู่ "Uncategorized"), พิมพ์ค้นหาแล้วกรองทั้งรายการได้, target ของโมเดลไม่โผล่ในตัวเลือก, ปุ่ม Save/Build Model ยังกดไม่ได้จนกว่าจะมี predictor อย่างน้อย 1 ตัว

- [ ] **BMW-13 — Predictor popup: เลือกได้หลายตัวก่อนกด OK, พับ/กางกลุ่มได้, Cancel/Esc ไม่บันทึกอะไรเลย** 🆕
  - เปิด popup "Add predictors…" ดูว่ากลุ่ม Component แต่ละกลุ่มเริ่มต้น**พับอยู่**
  - กดหัวกลุ่มเพื่อกาง แล้วติ๊กเลือกหลายตัวในกลุ่มเดียวกันและต่างกลุ่ม (ยังไม่กด OK)
  - ลองพิมพ์ค้นหา ดูว่ากลุ่มที่มีผลลัพธ์ตรงกางออกให้เองโดยไม่ต้องกดหัวกลุ่ม
  - กด Cancel (หรือกด Esc หรือคลิกนอก popup) แล้วเปิด popup ใหม่อีกครั้ง
  - **คาดหวัง:** ติ๊กได้มากกว่า 1 ตัวสะสมไว้ก่อนกด OK (ตัวนับ "N selected" ที่มุมล่างเพิ่มตามที่ติ๊ก), กลุ่มเริ่มพับเสมอจนกว่าจะกดเปิดเองหรือพิมพ์ค้นหา, Cancel/Esc/คลิกนอก popup **ไม่มีผลอะไรกับ predictor ที่เลือกไว้เดิมเลย** — เปิด popup ใหม่ต้องกลับไปเป็นค่าที่เคย OK ไว้ล่าสุด ไม่ใช่ค่าที่เพิ่งติ๊กค้างไว้ตอนยกเลิก

- [ ] **BMW-14 — Clustering: Y sensor และ Criteria sensor เปลี่ยนจาก dropdown ธรรมดา เป็น popup แบบเดียวกัน แต่เลือกได้ตัวเดียว (เพิ่ม 2026-09-22)** 🆕
  - เปิดแก้โมเดล Clustering ที่หน้า Overview ดูช่อง "Y sensor (target)" และ "Criteria sensor (optional)"
  - คลิกช่อง Y sensor เลือก sensor 1 ตัว
  - คลิกช่อง Criteria sensor ดูว่ามีตัวเลือก "None" ให้ด้วยไหม แล้วเลือก sensor 1 ตัว
  - **คาดหวัง:** ทั้งสองช่องเปิด popup ค้นหา+จัดกลุ่มตาม Component แบบเดียวกับ Predictor sensors — แต่**คลิกเลือกแล้ว popup ปิดทันที ไม่มีปุ่ม OK/Cancel และไม่ติ๊กได้มากกว่า 1 ตัว** (X sensor ที่อยู่ข้างๆ ยังคงล็อกไว้เหมือนเดิม ไม่ใช่ popup) — ช่อง Criteria sensor มีแถว "None" ให้เลือกล้างค่าได้ ช่อง Y sensor ไม่มี

- [ ] **BMW-10 — Predictor ที่เลือกไว้ต้องตามไปที่หน้า Build Model ครบ** 🆕
  - เปิดแก้โมเดล Relationship แล้วเลือก predictor 2-3 ตัว (ยังไม่กด Save)
  - กด "Build Model →" ทันที
  - ดูส่วน Predictor sensors ที่หน้า PM
  - **คาดหวัง:** predictor ที่เพิ่งเลือกอยู่ครบและตัวเลขนับถูก — **ต้องไม่ขึ้น "No predictors selected"** (เคยเป็นบั๊ก race: หน้า PM อ่านไฟล์ก่อนที่การบันทึกจะเสร็จ ทำได้ไม่ทุกครั้ง ให้ลองซ้ำหลายรอบ)

- [ ] **BMW-18 — Running Condition Filter: บังคับตั้งก่อน (Required) + เลือก "Filter by condition" หรือ "No condition" + sensor picker popup + AND/OR (แทน BMW-11; ช่วงเวลาย้ายไป BMW-25)** 🆕
  - เปิด workspace ที่ยังไม่เคยตั้ง Running Condition เลย (มีโมเดลแล้วหรือยังไม่มีก็ได้)
  - ดูแผง "Running Condition Filter" ด้านบนหน้า Overview
  - ตั้ง **Training periods** อย่างน้อย 1 ช่วง (ดู BMW-25) — ช่วงเวลาอย่างเดียว**ไม่ถือว่าตั้งเงื่อนไขแล้ว**
  - ในแผงมีตัวเลือก **"Filter by condition | No condition"** — เลือก "Filter by condition" แล้วกด "Add condition" คลิกช่อง sensor ต้องเปิด popup ค้นหา+จัดกลุ่มตาม Component (เลือกได้ทุก sensor) ตั้ง operation และค่า
  - เพิ่มเงื่อนไขที่ 2 แล้วสลับปุ่ม "Match" AND/OR — ดูข้อความสรุปตอนพับแผง
  - ลองเว้น "ค่า" ว่างไว้ / เลือก sensor ที่ไม่มีในไฟล์ข้อมูล แล้วดูว่ายังขึ้น Required อยู่
  - กด "No condition" แล้วกลับมากด "Filter by condition"
  - ปิดเปิดโปรแกรมแล้วกลับมาดูแผง
  - **คาดหวัง:** ตอนยังไม่ตั้งอะไร แผงมี**ป้าย "Required" สีเหลือง กรอบสีเหลือง เปิดแผงให้เองครั้งเดียวตอนเปิดหน้า** และข้อความ "Required — add a condition, or choose "No condition — use all rows"…" — มีเงื่อนไขที่กรอกครบอย่างน้อย 1 แถว (sensor อยู่ในไฟล์ + มีค่า, ถ้า between ต้องมีทั้งสองค่า) **หรือ**กด "No condition" ถึงจะหายป้าย Required — "No condition" ซ่อนรายการเงื่อนไขและมีข้อความว่า **เงื่อนไขที่เคยบันทึกไว้ยังอยู่แต่ไม่ถูกนำมาใช้** (ไม่ลบทิ้ง) แล้วกลับมา "Filter by condition" เงื่อนไขเดิมกลับมา — เพิ่มเงื่อนไขใหม่ขณะเลือก "No condition" ไม่ได้ (ต้องสลับกลับก่อน) และทั้งสองอย่างไม่ค้างพร้อมกัน — เลือก/เปลี่ยน sensor ได้จริง, สลับ AND/OR มีผลกับสรุป, Training periods + เงื่อนไขเป็น**ค่าเริ่มต้นของทุกโมเดล** (โมเดลที่ตั้ง Custom จะไม่ตาม — ดู PM-27), ค่าอยู่ครบหลังปิดเปิด — เงื่อนไขนี้**ไม่กระทบกราฟ/ตารางของ Dashboard** เลย

- [ ] **BMW-16 — ป้ายสถานะ Complete / Incomplete ต่อโมเดลแต่ละแท็บ** 🆕
  - ที่หัวแถว sensor ดูจุดสถานะบนชิป I / R / C และข้อความ "k of n complete"
  - กางแถว เปิดแท็บของโมเดลหนึ่ง แล้วกดป้าย "Incomplete" ที่มุมล่างซ้ายของแท็บ
  - กดอีกครั้ง
  - ดูที่ Group by Component / Model Type (แถวต่อโมเดล) ว่าป้ายยังกดสลับได้ที่หัวแถว
  - **คาดหวัง:** สลับ Incomplete ⇄ Complete ได้เฉพาะโมเดลของแท็บนั้น (ชนิดอื่นของ sensor เดียวกันไม่เปลี่ยน) จุดสีบนชิปและ "k of n complete" อัปเดตทันที และค่าอยู่ต่อหลังปิดเปิดโปรแกรม (ปุ่ม Finish ในหน้า PM ตั้งเป็น Complete ทางเดียว — ดู PM-22)

- [ ] **BMW-19 — หนึ่งแถวต่อ sensor ต่อกลุ่ม (Group by Failure Group)** 🆕
  - ให้ sensor ตัวเดียวมี Individual + Relationship + Clustering ในกลุ่มเดียวกัน (สร้างที่ Dashboard แท็บ Sensor)
  - ดูการ์ดของกลุ่มที่หน้า Overview
  - เพิ่ม/ลบชนิดโมเดลที่ Dashboard แล้วดูแถวอีกครั้ง
  - ให้ sensor เดียวกันอยู่ 2 กลุ่ม โดยกลุ่มหนึ่งมีแค่ Individual
  - **คาดหวัง:** ขึ้น**แถวเดียว**ต่อ sensor ต่อกลุ่ม หัวแถวมี ชื่อ sensor บรรทัดเดียวตัดด้วย … / ชิป Component / ชิป "also in FG-n" / "k of n complete" / ชิป I R C **เฉพาะชนิดที่มีจริง** (ไม่มีช่องว่างหรือเส้นประของชนิดที่ไม่มี) — **Clustering จัดเข้าแถวตาม X sensor** (เปลี่ยน Y แล้วแถวไม่ย้าย) — ฟิลด์ Failure groups ในแถวที่กางออกเป็นชิปอ่านอย่างเดียว และเขียน "(I only)" ต่อท้ายกลุ่มที่มีโมเดลไม่ครบทุกชนิด — การสร้าง/ลบโมเดลยังทำได้ที่ Dashboard เท่านั้น

- [ ] **BMW-20 — Category ตั้งครั้งเดียวที่หัว sensor: บันทึกทันที มีผลกับทุกโมเดลของ sensor นั้นทุกกลุ่ม + เตือนถ้ากระทบกลุ่มอื่น** 🆕
  - ที่หัวแถว sensor กด Performance หรือ Condition
  - ให้ sensor นั้นอยู่หลายกลุ่ม แล้วกดเปลี่ยน Category
  - ปิดหน้าต่าง Build Model เปิดใหม่ และดูแถวของ sensor เดียวกันในอีกกลุ่ม
  - ดูที่ Group by Component / Model Type
  - **คาดหวัง:** บันทึกทันทีโดยไม่มีปุ่ม Save และโมเดลทุกชนิดของ sensor นั้น**ทุกกลุ่ม**เปลี่ยนตาม — sensor ที่อยู่หลายกลุ่มขึ้นข้อความเตือนว่า Category นี้ใช้กับทุกกลุ่มไหนบ้าง (sensor ที่อยู่กลุ่มเดียวไม่เตือน) — **ไม่มีสถานะ "Mixed" ที่ใดเลย** — ยังไม่เลือก Category ขึ้นข้อความให้เลือก — มุมมอง Component / Model Type แสดง Category เป็นชิปอ่านอย่างเดียว พร้อมลิงก์ "Change in Failure Group view" ที่พากลับมาแถว sensor นั้น

- [ ] **BMW-21 — แจ้งเตือนครั้งเดียวเมื่อระบบปรับ Category ของ workspace เก่าให้ตรงกัน** 🆕
  - ใช้ workspace เก่าที่ sensor เดียวกันมีโมเดลคนละ Category (เช่น Individual = Performance แต่ Relationship = Condition) เปิด Build Model
  - ดูแถบเหลืองด้านบนรายการ
  - กด Dismiss แล้วปิดเปิดโปรแกรม เปิด Build Model ใหม่
  - **คาดหวัง:** ขึ้นแถบบอกว่าโมเดลกี่ตัวถูกปรับและจาก→เป็นอะไร (ตามลำดับ Individual ก่อน Relationship ก่อน Clustering) และค่าใหม่ถูก**บันทึกลงไฟล์ตั้งแต่เปิดครั้งแรก** — กด Dismiss แล้วหายและไม่กลับมาอีกแม้ปิดเปิดโปรแกรม — workspace ที่ Category ตรงกันอยู่แล้วต้องไม่ขึ้นแถบนี้เลย

- [ ] **BMW-22 — โมเดลใหม่ที่สร้างจาก Dashboard สืบทอด Category ของ sensor** 🆕
  - ตั้ง Category ให้ sensor ที่ Build Model (เช่น Condition)
  - ที่ Dashboard แท็บ Sensor เพิ่มโมเดลชนิดอื่นให้ sensor เดียวกัน (เช่น เพิ่ม Relationship) หรือเพิ่มเข้ากลุ่มอื่น
  - เปิด Build Model ดูแถวนั้น
  - **คาดหวัง:** โมเดลใหม่ใช้ Category เดียวกับ sensor ทันที (ไม่ต้องตั้งใหม่ ไม่ขึ้น "Pick a category") — sensor ที่ยังไม่เคยตั้ง Category โมเดลใหม่ยังไม่มี Category ให้เลือกที่หัวแถว

- [ ] **BMW-23 — ด่านบังคับ Running Condition: Build Model / ป้ายสถานะ Complete / Finish ถูกล็อกจนกว่าจะตั้ง (soft gate A)** 🆕
  - ใช้ workspace ที่ **ยังไม่ตั้ง** Running Condition และมีโมเดลอย่างน้อย 2 ตัวของ sensor เดียวกัน (Incomplete ทั้งคู่)
  - ดูหัวแถว sensor และแท็บของแต่ละโมเดล
  - เปิดแถว กด "Build Model →" และลองกดป้าย Incomplete
  - สลับไป "Group by Component" ดูป้ายและป้ายสถานะ
  - ที่แผง Running Condition ตั้งเงื่อนไขที่กรอกครบ 1 แถว (หรือกด "No condition") แล้วกลับมาดูซ้ำ
  - **คาดหวัง:** หัวแถว sensor มีป้ายเหลือง **"N blocked"** (นับเฉพาะโมเดลที่ยัง Incomplete), แต่ละแท็บ/แถวโมเดลมีป้าย **"Needs condition"** — โมเดลที่ Complete อยู่แล้วแต่ยังไม่มีเงื่อนไข ขึ้น **"Legacy · all data"** แทน — ถ้าเหตุที่ถูกล็อกคือ**ยังไม่มี Category** ป้ายเป็น **"Needs category"** และถ้า**ช่วงเวลาผิด (สิ้นสุดก่อนเริ่ม)** เป็น **"Fix periods"** (เอาเมาส์ชี้ป้ายเห็นเหตุผลเต็ม) — ปุ่ม "Build Model →" กดไม่ได้พร้อมเหตุผล (Save changes ยังกดได้) — ป้ายสถานะกดเปลี่ยนเป็น **Complete ไม่ได้** (จางพร้อม tooltip เหตุผล) แต่เปลี่ยนจาก Complete กลับเป็น Incomplete ได้เสมอ — พอตั้งเงื่อนไขแล้วป้ายทั้งหมดหาย ปุ่ม/ป้ายกดได้ และกด Build Model เข้าหน้า PM ได้ — โมเดลที่ตั้ง Custom ต้องมีเงื่อนไขของตัวเอง (หรือกด No condition ในหน้า PM) ถึงจะผ่าน แม้ workspace จะตั้งแล้ว

- [ ] **BMW-24 — Workspace เก่าที่มีโมเดลอยู่แล้วแต่ไม่เคยตั้ง Running Condition: แบนเนอร์ 3 ปุ่ม** 🆕
  - เปิด workspace เก่า (สร้างก่อนฟีเจอร์นี้) ที่มีโมเดลแล้วและไม่เคยตั้ง Running Condition
  - ดูแบนเนอร์เหลือง "This workspace has models but no running condition" ใต้แผง Running Condition
  - ลองปุ่ม "Remind me later" แล้วปิดโปรแกรม เปิดใหม่
  - ลองปุ่ม "Set a condition" แล้วตั้งเงื่อนไขที่กรอกครบ
  - เปิด workspace เก่าอีกอันแล้วกด "Keep using all data"
  - **คาดหวัง:** แบนเนอร์มี 3 ปุ่ม — "Remind me later" ซ่อนแบนเนอร์แค่ในรอบที่เปิดอยู่ (**ไม่บันทึก** เปิดโปรแกรมใหม่แบนเนอร์กลับมา) — "Set a condition" เปิดแผง Running Condition ให้ และแบนเนอร์หายเมื่อตั้งเงื่อนไขครบแล้ว **และไม่กลับมาอีกแม้ลบเงื่อนไขทีหลัง** — "Keep using all data" = ยืนยัน No condition แบนเนอร์หายถาวร โมเดลที่ Complete อยู่แล้วไม่ถูกแตะ — workspace ใหม่ที่ยังไม่มีโมเดลต้อง**ไม่**เห็นแบนเนอร์นี้ และ workspace ที่ตั้งเงื่อนไขไว้แล้วก็ไม่เห็น
- [ ] **BMW-25 — Training periods ในแผง Running Condition Filter: หลายช่วงเวลา (แทน Time start/end ช่วงเดียว)** 🆕
  - เปิดแผง "Running Condition Filter" ที่หน้า Overview ดูหัวข้อ **"Training periods"** — ตอนยังไม่มีช่วงเวลาต้องเห็นข้อความว่าไม่จำกัดเวลา (ใช้ทั้งชุดข้อมูล) กับปุ่ม **"Add period"**
  - กด "Add period" ครั้งแรก แล้วกดอีกครั้ง — ดูช่วงที่สองที่เพิ่มมาเอง
  - แก้วันที่ของช่วงหนึ่งให้เรียงสลับกัน (เช่น ช่วงที่ 1 ให้เริ่มหลังช่วงที่ 2) โดยพิมพ์ค่าแล้วยังไม่กดที่อื่น จากนั้นคลิกออกจากช่อง (หรือกด Enter)
  - แก้ให้สองช่วงเหลื่อมกัน แล้วกด **"Merge into one"**
  - แก้ช่วงหนึ่งให้วันสิ้นสุดอยู่**ก่อน**วันเริ่ม
  - ปล่อยวันเริ่มของช่วงแรกว่าง และวันสิ้นสุดของช่วงสุดท้ายว่าง / ลองปล่อยว่างในช่วงที่อยู่กลาง
  - เพิ่มช่วงจนครอบคลุมข้อมูลทั้งไฟล์ แล้วดูปุ่ม Add period
  - กดกากบาทลบช่วงจนหมด แล้วปิดเปิดโปรแกรมกลับมาดูแผง
  - **คาดหวัง:** ช่วงที่ 1 ที่เพิ่มครั้งแรกเริ่มที่วันแรกของข้อมูลและจบสิ้นเดือนนั้น เวลา 23:59 · ช่วงถัดไปเริ่ม**วันถัดจากวันสิ้นสุดของช่วงก่อนหน้า** และจบ 23:59 ของสิ้นเดือนนั้น (ไม่เกินวันสุดท้ายของข้อมูล) · **ระหว่างพิมพ์แถวไม่กระโดด — เรียงตามเวลาเมื่อคลิกออกจากช่อง/กด Enter เท่านั้น** · ช่วงที่เหลื่อมกันมี**คำเตือน + ปุ่ม "Merge into one" (ไม่ได้บล็อก)** กดแล้วรวมเป็นช่วงเดียว (ต้นสุด–ปลายสุด) · ช่วงที่วันสิ้นสุดก่อนวันเริ่มมีข้อความสีแดงใต้แถว และ**กด Build Model / Finish ไม่ได้** จนกว่าจะแก้ (ดู BMW-23) · วันว่างใช้ได้เฉพาะ**วันเริ่มของช่วงแรก ("Start of data")** และ**วันสิ้นสุดของช่วงสุดท้าย ("End of data")** ที่อื่นขึ้น error · ปุ่ม Add period กดไม่ได้เมื่อช่วงเวลาครอบคลุมข้อมูลทั้งหมดแล้ว (หรือช่วงสุดท้ายเปิดปลาย) · ลบหมด = ไม่จำกัดเวลา · ช่วงเวลาอยู่ครบหลังปิดเปิดโปรแกรม · แถวข้อมูลถูกนำมาใช้ถ้าอยู่ใน**ช่วงใดช่วงหนึ่ง (OR)** และผ่านเงื่อนไขค่า sensor (AND ระหว่างสองกลุ่ม) · ช่วงเวลานี้**ไม่กระทบกราฟ/ตารางของ Dashboard** (กราฟยังไม่มีแถบแสดงช่วง — เลื่อนไว้ทีหลัง)

- [ ] **BMW-26 — Workspace ที่สร้างใหม่ต้องไม่ขึ้นแบนเนอร์ "legacy" (แก้บั๊ก QA 2026-09-24)** 🆕
  - สร้าง workspace ใหม่ นำเข้าข้อมูล สร้าง Failure Group และสร้างโมเดลจาก Dashboard แล้วเปิด Build Model
  - **คาดหวัง:** ไม่มีแบนเนอร์ "Models built so far trained on all rows" (แบนเนอร์นี้ต้องขึ้นเฉพาะ workspace เก่าที่มีโมเดลก่อนมีด่านบังคับ) — ยังเห็นป้าย "Required" ที่แผง Running Condition ตามปกติ

---

## PM

### Predictive Model Build

_อยู่ในหน้าต่าง Build Model — เข้าจากปุ่ม "Build Model →" ในฟอร์มแก้โมเดล (กดแล้วบันทึกฟอร์มให้ก่อนเสมอ) · **หน้านี้ไม่มีปุ่มเทรน/เซฟโมเดลแล้ว** — เหลือแค่ตัวอย่างกราฟ/ผล fit และปุ่ม Finish (โค้ดเทรนฝั่ง Rust เก็บไว้สำหรับฟีเจอร์ health score ในอนาคต) · ชนิดโมเดลถูกล็อกตามที่เลือกไว้ที่ Overview_

- [ ] **PM-1 — Individual — กราฟ time series ของ target**
  - เปิดโมเดลชนิด Individual
  - ดูกราฟและแถบสถิติด้านล่าง
  - **คาดหวัง:** กราฟ "Standard Time Series" ขึ้นพร้อมเส้น Mean และเส้น ±1σ / ±3σ, แถบ Target Stats แสดง N / Mean / SD / Min / Max / ±1σ / ±3σ, ป้ายตัวเลขบนแกนไม่มีทศนิยมยาวเกินไป — ต้องไม่มีส่วน "Predictor sensors" และไม่มีคอลัมน์ขวา (Individual ไม่มีอะไรให้ตั้ง)

- [ ] **PM-3 — Clustering — preview**
  - เปิดโมเดลชนิด Clustering
  - ตั้ง X / Y sensor
  - ตั้ง Number of Clusters
  - กด Apply
  - **คาดหวัง:** ได้กลุ่มจุดพร้อมวงรี (ellipse) / center / angle ตามจำนวนที่ตั้ง — ข้อความในหน้าต้องไม่เรียกว่า k-means (อัลกอริทึมจริงคือ GMM)

- [ ] **PM-4 — Clustering — cluster ranges + criteria sensor**
  - ตั้ง Number of Clusters ≥ 2 โดยยังไม่เลือก criteria sensor แล้วกด Apply
  - เลือก criteria sensor แล้วดูช่วงของแต่ละ cluster
  - ปรับช่วงของ cluster ด้วยแถบเลื่อนแบบหลายหัว
  - **คาดหวัง:** ขึ้นข้อความ "Pick a criteria sensor to split rows into clusters." ตอนยังไม่เลือก, พอเลือกแล้วช่วงถูกแบ่งเท่า ๆ กันอัตโนมัติตามค่า min–max ของ sensor นั้น และปรับต่อด้วยแถบเลื่อนได้ (ค่าเหล่านี้ถูกบันทึกกลับเข้าโมเดลอัตโนมัติ)

- [ ] **PM-5 — ⭐ Relationship — Apply (fit จริง เรียก Python sidecar)** 🐍
  - เปิดโมเดลชนิด Relationship (ที่มี predictor แล้วจาก Overview)
  - ตั้ง Stiffness
  - กด Apply
  - **คาดหวัง:** **fit สำเร็จบนแอปที่ติดตั้งแล้ว** ได้กราฟ scatter + เส้นโมเดลและค่า R² — นี่คือ path เดียวที่เรียก `backend.exe` ที่ถูก bundle เข้า installer (ตอนนี้ไม่มีปุ่มเทรน/เซฟแล้ว Apply เป็นทางเดียวที่แตะ sidecar) ใน dev มันหาไฟล์จาก `src-tauri/bin/` แต่ในตัวติดตั้งมันอยู่ข้าง ๆ exe — คนละกลไก ทดแทนกันไม่ได้ · UI ต้องเขียนว่า "Relation model" เสมอ (ดู PM-17)

- [ ] **PM-6 — Relationship — ตัวเลขผลลัพธ์**
  - ดูหลัง Apply เสร็จ
  - **คาดหวัง:** มี R² / RMSE / Resid Mean / Resid SD และตัวเลขสมเหตุสมผล และเปลี่ยนตาม Stiffness ที่เลือก

- [ ] **PM-7 — Relationship — Sub-models**
  - เปิดมุมมอง Sub-models
  - **คาดหวัง:** เปิดได้และแสดงผลของ subset นั้น

- [ ] **PM-26 — หน้า PM: Training periods ต้องมีผลกับกราฟ สถิติ และการ fit จริง (แทน PM-8)** 🆕
  - ที่ Overview ตั้ง Training periods ให้ 2 ช่วงที่ไม่ติดกัน (เช่น มกราคม กับ มีนาคม) แล้วเปิดโมเดลเข้าหน้า PM (โหมด Workspace) — ดูส่วน "Training conditions"
  - ดูกราฟ Individual time-series และแถบ Target Stats ว่ามีเฉพาะข้อมูลในสองช่วงนั้น (ช่วงกลางขาดหาย) และตัวเลข N/Mean/SD สอดคล้องกัน
  - (โมเดล Relationship/Clustering) กด Apply ดูว่าผล fit ใช้ข้อมูลเฉพาะสองช่วงนั้น
  - สลับสวิตช์เป็น **Custom** — ต้องเห็นรายการ Training periods ที่**ก็อปมาจาก Overview** แก้ไขได้ (ดู PM-27) แก้วันที่ให้แคบลงแล้วคลิกออกจากช่อง ดูกราฟ สถิติ และการ fit เปลี่ยนตาม
  - ตั้งช่วงที่วันสิ้นสุดก่อนวันเริ่ม (โหมด Custom)
  - สลับกลับเป็น **Workspace** แล้วดูชิปช่วงเวลา (อ่านอย่างเดียว) แล้วกด "Edit on Overview →"
  - **คาดหวัง:** **กราฟ, Target Stats และผล fit ใช้ข้อมูลชุดเดียวกัน (เฉพาะแถวที่อยู่ในช่วงใดช่วงหนึ่ง)** ไม่มีกรณีกราฟกรองแต่ตัวเลขไม่กรอง · ชิปของช่วงเต็มวันแสดงแค่วันที่ (เช่น 2026-01-01 – 2026-01-31) ถ้ามีเวลาแสดงวันที่+เวลา ปลายเปิดแสดง "Start of data" / "End of data" · ไม่มี period = ข้อความ "no limit set — full dataset" · ช่วงที่วันสิ้นสุดก่อนวันเริ่ม: กราฟ สถิติ และ Apply **ไม่ทำงาน** (ไม่ตกไปใช้ทั้งชุดข้อมูลเงียบ ๆ) มีข้อความให้แก้วันที่ และปุ่ม Finish กดไม่ได้พร้อมเหตุผล · โหมด Workspace เป็นอ่านอย่างเดียว · filter ที่ตั้งจากแท็บ Filter ของ Dashboard **ไม่ถูกนำมาใช้ในหน้านี้**
- [ ] **PM-18 — "Training scope" อธิบายตัวเองว่าคุมทั้งกราฟและ training (ไม่ใช่ 2 filter แยกกัน) — เพิ่ม 2026-09-22** 🆕
  - เปิดโมเดลชนิด Individual เข้าหน้า PM
  - ดูหัวข้อแผงซ้ายเหนือรายการ Training periods และดูมุมขวาบนของกราฟ ข้าง legend (Target/±1σ/±3σ)
  - เอาเมาส์ชี้ป้าย "Zoom = view only" ที่กราฟค้างไว้
  - ลอง 🔍 Zoom → Horizontal zoom บนกราฟ แล้วดูว่า Target Stats/ผล fit เปลี่ยนไหม
  - **คาดหวัง:** หัวข้อแผงซ้ายเขียนว่า **"Training scope"** (ไม่ใช่ "Data filter") มีบรรทัดอธิบายว่าใช้ทั้งกับกราฟและโมเดล · ข้างกราฟมีป้าย **"Zoom = view only"** พร้อม tooltip อธิบายว่าไม่กระทบ training · หลังซูมกราฟด้วย 🔍 **ตัวเลขใน Target Stats และผล fit ต้องไม่เปลี่ยน** (ซูมเป็นแค่มุมมอง client-side)

- [ ] **PM-11b — Expand chart — ขยายกราฟเต็มจอ**
  - กดไอคอนขยาย (⤢) ที่มุมกราฟการ์ดใดก็ได้ (Individual time-series, Relationship/Clustering chart)
  - **คาดหวัง:** เปิด modal เต็มจอแสดงกราฟเดียวกันขนาดใหญ่ กด Esc หรือคลิกพื้นหลังปิดได้

- [ ] **PM-12 — ปุ่ม Back**
  - กด Back
  - **คาดหวัง:** กลับหน้าก่อนหน้าโดยไม่ทำค่าที่ตั้งไว้หาย

- [ ] **PM-13 — โมเดลเดียวอยู่หลายกลุ่ม ใช้ค่าตั้งเดียวกัน** 🆕
  - ติ๊กให้โมเดลหนึ่งอยู่ 2 กลุ่ม
  - ตั้งค่าในหน้า PM ของโมเดลนั้น (เช่น Stiffness / Training periods) แล้วออกมา
  - เปิดโมเดลนั้นจากอีกกลุ่มหนึ่ง
  - **คาดหวัง:** เป็นโมเดลตัวเดียวกัน (ไม่มีสำเนาแยก) ค่าที่ตั้งไว้เหมือนกันทั้งสองทาง และตั้งค่าของโมเดลหนึ่งไม่ไปทับโมเดลอื่น (รายงานเดิม "ต้องเทรนหลายครั้ง" แก้แล้วด้วยการเก็บ config แยกต่อโมเดล)

- [ ] **PM-22 — ปุ่ม Finish (ล็อกด้วย Running Condition, แทน PM-14)** 🆕
  - เปิดหน้า PM ของโมเดลที่ป้ายยังเป็น Incomplete ตอนที่ workspace **ยังไม่ตั้ง** Running Condition (เข้าผ่านช่องทางอื่น เช่นเปลี่ยนเงื่อนไขจากอีกหน้าต่างหลังเปิด PM ค้างไว้ หรือโมเดลที่เป็น Custom)
  - ดูปุ่ม "Finish" มุมขวาบนและข้อความข้าง ๆ
  - ตั้งเงื่อนไข / กด "No condition" แล้วกด "Finish"
  - เปิดโมเดลเดิมอีกรอบแล้วกด Finish ซ้ำ
  - (เพิ่ม 2026-09-24) แก้ค่าในหน้า PM (เช่น วันที่ของ period หรือติ๊ก No condition) แล้ว**กด Finish หรือ Back ทันที**
  - **คาดหวัง:** ระหว่างยังไม่ตั้ง ปุ่ม Finish **จางกดไม่ได้** พร้อมเหตุผลเดียวกับปุ่ม Build Model ของหน้า Overview (tooltip + ข้อความสีเหลืองข้างปุ่ม) — พอตั้งแล้วกด Finish จะ**กลับหน้า Overview อัตโนมัติ**และป้ายเป็น Complete (สีเขียว) — การแก้ค่าแล้วกด Finish/Back ทันที**ต้องไม่หาย** (ค่าถูกบันทึกก่อนออกจากหน้า เปิดโมเดลกลับมาต้องเห็นค่าล่าสุด และ Finish ต้องตั้ง Complete ได้จริงแม้เพิ่งแก้เงื่อนไขให้ผ่านเมื่อครู่) ถ้า Finish ไม่ผ่านด่านจริง ๆ จะกลับหน้า Overview พร้อมแถบเหลือง "The model was not marked Complete." บอกเหตุผล ไม่เงียบ — กดซ้ำกับโมเดลที่ Complete อยู่แล้ว**ต้องไม่สลับกลับเป็น Incomplete** (ยกเลิกต้องกดที่ป้ายเอง ดู BMW-16) และสถานะอยู่ต่อหลังปิดเปิดโปรแกรม — โมเดล Custom ที่ตั้งเงื่อนไขของตัวเองไว้ Finish ได้แม้ workspace จะยังไม่ตั้ง

- [ ] **PM-23 — หน้า PM: แบนเนอร์ "Running condition required" + ตัวเลือก "No condition — use all rows" ของโหมด Custom** 🆕
  - เปิดหน้า PM ของโมเดลที่ใช้ Workspace mode ตอนที่ workspace ยังไม่ตั้ง Running Condition (ดูวิธีเข้าใน PM-22)
  - ในส่วน Training conditions ดูแบนเนอร์เหลือง แล้วกด "Set on Overview →" (ถ้ากดแล้วกลับ Overview) และลองอีกรอบกด "Use Custom instead"
  - ในโหมด Custom ติ๊ก "No condition — use all rows" แล้วติ๊กออก จากนั้นกด "+ Add condition"
  - ปิดเปิดโปรแกรม
  - **คาดหวัง:** แบนเนอร์มีปุ่ม "Set on Overview →" (กลับหน้า Overview) และ "Use Custom instead" (สลับเป็น Custom ทันที) และหายไปเมื่อ workspace ตั้งแล้ว — Custom มีช่องติ๊ก "No condition — use all rows" พร้อมข้อความเตือนสีเหลืองว่าโมเดลนี้จะเทรนด้วยทุกแถวภายในช่วงเวลาที่ตั้งไว้ (ไม่มีช่วง = ทุกแถว) รวมช่วงเครื่องหยุด และปุ่ม Finish ปลดล็อก — ติ๊กแล้วรายการเงื่อนไขถูกซ่อน (เงื่อนไขที่บันทึกไว้ยังอยู่แต่ไม่ถูกใช้ กราฟไม่ถูกกรอง) — เพิ่มเงื่อนไขใหม่แล้วเครื่องหมายติ๊กหายไป — ค่าที่ติ๊กอยู่ครบหลังปิดเปิดโปรแกรม

- [ ] **PM-15 — ชนิดโมเดลล็อกตามที่เลือกไว้ที่ Overview + ข้อความ "Target sensor"** 🆕
  - เปิดโมเดลแต่ละชนิด (Individual / Relationship / Clustering) ทีละตัว
  - ดูแท็บชนิดโมเดลด้านบนกราฟ และหัวข้อการ์ดซ้ายบน
  - **คาดหวัง:** เฉพาะแท็บของชนิดที่โมเดลนั้นเป็นเท่านั้นที่ใช้งานได้ อีกสองแท็บจาง/กดไม่ได้, หัวข้อซ้ายบนเขียนว่า **"Target sensor"** (เดิมเขียน "Predict this sensor") พร้อมการ์ด sensor และข้อความ "From previous page", Relationship/Clustering มีส่วน Predictor sensors (ค้นหาได้) ส่วน Individual ไม่มี

- [ ] **PM-16 — Toolbar หน้า PM เหลือแค่ Back และ Finish** 🆕
  - ดูแถบบนของหน้า PM
  - **คาดหวัง:** มีแค่ปุ่ม **Back** (ซ้าย), breadcrumb (ชื่อ workspace › Target › sensor), และ **Finish** (ขวา) — **ต้องไม่มีปุ่ม Report, Preview, Save Model** (ถูกเอาออกทั้งหมดตามที่สั่ง ไม่มีทางเซฟไฟล์โมเดลจากหน้าจอนี้ตอนนี้ — ตั้งใจ ไม่ใช่บั๊ก)

- [ ] **PM-17 — ไม่มีคำว่า "LinearGAM" ปรากฏใน UI เลย** 🆕
  - เปิดหน้า PM ของโมเดล Relationship, กด Apply, เปิดมุมมอง Sub-models และเปิด modal ขยายกราฟ
  - ดูทุกป้าย หัวข้อ ปุ่ม tooltip ข้อความ error
  - **คาดหวัง:** ทุกที่เขียนว่า **"Relation model"** ไม่มีชื่ออัลกอริทึมจริง (เป็นความลับ — เคยหลุดที่ป้ายหนึ่งแล้วแก้)

- [ ] **PM-19 — ส่วน "Predictor sensors" ในหน้า PM เองก็เป็น popup แบบเดียวกับ Overview แล้ว (เพิ่ม 2026-09-22 — ดู BMW-9/BMW-13)** 🆕
  - เปิดโมเดล Relationship หรือ Clustering เข้าหน้า PM ดูส่วน Predictor sensors ฝั่งซ้าย
  - คลิกปุ่ม "Add predictors…" เลือกหลายตัว (ข้ามหลายกลุ่ม Component) แล้วกด OK
  - ลบ predictor ออกทีละตัวด้วยปุ่ม x บนชิป
  - **คาดหวัง:** popup หน้านี้ทำงานเหมือนที่ Overview เป๊ะ (ค้นหา/จัดกลุ่ม/พับ-กาง/เลือกหลายตัว/OK-Cancel) — target sensor ของโมเดลไม่โผล่ในตัวเลือกให้เลือกซ้ำ, ชิปที่เลือกไว้อยู่นอก popup ลบออกทีละตัวได้ทันทีโดยไม่ต้องเปิด popup ใหม่

- [ ] **PM-20 — Clustering ที่หน้า PM: X sensor และ Criteria Sensor ก็เป็น popup เลือกตัวเดียวเหมือนกัน (เพิ่ม 2026-09-22 — ดู BMW-14)** 🆕
  - เปิดโมเดล Clustering เข้าหน้า PM ก่อนเลือก predictor ใดๆ ดูช่อง "X sensor (vs target on Y)"
  - เพิ่ม predictor 2 ตัวขึ้นไป (ผ่านช่อง Predictor sensors ด้านบน) แล้วกลับมาดูช่อง X sensor อีกครั้ง
  - คลิกช่อง X sensor เลือก sensor อื่นแทน
  - คลิกช่อง Criteria Sensor เลือก sensor 1 ตัว (ไม่จำเป็นต้องเป็น predictor)
  - **คาดหวัง:** ช่อง X sensor เขียนว่า "No predictors selected" และกดไม่ได้ตอนยังไม่มี predictor, พอมี predictor แล้วเลือกได้ทันที (ตัวเลือกจำกัดแค่ predictor ที่เลือกไว้เท่านั้น) — เลือกแล้ว popup ปิดทันทีไม่มีปุ่ม OK — ช่อง Criteria Sensor เลือกได้จาก**ทุก sensor** ไม่ใช่แค่ predictor และมีตัวเลือก "None"

- [ ] **PM-27 — Training conditions: สลับ Workspace/Custom ได้ต่อโมเดล ครอบทั้ง Training periods และ Running condition (แทน PM-21)** 🆕
  - ที่ Overview ตั้ง Training periods อย่างน้อย 2 ช่วง + Running Condition Filter ไว้อย่างน้อย 1 เงื่อนไข (ดู BMW-18, BMW-25)
  - เปิดโมเดลใดก็ได้เข้าหน้า PM ดูส่วน "Training conditions" — ต้องเห็นสวิตช์ "Workspace / Custom" อยู่บนสุด เริ่มที่ Workspace, ใต้สวิตช์ต้องเห็น**ชิปช่วงเวลาอ่านอย่างเดียว** — ไม่มีช่องวันที่ให้พิมพ์
  - คลิก "Custom" — ต้องเห็นรายการ Training periods แบบแก้ไขได้ (พร้อมปุ่ม Add period) โผล่ขึ้นมา พร้อม**ช่วงเวลาที่ก็อปมาจาก Overview อัตโนมัติ**, เงื่อนไขจาก Overview ก็ถูกก็อปมาเป็นแถวที่แก้ไขได้เช่นกัน
  - แก้วันที่ของช่วงที่ก็อปมา / เพิ่ม-ลบช่วง / แก้ sensor-operation-ค่า ของแถวเงื่อนไข / "+ Add condition" / สลับ AND/OR ของโมเดลนี้เอง
  - สลับกลับไป "Workspace" แล้วกลับมา "Custom" อีกครั้ง
  - ปิดเปิดโปรแกรม
  - **คาดหวัง:** ค่าที่แก้ใน Custom (ทั้งช่วงเวลาและเงื่อนไข) ไม่กระทบค่าที่ Overview เลย — กราฟ preview ของโมเดลนี้กรองตามช่วงเวลา+เงื่อนไข Custom ไม่ใช่ของ Workspace แล้ว — สลับ Workspace→Custom→Workspace→Custom ครั้งที่ 2 **ไม่ก็อปทับ**ช่วงเวลา/เงื่อนไขที่แก้ไปแล้ว (ก็อปครั้งเดียวเมื่อยังไม่มีของตัวเอง) — ปิดเปิดโปรแกรมแล้วโหมด/ช่วงเวลา/เงื่อนไข Custom ยังอยู่ครบ — โมเดลอื่นที่ยังไม่ได้สลับเป็น Custom ต้องยังตาม Workspace ปกติ ไม่ได้รับผลกระทบ
- [ ] **PM-28 — กด Finish ทันทีหลังแก้ช่วงเวลา ต้องบันทึกค่าที่แก้ + Complete (แก้บั๊ก QA 2026-09-24)** 🆕
  - เปิดหน้า PM ของโมเดลที่ตั้ง Running Condition ครบแล้ว (โหมด Custom) แก้วันที่ของ Training period แล้วกด Finish **ทันที** (ไม่รอ)
  - **คาดหวัง:** กลับหน้า Overview โมเดลเป็น Complete และเมื่อเปิดหน้า PM อีกครั้งช่วงเวลาเป็นค่าที่เพิ่งแก้ (ไม่ใช่ค่าเก่า) — กด Back ทันทีหลังแก้ก็ต้องเก็บค่าเช่นกัน

- [ ] **PM-29 — ติ๊ก "No condition" ใน Custom แล้วกด Finish ทันที ต้องผ่าน ไม่เงียบหาย (แก้บั๊ก QA 2026-09-24)** 🆕
  - โมเดลแบบ Custom ที่ยังไม่มีเงื่อนไข ติ๊ก "No condition — use all rows" (ลองเอาติ๊กออกแล้วติ๊กใหม่ด้วย) แล้วกด Finish ทันที
  - **คาดหวัง:** โมเดลเป็น Complete ถ้ายังถูกบล็อกด้วยเหตุอื่นต้องเห็นข้อความเหตุผลที่ Overview ห้ามกลับหน้า Overview เงียบๆ โดยโมเดลยังเป็น Incomplete

- [ ] **PM-30 — Match = OR + เงื่อนไข between ที่ยังกรอกไม่ครบ ต้องไม่ทำให้ผ่านทุกแถว (แก้บั๊ก QA 2026-09-24)** 🆕
  - ตั้ง Running Condition แบบ Match = OR ใส่เงื่อนไขปกติ 1 ข้อ และเงื่อนไข "between" อีก 1 ข้อที่กรอกค่าต่ำสุดแต่**ไม่กรอกค่าสูงสุด**
  - เปิดหน้า PM ดูจำนวนจุดของกราฟ (ป้าย N Points)
  - **คาดหวัง:** จำนวนจุดเท่ากับที่กรองด้วยเงื่อนไขปกติข้อเดียว (เงื่อนไขที่ยังไม่ครบถูกข้าม) ไม่ใช่ทุกแถวของข้อมูล

---

## MULTI

### เปิดมากกว่า 1 โปรเจกต์ (ข้อมูลต้องไม่ปนกัน)

_เพิ่มใน 0.5.0 หลังเจอบั๊กว่า special sensor โชว์ component/ชื่อของอีกโปรเจกต์ และ Failure Group ขึ้นบ้างไม่ขึ้นบ้าง/ไม่ตรง — เกิดจากหน้าต่างลูกที่ค้างจากโปรเจกต์เดิม ต้องใช้โปรเจกต์ A และ B จาก PREP-5 ทุกข้อ_

- [ ] **MULTI-1 — Add Special Sensor ที่ค้างอยู่ต้องถูกปิดตอนสลับโปรเจกต์** 🆕
  - เปิดโปรเจกต์ A แล้วเปิดหน้าต่าง Add Special Sensor ทิ้งไว้ (ยังไม่ต้องสร้างอะไร)
  - กลับหน้า Import แล้วเปิดโปรเจกต์ B
  - **คาดหวัง:** หน้าต่าง Add Special Sensor ของ A **ปิดไปเอง** ไม่ค้างอยู่หลังหน้าต่างหลัก (พร้อมกับ Build Model ถ้าเปิดอยู่)

- [ ] **MULTI-2 — special sensor ของ A ไม่โผล่ใน B (ทั้งชื่อ, component, description)** 🆕
  - จาก MULTI-1 เปิดโปรเจกต์ B แล้วกด Add Special Sensor
  - ดูรายการ sensor และแท็บ Manage
  - สร้าง special sensor ใหม่ใน B ตั้ง Component ต่างจากของ A
  - กลับไปเปิดโปรเจกต์ A
  - **คาดหวัง:** เห็นเฉพาะ sensor / recipe ของ B ใน B, sensor ที่สร้างใน B อยู่ใต้ component ที่เลือกจริง (ไม่ใช่ component ของ A), และ A ไม่มี sensor ของ B โผล่มา — recipe ของสองโปรเจกต์แยกกันเด็ดขาด

- [ ] **MULTI-3 — Build Model แสดง FG / ชื่อ / component ของโปรเจกต์ที่เปิดอยู่เท่านั้น** 🆕
  - เปิดโปรเจกต์ B กด Build Model
  - เทียบกลุ่ม ชื่อโมเดล และ component กับที่สร้างไว้ใน B
  - **คาดหวัง:** ตรงกับ B ทุกอย่าง ไม่มีชื่อกลุ่ม/โมเดลของ A ปน

- [ ] **MULTI-4 — สลับ A ⇄ B เร็ว ๆ หลายรอบ** 🆕
  - สลับ A → Import → B → Import → A → B อย่างน้อย 5 รอบเร็ว ๆ
  - ทุกรอบเปิด Build Model ทิ้งไว้ก่อนสลับรอบถัดไป และเปิด Add Special Sensor สลับกัน
  - **คาดหวัง:** ทุกรอบ Failure Group และ sensor ตรงกับโปรเจกต์ปัจจุบันเสมอ ไม่สุ่มขึ้นบ้างไม่ขึ้นบ้าง (บั๊กเดิมเกิดแบบสุ่มเพราะมีตัวตอบข้อมูลเก่าค้างหลายตัว จึงต้องลองซ้ำหลายรอบ)

- [ ] **MULTI-5 — กด Add Special Sensor ซ้ำตอนหน้าต่างเปิดอยู่ ต้องได้ข้อมูลของโปรเจกต์ปัจจุบัน** 🆕
  - เปิดหน้าต่าง Add Special Sensor ค้างไว้ใน B
  - ที่ Dashboard สร้าง Failure Group / ติ๊ก sensor เพิ่ม แล้วกด Add Special Sensor อีกครั้ง
  - **คาดหวัง:** หน้าต่างเดิมถูกดึงขึ้นมาหน้าสุด (ไม่เปิดซ้อน ไม่ขึ้น error) และรายการ sensor เป็นของ B ล่าสุด

- [ ] **MULTI-6 — แก้ FG ที่ Dashboard ระหว่างที่ Build Model เปิดอยู่ (หลายโปรเจกต์)** 🆕
  - เปิด B แล้ว Build Model ค้างไว้
  - ที่ Dashboard ของ B ติ๊ก sensor เข้ากลุ่มใหม่
  - **คาดหวัง:** Build Model ของ B เห็นทันที และไม่มีข้อมูลของ A โผล่มาแทรก

- [ ] **MULTI-7 — Failure Group ต้องไม่หาย (สร้างเยอะ / ปิดเปิด)** 🆕
  - สร้าง FG และโมเดลหลายอัน (10+) ในโปรเจกต์หนึ่ง กด Build Model รัว ๆ
  - ปิดโปรแกรมสนิทโดยไม่ทำอะไรต่อ เปิดใหม่ แล้วเปิดโปรเจกต์เดิม
  - **คาดหวัง:** FG และโมเดลครบทุกตัว — **บั๊กร้ายแรง 2 กลไกที่แก้แล้ว: (1) เขียนไฟล์แข่งกันเองตอนกดรัว ข้อมูลหายเกือบหมด (2) แค่ปิดแล้วเปิดใหม่ก็หายได้ เพราะ Dashboard เขียนทับข้อมูลของ Build Model ด้วยสำเนาเก่า** ⚠ เป็น race ข้ามหน้าต่าง ทดสอบรอบเดียวยืนยันไม่ได้แน่นอน ให้ใช้งานปกติต่อเนื่อง 2–3 วันแล้วเช็คซ้ำ

---

## PER

### ปิดเปิดโปรแกรม & การบันทึก

_หมวดที่สำคัญที่สุด — ทุกอย่างข้างบนไม่มีความหมายถ้าเปิดกลับมาแล้วหาย_

- [ ] **PER-1 — ตั้งค่าทุกอย่างแล้วปิดเปิด**
  - ในหนึ่ง workspace: เลือก sensor หลายตัว, ซ่อนบางตัว, เปลี่ยนสี 2 ตัว, ตรึงแกน 1 ตัว, เปิด alarm 1 ตัว, ตั้ง filter, ตั้ง highlight by time, ตั้งช่วงเวลา, เปลี่ยน aggregation, ลากปรับขนาดแผง, ซ่อนแผงหนึ่ง, สลับไป Scatter แล้วตั้งแกน X/Y + ตรึงสเกล
  - ตั้ง Running Condition Filter 1 เงื่อนไขที่ Build Model และตั้งค่าโมเดลหนึ่งตัว (Training periods, Stiffness หรือ predictor)
  - ปิดโปรแกรมสนิท
  - เปิดใหม่แล้วเปิด workspace เดิม
  - **คาดหวัง:** **ทุกข้อข้างบนต้องกลับมาครบ** — ถ้าข้อไหนหาย ให้จดเฉพาะข้อนั้นไว้ (ไม่รวม tag ที่ปักบนกราฟ ซึ่งตั้งใจไม่บันทึก)

- [ ] **PER-2 — แก้อะไรแล้วปิดโปรแกรมทันที**
  - เปลี่ยนสีเส้น 1 เส้น
  - กดปิดโปรแกรมภายใน 1 วินาที
  - เปิดกลับมา
  - **คาดหวัง:** สีที่เพิ่งเปลี่ยนยังอยู่ (autosave มี debounce 250ms + flush ก่อนปิด)

- [ ] **PER-3 — special sensor กลับมาพร้อมข้อมูลจริง**
  - สร้าง special sensor แล้วปักบนกราฟ
  - ปิดโปรแกรม เปิดใหม่
  - **คาดหวัง:** **เส้นมีข้อมูลจริง ไม่ใช่มีแต่ชื่อแล้วกราฟว่าง** — recipe ถูก replay ใหม่ตอนเปิด

- [ ] **PER-4 — special sensor ที่ต่อกันหลายชั้นกลับมาถูกลำดับ**
  - สร้าง A → B → C
  - ปิดเปิดโปรแกรม
  - ปักทั้งสามบนกราฟ
  - **คาดหวัง:** ค่าของทั้งสามถูกต้อง (replay ต้องเรียงตามลำดับสร้าง ไม่งั้น B จะหา A ไม่เจอ)

- [ ] **PER-5 — สลับ workspace ไปมา**
  - เปิด workspace A ตั้งค่าอย่างหนึ่ง
  - กลับหน้า Import เปิด workspace B ตั้งอีกอย่าง
  - กลับไป A
  - **คาดหวัง:** ค่าไม่ปนกันเลย

- [ ] **PER-6 — Rename workspace**
  - File → Rename Workspace
  - ตั้งชื่อใหม่
  - กลับหน้า Import
  - **คาดหวัง:** ชื่อใหม่ขึ้นทั้งบน title bar และในรายการ Recent Workspaces

- [ ] **PER-7 — เปิด workspace ที่สร้างจากเวอร์ชันเก่า**
  - เปิด workspace เก่าที่สร้างไว้ก่อนหน้านี้นาน ๆ (เช่นสร้างด้วย 0.4.x)
  - **คาดหวัง:** เปิดได้ ไม่ crash และโมเดลที่เคยเก็บ `groupNo` เดี่ยว ๆ ถูกแปลงเป็น `groupNos` เป็น array ให้อัตโนมัติ — โมเดลที่เคยมี Sensor value filter ต่อโมเดลยังเปิดได้ปกติ (ค่านั้นไม่ถูกใช้แล้ว ใช้ Running Condition Filter ระดับ workspace แทน)

- [ ] **PER-8 — ไฟล์ workspace อยู่ที่ไหน**
  - เปิด `%APPDATA%` แล้วหาโฟลเดอร์ของ `com.prompt-solution.tauri-app`
  - **คาดหวัง:** เห็นไฟล์ของ workspace และเวลาแก้ล่าสุดขยับตามที่ทำจริง

- [ ] **PER-9 — ช่วงเวลา (Training periods) ของ workspace ต้องไม่หายเมื่อไปแก้อย่างอื่นที่ Dashboard (แก้บั๊ก 2026-09-23)** 🆕
  - ที่ Build Model → Overview เปิดแผง Running Condition Filter แล้วเพิ่ม Training periods ของ workspace (2 ช่วง)
  - กลับไปหน้า Dashboard ทำอะไรก็ได้ที่เป็นการแก้ค่า (เลื่อนกราฟ, ติ๊กเลือก sensor, สลับ sensor เข้า/ออก Failure Group)
  - รอ 1 วินาที แล้วปิดโปรแกรม เปิดใหม่ ไปดูแผงเดิม
  - **คาดหวัง:** Training periods ที่ตั้งไว้ยังอยู่ครบ (เดิมหายไปหลังแก้อะไรที่ Dashboard ทั้งที่หน้าจอยังโชว์ค่าเดิมอยู่จนกว่าจะปิดเปิด) — หน้า PM ของโมเดลที่ยังเป็นโหมด Workspace ก็เห็นช่วงเวลานั้นเช่นกัน

- [ ] **PER-10 — Workspace จากเวอร์ชันก่อนหน้าที่ตั้ง Time start/end ช่วงเดียวไว้ ต้องเปิดได้เป็น 1 period และคีย์เก่าถูกลบ** 🆕
  - เตรียม workspace ที่สร้างด้วยเวอร์ชัน 0.6.x โดยตั้ง Time start/end ที่แผง Running Condition Filter และตั้ง Custom time ของโมเดลหนึ่งตัวไว้ (หรือใช้ไฟล์สำรองของ workspace นั้น)
  - เปิดโปรแกรมเวอร์ชันนี้ เปิด workspace นั้น เข้า Build Model → Overview เปิดแผง Running Condition Filter
  - เปิดหน้า PM ของโมเดลที่ตั้ง Custom time ไว้ (โหมด Custom)
  - ปิดเปิดโปรแกรม เปิด workspace เดิมอีกครั้ง
  - **คาดหวัง:** แผงแสดง Training periods **1 ช่วง** ตรงกับ Time start/end เดิม (ปลายที่เคยว่างเป็น "Start of data"/"End of data") · โมเดล Custom ก็แสดงเป็น 1 period ตรงกับช่วงเดิมของมัน · กราฟ/สถิติใช้ช่วงเดิมเหมือนก่อนอัปเกรด · ปิดเปิดแล้วค่ายังเหมือนเดิม ไม่ซ้ำ ไม่หาย (ระบบบันทึกกลับครั้งเดียว) · ข้อจำกัด: เปิดไฟล์ที่ผ่านการอัปเกรดแล้วด้วยเวอร์ชันเก่ากว่าจะไม่เห็นช่วงเวลา

---

## ERR

### ความผิดพลาด & ขอบ

- [ ] **ERR-1 — Toast แจ้ง error**
  - ทำให้ backend fail (เช่น ย้ายไฟล์ CSV ออกระหว่างใช้งาน)
  - **คาดหวัง:** ขึ้น toast บอก ไม่ใช่เงียบ — ในตัวติดตั้งไม่มี console ให้ดู toast จึงเป็นทางเดียว

- [ ] **ERR-2 — ไม่มีจอขาว**
  - ลองทำอะไรแปลก ๆ ให้ component พัง
  - **คาดหวัง:** ErrorBoundary จับไว้และแสดงข้อความ ไม่ใช่จอขาวเปล่า

- [ ] **ERR-3 — ไฟล์ log ถูกเขียนจริง**
  - หลังเจอ error ไปหาไฟล์ log ใน `%APPDATA%` ของแอป
  - **คาดหวัง:** มีบรรทัดของ error ที่เพิ่งเจอ พร้อมรายละเอียด

- [ ] **ERR-4 — ปิดหน้าต่างลูกด้วยปุ่ม X**
  - เปิด Add Special Sensor แล้วกด X
  - เปิด Build Model แล้วกด X
  - **คาดหวัง:** ปิดได้ทั้งคู่ ไม่ค้าง (เคยมีบั๊ก capability `core:window:allow-destroy` ที่เห็นเฉพาะตอนติดตั้งจริง)

- [ ] **ERR-5 — เปิดหน้าต่างลูกซ้ำ**
  - กด Add Special Sensor ซ้ำ ๆ ตอนหน้าต่างเปิดอยู่แล้ว
  - กด Build Model ซ้ำ ๆ ตอนหน้าต่างเปิดอยู่แล้ว
  - **คาดหวัง:** ไม่เปิดซ้อนกันหลายบาน — หน้าต่างเดิมถูกดึงขึ้นมาหน้าสุด ไม่ขึ้น error และข้อมูลไม่เพี้ยน

- [ ] **ERR-6 — ไม่ได้เลือก sensor เลย**
  - Clear all แล้วสลับ chart type ไปมา
  - **คาดหวัง:** กราฟว่างพร้อมข้อความ ไม่มี error

---

## PERF

### ประสิทธิภาพ

_ทำกับไฟล์ใหญ่จริงเท่านั้น_

- [ ] **PERF-1 — เปิดไฟล์ใหญ่**
  - import CSV ใหญ่สุดที่มี
  - จับเวลา
  - **คาดหวัง:** เสร็จภายในเวลาที่รับได้ ไม่ค้างจนต้อง force close

- [ ] **PERF-2 — โหลดกราฟและข้อมูลพร้อมกัน**
  - เลือก sensor เยอะ ๆ พร้อมกันทีเดียว
  - สลับ chart type ระหว่างที่ยังโหลดอยู่
  - **คาดหวัง:** ไม่ค้างและไม่ deadlock (เพิ่งเปลี่ยนจาก Mutex เป็น RwLock ให้อ่านพร้อมกันได้)

- [ ] **PERF-3 — สลับ Raw ↔ Avg บนข้อมูลใหญ่**
  - สลับไปมา 5 รอบ
  - **คาดหวัง:** แต่ละครั้งใช้เวลาใกล้เคียงกัน ไม่ค่อย ๆ ช้าลง

- [ ] **PERF-4 — RAM ไม่พุ่ง**
  - เปิด Task Manager ดู memory ของ Wizard
  - ใช้งานหนัก ๆ 10 นาที (สลับกราฟ เปลี่ยน filter สร้าง special sensor)
  - **คาดหวัง:** memory ไม่ไต่ขึ้นเรื่อย ๆ ไม่ลง (leak)

- [ ] **PERF-5 — ซูมเข้า/ออกถี่ ๆ**
  - ใช้ Horizontal zoom ลากซูมเข้าหลายชั้นแล้ว Zoom out สลับกันเร็ว ๆ
  - **คาดหวัง:** ภาพไม่กระตุก ไม่ค้าง ไม่ยิง request ท่วม backend (มี debounce 250ms ที่การเปลี่ยนช่วงเวลา)

- [ ] **PERF-6 — กราฟแกนเวลาเทียบกับเวอร์ชันเดิม** 👀
  - เลือกหลาย sensor บนข้อมูลใหญ่ แล้วสลับ aggregation / เลื่อนช่วงเวลา / ซูม
  - ถ้ามีเครื่องที่ลง 0.4.1 ไว้ ลองเทียบความรู้สึกกัน
  - **คาดหวัง:** ใช้งานได้ไม่ค้าง — **ทราบแล้วว่าช้ากว่าแกน category เดิมพอสมควร** (ECharts time axis ต้องทำงานมากกว่า ลองแก้ 2 วิธีแล้ว revert ทั้งคู่ เก็บไว้ใน BACKLOG ข้อ 21) จดเฉพาะกรณีที่ช้าจนใช้ไม่ไหว พร้อมจำนวน sensor / ขนาดไฟล์

---

## VIS

### กวาดหน้าตา (หลังล้าง CSS ที่ไม่ได้ใช้ และลบโค้ดตาย)

_ใน 0.5.0 ลบ CSS ที่ไม่มีใครอ้างถึงออกราว 3,200 บรรทัด (~7,000 → ~3,800) และลบไฟล์ frontend ที่ไม่มีใครเรียก 20 ไฟล์ (ตารางข้อมูลเก่า, หน้าต่าง Save As, คอมโพเนนต์อัปโหลดเก่า) — ตรวจแล้วว่าไม่มีคำสั่งเรียกใช้ แต่ **jsdom ไม่โหลด stylesheet เทสต์อัตโนมัติจึงจับ style ที่หายไม่ได้** ต้องดูด้วยตา_

- [ ] **VIS-1 — กวาดทุกหน้า/แท็บ/หน้าต่าง หา style ที่หาย** 🆕
  - เปิดไล่ทีละหน้า: Import (ทั้ง 3 ขั้น + รายการ Recent), Dashboard (ทั้ง 3 แผง, แท็บ Sensor / Selected Sensor / Filter / Highlights / Failure Groups), กราฟทั้ง 3 แบบ, Add Special Sensor (Create + Manage + ฟอร์มแก้ไข), Build Model (Overview + ฟอร์มแก้ + หน้า PM + Sub-models + modal ขยายกราฟ)
  - **คาดหวัง:** ไม่มีปุ่มที่กลายเป็นตัวหนังสือเปล่า ๆ ไม่มีช่องกรอกที่ไม่มีขอบ ไม่มีการ์ด/แผงที่พื้นหลังโปร่งจนอ่านทับกันไม่ออก ไม่มี layout ซ้อน/เว้นว่างผิดปกติ ตัวอักษรและระยะห่างสม่ำเสมอ — ถ้าเจอให้จดหน้าที่เห็น + ภาพหน้าจอ

- [ ] **VIS-2 — สถานะ hover / focus / disabled / dropdown / tooltip** 🆕
  - เอาเมาส์ชี้ปุ่ม แท็บ แถวในรายการ
  - กด Tab เดินผ่านช่องกรอก
  - ดูปุ่มที่ควรจาง (Save changes / Build Model → / Add sensor ตอนกรอกไม่ครบ)
  - เปิด dropdown ทุกชนิด (Component, ค้นหา sensor, เมนู Zoom, เมนูปุ่มสามจุดของ workspace) และ tooltip บนกราฟ
  - **คาดหวัง:** hover เปลี่ยนสี, focus เห็นกรอบชัด, ปุ่ม disabled จางและชี้แล้วไม่ขึ้นมือ, dropdown/tooltip มีพื้นทึบอ่านได้ ไม่ถูกตัดขอบ ไม่ทับกับเนื้อหาข้างหลัง

- [ ] **VIS-3 — รอบเร็วผ่านหน้า Import หลังลบคอมโพเนนต์อัปโหลดเก่า** 🆕
  - สร้างโปรเจกต์ใหม่ทั้ง 3 ขั้น (IMP-6 ถึง IMP-17 แบบย่อ): ตั้งชื่อ → อัปโหลด CSV 1 ไฟล์ → mapping → Continue
  - **คาดหวัง:** ผ่านตลอดสายเหมือนเดิม ไม่มีปุ่ม/ขั้นตอนที่หาย (ไฟล์ที่ถูกลบเป็นตัวที่ไม่ได้ถูกเรียกใช้อยู่แล้ว แต่ต้องยืนยันด้วยการใช้จริง)

---

## ของที่รู้อยู่แล้ว — ไม่ต้องรายงาน

- ชื่อไฟล์ exe ที่ติดตั้งคือ `tauri-app.exe` ไม่ใช่ `Wizard.exe` — รู้อยู่แล้ว (BACKLOG ข้อ 17)
- Moving Average / Rate of Change ถูกถอดออกจากรายการ operation แล้ว เพราะไม่เคยมีการคำนวณจริงอยู่เบื้องหลัง
- Line chart แสดงภาพย่อ (downsampled) เสมอแม้อยู่โหมด Raw — ยังไม่มีทางดูทุกแถวจริงนอกจากย่อช่วงเวลา (BACKLOG ข้อ 10 เลื่อนไว้)
- แท็บ "Data Insight" (ตารางข้อมูลดิบ) ถูกเอาออกไปตั้งแต่ 2026-08-16 — ไม่ใช่ของหาย
- **หน้า Build Model ไม่มีปุ่ม Report / Preview / Save Model และไม่มีทางเซฟไฟล์โมเดล (`INDV_INFO` / `REL_INFO` / `CLUS_INFO`) จากหน้าจอ — ตั้งใจ** (เอาออกตามที่สั่ง 2026-09-17; โค้ดเทรนฝั่ง Rust เก็บไว้สำหรับฟีเจอร์ health score ที่ยังไม่ได้ทำ)
- กราฟเส้นแกนเวลาช้ากว่าแกน category เดิมพอสมควร — ลองแก้แล้ว 2 วิธีแต่ revert ทั้งคู่ (BACKLOG ข้อ 21, Notion: "Line chart time-axis ช้ากว่า category axis เดิม")
- การกลับจากแท็บ Failure Groups ที่บางครั้งเด้งไปหน้าเริ่มโปรแกรม (FG-6) — ยังไม่ได้ทำ
- **ข้อจำกัดของการแก้บั๊กหลายโปรเจกต์ (0.5.0):** การเปลี่ยนชื่อ workspace ยังเป็น event แบบ global ไม่ระบุโปรเจกต์ และข้อมูลใน Rust มีชุดเดียวใช้ร่วมกันทุกหน้าต่าง — วิธีกันคือปิดหน้าต่างลูกทุกบานตอนสลับโปรเจกต์ (MULTI-1) ไม่ใช่แก้เชิงโครงสร้าง; และ race ตอนเขียนไฟล์ workspace ข้ามหน้าต่างยังเหลือโอกาสน้อยมากในทางทฤษฎี (MULTI-7)
- คำเตือน bundle ของ echarts เกิน 500kB ตอน build — ไม่กระทบการใช้งาน (BACKLOG ข้อ 9)
