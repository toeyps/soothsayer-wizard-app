# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

**Wizard** (repo: Soothsayer-wizard-app) is a Tauri v2 desktop app for importing sensor CSV data, exploring it (dashboard with table/line/scatter/pair-plot), building failure groups, and training predictive models. Three tiers:

- **Frontend**: React 19 + TypeScript + Vite + Tailwind CSS v4. ECharts for line charts; `regl-scatterplot` (WebGL) for scatter and pair-plot cells; `split.js` for resizable panes; `lucide-react` icons.
- **Rust core** (`src-tauri/`): Tauri v2 commands — CSV parse/merge (`csv` + `rayon`), in-RAM columnar store, filtering/stats, clustering (`nalgebra` GMM ellipse fits), formula engine (`fasteval`).
- **Python sidecar** (`src-tauri/bin/backend-<target-triple>`, Nuitka-compiled from `src-tauri/python/backend.py`): LinearGAM relationship-model fit/predict. Spawned via the shell plugin's sidecar API; JSON over stdin/stdout. The sidecar binary must exist before `tauri dev`/`tauri build` — see README for the Nuitka compile command (output filename must carry the target triple).

## Commands

```bash
npm run tauri dev                 # full app (compiles Rust, needs sidecar binary)
npm run dev                       # frontend only (Vite, port 1420)
npm run build                     # tsc type-check + vite build — run before handing off
npx vitest run                    # frontend tests (jsdom, src/__tests__/)
npx vitest run src/__tests__/useScatterSample.test.ts   # single test file
cd src-tauri && cargo test        # Rust unit tests (in-module) + integration
cd src-tauri && cargo test --test predictive_model_tests # integration only
npm run tauri:build               # release bundle; per-target + installer scripts in scripts/
```

## Architecture

### Data path (the core of the app)

1. `load_csv` (Rust) parses one or more CSVs in parallel with rayon (**2 GB/file hard cap** — `MAX_CSV_BYTES` in `csv_processor.rs`), merges multi-file datasets by timestamp (later-non-null-wins on duplicate timestamps; single-file loads take a fast path that skips the merge), and stores the result in Tauri managed state.
2. The in-RAM store is **`ColumnarData`** (`csv_processor.rs`) — column-major on purpose: one contiguous `Vec<f64>` per sensor, **NaN = missing**; `timestamps` keeps the CSV text (see Buddhist-Era note below — not always byte-for-byte verbatim anymore); `ts_parsed` holds epoch-microseconds parsed **once at load** (`TS_MISSING` when absent). `headers[0]` is the canonical timestamp column. Filters must compare against `ts_parsed` — never re-parse timestamp strings per query. New aggregations should walk columns, not rows.
   - **⚠️ 2026-09-22: Buddhist-Era (พ.ศ.) years are auto-detected and converted to Gregorian (ค.ศ.) at load, in BOTH `timestamps` and `ts_parsed` — `ColumnarData::from_parts` runs every row through `normalize_buddhist_era` before parsing.** Detection rule: a leading 4-digit year `>= 2400` (BE 2568 ≈ CE 2025; BE 2400 = CE 1857 — no real sensor CSV needs either side of that threshold to mean something else, so this runs unconditionally, no per-import opt-in). This is deliberate, user-confirmed scope (not a partial patch): the user chose "display ค.ศ. everywhere" over "keep showing พ.ศ." specifically because `timestamps` (verbatim display text, Raw-mode) and `ts_parsed` (aggregated/computed display, via `micros_to_naive`) would otherwise disagree on the year for the same row once only one of them was corrected. The load report (`CsvLoadReport.warnings`, surfaced in `DataUploadPage`'s dataset card — see next bullet) tells the user this happened; it is not silent. Root motivation: a BE leap day like `2567-02-29` (BE 2567 = CE 2024, a real leap year) fails to parse AT ALL if read as literal CE — CE 2567 isn't a leap year, so the row was silently dropped as `TS_MISSING` before this fix (288 rows in one reported case). If you add another timestamp ingestion path, route it through `ColumnarData::from_parts` (the single choke point every load path already funnels through) rather than reimplementing normalization.
   - **`CsvLoadReport.warnings` is now actually shown to the user** (2026-09-22) — previously computed by Rust (duplicate columns, duplicate timestamps merged, non-numeric values replaced, and now the Buddhist-Era note above) but never rendered anywhere in the frontend. `DataUploadPage.tsx`'s dataset card now renders `report.warnings` as an amber banner (distinct from the red `dataUpload.error` banner right above it) — non-fatal, parse still succeeded, but the user should see it before clicking Continue.
3. **The frontend never receives the full dataset** (since v0.2.1). Display queries run Rust-side in `chart_query.rs` — filter → operation transform → optional hourly aggregation → min/max downsample — so the WebView only ever gets O(max_points) rows:
   - `get_chart_data` (hard ceiling 100k points) via `useChartData` — line chart. (There is no data-table query any more: `get_table_page`, `useTablePage` and `DataTable` were deleted 2026-09-20, after the Data Insight tab was removed 2026-08-16.)
   - `get_scatter_sample` (reservoir sampling, `max_points` cap) via `useScatterSample` — scatter + pair plot.
   - `export_chart_csv` writes CSV exports straight to disk from Rust instead of round-tripping rows through JS.
4. Chart-level GPU guards: `ScatterChart` and `PairPlotCell` handle WebGL context loss (retry overlay) and apply defensive stride caps (500k / 100k points) as a last line of defense.

### IPC contract — conventions that break silently if violated

- **There is no central command-signature map.** `src/types/commands.ts` holds the shared *payload* types that hooks and components import (`ChartViewData`, `DashboardDataFilter`, `RelationshipPreviewResult`, …) — it is not a registry of every command. The `TauriCommands` type-map that used to live there was deleted on 2026-09-01 after an audit found nothing in the codebase imported it (see `docs/PROJECT_HANDOVER.md`); **don't recreate it**. A command's contract is enforced by the two rules below plus the explicit return type at each `invoke()` call site.
- **Arg-key casing (the #1 silent-failure trap)**: this codebase sends **snake_case keys from TS** matching the Rust parameter names (`max_points`, `first_sensor`, `save_path`). But Tauri v2's command macro expects **camelCase keys by default**, so every command with a multi-word parameter MUST carry `#[tauri::command(rename_all = "snake_case")]`. Missing it → invoke rejects with `missing required key maxPoints`-style errors, which hooks may swallow, so the feature just silently does nothing.
- Never call `invoke()` without an explicit TypeScript return type.

### Windows & capabilities

Multi-window app: `main` (upload → dashboard) plus sub-windows `build-model` (full capability, `src-tauri/capabilities/default.json`, same as `main`) and `add-sensor` (slimmer capability, `add-sensor.json`). The fs plugin is scoped to `$APPDATA` — writes to arbitrary user-picked paths (CSV/PDF/PNG exports) must go through the Rust `write_user_file` command, via `writeUserTextFile` / `writeUserBinaryFile` in `workspaceManager.ts`.

**2026-08-06**: the standalone `failure-group` sub-window (`FailureGroupCreation.tsx`) was deleted — failure-group management now lives inline in the Dashboard window as a "Failure Groups" tab in the Sensor panel (`FailureGroupsPanel.tsx`), plus a per-sensor quick-assign in the existing Sensor tab (`SensorSelection.tsx`). **2026-09-20**: the `save-as` window (`SaveAsWindow.tsx`) and its only spawner `useSubWindowMenu` were confirmed unreachable and deleted — renaming a workspace is done inline in `DataUploadPage` (`handleRenameWorkspace`). Don't re-add a `save-as` route/capability label. See `docs/PROJECT_HANDOVER.md` for the full account.

### Workspace persistence & auto-resume

- `src/workspaceManager.ts` owns save/load/list/delete/duplicate/rename — frontend-only, via plugin-fs + plugin-store.
- Lightweight state (recent-workspace list, settings) → plugin-store `settings.json`. Heavy `WorkspaceState` → JSON files under `$APPDATA`.
- **No auto-resume on launch** (corrected 2026-09-18 — this section previously said the opposite, which was stale): the app always boots into the Import page (`App.tsx`'s own comment: "there is no auto-resume"). The user picks a Recent Workspace to reopen past work — `DataUploadPage.handleLoadWorkspace` decides which page/tab to land on from the loaded `lastRoute`. Keep React state synced to storage frequently regardless — a workspace reopened mid-session must reflect the last save, even without auto-launching into it.
- **⚠️ 2026-09-18 [CRITICAL, real data-loss report] Dashboard's full-overwrite autosave used to be able to erase another window's fresher `failureGroupState`, with no rapid clicking needed.** Symptom: user closes and reopens the app (no other action at all) and every Failure Group/model is gone. Root cause: `Dashboard.tsx`'s debounced autosave (`saveWorkspaceData`, a full-object overwrite, fires ~250ms after *any* Dashboard state change — panning a chart, toggling a sensor, anything) used to bake in Dashboard's own in-memory `fgGroups`/`fgModels` mirror. That mirror is kept current only by a `'failure-group-state-changed'` broadcast from `BuildModelWindow.tsx`/`PredictiveModelBuild.tsx` (separate OS windows) — best-effort cross-window IPC, not a guaranteed-ordered channel. If the broadcast hadn't landed yet, the very next unrelated Dashboard edit clobbered the other window's fresher on-disk write with Dashboard's stale copy. Fixed by re-reading `failureGroupState` fresh from disk (`loadWorkspaceData`) right before every Dashboard autosave write, instead of trusting the local mirror — see `docs/PROJECT_HANDOVER.md`'s matching entry for the full investigation and why this is a *different* bug from the earlier `workspaceManager.ts` same-window write-queue fix (that one only serializes writes within one window; this one needed no rapid same-window actions to reproduce at all). This narrows but does not fully eliminate the race — a genuinely simultaneous cross-window write is still theoretically possible; a fully race-proof fix needs a Rust-side atomic read-modify-write shared across processes, not yet built.
- **⚠️ 2026-09-21 [multi-project isolation — rules for any cross-window code]**: the user only ever tested with ONE project; with two, special sensors showed another project's component/name and Failure Groups appeared/vanished/belonged to the wrong project. Root causes, all fixed — don't reintroduce them: (1) **Every Tauri `emit` is a global broadcast to every window and there is ONE shared Rust `AppState` dataset**, so any window left over from the previous project keeps talking to (and computing columns in) the new one. `Dashboard` now closes BOTH the `build-model` and `add-sensor` windows on unmount, and re-points an already-open one (`emitBuildModelData` / re-sent `sensors-data`) instead of merely focusing it. (2) **Every cross-window payload must carry `workspaceId` and the receiver must drop a mismatch** (`failure-group-state-changed`, `sensors-data`, `add-sensor-selection`, `delete/update/rename-special-sensor`); a payload with no id is treated as foreign, never guessed at. `failure-group-state-changed` also carries `origin` (`dashboard` | `build-model` | `predictive-model`) so a window skips its own echo — Dashboard now broadcasts its own FG edits too (it used to be the only writer that didn't, so an open Build Model window went stale). (3) **Never write `unlisten = await listen(...)` + `return () => unlisten?.()`** — cleanup that runs before the promise resolves (StrictMode, or a dependency change right after mount) leaves the handler alive forever as a stale closure that answers requests with an OLD project's data. Use `subscribe()` from `src/utils/tauriEvents.ts` (synchronous, leak-proof cleanup; `await off.ready` before emitting a request whose reply must not be missed). Subscribe once and read fresh values through a ref rather than re-subscribing on every dependency change (`request-build-model-data`). (4) `BuildModelWindow` guards its async loads with a sequence counter and resets its PM page/form when re-pointed at a different workspace. `App.tsx` also keys `<Dashboard>` by workspace id. Still open (not fixed): `workspace-renamed-internal` is still an unscoped global event, and the single shared Rust dataset means any window that outlives its project can still run commands against the wrong data — closing them on unmount is the mitigation, not a structural fix.

### Predictive models (`PredictiveModelBuild.tsx`)

- **Individual**: Rust — per-sensor stats/boundaries → `INDV_INFO_*.json`.
- **Clustering**: Rust — GMM ellipse fits (nalgebra), 1..N clusters split by criteria-sensor ranges → `CLUS_INFO_*.json`.
- **Relationship**: Python sidecar (LinearGAM) — Rust pre-cleans/projects rows and ships small arrays over stdin; sidecar returns JSON; the sidecar writes the `.pkl`, Rust writes `REL_INFO_*.json` alongside. Output layout matches the legacy `wizard.py` (`{save_path}/output/{target}/`).
- **Naming rule: the UI must say "Relation model" — the LinearGAM algorithm name is confidential and must never appear in user-facing UI** (code comments are fine).
- **⚠️ 2026-09-17: no UI path currently trains/saves a model — kept intentionally.** The user had the toolbar's "Preview" button (and everything gated behind it — the Preview modal, the Confirm Save modal, `handleSaveModel`, `pickSavePath`, the save-plan derivation) removed from `PredictiveModelBuild.tsx` as unused, on top of an earlier pass the same day that already removed the toolbar's "Save Model"/"Report" buttons. `train_individual_model` / `train_relationship_model` / `train_clustering_model` (`src-tauri/src/lib.rs`) are unchanged and still grep-confirmed as the only callers of the GMM/LinearGAM training code, and nothing in `src/` invokes them anymore. **This is deliberate, not an oversight**: the user confirmed the `INDV_INFO_*.json`/`REL_INFO_*.json`/`CLUS_INFO_*.json` files these commands write are needed for a planned health-score feature (not built yet) — they aren't using the saved files today, but will need this exact backend later, so don't delete it. When that feature gets built, add its own save/export trigger rather than assuming this page's old Preview/Save Model UI should be resurrected — it may not be the right shape for that flow. The Sub-models modal (multi-predictor Relationship comparison, opened via the `LayoutGrid` button on the Relationship chart) is unrelated and still works — it's preview-only, no save path.
- **"Finish" button (added 2026-09-17, right after the Preview removal above)**: the toolbar's replacement for that empty space — has nothing to do with training. It calls `onFinish` (passed down from `BuildModelWindow.tsx`), which sets that `FailureModel`'s `status: true` (the same boolean the overview list's "Complete"/"Incomplete" pill reads and toggles via `toggleModelStatus`) and navigates back to the overview. One-directional by design (`markModelComplete` in `BuildModelWindow.tsx`, distinct from `toggleModelStatus`) — re-clicking Finish on an already-complete model can't accidentally flip it back to Incomplete; un-marking still goes through the overview's own pill.
- **⚠️ 2026-09-18 [bug fix]: `BuildModelWindow.tsx`'s "Build Model →" must `await commitForm()` before navigating to the PM page.** `commitForm()`'s `persist()` write used to be fire-and-forget — `buildModelFromForm` called it, then immediately called `trainModel(modelId)`, switching to `PredictiveModelBuild.tsx` synchronously. That page's own hydration effect calls `loadWorkspaceData` **directly**, not queued behind the still-in-flight `updateWorkspaceData` write (same window, but `loadWorkspaceData` doesn't go through `workspaceManager.ts`'s write queue at all) — so it could win the race and hydrate from the model record as it was *before* this commit. Reported symptom: predictors just picked on the Overview form (required for Relationship — `formValid` demands ≥1) showed as "No predictors selected" the instant the PM page opened. Fixed by making both `commitForm` and `buildModelFromForm` `async` and awaiting the persist before calling `trainModel`. If you add another path that commits form state and then immediately navigates to a page that reads the same workspace file, await the commit first — this is a general pattern, not specific to predictors.
- **`SensorAutocomplete` (exported from `PredictiveModelBuild.tsx`, shared by both it and `BuildModelWindow.tsx`) supports optional component-grouping (added 2026-09-18)**: pass `getComponent?: (tag: string) => string` to group the dropdown's matches under per-component headers (alphabetical, "Uncategorized" fallback), instead of one flat list. Omit it to keep the old flat behavior — every existing caller except `BuildModelWindow.tsx`'s "Add a predictor…" field (which also replaced a plain native `<select>` with this component, gaining search for free) still does. `BuildModelWindow.test.tsx` mocks this component out entirely, so it can only assert the `getComponent` prop is wired through correctly — the actual grouped/searchable rendering is tested directly against the real component in `PredictiveModelBuild.test.tsx`'s own `describe('SensorAutocomplete', ...)` block. Gotcha hit while writing those tests: `fireEvent.change(input, { target: { value: '' } })` is a silent no-op when the input's value is already `''` — React's internal value tracker sees no diff and never calls `onChange` — so opening the dropdown in a test needs a non-empty query.
- **`PredictorPickerModal` (exported from `PredictiveModelBuild.tsx` next to `SensorAutocomplete`, added 2026-09-22)**: popup multi-select for predictor sensors — search + collapsible per-component groups (mirrors Dashboard's `SensorSelection.tsx`: everything starts collapsed, a non-empty search forces every matching group open) + checkboxes, staged in local `pending` state and only committed via `onConfirm(tags)` on OK (Cancel/Escape/backdrop-click all discard). Replaces the inline `SensorAutocomplete` that both predictor fields (this page's own "Predictor sensors" section, `BuildModelWindow.tsx`'s Relationship form) used to render directly — `SensorAutocomplete` closes on every single selection by design (it's built for single-value pickers like Target/criteria sensor, which still use it unchanged), so adding N predictors took N reopen-search-click cycles, and a long ungrouped list read as cluttering the page. **Don't add a second `getComponent`-driven flat/grouped toggle to `SensorAutocomplete` itself to solve this** — the "pick several, confirm once" interaction is different enough from "pick one, close" that forking `SensorAutocomplete`'s behavior would tangle two callers' needs into one component; a separate component was the point. `BuildModelWindow.test.tsx` mocks `PredictorPickerModal` out (same treatment as `SensorAutocomplete`) — its own search/grouping/collapse/multi-check/confirm/cancel/Escape behavior is tested directly against the real implementation in `PredictiveModelBuild.test.tsx`.
- **⚠️ 2026-09-22: the "Data filter" sidebar section is renamed "Training scope" — do not reintroduce a second date filter.** User-reported confusion: Time start/end + Running condition looked exactly like Dashboard's own display-only "Filter" tab, but they're not — this filter feeds BOTH the chart preview on this page AND every `train_*` command (same `dashboardFilterPayload`, one query, no separate display-only version). Considered and explicitly rejected: adding a second, independent "chart-only" date filter — that would just relocate the confusion (which of the two date pickers is which?) instead of resolving it. What shipped instead: (1) renamed the section "Training scope" with a `.pm-section-hint` line stating it feeds both; (2) the chart's own 🔍 zoom (`LineChart.tsx`'s zoom-menu button — genuinely client-side/view-only, never re-queries) gets an explicit `.pm-chart-zoom-note` badge next to the legend ("Zoom = view only") so the ONE real display-only control on this page is called out by name instead of being an unlabeled icon. If a future request asks for the chart to preview a narrower range than the model trains on, that's a materially different feature (an actual second query) — don't build it by just resurrecting a second date-filter UI without confirming that's really what's being asked for.

### Report export (PM page) — removed

The "Report" (PNG export) button was removed from `PredictiveModelBuild.tsx` on 2026-09-17 (unused per the user). `usePMReport.tsx` and its test are deleted along with `workspaceManager.ts`'s `writeUserBinaryFile` (its only caller). `src/components/reports/pmReportTypes.ts` stays — it holds the unrelated Relation-model stiffness presets, not report code.

**CSP note — `'unsafe-eval'` is REQUIRED, do not remove it**: the production `script-src` in `tauri.conf.json` carries `'unsafe-eval'` because **`regl-scatterplot` cannot work without it**. regl generates its draw commands as source strings and compiles them at runtime through the Function constructor (`node_modules/regl/dist/regl.js:6015`, `Function.apply(null, …)`) — that codegen *is* regl's execution model, not an optional path. Block it and creating a scatterplot throws `EvalError`, so the Scatter and Pair Plot charts render as empty panels.

This was tried and reverted on 2026-09-03 (shipped broken in 0.4.0, fixed in 0.4.1). Two traps to know about if anyone reconsiders it:
- **Grepping the bundle is not enough.** The removal was justified by searching the built chunks for `new Function(` / `eval(` and finding only a dead ECharts fallback. regl reaches the constructor via `Function.apply`, so every one of those greps missed it. Only executing the code under the policy found it.
- **Dev mode cannot see it.** `devCsp` still allows `'unsafe-eval'`, so `tauri dev` passes regardless. Any CSP change has to be checked in an installed build, or against the built `dist/` served under the production policy.

`'wasm-unsafe-eval'` does not help here — it permits WebAssembly compilation, not JavaScript eval.

## Release checklist — required before every `.exe`/installer build

**Every time this app is built into an installer (`npm run tauri:build`, `installer:win`, or equivalent), `docs/release/CHANGELOG.md` must get a new version entry in the same pass — no exceptions, don't wait to be asked.**

**Builds happen rarely — often weeks apart with a lot accumulated in between — so `git log <last-shipped-tag>..HEAD` alone is not a reliable source for either step below.** This project's commits routinely bundle many days of distinct, unrelated fixes into one commit message (see the actual git history — e.g. one commit covers code-splitting, PDF-export removal, 6 dependency removals, and 4 separate Notion tasks all at once), because commits themselves aren't made after every finished task, only every so often. **`docs/PROJECT_HANDOVER.md`'s dated `🆕 YYYY-MM-DD` entries are the primary source for both steps** — they're written per finished task with full context, unlike commit messages. Read every entry dated after the previous shipped version's date, use `git log <last-shipped-tag>..HEAD` only as a completeness cross-check (catch anything that landed in code but was never written up in the handover), and flag to the user anything in git history with no matching handover entry rather than silently guessing at what it was.

1. **Decide the version bump using this table** — from the categories found across those handover entries (same categories as the changelog entry itself, step 2 below), bump by the single highest-severity category found. Every real (non-dev) build bumps at least PATCH, even for a one-line fix, so no two shipped builds ever share a version number:

   | Highest category present since last shipped tag | Bump |
   |---|---|
   | ⚠️ Breaking change (old workspace files stop working, a major feature is removed) | **MINOR** (reset patch to 0) — do **not** bump MAJOR for this; MAJOR stays `0` until the user explicitly declares the app has reached a "1.0, production-ready" milestone. That's a deliberate call the user makes, not something to infer from a changeset. |
   | ✨ New feature / major improvement (no breaking change) | **MINOR** (reset patch to 0) |
   | Only 🐛 Bug fixes / ⚡ Performance / 🧪 Tests / 📝 Docs / 🗑️ Removed (non-breaking) | **PATCH** |

   Apply the resulting version to `package.json`, `src-tauri/Cargo.toml`, and `src-tauri/tauri.conf.json` — all three must match.
2. Before building, write a new `## [x.y.z] — YYYY-MM-DD` section at the top of `docs/release/CHANGELOG.md` (newest first) summarizing what changed since the previous shipped version — condense the relevant `docs/PROJECT_HANDOVER.md` entries (not raw commit messages) and group into Breaking changes / Features / Bug fixes / Performance / Tests / Docs / Removed, matching the style of the existing 0.2.1 entry (this is also the exact category list step 1 reads from — decide the bump and write the note from the same pass over the same set of entries, don't do them independently). Written for the end user, not just developers — explain user-visible symptoms fixed, not just function names touched. Reverted/superseded work documented in the handover (e.g. a feature tried then rolled back the same day) shouldn't get its own changelog line — only the net effect that actually shipped matters to the end user.
3. Run the build (`scripts/build-installer-windows.ps1` on Windows — verifies prerequisites, builds the Python sidecar if missing, then `tauri build`).
4. After a successful build, tag the release (`git tag vX.Y.Z`) so the next changelog entry has an anchor to diff against.

**Windows output is `.exe` only** — `src-tauri/tauri.windows.conf.json` sets `bundle.targets: ["nsis"]`, overriding the main config's `"all"` just for Windows builds so `tauri build` on this platform produces only the NSIS `.exe`, no `.msi`. Don't remove this without asking — it's a deliberate user preference, not an oversight. (macOS builds are untouched — the main `tauri.conf.json`'s `"all"` still applies there, giving `dmg`/`app`.)

## Testing discipline

**Every code change ships with a test change in the same pass — no exceptions, don't wait to be asked.**

- New file/function/component → new test file/case covering it.
- Changed behavior (UI flow, hook return shape, command args, business logic) → update the existing test(s) that cover it so they assert the *new* behavior, not the old one. A test that still passes after a behavior change but never actually exercises the new behavior is worse than no test — it hides the drift.
- Renamed/removed a prop, field, command name, or UI copy string? Grep `src/__tests__/` (and `src-tauri/tests/` + `#[cfg(test)]` blocks) for the old name before considering the change done.
- This applies to *modifications*, not just additions — editing `Dashboard.tsx` means checking `Dashboard.test.tsx` still matches, not just leaving it alone because it currently passes.
- Verify with `npx tsc --noEmit` + `npx vitest run` (frontend) and `cargo test` (Rust) before calling a task done.
- Coverage reached 586 frontend tests / 45 files + 139 Rust unit tests as of 2026-08-10 specifically because this discipline was absent for most of the project's history — UI was reshaped multiple times (step-based onboarding, Failure Group panel moves, Class B persistence) while old tests sat untouched. They happened to still be accurate on inspection, but that was luck, not process — see `docs/PROJECT_HANDOVER.md` entry 2026-08-10 for the audit. Don't rely on luck going forward.

## Manual test plan — two files, one source of truth

`docs/testing/MANUAL_TEST_PLAN.md` is the source; `docs/testing/manual-test-plan.html` (the interactive page testers fill in) is **generated** from it. Edit the `.md`, then run `python scripts/build-manual-test-html.py` (`--check` verifies the two match — don't hand-edit the HTML's data line). Whenever a user-visible feature is added, changed or removed, update the matching plan items in the same pass: a removed feature's items must be deleted (a stale item sends the tester looking for a button that no longer exists), and a changed one needs a **new id** if its meaning changed enough that an old saved pass/fail would be misleading (results in the HTML are keyed by id).

## Post-task checklist — Notion sync + git push, required after every task, no exceptions, don't wait to be asked

The user tracks all work in the Notion database **"wizard application plan improvement"** (`collection://39e959a6-c718-8039-b30b-000bbea5ca96`). Schema: `Project name` (title), `Status` (status type: `Not started` → `In progress` / `wait for advisor` / `waiting re design` → `Done`), `Note` (text), `technical stack` (multi-select: `UX/UI`, `algorithum`, `backend`, `Sequence UX/UI`).

**Whenever a task finishes (whatever its origin — a Notion item the user pointed at, or something asked directly in chat that was never in Notion):**
1. Query the database for an existing page matching the task (fuzzy-match `Project name`/`Note` against the task's subject — if genuinely ambiguous which page it is, ask rather than guess).
2. If found, update it. If not found, create a new page (`Project name` = short task title, `Note` = 1-2 line summary, `technical stack` = best-guess tag(s) from the work's nature).
3. **Never set `Status` to `Done` on your own judgment — not even after `tsc`/`vitest`/`cargo test` all pass.** Set it to `In progress` (or leave whatever in-progress-family status it already had) instead. Only flip it to `Done` when the user explicitly confirms they've checked the real running app and it's fine — this project's whole history is full of fixes that passed every automated check yet still needed 2-3 more rounds after real manual testing surfaced a regression (see `docs/PROJECT_HANDOVER.md`'s many "ยังไม่ได้ทดสอบบนแอปจริง" entries); a prematurely-"Done" Notion item would misrepresent that.
4. **Design tasks — Figma only for genuinely new designs, Artifact is fine for small tweaks:** if the task is a full new design (a new screen/feature mockup the user wants to hand-edit themselves), push the actual design into a real Figma file via the Figma MCP tools (`create_new_file` / `use_figma` / `upload_assets`) — not just an Artifact — after the direction is validated in chat (per the confirm-before-implementing habit below). Set the Notion item's `Status` to **`waiting re design`** with the Figma file URL in `Note`, so it's visibly "in the user's court." When the user later says they've finished editing in Figma and it's ready to read back, re-fetch the Figma file (`get_design_context`/`get_screenshot`) before implementing, and move `Status` back to `In progress` (then `Done` per rule 3 once they confirm it works). If the task is just a small tweak/bug fix to an existing design (not a from-scratch design), an Artifact mockup is enough — don't push those to Figma. This is a different thing from `docs/reference/figma/*.svg` in this repo — those are static reference mockups from earlier design rounds and are explicitly *not* kept in sync with code changes (don't touch them unprompted, per standing user feedback); this rule is about actively pushing genuinely new design work into Figma when the user wants to edit it there.
5. **Commit locally, unasked, in the same pass — but hold the `git push` until the user confirms they tested the real running app** (`npm run tauri dev`), per their explicit 2026-08-18 correction. Reasoning: `tsc`/`vitest` can't exercise things like Tauri capability/permission config (a spawned window can pass every automated check yet render completely blank at runtime because its label wasn't granted any capability — this exact bug reached a pushed commit and prompted the rule change). Once the user says the app works, push everything accumulated since the last push to the `personal` remote (`toeyps/soothsayer-wizard-app`) — **never** `origin` (`Alpha-Com-Thailand`), even though `origin` exists and tracks `main` and even though push access to it may work; that remote choice is a separate, still-standing user decision. Follow the repo's standard commit-message conventions (see git history) and include the `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>` trailer on every commit.

## Security constraints

- 2 GB per-file CSV cap (`MAX_CSV_BYTES`).
- Any new file-I/O command must use the existing helpers in `lib.rs`: `validate_read_path` (frontend-supplied read paths) and `sanitize_filename_component` (model names → filenames).
- Dataset CSV exports escape Excel formula injection; the formula engine enforces expression length + nesting-depth limits.
- Don't widen the CSP or fs scope without explicit approval.

## Agent Roles & File Ownership

> Only for large, contract-clean features (new Tauri command + its consuming hook + UI, cleanly phased — e.g. the original Data Upload Page redesign and the Predictive Model Rust port, both in `docs/planning/task.md`). Most work on this project is small, iterative, cross-cutting fixes done directly in the main session — see `multi_agent_orchestration_design.md` for the explicit criteria on when to spawn this pipeline at all. Each agent must ONLY read and modify **application code** within its own zone (test files are the one deliberate exception below). Architectural decisions must be approved by the PM Agent.

| Agent            | Zone (app code)                                  | Also writes                    | Responsibility                                  |
|------------------|---------------------------------------------------|---------------------------------|-------------------------------------------------|
| `pm-agent`       | `docs/` (incl. `docs/planning/task.md`)                   | —                               | Requirements, task breakdown, coordination, final sync-check |
| `fe-ui-agent`    | `src/components/`, `src/App.tsx`                 | `src/__tests__/` (its own components) | React UI, Tailwind styling, charts        |
| `fe-logic-agent` | `src/hooks/`, `src/types/`, `src/workspaceManager.ts` | `src/__tests__/` (its own hooks/types) | Hooks, Tauri bindings, state management |
| `rust-agent`     | `src-tauri/src/`, `src-tauri/Cargo.toml`         | inline `#[cfg(test)]` in the same file | Tauri commands, CSV parsing, file I/O   |
| `qa-agent`       | —                                                 | `src/__tests__/`, `src-tauri/tests/` | Final integration/regression sweep across everything workers built — not the sole test author |

**Workflow**: pm-agent plans in `docs/planning/task.md` → workers implement phase by phase, **each writing the tests for the code they just wrote in the same pass** (matches the "test with every change" rule above — don't defer your own unit tests to qa-agent) → **shared types first**: backend-facing features add their arg/result types to `src/types/` before UI (there is no central command map — see "IPC contract" above) → qa-agent runs last, writes cross-cutting integration tests, and re-runs the full suite → **pm-agent runs `npx tsc --noEmit` + `cargo check --lib` + the full frontend/Rust test suites itself before reporting to the user** — don't just trust workers' individual HANDOFF claims (a signature change in one zone silently broke a test file owned by another agent once already; see `docs/planning/task.md`'s Feature 3 closing note). Load only files needed for the current task; do not scan the full repo.

When your task is complete, output this block before stopping:

```
## HANDOFF
- Completed: [what was built]
- New commands added: [list or "none"]
- Files changed: [list]
- Needs qa-agent: [yes / no — and what to test]
- Blocking issues: [none / describe]
```

## Anti-patterns

- ❌ `invoke()` without a TypeScript return type, or with camelCase arg keys
- ❌ A new command with multi-word args but no `#[tauri::command(rename_all = "snake_case")]`
- ❌ Storing large data in `plugin-store` (use the filesystem)
- ❌ Resetting full state on reload (breaks Auto-Resume)
- ❌ Re-parsing timestamp strings per query, or row-major loops over `ColumnarData`
- ❌ Shipping unbounded row counts to the frontend (use the bounded `chart_query` commands / `get_scatter_sample`)
- ❌ Exposing "LinearGAM" in user-facing UI (say "Relation model")
- ❌ Using Tauri v1 API syntax
- ❌ An agent modifying files outside its zone
- ❌ Landing a code change (new or modified) without a matching new/updated test in the same pass

## Repo state snapshot (2026-07-14; last shipped release updated 2026-09-21 — verify with `git log` before trusting)

- **Last shipped: v0.5.0 (2026-09-21)** — next changelog entry diffs against tag `v0.5.0`. The RAM-optimization notes below are from v0.2.1 and still describe the architecture.
- `main` @ v0.2.1 (`6ed6559`) was canonical at the time of that snapshot. It contains the full RAM-optimization work: **step 1** (columnar store, ~52% RSS reduction, merged via PR #1) and **step 2** (bounded chart pipeline `chart_query.rs` + error reporting `log_frontend_error`/`get_error_log_path` + chart perf + sidecar fixes, `3620eb6`).
- **Step 3** (disk-backed store, e.g. DuckDB / mmap Arrow) is planned only if RAM is still insufficient after step 2.
- Branches `feat/columnar-ram-opt` and `claude/gifted-kepler-*` are superseded by main — don't base new work on them.
